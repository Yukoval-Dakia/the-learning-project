/**
 * Copilot currently exposes no server-side cross-capability port.
 *
 * Keep this explicit provider-owned entrypoint so future consumers cannot
 * normalize a deep import into Copilot internals.
 */
export {};
