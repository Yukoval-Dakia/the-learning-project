import { create } from 'zustand';

// TODO (Phase 1c.2): flesh out with LearningSession state shape
// - active session id + type
// - session status
// - event stream subscription
export const useSessionStore = create(() => ({}));
