{
  "DELETE /api/assets/[id]": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/_/export": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/admin/conjecture-scores": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/admin/cost": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/admin/coverage-lattice": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/admin/failures": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/admin/judge-calibration": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/admin/runs": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/admin/runs/[id]": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/admin/subjects": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/admin/subjects/[id]": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/admin/subjects/[id]/traits": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/admin/traits": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/admin/traits/[id]/journal": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/artifacts/[id]/ai-changes": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/artifacts/[id]/backlinks": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/artifacts/[id]/correct": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/artifacts/ai-changes/recent": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/artifacts/search": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/assets/[id]/content": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/copilot/nudges": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/copilot/turns": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/cost/today": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/events/[id]": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/ingestion": "Compatibility route; remove after successor caller migration is verified.",
  "GET /api/ingestion/[id]/blocks": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/ingestion/[id]/events": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/jobs/[kind]/[id]/events": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/knowledge": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/knowledge/[id]": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/knowledge/[id]/misconceptions": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/knowledge/edges": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/knowledge/edges/[id]": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/knowledge/frontier": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/knowledge/proposals": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/knowledge/review-due-summary": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/mistakes": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/notes/[id]": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/observability/calibration-maturity": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/observability/effectiveness-trend": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/placement-sessions/[id]": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/placement/profile": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/practice": "Compatibility route; remove after successor caller migration is verified.",
  "GET /api/practice/[id]": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/practice/stream": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/review-sessions/[id]/answer-drafts/[answerId]": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/review/drafts": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/review/drafts/[id]": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/review/due": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/review/weekly": "Legacy route; declare request and response schemas when this surface is migrated.",
  "GET /api/today/copilot-summary": "Legacy route; declare request and response schemas when this surface is migrated.",
  "PATCH /api/admin/subjects/[id]": "Legacy route; declare request and response schemas when this surface is migrated.",
  "PATCH /api/artifacts/[id]/body-blocks": "Legacy route; declare request and response schemas when this surface is migrated.",
  "PATCH /api/artifacts/[id]/sections/[sectionId]": "Legacy route; declare request and response schemas when this surface is migrated.",
  "PATCH /api/placement-sessions/[id]": "Legacy route; declare request and response schemas when this surface is migrated.",
  "PATCH /api/practice/stream/items/[id]": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/_/import": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/admin/subjects": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/admin/subjects/[id]/reset": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/admin/subjects/[id]/restore": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/admin/subjects/[id]/retire": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/admin/subjects/[id]/traits/[kind]/fork": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/admin/subjects/[id]/validate": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/admin/traits/[id]/reset-to-seed": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/admin/traits/[id]/rollback": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/appeals": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/artifacts/[id]/ai-changes/[eventId]/undo": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/artifacts/[id]/correct": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/assets": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/attempts": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/copilot/chat": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/copilot/nudges/[id]/dismiss": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/copilot/nudges/[id]/opened": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/editing-session/blur": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/editing-session/heartbeat": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/events/[id]/correct": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/events/[id]/corrections": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/hubs/[id]/dismiss-link": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/ingestion": "Compatibility route; remove after successor caller migration is verified.",
  "POST /api/ingestion/[id]/extract": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/ingestion/[id]/import": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/ingestion/[id]/make-paper": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/ingestion/[id]/rescue": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/ingestion/[id]/revert": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/ingestion/docx": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/ingestion/pdf": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/knowledge/edges": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/knowledge/edges/proposals/[id]": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/knowledge/misconceptions/[id]/veto": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/knowledge/proposals/[id]": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/mistakes": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/placement-sessions": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/placement-sessions/[id]/question-selections": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/placement/[id]/end": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/placement/[id]/next": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/placement/start": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/practice": "Compatibility route; remove after successor caller migration is verified.",
  "POST /api/practice/[id]/answer": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/practice/[id]/submit": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/practice/calibration/anchors": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/practice/stream/recompose": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/review-sessions/[id]/answer-drafts": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/review-sessions/[id]/submissions": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/review/advice": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/review/appeal": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/review/drafts/[id]/enable": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/review/drafts/[id]/force-enable": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/review/sessions": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/review/sessions/[id]/end": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/review/sessions/[id]/pause": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/review/sessions/[id]/reopen": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/review/sessions/[id]/resume": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/review/submit": "Legacy route; declare request and response schemas when this surface is migrated.",
  "POST /api/teaching-sessions/[id]/accept-chip": "Legacy route; declare request and response schemas when this surface is migrated.",
  "PUT /api/admin/subjects/[id]/traits/[kind]": "Legacy route; declare request and response schemas when this surface is migrated.",
  "PUT /api/admin/subjects/[id]/traits/[kind]/binding": "Legacy route; declare request and response schemas when this surface is migrated.",
  "PUT /api/admin/traits/[id]": "Legacy route; declare request and response schemas when this surface is migrated."
}
