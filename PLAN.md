# PLAN — 活看板 (cockpit)

> 本项目的「手边」全局看板：比 `.remember/` 结构化、比 Linear 近手。**driver session 持续更新；收尾必同步**（见 `CLAUDE.md` →「Session Discipline · Cockpit & 全局视角」）。Linear 是**权威**驾驶舱（projects/issues 的真相），本文件是工作面镜像 + 当下决策态 + 在飞清单。四栏：NOW / NEXT / PARKED / BLOCKED-ON。
>
> 更新于：2026-06-23（owner 拍 **方向 B = 诊断档案** + 机械基底先行 + **YUK-471 event-sourcing 先做**；W0 **merged #575 → main @ac9c3f45**；**W1 三 fork 已拍 = (a)A2 专用反查表 · (b)B3 全 gated 不浸泡 · (c)C1 全合流意图→reconcile 钉 live 写入咽喉(非 fold tx，保确定性)+fold 只重放+纯拓扑**；W1 driving PR-A1 基底中）
>
> **本 session（2026-06-26）owner 拍：下一步主推 = 🦀 Rust Phase 1 推完。** **babysit-merge ✅ 完成（ulw）：#613 WASM-parity · #610 S4 schema 加宽 · #607 S6+S3 三 PR 全 MERGED 进 main**（origin/main @99ef0e0a；rebase-merge 保留 per-commit）。所有 OCR thread 已回帖+resolve（#610 legacy-snapshot=已捕获潜伏 edge；#607 非二值校验已修+grid_theta OnceLock，b_prime/unnorm 两 nit reply-skip=加 guard 反破位精确）。**Phase 1 剩 = S5 #41 recompute 徽章实现**（owner「不要 defer 数学上 rust」=Rust/WASM 位精确路径，#613 已解除 WASM-in-browser 底座；视觉稿已从 claude.ai/design 导入 recompute-profile.jsx/css 到 yuk-495-s5-recompute-badge）。其余非-Rust 线全捕获进「在飞」末尾，脑子不留。

> **【更新 2026-06-27 · #41 收尾 SHIP-READY】** owner 拍范围 = **#41 = D2 + E1 → Closes YUK-495**；A(WASM-browser)/E2(σ-flip) 拆出。ralph 自主跑完 US-001~006：**D2** RcMaturityBadge（5 文件，median bit-exact mirror server + dFirm=!cold_start，σ-independent 无 preview）· **E1** `RECOMPUTE_BADGE_ENABLED` 点亮（视觉验收 playwright+mock 丰富数据通过，走 libm preview honesty）→ **PR #634**：CI 主 gate（typecheck·lint·audits·test·build）+ CodeQL + Analyze + **独立 opus APPROVE** + Cursor Approval pass 全绿，**停等 owner merge**。base-不动决策代价 = CI merge-with-main 暴露 `MasteryProjection` 新增 required `beta` 的连带（2 个 main test mock）→ merge origin/main + 补 beta 修复。**拆出：YUK-508**(σ-flip 高风险 prod op + 2 前置 gate + runbook)·**YUK-509**(WASM-in-browser spike)；**follow-up：YUK-510**(biome ignore crates 产物)·**YUK-511**(drift 卡 median-only under-report，bot+opus 双标，near-unreachable tripwire)。PR-Agent 的 stale-closure finding = 误报（已回帖验证：run useCallback([outcome])+effect[run] 正确传播）。

> **【更新 2026-06-27 · 教研团路线 / A13 U8 SHIP】** owner「进入教研团路线」→ driver 自主推进。Phase 0 关系脑第一轮 ulw 的 **U1-U7 早已 merge**（conjecture 引擎/accept/probe/备课台/`kc_typed_state`/scoring stub）；**唯一剩的 U8 = A13 reconcile loop 本 session 落地 → PR #638（`Closes YUK-440` + `Refs YUK-406`，worktree `tlp-wt-u8`）**：unscored `probe_result`→按 `conjecture_event_id` join→`scorePrediction`→append LOG-only `experimental:prediction_score`(幂等锚)→`upsertKcTypedState`(仅 probe-resolution)，**接进 `research_meeting_nightly`(reconcile-before-propose)**——闭合 A13 dark-loop（roadmap §5 tripwire：producer 终于有消费者，prediction_score 开始累积）。FLIP-inert 三红线全守(score 不动 label·`mastered` 不可达·R(t) 只进 log event·绝不写 FSRS)；`prediction_score` loose 校验零 schema 改。**对抗 review = 3 opus 分镜(correctness/redline/integration)+仲裁全收敛同两 finding** → 修：① major=写序(upsert 先/score 锚后，防 partial-failure 永久丢 ledger) ② minor=只 catch read(防 parseEvent throw 毒丸整夜 job)；各补回归测试。gate 全绿(typecheck0·lint·audit×4·test 266f/2699·build·reconcile unit11+db5)。**停等 owner merge（不自动 merge）**。**YUK-440 = code-complete（U5-U8 全落）；YUK-406 backend 完，剩 felt 备课台卡片 UI = claude-design-gated**。**always-soft cell**（Phase 0 conjecture 不命名 confused_with-X → typed-state 恒 no-evidence/open）= 设计取舍非缺陷（证据累积才是点；硬判定 gated on misconception 成熟 YUK-344/Phase 1，无需新单）。**Lane D（P1 B3 YUK-349）已并行推进**：rebase 时纳入 origin/main 6 新 commit（B3 frontier PR-1 #635 + B3 PR-2 + `frontier_fill_nightly`）。

> **【更新 2026-07-01 · 全项目逻辑打磨 program 启动】** owner「按 misconception 方法论把项目现存逻辑全部翻出来，挨个打磨」。**方法论 = 独立调研(不困于既有 doc)→ code-ground → 对抗 review → 设计 doc + Linear**。两阶段：**① Map(浅→深两 pass)**——浅 pass ✅(`wf_48fbd857-05d`，15 lane / 174 raw → ~90 deduped units；register 落 `scratchpad/shallow-inventory.md` 当兜底)；**深 sweep 在飞**(`w0z690ik1` / `wf_cfacfdbb-377`；脚本 `scratchpad/deep-sweep.js`；每 deep 单元 mini-pipeline = 双 ground→diagnose→对抗×2→reconcile，~290 agent)；产出 **master register + 挨个打磨排序 worklist**。**② 挨个打磨**——按 worklist 一次深潜一条，每条走完整 misconception 待遇。**模型路由(owner 2026-07-01)**：读代码/知识/综合/诊断/reconcile = Sonnet 5，Opus 只留对抗验证 lane（记忆 `feedback_subagent_model`）。**已浮现 P0 = `pfa-plearn-formula`**（用户可见 mastery 数 = σ(γ·success + ρ·fail − β)，γ=0.4/ρ=−0.2 手拍，承诺的 nightly refit job 全代码库不存在）。misconception(model + PROMOTE) = DONE 范例，不再开。**✅ deep sweep 完成（2026-07-02，258 agent / 23M tok / ~2.7h，历 token 过期 + session 重启两挫折靠 resume-缓存续跑）→ master register 落 `docs/design/2026-07-02-project-logic-master-register.md`（1136 行：执行摘要 8 findings + 7 横切 F1-F7 + 全 register + critique + 100 单元 verdict）。关键：不全是"设计打磨"，含真·live bug——`kc-dedup-attribution-rewrite-gap`（KC merge 孤儿化 mastery_state/knowledge_ids，4 注释+ADR-0045 谎称已处理）· `pfa-plearn-formula`（β=0 → 3 对就误翻 mastered 掉出 frontier 池）· `default-softmax`（ε-mix 首次不可逆 b_calib 写无 trim）· `tagging-match` silent 零 event。critique 抓 4 gap（G1 backup/restore + G2 isomorphic-parity 其实是"已强 enforce"正例、G3 revert-bracket-dark 真横切簇、G4 draft-status #11 无 register 行）+ 2 修正（induce-self-consistency 该进 list；4 个 P1 是 lite-pass provisional 需先 trace live caller）。**下一步 = 挨个打磨，owner 定起点**（register 荐 #1 kc-dedup；#2 pfa 注释修=免费快赢）。**worklist 里每条打磨仍走完整 misconception 待遇（独立调研→code-ground→对抗→设计 doc + Linear）。** **✅ 补洞+重排完成（2026-07-02，workflow `w95x9tr02`，9 agent）→ addendum 落 register doc，base-ref 更 `685b2c27`。** 最终 **14-item grounded worklist**：①kc-dedup ②pfa ③tagging ④frontier-gate ⑤SoT-flip ⑥kg-borrowing ⑦softmax ⑧**verify-check(P1 坐实**：solve-check 给 tier3/4 声明却 `quiz_verify.ts` 从不 import `runSolveCheck`，不可靠题今天进 live/FSRS 池) ⑨**memory-reconcile(P1 坐实**：0.6 单标量唯一门→不可逆 DELETE 无墓碑) ⑩**F8 revert-bracket decide-and-record(P1-systemic**，gate 4 下游；`orchestrateCascadeRevert` 零 live caller 且被调也结构不可达=classifyRow 把 review/attempt 路由 irreversible) ⑪ocr-vlm(P2，~3 行渲染 gap) ⑫induce-self-consistency(P2，flag-OFF dark) ⑬conjecture wire/retire ⑭draft-status(~17 站点跨 3 方言 DRY，末位；`audit:draft-status` 不 gate 读侧)。**副产品**：G2 isomorphic parity 两套件 CI 静默 `describe.skip`（gate git-ignored 未构建 `.node`），parity 只 dev 机验证=关联 YUK-501。**Linear 已落**：program tracker **YUK-538**（14-item checklist + 2 镜像 Document）；快赢批 = **YUK-539**(pfa)/**YUK-540**(tagging)/**YUK-541**(ocr-vlm) 全 In Progress。**owner 拍「先扫快赢批止血」+ pfa 两决策（注释=诚实条件式 IOU 保留；retune=B γ=0.5/ρ=−0.25 含三 fixture 同步调）**。设计 spec+对抗审 = `docs/design/2026-07-02-quickwin-batch-pfa-tagging-ocrvlm.md`（tagging/ocr-vlm SOUND · pfa NEEDS-AMENDMENT 已折入：IOU 不撤、删 ADR-0042/0035 两伪引用、evidence-floor=4 结构修法双方零分歧）。**✅ 实施+独立 review 全闭环（2026-07-02）**：3 worktree 3 agent 并行 → **PR #690(YUK-541) / #691(YUK-539) / #692(YUK-540) 全 OPEN 停等 owner merge**。独立 review（review skill，每 PR 8-finder→Opus 验证）：#690 零 finding；#691 存活 1 低危（MASTERED_PL_THRESHOLD 孤儿 export 无指引）→ 已修 `b72c3f1d`；#692 存活 1 CONFIRMED（stale「ADDITIVE-ONLY 未接线」头部假话，F1 同款）→ 已修 `45113a4a`。**#691 CI 全量抓到 targeted 漏网**：frontier_fill_nightly dense fixture 是 3/3=「3 连对」形状被新 floor 有意拒 → 升 4/4 已修 `bd3004e8`（CI 重跑中）。#690 CI 全绿（含 OCR/PR-Agent SUCCESS）。follow-up 已捕获 **YUK-542**（landing 视图 warning 露出）。**✅ 快赢批全收官（2026-07-02）**：owner merge 三 PR → main `9ef22630`；YUK-539/540/541 Linear 自动 Done；YUK-538 checklist ②③⑪ 已勾（+快赢批实录段）；3 worktree+分支已清；OCR minor（JSDoc 契约）修于 merge 前并回帖。**✅ #1 kc-dedup 设计相收官（2026-07-02，workflow `w3y6qks05`/`wf_fa3ba23b-877`，6 agent）**：原设计被对抗双 attack（Lens A ownership 轴 / Lens B event-sourcing 轴）独立击中同一 MAJOR——fold-owned 表不许 raw UPDATE，须走事件层（恰是 register target-shape 第(4)条自己写明的要求）；reconcile 版 spec 对 `9ef22630` 全量重接地 → `docs/design/2026-07-02-kc-dedup-attribution-rewrite-spec.md`（决策表 9 surface event-native×imperative 混合 + 4 retire fn 带 advisory lock + backfill 链解析 + report-only 常驻巡）。**升级发现：`knowledge_edge` 是 LIVE fold-owned（`PROJECTION_IS_WRITER=1` W1 起）——缺口在生产活跃**；learning_item Q3 扩展 = #5 B3 gate 结构性前置。**✅✅ #1 全收官（2026-07-02）：owner merge PR #693 → main `fb487c3d`**；OCR 9 findings（1 major reactivate 契约守卫 + 8 minor）8 修 1 skip-带理由（O8 指针竞态=已知残留类,sweep 指针谓词+YUK-544 自愈）,9 threads 回帖+resolve,复审轮零新 finding;YUK-543 自动 Done,YUK-538 ①已勾;worktree/分支已清;**owner 同日三连授权:merge 自主 + deploy 自主（mac compose=生产=测试用）+ 巡检 auto-invoke（YUK-544 决策 a）**——记忆 feedback_no_auto_merge_pr 已改写;生产栈重建+健康验证 ✅（/api/health 200 · migrate exit0 · worker 注册 · `pgboss.schedule` 见 `merge_attribution_sweep | 0 4 * * 1 | Asia/Shanghai`）;**在飞**：YUK-544 sweep auto-invoke 实施 agent 跑在 `tlp-wt-yuk544-sweep`（两相 census+bounded-repair 主体已写完,API 流中断一次已续命,剩 manifest 注释+测试+doc 对齐+gate+commit）;**✅ docs hygiene PR #694 已自主 merge → main `0291987e`**（master register 三处 #693 RESOLVED 戳 + quickwin + misconception ×2 四 doc 落库;CI 全绿,OCR skipped 0 可评,CodeRabbit/Bugbot/Codex usage-limit,PR-Agent 无 finding;docs-only 免 deploy;worktree/分支已清——首次行使 merge 自主授权）。**✅✅ YUK-544 全收官(2026-07-03,自主 merge #2):PR #695 → main 尾 commit `bb5e115f`**(3 commits rebase 保留:实施 f52784c6 + 独立 review 修复 07a73e9b + bot 轮 bb5e115f)。sweep 升级 census+bounded auto-repair:per-winner tx 隔离(failedWinners 计数,毒 from_id 不饿死同侪)+ winner 活性 FOR UPDATE 重验(TOCTOU)+ 事件就地写 + WARN=10/CAP=50 + 复扫归零断言(E1 doctrine:唯一 post-commit 读)+ `ingest_at` stamp(O1:防取证事件灌 Mem0)。验证链:CI 两轮全绿 + 独立 review(8 finder→5 Opus verifier,9 CONFIRMED 全修/3 REFUTED 留档 spec ledger 23-25)+ bot 三源(OCR 5 threads 回帖+resolve,2 修 3 skip-带理由;CodeRabbit 1 修;Cursor APPROVED;复审轮零新实质)。8 db 测试(含 winner-throw 隔离/TOCTOU/WARN 分支)。生产重建+sweep 新语义验证 ✅(health 200/migrate 0/worker 注册/schedule 在册/DLQ 桶自愈挂载)。**✅ #5 设计相收官(2026-07-03,workflow `wbbubujfh`,4 agent):owner「开始下一刀」→ designer→双 Opus attack(13 MAJOR 全裁决修入:Lens A 8/8 含 M1 交付 Q4b retained-golden 非重言腿 + M7 每实体三条 NO-GO 腿禁 CLEAN 冒充;Lens B 5/5 含停机翻转禁 rolling + 回滚≠数据修复诚实化)→ reconcile 终稿落 `scratchpad/research/2026-07-03-sot-flip-oracle-draft.md`(随 PR 进 docs/design/2026-07-03-sot-flip-oracle-spec.md)。headline 裁决:Q1 拒运行时 shadow-write 取离线 retained-clone 腿;Q4 拆 Q4a(report-only sweep,诚实定位=rowset 存在性探测器对 reducer bug 结构失明)+Q4b(retained-golden 独立 oracle,learning_item flip 前硬性)。Linear = **YUK-548**(High,parent YUK-538,关联 YUK-547/546,In Progress;spec 镜像 doc 待 PR 落库后同步)。实施 = 切片 0-5(slice 0 折入 YUK-547 带 Closes;slice 6 实际翻转 gated 非本次交付,逐实体独立 issue)。**✅✅ #5 全收官(2026-07-03):owner merge PR #696 → main `edc7cf62`**(8 commits rebase 保留:spec + 切片 0-5 + 独立 review 修复 + bot 轮修复)。验证链:实施/修复/bot 三轮 gate 全绿(153-154 db + 161 unit)→ 独立 review 环(16 候选:9 修含 **K1 edge 锚 action 过滤缺失** major——propose-only legacy 边会被误锚→假 MISSING 永久 forensic,+2 直测;4 REFUTED doctrine 进 spec 附录)→ bot 轮 24 条(19 修含 **O9 单坏边 crash 整轮 sweep→DLQ** 隔离哨兵化 + **O12 FK 簇穷尽双保险** + O3 顺带修 42 次冗余全表扫/跨簇 genesis 写;5 skip 带理由)→ 复审轮零新 finding → 17 OCR threads 回帖+resolve。生产部署验证:health 200/migrate 0/worker 注册/**`projection_oracle_sweep` 排班在册(周一 04:30 错峰)+DLQ 挂载+`[sot-flag] flag vector at boot` 日志 live**。YUK-548 Done(trailer 自动)/YUK-547 Done(slice 0 折入)/follow-up=**YUK-549**(knowledge prefetch+golden 砍 propose/generate+registry dispatch 字段化)。**owner 待办**:question_block 停机回滚演练(runbook §5 记录);slice 6 逐实体翻转(goal/mistake_variant/learning_item)按 Q3 checklist+Q4b golden 排期各开 issue。**#4 frontier-gate 进行中(2026-07-03)**:P1 回填 ✅(UI 文案 CLEAN——全是「前置已满足」结构性表述;ALEKS 27%/90-95% = 不同漏斗段不矛盾,27% 主锚;**副产品抓到真·潜伏 crash:PfStream SRC_META/StreamSource 缺 'frontier' case,后端今天就能发**)→ 设计相 ✅(workflow `wno2ym14b` 4 agent,6 MAJOR 全修入——overflow 有三消费点改单点 emit、bare SRC_META 两站点改集中 accessor、0.7-vs-BKT-0.95 = p(L)≠后验 off-scale doc-only、Q5 写时物化 REJECT 账已算)→ **Linear YUK-551**(High,parent YUK-538,In Progress)→ **UI pre-flight owner 双批**(借 knowledge-a5 token ✅ + 换未占用 tone ✅)→ **✅✅ #4 全收官(2026-07-03,自主 merge #3):PR #697 → main `b7d6dd27`**(7 commits:spec+4 切片+review 修复+bot 轮)。验证链:独立 review 16 项全落(**headline = tone 重裁**——执行者「唯一未占用 tone」前提被 review 证伪(neutral 本就双占,唯一未占用的 hard 是全仓警示琥珀),带矩阵回抛 owner 重裁 neutral;spec 批准实录回写消除三 finder 同抓的叙事矛盾;A2 测试诚实化——真 borrow 恒 0/0 且 p(L)=σ(−β) 到不了高值)+ bot 轮 3 条(1 修 nodeOverflow 断言(二部图从常量推导尺寸)2 skip 带理由)+ 复审零新。生产部署验证 ✅(health 200/migrate 0/worker+sot-flag 日志)。YUK-551 Done(trailer);follow-up = YUK-552(evidence-floor 进 UI,gated-future)/YUK-553(makeLookup 收敛);owner 观察后决 = Q3a masteryTone 对齐、Q1 overflow 结构化 meta 升级。**✅✅ #8 verify-check 全收官(2026-07-03,自主 merge #4):PR #698 → main `d705dfe1`**(6 commits:spec+M1 flag/seam+M2 接线/分轴谓词+M3+review 修复)。设计裁决:Q1 否决权按 compared_by 分轴(semantic 0.8 门配否决;exact 裸比对 → **owner 拍板 (a) hold-for-review** 落 /drafts);source_verify 零改(Q5)。**review 环 headline = A1 假 fail 量级**(Opus 钉死:exact 轨 normalize-only 无 semantic 回退 + quiz_gen 行 reference_solution 恒缺 → 近乎全量假 fail = 自动化实质停摆;spec 原文「必然」非低估,真 gap = 无削减)→ 修削减三件套(reference_md 首行候选+全角标点首句候选(3.14 防截断)+尾标点 trim 双向);ALT-1 双独立确认(solve_check 否决理由到不了 /drafts 驳回理由——显示的是模型 solve 前的 pass 自评)→ deriveVerifyVerdict 服务端合成;EFF-1 cost/task_run_id 穿线。bot 轮:**owner 指示 OCR 跳过只看 CR**;CR 唯一 Critical = 假阳性(两 const 分属不同 it 块,CI 双绿佐证)已回帖驳回。生产部署+健康验证进行中;follow-up = YUK-555(手动端点硬顶)/YUK-556(quiz_gen 补写 reference_solution 治本)。**下一刀 = worklist #9 memory-reconcile**(dossier+P0-1 考古就绪:0.6 单闸前加非 LLM 佐证 + 可逆性分层;软取代锁 raw SQL 有真依据(官方 update() 清 metadata+textLemmatized 破 BM25),硬删可切官方 delete() 免费拿 mem0 tombstone)。**✅ worklist 前置 research 完成(2026-07-03,owner「把接下来要碰的先 research」→ workflow `w82ldd26h`,21 agent/3.15M tok/43min)**:#5/#4/#8/#9 三腿 dossier + #7/#6/#10/#13 scout 全落 `scratchpad/research/2026-07-02-worklist-*-research|scout.md`;Opus critic 全批抽验零编造零失败,4 主 dossier PASS、#7/#6 scout 标 GAP(OSS 腿缺席,升格 design 前回填)。**关键新事实**:①#5 背景已 stale——artifact/question_block 已于 06-28 翻 ON 且**未走完整 b3-gate**(rebuild+对称 parity 跳过),post-flip audit:projection 是 tautological(同 reducer 自证);②#6 的 register P1 判据已被同日更早的 YUK-539 evidence-floor 关闭(git 时序核实,register 文本待更新);③#8 三腿收敛「纯接线遗漏」但 promote 否决式 vs 软信号有真张力待裁;④#9 P1 三腿全确认,mem0 自带 tombstone 已装未用是最便宜修复。**Critic P0 补洞在飞**:P0-1 #9 raw-SQL 因果考古(git blame)、P0-2 #5 artifact/qb 生产库只读实测 parity;P1 回填(#6 Rust 核+OSS 腿、#7 OSS 腿、#4 UI 文案核查)钉进各单元 design 相前置。原实施记录：Linear **YUK-543**（High，挂 YUK-538）；13 提交（9 surface 编排 + 4 retire fn 带锁 + edge 事件对 + backfill/sweep + 修复轮 6 提交）。**owner 追加两问已固化**：①设计相查文献→七路文献/产品源码验证 + Opus 对抗（spec Appendix C）——五决策全 UPHELD 零 blocking（freeze-and-log 有 LFA/CDM 背书、Sentry unmerge 解剖反证 D-D、Datomic/Wikidata 一手先例撑 abort-tx）；②记忆已存「设计相三腿 grounding=文献+产品源码+code-ground」（feedback_design_lit_and_product_lanes）。**独立 review 环**：8 finder→12 候选簇→7 Opus verifier→4 CONFIRMED 全修（**mesh LIST_LIMIT 截断**=最重,501/502 边回归测试钉死；knowledge_edge/misconception 双全局唯一索引墓碑假阳性→reactivate/走 throat；edges_rewired 取证 outcome 枚举）+2 REFUTED 留档（4 retire fn 不抽共享 helper——动态传表会致 step9 fs-walk guard 正则失明）。**follow-up 已落 Linear**：YUK-544（巡检有界动作,owner 拍）/YUK-545（解冻门）/YUK-546（edge 生命周期硬化三件套）/YUK-547（Q3 预取,#5 flip 前）。**merge 后运维**：backfill 脚本需 NAS 生产库手动跑一次（幂等,先 dry-run）。注：#4 frontier-gate 的部分诉求（evidence-floor+阈值 docblock）已被 #2 顺带落地，届时先 re-ground。**#9 memory-reconcile 进行中(2026-07-03)**:设计相 ✅(workflow `wss6sqkxd` 4 agent,designer→双 attack(裁决语义/存储回滚)→reconcile;10 MAJOR 全修入——A5-1 降级对称重写 reason 硬验收、A4-2 检测面提同波、M1 write-ahead 捕获防重放污染、M2 insertPlannedRows 显式列映射、M5 restoreVerbatim 替换坏的 addEventMemory hop;mem0 官方 delete() 装机源码逐行核验真 tombstone 但只存文本+在备份边界外→主次颠倒 WAL prev_text 为主恢复源)→ **owner 三拍**(Q1b weakness/event 硬禁 MERGE ✓ / floor=0.5 ✓ / Q3(a) 本波不做 ✓;自决:admin 端点暂不建、tombstone 测试必 author+不可 hermetic 则 YUK-501 式 CI-skip)→ **Linear YUK-557**(High,parent YUK-538,In Progress)→ spec 落 worktree `tlp-wt-yuk557-memrec`(docs/design/2026-07-03-memory-reconcile-spec.md,随 PR 进库)→ **✅✅ #9 全收官(2026-07-03,自主 merge #5):PR #699 → main `f7726c6c`**(8 commits rebase 保留:spec+S1-S4+类型传播+独立 review 修复+bot 轮)。实施:双闸+WAL 快照(migration 0058)+官方 delete() tombstone+restoreVerbatim+runbook,tombstone 集成测试 hermetic 成功非 CI-skip。**独立 review 环**:8 Opus finder(23 候选)→8 verifier(**fable 首用×2**+Opus×6)→修入 6 簇——headline = **hardDelete message 正则会吞 role/relation 类真错→markApplied 孤儿**(fable 钉死,改 verify-absence 删后核行)+ **WAL 非 KEEP_BOTH 行 prev_text fail-closed 守卫**(throw 零误杀穷尽证明);V4 abstain as-designed/V5 parse-barrier 不可达等 5 doctrine 留档 spec 附录 B。**bot 轮**:CodeRabbit 5/5 修(m7 确定性注入/collectionName 免 key 解析/越界 old_index 降级/prev_text 快照优先/resetDb 规范)+OCR 两轮 8 修 5 skip-带理由(1 REFUTE:store 守卫集合刻意含 SUPERSEDE,委托 isHardDelete 反是语义回归)+Cursor 1 skip(空串=忠实快照);13 OCR threads 回帖+resolve;复审轮纯边角收口(不追零)。生产重建+健康验证 ✅(health 200/migrate 0/worker 注册/**0058 两列已in生产库**)。YUK-557 Done(trailer);无新 follow-up issue(WONTFIX/deferred 留档 spec 附录 B)。**注**:merge 后发现 owner 本地分支 `workflow/upgrade-ocr-idempotency-v2`(ce046975,OCR workflow 幂等+限流,只改 .github,基于旧 main)——已 autostash rebase 到 f7726c6c(现 8acc83dd),**未替 push,owner 自行决定 PR**。**模型路由修正(owner 2026-07-03)已生效并入 memory**:subagent 默认 Opus、fable 复活=顶档稀缺(本单元 verifier 首用)、Sonnet 只留轻量机械活;effort 硬旋钮只在 Workflow agent() per-call(low→max),Agent tool 无。**✅✅ #7 softmax 全收官(2026-07-04,自主 merge #7):PR #702 → main `cb0abb09`**(5 commits rebase 保留:spec+PR-1 截权/可观测+PR-2 种子化/pin+独立 review 修复+bot 轮)。设计相 headline(fable reconcile 首用):绝对 π-floor 被定量否证改判 **median-相对 cap**(C=4 owner 拍,回滚=C=∞ 逐位还原);owner 双拍(保留+加固/C=4)。**实施特记**:opus 起→额度限额掐断→**glm-5.2 续作**(自审半成品逐行完整)→主 session 独立 gate 复验——降级模型协作链可信。**独立 review 环**:8 Opus finder(27 候选,多数带数值实验)→**fable 终裁**(逐簇自跑数值复核,纠正 finder 方向转置,挖出 **λ* FP 逃逸系旧代码既存缺陷**——非 2 的幂锚下 varM≈1e-31 逃过守卫吐垃圾 λ*,修=恒锚短路,独立 bug 修复不随 C=∞ 回滚)→修入 11 项(estimator 逐位回滚(50 随机批 toBe)/seed 惰性自记日志零 decoy/recompose nonce 保「每按新抽」/测试强度五修含重言断言替换+聚合 1+2=3 击杀单赋值)+defer/REFUTE 留档 spec 附录。**bot 轮**:CR 1 修(parsed-empty 死分支,parse barrier 保证不可达)+OCR 3 修 1 moot、复审 1 skip 带理由;9 threads 回帖+resolve。生产重建+健康验证 ✅。YUK-558 Done(trailer 自动)/spec 386 行全文镜像 Linear/YUK-538 勾 ⑦+实录。无新 follow-up issue。**同日插单 PR #701(YUK-436 BKT 嫁接,owner 亲写)也已代收官(自主 merge #6):fable 对抗审数学核零错误(3PL Fisher 对标 Lord 恒等/坐标变换/浮点格点全验算),4 小修(毕业闸 fail-closed+3PL 解析 pin(区分错误分母)+θ* per-KC 歧义 docblock+c 定义域 pin)+inc-2 交接三条补进 PR body;CR 2 REFUTE 带机理纠正 1 修,OCR 复审 1 skip;main `9e355a10`,生产验证 ✅**。**✅✅ #6 kg-borrowing 全收官(2026-07-04,自主 merge #8):PR #703 → main `ec4693d8`**(7 commits rebase 保留:spec+S1/S2+S4+S3+S5+review 修复+bot 两轮)。设计相(workflow `wsqsf3lz7`,fable reconcile):12/12 findings ACCEPT,**收敛为零算法数值变更/零 Rust/零 schema/全 dark 纪律波**——M1 撤回(borrow 条目 .mastery=σ(−β)/.theta_hat=θ̃ 是 by-design 双轴)、Q2a 半径判决反转(半径纪律落 **A5 分量守卫** cap=256+fail-safe-to-no-smoothing,A13 ≤2 跳与 weight modulation 双撤回)、遥测改 **shadow 周 sweep**(per-read emit 会经 memory outbox 扇出 brief-regen)。交付:provenance 判别式+theta_hat_raw / audit:mastery-provenance(report-only) / kg_borrow_shadow_sweep(周一 05:00,**a5_only/a6_only/joint 三 variant 归因**共享单次 solve,type-7 分位数,ingest_at:now,零新表)/ ADR-0047 收敛(ratify-ready,9+1 待决项)。**review 环**(8 Opus finder+fable 终裁,经历分类器不可用+额度触顶两次中断靠 Workflow 重启续上):headline = **audit guard-token 假 guarded**(裸 substring 让两个真·未设防消费者被注释 token 假放行,五 finder 收敛;修=注释/字符串剥离+code-shaped regex,verdict 位移恰如 fable 预测)+数值对抗专攻承重数学全 CLEAN(块对角恒等真逐位/σ(−β) 锚/cap 退化);修入 C1-C11。**bot 三轮**:首轮 7 修 1 skip(sweep 自吞=spec Q6'-L 裁决);OCR 复审 3 修(allowlist 根形态守卫/orphan 反查/Array 判别,主 session 直修);末轮 1 skip 带理由(file-scope 启发式=三处已成文的已知限制,--strict 升级绑人工复核);13 threads 回帖+resolve。生产重建+健康验证+sweep 排班核验(后台)。YUK-559 Done(trailer)/Linear 收官三件 agent 在飞;owner ratify 9+1 条全在 YUK-559 body+ADR-0047,gate 未来翻转不 gate 本波。**✅✅ #10 revert-bracket 全收官(2026-07-04,自主 merge #9):PR #704 → main `c54bd1de`**(9 commits rebase 保留:spec+O2 双 sibling 对齐+S1 schema+S2 双 checkpoint 写点+S3 orchestrator+S4 rejudge live caller+S5 ADR/runbook+review 修复 P0+bot 轮)。**Owner 拍 A**(修可达+接线,推翻材料推荐的 B append-only;θ̂/FSRS 不应是全系统唯一残留的结构性单向写面)+ **O2 完整案**(θ̂/FSRS 双 sibling checkpoint 事件,段撤销天然正交)+ O3 零 marker。交付:每 attempt 双 sibling checkpoint(grading_checkpoint segment 判别 + state_snapshot rich union,before=全列含 rt/grid——SRT LIVE)+ cascade-revert tx-aware(SAVEPOINT 四态 refusal)+ **rejudge live caller**(双触发门 judgeDriven AND bitFlip + θ̂-skipped prior 分句 + 原子 tx + reproject_deferred 四态 marker,happy-path 也写残留在可见)+ verbatim 全行 restore + 嵌套 jsonb 形状编译期锁。中间态 inert(S4 前系统行为零变)。**review 环**(8 Opus finder 含新增因果/原子性命门专攻角;fable quota 归零改 Opus xhigh 终裁,resume 缓存回放零成本):核心机制全 UPHELD;headline = **P0 correctness**(unsupported→incorrect 静默丢 residual marker——outcomeBit 把 θ̂-skipped 与失败位都当 0)+ 2 P1 测试 + 加固全修;5 REFUTE 留档(两个是终裁独立验证到 node_modules 源码级推翻 finder:drizzle/postgres-js savepoint rollback-to 保外层 tx、double-precision f8/jsonb round-trip 精确)。**bot 轮**:Cursor 1 Medium 打在刚修的 P0 路径,**派独立 Opus verifier 追事实链 REFUTED**(placeholder judge 恒带 coarse_outcome 且申诉恒指 placeholder,over-revert 交集可证为空,且 Cursor 隐含修法会重引 P0)+ OCR 首轮 1 修(silent-partial revert→fail-loud)+ 复审轮纯 schema 收紧 defer。生产重建(tlp-deploy 钉 origin/main,新规矩)+健康验证 ✅(镜像 19:48)。YUK-561 Done(手动补置)/spec 460 行全文镜像/YUK-538 勾 ⑩+实录;follow-up **YUK-564**(并发双投递)/ **YUK-565**(memory-brief 污染)已开;DEFER-3(批量化)n=1 可忽略不单开;reproject_deferred schema 收紧 + 第二实例重投影引擎(judge-spof 下半)留独立 issue。**worklist 11/14 收官(①②③④⑤⑥⑦⑧⑨⑩⑪)。下一刀 = #13 conjecture wire**(owner 拍 A 隔离路径,前置:先修 misconception-promote 的 unlocked onConflictDoUpdate;Producer serveProbeOnce/answerProbe 建好零 live caller,Consumer research_meeting_nightly live 却恒空跑);之后 #12 induce-self-consistency → #14 draft-status(末位)。**✅✅ #13 conjecture-wire 全收官(2026-07-04,自主 merge #10):PR #705 → main `6baea8aa`**(7 commits rebase 保留:S1 schema/proposal/induce-prompt+reference+S2 accept 同步 serve(tx 原子)+S3a answer route+S4 admin 双读 reader+S5 ADR-0049/runbook/YUK-566+fix-lane 三 commit)。owner 拍 **wire(不 retire)+ 推荐组合 4A+3A+2A+1A**:producer(`serveProbeOnce`)经 `acceptConjectureProposal` 同 tx 派发判别探针(≤3 cap advisory lock,原子性 INTENTIONAL 非 best-effort per spec §4 Q1)+ consumer answer route(owner 作答→judge→outcome→`answerProbe` 写单 `experimental:probe_result` event)+ A4 双读 admin reader(`prediction_score` + auto-minted `kc_typed_state` confused-with-X)。**fix-lane 关键**:独立 Opus review 捉 CRITICAL——原 route 走 base registry `resolveJudge('semantic').run()` 是 STUB(返 unsupported→每个 free_text probe fail-closed→dark loop 永黑,测试 mock 盖住);修=切 `createDefaultJudgeInvoker().invoke()`(judge-only chokepoint)+ `peekExistingProbeResult` 幂等预检查(LLM cost guard)+ ADR-0049/spec ND-5 推理更正(边界是 `answerProbe` 非 dispatch,invoker.ts grep 零 FSRS/attempt 写;早先「isolated registry path」是缺陷)+ `judge_kind_override` guard 死代码修(`!overrideParsed`→`!overrideParsed.success`,CodeRabbit+OCR 抓)+ runbook error code 对齐 + conjecture-accept 注释更正。验证链:CI 两轮全绿(主 gate 8m23s+OCR 13m27s)+ 独立 Opus review APPROVE(CRITICAL 真解决/ND-5 未破/peek TOCTOU-safe/mapOutcome 穷尽/测试 mock 对模块能抓回归,反橡胶章非言)+ bot 轮(override guard/runbook/comment 三修,S4 预存问题无界查询/typed_state fail-closed/num helper 静默默认捕获 **YUK-568**)。**S3b prep-desk card UI = design-gated 拆 YUK-567**(card 组件不存在,需 claude.ai/design pass 后 slice-by-slice 实现)。**worklist 12/14 收官(①②③④⑤⑥⑦⑧⑨⑩⑪⑬)。下一刀 = #12 induce-self-consistency → #14 draft-status(末位)**。**✅✅ #12 induce-self-consistency 全收官(2026-07-05,workflow `wf_0b296dd8-b3d`,14 agent/1.16M tok):PR #706 → main `待 owner merge`**(2 commits:实施+spec)。**严重度重裁 P2(非 P1)**：`induceConjecture` 无独立 flag——LIVE 调用，但 `MISCONCEPTION_PROMOTE_ENABLED=0` 把 blast radius 锁在 proposed_change 表内（无 misconception node 权重受影响）；upgrade-trigger = 翻 MISCONCEPTION_PROMOTE_ENABLED 前必先有本 fix。**根本原因**：`claimKey` 仅 trim+lowercase+whitespace，N=3 Opus 在 temperature>0 下恒产生语义等价但字面不同的 paraphrase → 三单例桶 → `confidence = 0.333`（应为 1.0）。**文献判决(三腿)**：USC(arXiv 2311.17311,Google DeepMind)和 DSPy `MultiChainComparison` 对自由文本采 LLM-as-judge 是生产标准；embedding 余弦相似度对抗义词混淆率高达 99.9% 不推荐；字符串 normalization 是 lower-bound 非准确估计。**修复 = Option E**：新增 `ClaimGroupingTask`（mimo 默认，非 Opus，结构化输出 `{groups: number[][]}`，CJK-aware 系统 prompt）+ `deduplicateClaims` 后置聚合（fail-closed 降级→恢复原 claimKey 行为，flat-count coverage guard 防 LLM 重复下标）；触发条件 `dominant.length < drafts.length && drafts.length > 1`（temperature>0 下每轮 nightly 几乎必触发）；`InduceConjectureInput/Result/ConjectureDraft` 接口**全不变**，零 schema 改动。cost：+1 mimo call per conjecture per 夜 = +3 mimo/run。adversarial review 接受8项（覆盖 flat-count guard/try-catch/cost 穿线/无 anthropic-sub override/ClaimGroupingTask 独立 kind）。**独立 review APPROVE**：3 LOW findings 全 spec-acknowledged（out-of-range index guard/within-group 排序/call-count assertion），无 blocking。**gate 全绿**：typecheck/lint/audit:schema/audit:partition/test:unit 3661passed-0failed/build。**YUK-538 ⑫已勾**；follow-up = ClaimGroupingTask 集成测试(task-runner boundary)/out-of-range index guard/temperature=0 可确定性/runbook 说明 confidence=expected-value/task_run_ids 全量 provenance；**无独立 Linear issue（P2 worklist item，YUK-538 tracking 够）**。**生产重建+健康验证 ✅(2026-07-05)**：tlp-deploy @ `81d67345`；health 200/migrate exit0(+3 seed)/worker 注册；OCR bot 轮 3 findings 全修(O1 partition guard+O2 covered+O3 console.warn)+3 threads resolve；biome format hotfix `f488ca02`；最终4 commits rebase进main(`6fa10e70/b7999b79/c1ae06ed/81d67345`)。**worklist 13/14 收官(①②③④⑤⑥⑦⑧⑨⑩⑪⑫⑬)。下一刀 = #14 draft-status(末位，~17 INSERT 站点跨 3 方言 DRY)**。 **✅✅ #14 draft-status 全收官(2026-07-05,自主 merge #11):PR #707 → main `ee5186ad`**(3 commits rebase:impl `5058683b`+独立 review 修复 `1d220388`+CodeRabbit bot 轮 `ee5186ad`;**YUK-569 Done**)。register RESHAPE:**19 F1 fail-open 池可见性谓词跨 4 方言**(7 Drizzle or(isNull,ne)+8 raw-SQL+2 IS DISTINCT FROM+2 JS 孪生)抽 `notDraftPredicate`/`isPoolVisible`(src/db/predicates.ts)+读侧 `audit:draft-status-reads`(Engine A SQL 正形检测,--strict 进 pnpm test 补红线-4 读侧;Engine B JS 账本 deferred);真值表§5 证{NULL,draft,active,final,其它}四方言 byte-identical→**零结果变更**;family-2 fail-closed 精确门(FSRS/编辑/promote/demote/draft-review-SELECT)全程不碰+4 处 look-alike 守卫注释。设计相 6-agent workflow(分类+方言等价证明+对抗 critic)**零误分类**→独立 Opus review(3 lens+xhigh)SHIP,3 non-blocking 全修(UNKNOWN 可 allowlist+7 悬垂注释)→CodeRabbit bot 轮(CR-1 注释/CR-2 混合模板 FN 修+2 测试;CR-3 extractObjectBlock 嵌套模板单布尔 state 机=**pre-existing**/非 manifest/读侧不调它→**YUK-570**)。gate 全绿(typecheck/lint/audit×6/pnpm test 3040/build)+CI 主闸 pass 8m16s+CodeRabbit/Cursor Approval/PR-Agent/CodeQL 全 pass;OCR job infra 挂起(两轮 pending 15min+,0 findings)超 13-min cap 凭权威闸+独立 review skip。生产 mac compose 重建 pinned `ee5186ad`(app 13:11:52Z 新鲜/health 200/migrate exit0/worker healthy;**cloudflared 隧道 pre-existing 配置问题——缺 tunnel-ID arg crash-loop,deploy 前本就未运行,已 stop 恢复基线,与 DRY 变更无关,待 owner 修 tunnel 配置**)。**worklist 14/14——YUK-538 全项目逻辑打磨 program 达成**(按 misconception 方法论翻遍现存逻辑挨个打磨,14 条全走独立调研→code-ground→对抗→设计 doc+Linear)。follow-up **YUK-570**(tokenizer 嵌套模板加固,Low)。**下一刀 = owner 定(YUK-538 program 已收官;方向 B 诊断 payoff 主线未动)**。

> **【更新 2026-07-06 · 方向 B 启动 = 「可开始用」milestone】** owner「方向B，lets go」+ 承重更正「**暂时是没数据的，我要把产品做完再开始用**」（历史 ~65 events 是测试性质；策略 = 先做完→再开始用→数据自然积累）。**re-ground workflow `wf_c75efb1f-287`**（9 agent / 777k tok：5 路 ground〔UI 面/后端 flag/Linear 树/设计 doc/数据信号〕→ Opus 综合 → 双 Opus 对抗 → fable 终裁；G5 生产库 census 被 classifier 拦，降级仓库内间接证据——owner「没数据」后此问题消解）。**终裁 headline**：练习/复习/露出面**全绿**（solo `submit.ts:791` + paper `paper-submit.ts:823-830` mastery_progress emit 均 LIVE〔G2 曾误报 paper 死线，双 critic 击破——:816 注释是历史描述〕；/coach 三视图**含 A7 趋势面 `EffectivenessTrendPanel.tsx` 已接线**〔「A7 缺屏」旧报告 stale〕；/knowledge band/三维/candidate 误区、/mistakes 归因三轴全 live）。**「可开始用」未满足硬前置只剩 3 项**：① S1 修 YUK-516（placement-profile frozen-only vs start 三层解析漂移→空档案）② S3 YUK-478 残留收口（upload→KC bridge）③ 翻 `PLACEMENT_PROBE_ENABLED`（owner 运维）。非前置明确归位：3b day-one 先验档 = θ-grid 三重缺（const 翻转+prod 无 `.node`+UI design-gated）→ 移出 bar；5d probe-answer UI = design-gated（→YUK-567，producer live 无插头）；5e misconception confirmed 段 = 数据积累后才有意义。**⚠️ 三 flag 字面量不一致（写错静默 no-op，已 code-verify）**：placement 仅 `'true'`（placement.ts:41）/ promote 仅 `'1'`（agency/misconception-promote.ts:76）/ refill `'true'|'1'`（refill.ts:76-79）。**Linear**：YUK-571 开单（flag 翻转顺序①→②→③ + day-zero census 运维承接单，parent YUK-452，承接 YUK-475 未兑现腿）；YUK-516 → In Progress（第一刀）。**S1 进行中（本 session，分支 `yuk-516-placement-profile-scope-parity` @71abfcbf）**：TDD 3 新 DB 测试 RED（tier-2/tier-3×2 空档案症状）→ 抽共享 `resolveGoalPlacementScope`（`src/capabilities/practice/server/placement-scope.ts`，issue 倾向①单一真相源，option②session-scope 语义 tradeoff 记入头注）→ GREEN 32/32（start 侧全绿=refactor 零行为变化）→ 全量 gate 全绿（typecheck0/biome/audit×5/unit 3711/db 3043/migration 22/build）→ 独立 Opus review **SHIP-WITH-NITS 0 正确性缺陷**（F1 never-probed 加宽→YUK-571 comment 翻flag前验证项；F3/F4→补 4 helper 契约直测 `45581fe9`；F2 pre-existing skip-带理由）→ **✅ PR #709 owner merged → main `99779fb5`，YUK-516 Done**（自主 merge 被 classifier 按 CLAUDE.md「owner-merged」成文拦——口头授权 vs 成文冲突连续第二 session 复现，待 owner 拍：改 CLAUDE.md Code Review Workflow 或保持人工合）。**S3 判定 ✅（2026-07-06，Opus tracer 逐环 file:line @99779fb5）= 无代码活**：upload→placement 链端到端完整——tagKnowledge match-or-propose 薄树 PROPOSE auto-approved 子 KC（root 无 embedding 非 match 候选 → 06-22「题堆 root」被 YUK-489 结构性解决）、reference_md 走 05:20 夜链、auto-promote/NULL≡active、tier-2/3+selectNextPlacementItem 全可达（approval 全链不设卡）、bridge=窄化角色非 dead code；两 🟡（跨 session 重复 KC→P5 dedup 兜底 / ref_md 滞后一夜链 judge 降级 prompt-only）非断链。**tracer 挖出第二 go-live flag `WORKFLOW_JUDGE_AUTO_ENROLL_ENABLED`**（workflow-judge-config.ts:28,90-93，大小写不敏感 'true'；OFF=上传 observe-only 不产题）→ 已补 YUK-571 翻转表、与 placement flag 同批①。**S2 e2e ✅ = PR #710 OPEN（`Closes YUK-478` + `Refs YUK-571`）**：`placement-coldstart-e2e.db.test.ts` 把「可开始用」契约钉进 CI（真 seed 根→真 tagKnowledge PROPOSE→tier-2 goal→start 真 serve「scope 有题」→trail→profile tested band+诚实 root；honesty leg sourcingNeeded），2/2 一次全绿=实证链完整；独立 review 环在飞。**milestone 代码侧全部完成——剩 YUK-571 flag ①批（owner 运维）**；判定/字面量全档在 YUK-478 与 YUK-571 comment。**待 owner**：merge #710；翻 flag ①批 + day-zero census；Linear stale 状态对齐批准（YUK-519/531/476/407 终裁建议翻 Done/收口）；YUK-567 design pass 时机。环境注：本机（yuqi 账号）Docker/OrbStack 冷启已拉起；生产 compose 栈不在本机（/Users/yukoval 路径全 prunable）——**flag 翻转与部署都在 owner 侧机器执行**。

## 🎯 主线方向（当前）

**方向 B = 诊断 payoff（owner 拍定 2026-06-23）。** 头号留存钩子 = 「看到我哪错/哪长」的学习者诊断档案（私人教研团终局），非纯机械「记+练+复习」闭环。但**机械架构基底先打牢**——诊断轴消费底层引擎/检索/matcher。状态 check 结论（`scratchpad/PROJECT-STATUS-REPORT.md`，2026-06-23）：B1 θ̂ SoT + 检索 + owner 门 + poolFetch **已够硬撑 day-one B**；唯一 day-one 软缺口 = 档案露出 **YUK-476**（后端 live、UI 零渲染）。冷启 day-one 先验仍是底色（记忆 `feedback_cold_start_first`）。

**两条主 project（B 的地基）**：① **领域模型重构 YUK-203**（机械基底：matcher/检索/B1-B5/event-sourcing/note 域）② **学习者全面档案 YUK-452 / A1-A15**（诊断档案）。**owner 排序：先把 event-sourcing 脊柱 YUK-471 浇筑好**，再做诊断露出（YUK-476 → ...）。状态 check 还发现：domain 0 假 Done、profile 0 假 Done；matcher（YUK-397）+ event-sourcing impl（YUK-471）+ misconception 一等节点 = 已建未通电/未落，但**不在 day-one B 关键路径**（act/later 深化弧）。

## NOW（单项目深度冲刺 = Cold-Start Openable Sprint）

> 模式转向（owner 2026-06-21）：从「捡 issue 吞吐」转「单项目深度冲刺到一条可感 milestone」。原则 [defer flip not build]（记忆 `feedback_defer_flip_not_build`）。执行图 `docs/planning/2026-06-21-cold-start-openable-sprint.md`（镜像 Linear Document 挂 YUK-452）。

- **目标 milestone = 「产品第一次能打开」**。**day-one 后端链路全通（2026-06-21，6 PR merged）**——剩余全是 UI，gated on claude design。
- **后端 ✅ 全落 main**：
  - **S** YUK-477 ✅ 薄科目-root seed + migrate（#546）· **P0** YUK-472 ✅ goal at-entry 直写（#543）
  - **U 后端** YUK-478 ✅（#550）上传无KC匹配→LLM分类subject+建子KC+AI生成参考答案（一次pass）+ 结构校验过 auto-promote；subject 走 LLM 内容分类（owner 拍 A，纯后端）
  - **placement 硬化** YUK-470 ✅（#547 /next锁+scope持久化+migration · #549 orphan-sweep）· **paper mastery_change** YUK-459 ✅（#548）
  - ~~P2 冷子图即时派题~~ → 降级 refill（YUK-474；discover 前沿=learning_item，day-one 空）
- **UI 流 ✅ 全建 + merged（2026-06-21，#551 Slices 1-3 + #552 Slice 4）**：
  - **Slice 1** welcome ① + 冷 `/today` 拦截 + goal（YUK-473）· **Slice 2** 上传屏 OnboardRecord ②a（YUK-478 余项，接自动入池骨架）· **Slice 3** placement 作答环 ③（YUK-475，flag 改 env-driven 默认 false、dev on）· **Slice 4** 起始档案 ④（YUK-476，per-KC p(L)/mastery + 可见 SE/precision/confidence）
  - Linear：YUK-473 ✅Done · YUK-475 ✅Done(已对齐) · **YUK-476 已交付但仍 Backlog（classifier 拦了我的状态翻转——待 owner 翻 Done）**。
  - #552 review 全闭环：12 thread resolved（OCR×10 修复 + CodeRabbit answeredCount/goalId 修复；window.location.search 一条回帖说明 skip）；独立 Opus review = SHIP。
- **承重缺口 YUK-482 = 富信号录入 bridge（3 决策已拍 2026-06-21）**：① propose = **内容驱动**（题面→matcher，与答对错无关；学生错误走错因+mastery 表现轴，见 `feedback_propose_is_content_axis`）② 冷启 review 闸 = **auto-approve + propose 事件留痕** ③ scope 链 = **placement-start live-resolve**（空冻 scope 时重算，显式窄 scope 保留冻列）。
  - **Lane A+B（承重，✅ merged #553）** 分支 `yuk-482-cold-start-bridge`：A=auto-enroll 加 `runColdStartBridge` caller（镜像 image-accept，空 knowledge_ids 分支建子 KC+参考答案+审计留痕）· B=placement-start 空冻 scope→`resolveSubjectKnowledgeIds` live 重算。→ 冷树上传后 placement 真有题。独立 review SHIP（HIGH「trail accept 即重复建 KC」已修+回归测试+验证轮）；gate 全绿。
  - **Lane C（③ 富信号，gated on owner 提取测试）**：手写保持像素→`student_answer_present`→`multimodal_direct` judge→错因/mastery。**owner 决策（2026-06-21）：ingestion 前上传按组处理**——extraction per-page→per-group（多图一起喂），跨页 spanning 的题/作答 stitch 成一单元。前置 cross-page；不扰 A/B（下游）。待定：grouping 单位（上传 session 即一组/一卷 vs 用户标分组）→ 是否需 asset group schema + 上传 UI。owner 在测：OCR 切图 / VLM 内容识别（草稿误判 figure 假阳性）/ 题图手写对应（a/b 分水岭）/ 跨页 / 图像增强。**→ 2026-06-22 端到端验证收口（记忆 `grading-vision-architecture` + `whole-page-vision-judging`）**：① 跨页 mimo StructureTask 自动合并已验；② **整页 Opus vision 判分 = 通用解**（看图独立解+读手写+评分，sample1/2 全对、修了 mimo Q8 翻车）；③ 本地切割全不可靠（GLM 手写 OCR 成幻觉、PP 碎框、mimo figure 门控 false-positive 把草稿当原卷图）→ 判分不需裁图；④ 统一契约 `grade(question, studentAnswer)`（文本/整页图/单题图），判分=小题。**新 follow-up：YUK-484（per-小题存储分叉 A/B）· YUK-485（ingestion 整页归属串题）。** 图像增强：自托管弱、Opus/VLM 不需要预处理（throwaway harness `scripts/figtest-*.ts` 未跟踪）。
  - **Lane D（⑤ 轴对齐清理）✅ merged #559（review-fix 9ba5a9f9 我审过=cleanup-only 干净）**：全拆「答错 → propose 新 KC」3 处(cron 删 + import/mistakes 外科式摘 propose,attribution/错因/mastery 全留)+ 死代码级联(propose.ts + KnowledgeProposeTask 删)。content-driven propose(proposals.ts)未动、KC 创建不丢。owner 拍全拆(非只 cron)。独立 review SHIP-WITH-NITS。
  - **prod flag flip**：`PLACEMENT_PROBE_ENABLED` + `WORKFLOW_JUDGE_AUTO_ENROLL_ENABLED` 在 NAS `.env`（YUK-470 硬化 ✅，剩部署翻转）。
  - **本机部署验收（2026-06-22）✅ 链路通**：4 flag 全开本地 dev:local 跑通整条冷启链——upload→R2→ingestion session→GLM-OCR+StructureTask+BlockAssembly+Tagging（全 mimo success）→auto_enroll(enroll 模式) enroll/review 闸正确→active question 入库挂知识节点→`/api/placement/start` 探测环服务该题（PLACEMENT_PROBE_ENABLED 识别、选题、KLP 打分）。本地老 DB 修复：补应用 drizzle 0046-0049 + reconcile journal + `scripts/migrate.ts` 种 `seed:{math,physics,wenyan}:root`（thin-seed 此前从没进过这库）。测试数据已清、seed root 保留。
    - **Opus OAuth vision judge ✅ 已验（2026-06-22，合成卷 + 真手写卷两次直 invoke）**：`ai_task_runs` 实测 `MultimodalDirectJudgeTask | prov=anthropic-sub | model=claude-opus-4-8 | success`（合成 $0.176 partial/0.5、真手写 $0.147 correct/0.95），无 `[vision-judge]` warn = **未静默回落 mimo**。真手写卷直验：Opus **精确转写手写过程**（Q'=Q/8、叠加法、7kQq/(36R²)）+ 正确判分。**judge 本体对真手写完全可靠。**
    - **❌ 组织内路径在真手写上 FAILS（实测）→ YUK-487（High）**：真手写卷走完整 auto_enroll，**StructureTask 失败 + GLM-OCR 只抓印刷文本** → block 的 `student_answer_present`/`extraction_evidence.handwriting` 未 set → `detectStudentWork=false` → 整页判分分支**从未被调**（`MultimodalDirectJudgeTask=0`，enrolled=0/review=1）。**瓶颈 = 可靠的 Opus judge 被 gated 在不可靠的 extraction flags 后**（与 whole-page-vision 哲学自相矛盾）。YUK-482「上传学生作答→挖信号」承诺在真手写上断在 gate。
      - **Fix A 已 ship（PR #560，待 owner merge）**：`detectStudentWork` 门 → `shouldGradeStudentWork`（scan source + 未评估手写则 fail-open 让 judge 自探；收窄到 SCAN_SOURCES 防 docx/manual 题库块被转走）。gate 全绿（typecheck/lint/db 54 passed/CI SUCCESS）+ OCR(承重)SUCCESS + 独立 opus review SHIP-WITH-NITS(已修)。`Closes YUK-487`，merge 时自动关。
      - ~~**Fix B = YUK-488（target，立项）**：整页 holistic~~ → **YUK-488 ✅ ship 窄版（PR #573，page-scope 判分图到该题页）**；owner 收窄、延迟 holistic。整页 holistic + 同页密集归属（YUK-485 headline，单页 no-op）残留 → **YUK-492**（Backlog）。
    - **cold-start bridge 本体未触达**（建子 KC + 生成参考答案）——seed root 种入后 tagging 匹配到 root → 走 WorkflowJudge 正常 route（非 bridge），故无子 KC、无 ref 答案；bridge 仅在 `knowledge_ids` 全空时触发 → **roots 一旦在，bridge 基本不触发，题全堆 root 下无子概念无参考答案**（设计取舍待 owner 拍）。
    - **findings**：YUK-486 ✅ merged（#572，auto_enroll 幂等门 = singletonKey+singletonSeconds + per-block FOR UPDATE claim）· `pnpm db:migrate`（drizzle-kit CLI）本地静默崩（journal=50 无 pending 仍 exit 1）；`scripts/migrate.ts` 程序化 migrate 路径正常 → dev tooling 问题非 schema 问题，记 `reference_dev_db_migration_divergence_fix`。
    - **NAS 翻转剩余 gate（YUK-479 已修）**：~~placement flag 被 YUK-479 单向门硬卡~~ → **YUK-479 ✅ merged（#572，source_verify FAIL 的 scoped demote）**；剩 vision lane 需 NAS `.env` 放 `CLAUDE_CODE_OAUTH_TOKEN`（缺则**静默回落 mimo**不报错，只能看 worker `[vision-judge]` warn）= 部署翻转项。
- **翻 placement flag 前必做**：YUK-470 orphan-sweep ✅ + **YUK-479 scoped demote ✅（#572）** → 代码侧前置全清，剩纯部署翻转（flag + OAuth token）。
- **UI 实现指令**：最新 claude design 原型 1:1；design↔backend 差异自主判断；动 UI 前 pre-flight（`feedback_claude_design_workflow`）。
- inc-A 锚题 ✅（#512）；inc-B 后端 ✅（YUK-468）。

## NEXT（就绪，排队）

- **🦀 Rust 滩头堡 Phase 0+ = YUK-495（新 project「Rust 同构核心 — 确定性 cold-start 引擎」`:crab:`，挂 initiative「私人教研团」；Phase -1 ✅ YUK-493 Done 移入同 project）**。owner 拍方向 2026-06-24/25，polyglot 工具链已立、续做诊断同构核（服务方向 B）。Rust 价值仅三机制：**WASM 进浏览器 · napi 原生重算 · 位精确确定性同构核**——绝不蹭 AI/LLM/OCR/DB（Rust ≈0）。红线：n=1 唯一禁区=拟合 item 参数、`b` 处处只读、flip owner-in-loop。
  - **owner 4 子决策（2026-06-25，全选更进取）**：① 建造顺序 = **#41 徽章先 + #125 一-KC EM 差分测试 rider**（硬风险先证，非 #123 能见 WOW 先）② **σ/exp = 共享多项式 exp (b)**——JS+Rust 同一固定多项式 exp/σ，σ 派生也 `Object.is`、**取消 ULP 豁免全核位精确**，Phase 0 头号交付 ③ **FSRS 也端口进 Rust**（#105 字面含 FSRS、Twin #30 用 Rust FSRS 非 JS 壳）④ **schema 现在加宽**（`mastery_state.theta_hat`/`precision` real→double precision，走 5-面登记仪式）。
  - **阶段**：Phase 0 WASM spike + 共享多项式 exp/σ → **Phase 1 首 demo** = #105 核 + #41 recompute 徽章 + #45 校准卡 + #125 diff-test rider + schema 加宽 → Phase 2 #123 prior 传播（inc-E dark-ship）→ Phase 3 #125 全量 → Phase 4 #124 plan + #30 Twin（+FSRS port）。
  - **⚠️ Phase 1 precondition（owner flag 2026-06-25，已 code-ground）= event-sourcing 状态**：SoT flip `PROJECTION_IS_WRITER` 只 gate knowledge/knowledge_edge 投影（与 θ̂ 解耦）；**mastery_state/θ̂ 是命令式 UPSERT/UPDATE + W0 state_snapshot 事件、非投影**。故 **#41 Tier-1 徽章 + sigmoid 替换 NOT blocked on SoT flip**（#41 从 4 标量重导显示算术，非事件重放）。动 mastery_state 前确认 3 件：① decision④ schema 加宽 ↔ W0 snapshot θ̂ 精度形态一致；② sigmoid→polySigmoid byte-identical-off flag 锚；③ #41 诚实边界=Tier-1 显示算术（现可做），**完整 θ̂-从-事件-重放(Tier-2/#46)需 θ̂ 进溯源脊柱=更大未来项非 Phase 1**。详见 YUK-495 Linear comment。
  - **🦀 进度（worktree `tlp-wt-rust-p0`）**：**Phase 0 ✅ MERGED（#596）**。**Phase 1：S6+S3 → PR #607 · S4 → PR #610，均 OPEN `Refs YUK-495` 等 owner merge**。**S4（schema 加宽 ④）= mastery_state.theta_hat/precision real→double**（4-agent grounding workflow 抓两隐面：① audit-schema-writes 的 NATIVE_COLUMN_CONSTRUCTORS 漏 doublePrecision→两列静默掉出 drift 检测，已补 ② cascade-revert `float4Eq→thetaExactEq`——加宽后 Math.fround 会掩盖亚-float4 漂移废掉护栏，flip 成 exact `===`+加 drift-catch 证明 test）；SCHEMA_VERSION 不 bump（列改）；gate=test:migration 20(data_type teeth)+db 92+unit 52；独立 opus review SHIP-WITH-NITS（无法构造 false-conflict θ̂，~20 对抗 f64 全位精确 round-trip；NIT-1 stale real 注释已修）。**🔖 follow-up（潜伏非阻塞，Linear 断连待补 YUK-495）**：legacy 迁移前 float4 行 + exact guard 会 false-conflict，但 `orchestrateCascadeRevert` 无 live caller=潜伏；live wiring 前需硬化（一次性 normalize 或 fround-tolerant legacy 路径）。
  - **🦀 Phase 1 S6+S3 → PR #607 OPEN（rebase 在新 main=53bc8ba8/W3 上，零重叠）等 owner merge**：S6=#125 一-KC solver（grid-fold 累加序最大新风险位精确退役，TS↔Rust Object.is）· S2=event-sourcing 3 点 precondition code-ground 清（decision④ widen 干净=snapshot 已 f64）· S3=σ→polySigmoid dark-ship swap（flag OFF=Math.exp byte-identical，138 测试绿）。**剩 S4（schema 加宽 ④，已 unblock，后端 migration）· S5（#41 徽章+#45+WASM loader，design-gated）**。
  - **🦀 Phase 0 详情（已 merged #596）**：**U2（decision②）= 共享多项式 exp/σ 跨语言 Object.is 位精确**（stress grid + 1000 随机），准度 vs Math.exp = polyExp 1 ULP / polySigmoid 2.2e-16 → 替 live sigmoid 不可见 = σ 确定性缺口闭合。**U1 WASM = build de-risked**（napi crate 原样编 wasm32 unchanged；发现 napi v3 硬链 futures → wasm 需 WASI threads loader，execution-parity = Phase-1 plumbing 非风险）。独立 opus 对抗 review SHIP-WITH-NITS（8.2M+ 对抗输入 0 divergence；抓修 polyExp 负尾 -745→-708 守卫 bug）。gate 全绿（cargo/biome/typecheck/poly 6+parity 3+native-parity 12/audit:partition no-P0）。findings `docs/design/2026-06-25-rust-coldstart-phase0-spike.md`。
  - **126-点子 triage 全过 workflow（2026-06-25，55 agent / 4.9M tok，逐点对抗 verify）→ doc `docs/design/2026-06-25-rust-catalog-triage.md`**。净结果 **COMMIT 28 · DEFER 92 · DROP 红线 7 · DEV-TOOL 1**：
    - **COMMIT spine 6**（#105/#41/#123/#125/#30/#124）+ fast-follow 22（Trust 簇 #44/#42/#47/#110/#52 · 选择器 #67/#69/#14/#16 · WASM judge #2/#1/#4/#5 · 图 #56/**#58 ring 现在建**/#61 · meter #88/**#91 needle 现在动** · #45/#82/#87）。
    - **DROP 红线 7（do-not-build register，记 why 防再提）**：#66/#68/#76（目录已 ❌）+ **triage 新抓 #70 BKT/PFA slip-guess · #71 item-fit · #74 temporal-DIF（"within-person"伪装，最险）· #77 EB b prior**——全在拟合 item 参数/需跨人方差。
    - **机制伪装（建则 plain-TS 非 Rust）**：#15/#54/#59/#84/#96/#119（LLM 伪装或 JS 可扛）。
    - **DEFER 92** 按成熟度分层（Tier-0 off-spine / T1 ~tens attempts / T2 ~weeks decay / T3 sessions·forecasts），tag unlock_condition 随数据自浮。
  - **每 PR gate**：cargo build + 差分 `Object.is`（决策②后 σ 也纳入、无 ULP 豁免）+ typecheck + biome；不变量 seed-not-closure / 无 FMA / 冻结 GRID_THETA / EM sweep 序确定；addon dev/CI-only 不进 prod。`Refs YUK-495` 全 scope 完才 `Closes`。
- **openable sprint 之后**：inc-E prereq mastery 传播（YUK-455，dark-ship）→ inc-D AutoElicit / inc-F 模拟 elicit。
- **旧 loop-wiring 步骤**（Step1 /mistakes 已落 #509 系列；Step2 never-attempted source / Step3「练这道题」门 / Step4 死 CTA+notFound / Step5 `pnpm up`）——与 openable sprint 部分重叠，未折入的边角随冲刺顺手清。
- **profile/教研团 冷启 prior 设计**（openable 通电、有真实交互后据外部模式落地）。

## PARKED（已捕获，不是现在）

- **🧠 误区(misconception)建模调查报告 + 实施建议 ✅（2026-07-01，doc `docs/design/2026-07-01-misconception-model-investigation-and-implementation.md`）= 待 owner 拍 phases + 5 open questions**：owner 问「misconception 该是什么形态 / A vs B 之外」→ 13-agent 研究工作流（5 路文献 R1-R5 + 2 路 code/design 对账 + 综合 + 3 视角对抗 + 和解）。**结论 = 「B-live + authored 内容层缝」**：既非 A（M 自带 mastery，被 `.strict()`+ADR-0036 §3 硬禁 + 全文献否定单调 belief_strength），也非 B 独存（纯 KC 状态枚举，只覆盖 KC-pair 混淆）；正确形态 = 内容层身份实体（无 mastery、confidence-only、`caused_by→多 KC`）+ 每学习者 `kc_typed_state` 推断态 + **mastery 永归 KC**（CBM 兜底）。**载重修正**：本地曾落后 origin/main **20 commit** → YUK-531 S4 其实**已 dark-ship 整套 RT1 身份基建**（misconception+`misconception_edge`+promote writer+reconcile 环+`MisconceptionList` UI，flag `MISCONCEPTION_PROMOTE_ENABLED` OFF）；已 `git pull --ff-only` 同步至 `b0ceac0b`（我此前口头「edge 不存在要建表」是读陈旧本地树，已更正）。**真正未做 = 接线非建表**：① Phase 1 probe-resolution loop（`reconcile.ts:268` 硬编码 `confused_with_kc_id:null` → kc_typed_state 混淆轴恒 inert，**90% 的解锁**）② Phase 2 人工 authoring 端点（真新增量→批准后落 YUK-531 follow-up）+ 翻 flag 决策（建议先只开人工 authoring、机器自动晋升 defer——n=1 warrant 不成立）③ Phase 3 YUK-533 `confusable_with` 死边消费 ④ Phase 4 开放题 cause→misconception retrieve-rerank（YUK-454 embedding deferred）。doc 经独立对抗 review（2 BLOCKER〔虚构审计钟 + 3 处陈旧行号〕+ 5 MINOR 全修，publish-ready）。**Linear Document 挂 YUK-531。**
  - **↳ 深答 open-Q#4「翻 MISCONCEPTION_PROMOTE flag 吗/怎么翻」= 第二份 doc `docs/design/2026-07-01-misconception-promote-mechanism.md`（11-agent 设计工作流）**：当前晋升 = `recurrence≥2 → accept → 铸 soft`，证伪原语（scorePrediction/discriminating/probe）**全建好但喂 inert kc_typed_state 轨、没接进晋升**。"更科学" = **把证伪机器接进 promotion + 可区分(held-M vs 缺技能, R3 SISM)两轨决策替换裸计数**。三对抗 reviewer 收敛：**区分是定性、n=1 安全的，不需贝叶斯**。**分 Tier：Tier1（区分门+稳定性+判决性实验二值+dedup+可达但 dark 的 hard 轨+F1 bug 修，全 JS 零新列零 ADR-0046 张力，推荐 ship）/ Tier2 贝叶斯后验（Rust-first，defer）/ Tier3 共存动力学（n=1 不建）**。抓到真 bug **F1**：promote.ts:175-188 onConflictDoUpdate 无条件覆盖 source+archived_at 且无 advisory 锁 → 两轨并存后重 accept 会静默降级 hard→soft（Tier1 硬前置同 PR）。**修正投资 doc**：reconcile.ts:268 = confused-with-X 轴（KC↔KC），与 promote 的 held-M 轴**不同轴**，正交、不在 promote scope。**Linear = YUK-536（提案，Backlog，parent YUK-531，related YUK-454 晋升 owner + YUK-532 flag 前置）+ Document 镜像挂 YUK-531**。待 owner 拍 Tier + 4 旋钮。

- **🦀 Rust 同构核「算力兑现」tripwire（2026-06-26，实测埋线）**：教研团负载实测**尚未起来**——两层皆空：① 数据侧 n=1，live `event` 表仅 **65 个 scorable**（49 attempt + 16 review）；② 计算架构侧 worker 23 个 job **全 `queue: llm/agent`（I/O-bound），零纯算力 job**，重计算数值核（grid-Bayes `v-grid-fwd` / bootstrap CI / MC / EM）现仍是 dark 离线脚本+测试，未进队列。**生产级合成基准**（`scratchpad/grid-bayes-bench.mjs`，镜像 v-grid-fwd 内循环，node 26.3.0）实测:5万 event×200 网格×2000 bootstrap = 峰值 RSS **187 MiB**、前向吞吐 **42万 event/s**；拉到 200 万 event 峰值才 **330–376 MiB**（/7.8GiB VM = 噪音）；`Float64Array` vs `number[]` 算术近乎同速（V8 packed-double）。**结论**：① TS 纯算术不慢、内存峰值是噪音 → 现状不需要 Rust；② 真正 TS 税在 **allocation/GC churn**（bootstrap 5万×2000 = 19.3s 全是分配回收），这才是 Rust（零分配/无 GC/原地 sort）唯一甜区，且正是同构核 YUK-495 瞄准的负载形态；③ Rust 算力理由现在是**预支**（cold-start-first 照建照接线，per [defer flip not build]），兑现要等数据门。**tripwire（重跑基准量真实加速比再决定 napi 版是否 promote）= 当 ① 单学习者 scorable event > ~5,000 **或** ② 任一数值引擎转 recurring worker job 时**。关联 BLOCKED-ON「教研团 YUK-405 ← 真实数据/交互历史」+ NEXT「Rust YUK-495」。

- **🆕 栈瘦身 / overhead audit（2026-06-24，owner 起念「砍 AI+math → Rust」→ 调研收口）= 待 owner 拍是否进实施 + 是否落 Linear**：两 workflow / 11 agent / 1.1M tok 调研。**Rust 否决**——AI pipeline I/O-bound（LLM 往返主导，Rust 提速 ≈0 还丢 Agent SDK）、math 路径实测 sub-µs（`bench.mjs`：θ credit / BKT @1M iter 均 <1µs/call）= 无 CPU 热点。**真 ROI 杠杆（非 Rust）按序**：① Phase 0 deps 卫生（删孤儿 Next.js ~712MB）→ node_modules −65% ② Phase 1 进程合并 3→1 → −~100MB ③ Phase 2 镜像瘦身 → −461MB。**⚠️ Phase 1 需审视**：与「pg-boss worker 必须独立进程（LISTEN loop 不消费 boss job）」记忆 `project_dev_topology_worker_required` 表面冲突——合并前先核是 dev 拓扑还是 prod 容器面、会否破背景 AI job 消费。综述只活在 `.remember/now.md`，**未落 doc / 未开 Linear**（要进实施先建 issue + 写 planning doc）。
- **🆕 Rust 滩头堡 Phase -1 ✅ 全落 main（2026-06-25，PR #584 impl + #591 docs 均 merged）= YUK-493 Done**。详见 NEXT「Rust 滩头堡 Phase 0+」。`crates/calibration-native`（mulberry32+forwardAuc+deltaAucClusterBootstrap+resolveBootstrapB+percentile napi）+ 差分测试 `native-parity.unit.test.ts`（导真实 JS oracle，**12/12 Object.is 位精确**含 Layer 3 DeltaAucCi）+ `pnpm build:native`；`.node`/target gitignore、Cargo.lock 提交、dev/CI-only 不进 prod。review 轮 augment×3（labels `Vec<f64>` 修 ToUint32 parity gap + 差分用例锁死 · skip-if-absent · mulberry n-guard）+ OCR×4（napi-cli 钉 @3.7.2 via dlx 守 §5.1 · total_cmp 去 clone · profile 注释）全修；Cursor Approval PASS；owner merged。设计 SOT 三份在 main：`docs/design/2026-06-24-rust-napi-calibration-beachhead.md` · `2026-06-25-rust-product-ideas-catalog.md`（126 点子 + user=1 过滤 ✅91/⚠️31/🔻1/❌3）· `2026-06-25-coldstart-diagnostic-coach-sketch.md`（冷启五件套红队修正版）。
- ~~**活 bug**：YUK-358 Decision1 — apply-note-patch 无 user_verified guard~~ **已修正（2026-06-21 re-excavation）**：guard 已存在——`src/core/blocks/is-verified-block.ts`（`isVerifiedBlock`，RED-4/ADR-0040 决定7）+ applyNotePatch guard（`note-refine-apply.ts:36` 明示「guard ON，仅 accept-path 豁免」）。先前条目是 stale，审计「决定1 已落」才对。
- **Linear 卫生**（审计 `w8iz32mse`，待 owner 批）：close YUK-303/306（已验证 done）、YUK-373/375/360 移出 In Progress、YUK-397/310 重写 scope。
- **C7-C10 matcher cleanup**：折入 YUK-397，随 inc-5 / live-caller（C7 cosine 标定 data-gated / C8 cause→JobData / C9 verifyEventId 直返 / C10 promote-FSRS 去重）。
- Step6 [ops] NAS always-on 部署（`TUNNEL_TOKEN` + compose up，运维决策）。

## BLOCKED-ON（在等什么 — 冷启修正后多为「需先验」非「纯等数据」）

- **profile P2 翻 flag**（misconceptionRecurrence / B4 answer_class filter）← 需 ① 真实 attempt 数据 ② **冷启 prior 设计**（不是纯等数据）+ B4 seeded 验证 + misconceptionRecurrence N+1 批处理 `TODO(flag-on)`。
- **matcher 接 live caller** ← 题库规模 + Step2 feeder 验证（小题库 cosine 无意义）。
- **教研团 YUK-405 / 记忆 YUK-322** ← profile 有真实数据 / 交互历史。
- **A9 step-grading 倍增器** ← gated B5 / YUK-350 judge 校准。
- **D4 RT1 misconception 图节点**（渲染「指向此点的误区」）← **✅ UI + RT1 backend 已交付（YUK-531 5-PR 链 @b0ceac0b，dark flag OFF）**；剩 confirmed 段真数据 = 翻 `MISCONCEPTION_PROMOTE_ENABLED` flag（owner 运维决策）+ confirmed-archive 写 YUK-532。

## 在飞（PRs / workflows / worktrees）
- **✅ 2026-06-30 = 🧠 YUK-531 A5 S4 误区 MisconceptionList + RT1 误区身份基建 = 5-PR 链全部 MERGED → main @b0ceac0b，YUK-531 → Done（全 dark，flag `MISCONCEPTION_PROMOTE_ENABLED` OFF）**。owner 拍 B = 建完整 backend；driver ulw 自主推进 5-PR 链，每 PR gate 绿 + 独立 opus review + 1-2 轮 OCR → 自主 rebase-merge。8-agent grounding workflow 判定 **GO**（gated 的只是 `k` 阈值 runtime FLIP，代码不 gated；前提 3 处 stale 已校正：kc_typed_state=YUK-440 非 437 / reconcile 已解 / recurrence substrate mis-wire）。owner 二问：day-one=RT1+候选 fallback funnel / scope=完整一次推完（含 confusable_with + reconcile 环 + 全多态拓扑闸）。doc-of-record `docs/design/2026-06-29-yuk-a5-knowledge-explorer-build-plan.md` S4 段已补全；决策+校正+收官落 YUK-531 Linear comment。
  - **PR-1 schema 基底（dark）→ PR #682 OPEN（base=main，`Refs YUK-531`）**：misconception 加 status/source/seen/evidence 4 列 + 新 `misconception_edge` 多态表 + 全 5-surface（SCHEMA_VERSION 4.11→4.12 + FK_ORDER + ALL_TABLES 补漏登记的 misconception）+ Zod `.strict()` 锁步。3 commits（schema + 独立 review fix[1 MED+3 LOW+2 NIT] + bot fix[OCR+CodeRabbit 共识 from_kind=literal/weight bound]）。**独立 opus review=APPROVE（0 CRIT/HIGH）；OCR 5 thread 全 resolve+rationale；CodeRabbit confusable_with major=defer PR-3 已回帖**。gate 全绿（typecheck/biome/audit:schema 0-unallowed/migration 20/backup-reverse-lockstep 26/build/unit 78）。**✅ rebase-merged → main @1093c045**（CI 全绿 + opus APPROVE + threads 全 resolve；ulw 自主合）。worktree `tlp-wt-yuk531-schema` 可清。
  - **PR-2 平行多态拓扑闸（opus，纯逻辑）→ reland PR #684 OPEN（base=main；#683 closed superseded）**：`checkMisconceptionEdgeTopology` = endpoint-kind（from 钉 misconception + per-relation to-kind，**Object.hasOwn 防原型链 crash**）+ self-loop + symmetric_redundancy(confusable_with warn)；非克隆同構闸。gate 绿（typecheck/biome/unit 22/partition）。**独立 opus review = COMMENT（0 CRIT/HIGH）；MED 原型链 crash 已修+test**。**#683 stacked base 因 #682 合后失效 → 按 #644 教训 rebuild 到 fresh main（cherry-pick gate commits）而非 force-push（git-guard 拦）**。**✅ OCR 2 轮 pass（round-1 DRY:EXPERIMENTAL_RELATION 单一真相源修）+ 主 gate 绿 → rebase-merged → main @b324b839（ulw 自主合）**。worktree `tlp-wt-yuk531-topo` 可清。
  - **PR-4 读模型+route（fallback funnel）→ ✅ rebase-merged → main @df1a1b0e**（reland #685）：`GET /api/knowledge/[id]/misconceptions` = confirmed(caused_by join, status=active) ⊎ candidate(listProposalInboxPage filter knowledge_id，**非 loadPrepDeskConjectures**——grounding 抓 build-plan §98 不可行：全局 cap-3 无 knowledge_id)；conf server 端离散定性；honest empty。独立 review COMMENT（MED evidence kind-filter 修 + 2 NIT test + LOW comments）+ OCR 2 轮（confirmed LIMIT + sort tiebreaker 修）全 resolve。
  - **PR-3 晋升 writer + 命令式 reconcile 环（opus, dark）→ ✅ rebase-merged → main @8083a052**（#686）。misconception-edges.ts 写咽喉 + misconception-promote.ts env-getter+确定性 id（sha256 cause::kc）+ 命令式 reconcile 环 dark-未接 + DB CHECK + 删 allowlist 18 列 + 审计表 5-surface 4.12→4.13；conjecture-accept 包 db.transaction、promote hop flag+k gated、flag-OFF byte-identical。**独立 opus review = APPROVE（0 CRIT/HIGH；tx 原子/ND-5 软轨机检全验）**。CI step9 dormancy 不变量随加 writer 过时 → fix(step9 休眠→单写者 + NaN-confidence guard + 跨-proposal upsert test，跑全量 unit 3460)；OCR 2 轮(atomicity major=opus-approved 设计 skip-rationale / `== null` minor=biome 允许 skip-rationale) 全 resolve。**flag-flip 前置（status active/draft、evidence union/replace、reconcile wire、atomicity）落 YUK-531 Linear comment**。
  - **✅ 全 4 backend PR 落地 main @8083a052（PR-1 schema + PR-2 拓扑闸 + PR-4 读模型 + PR-3 writer）= S4 RT1 误区 backend 完整 ship（dark, flag `MISCONCEPTION_PROMOTE_ENABLED` OFF）。**
  - **PR-5 UI MisconceptionList → ✅ rebase-merged → main @b0ceac0b**（#687，收官片）。**owner 拍 veto scope = A**（UI section+card + candidate-dismiss veto 接 dismissAiProposal[live] + 两段视觉区分；confirmed-archive 写延后 = YUK-532）。pre-flight 批准（SECTION in /knowledge/$id，port screen-knowledge-a5.jsx）。**review = 4-finder 聚合（correctness/removed-cross-file/⑥红线-约定[CLEAN]/cleanup-测试）+ 组件隔离视觉验证（真 Tailwind 渲染 + DOM/computed-style/几何/交互四探针，整体与 loom 同构、⑥红线 PASS、诚实空态 PASS）**。fix 批次 = 7 findings（kind-guard/onError 回滚/confirmed 诚实化-disabled/evidence 防御/segment+409 测）→ d7a548d0；**OCR 3 轮**（round-1 clean / round-2 修 7+4 minors[重入守/422 不泄 kind/apiJson 类型/disabled hover]→b63710fc / round-3 残留 3 全 dormant-or-polish 捕获）；**全 bot thread resolve（含 round-3 的 applyVeto undefined-guard major = 无 live caller 硬化点，折 YUK-532）** → ulw 自主合。
  - ⚠️ stacking 教训：父 PR rebase-merge 后子 PR 必 cherry-pick rebuild 到 fresh main（force-push 被 git-guard 拦）；但**兄弟 PR 都 off 同一 origin/main 基则互为可合**（PR-3/PR-4 不相交、base 仍祖先，无需 rebuild）。
  - merged worktree 全清（schema/topo/read/promote/**ui** 已清）。
  - **follow-up Linear（收官已建/对齐）**：**YUK-532**（confirmed-archive veto 写 + confirmed-live UI 硬化[applyVeto segment 不变量 + .kd-misc-veto-deferred CSS]，翻 flag 前置）· **YUK-533**（confusable_with 边下游消费者 = 组卷对比辨析题，死边接 live）· **YUK-534**（针对性练习 KC-scoped nav + veto error live-region a11y）· **YUK-529 → Canceled**（(a)派生读被 B 取代，candidate 段实质已交付 PR-4/5）· YUK-530（S3 DiagnosticDrill CDM/IRT，gated ADR-0035 #4）· YUK-528（S3 difficulty hasAnchor）。

- **✅ 2026-06-29 ulw babysit → MERGED**：**PR #670（YUK-521 A4 出手强度轴）rebase-merged → main @ `7cb0696d`**（8 commit per-commit Refs 保留）。CI 全绿 + 本地独立 opus review 过；8 条 review 线程逐条 code-ground = 失败模式全 fail-safe（`completion:'A'` auto-apply LIVE，breaker 全局 rate 计数/TOCTOU/锚非原子皆 fail-safe，3/4 已 in-code 文档化）→ 全捕获 **YUK-525**（父 YUK-521 硬化单，镜像 YUK-499/512）+ 8 线程全 resolve。**YUK-521 → Done**（完整 A4 infra scope，选设计约束 §1 出路 (b) 建 A 档 auto+undo 车道）。worktree `yuk-521-a4-strength`/分支 `worktree-yuk-521-a4` 合后可清。

- **✅ 今天（2026-06-27）全部 merged 进 main（HEAD `f7609b9d`=origin/main，0/0）**：#632(YUK-406/440 U3 probe)·#633(U4 备课台)·#634(YUK-495 #41 recompute，**Closes YUK-495→Done**)·#635(YUK-349 B3 frontier PR-1)·#625(YUK-504 tsgo)；🦀 **Rust Phase 1 全 merged**：#596(P0 poly-exp/σ)·#607/#610/#613(S2-S6)。follow-up 已建：YUK-508/509/510/511/512。stale worktree `tlp-wt-rust-p0`/`tlp-wt-laneB`/`tlp-wt-rust-wasm` 可清。
- **🟢 教研团 A13 U8 在飞 → PR #638 OPEN（`Closes YUK-440` + `Refs YUK-406`，停等 owner merge）**：reconcile loop 接进 `research_meeting_nightly` 闭合 A13 dark-loop（producer U3 终有消费者）；3-opus 对抗 review+仲裁（收敛同 2 finding：anchor 写序防 partial-failure 丢 ledger + read-only 毒丸 guard，已修+回归测试）；gate 全绿（typecheck0·lint·audit×4·test 266f/2699·build·reconcile unit11+db5）。worktree `tlp-wt-u8` 合后可清。**YUK-440 code-complete（U5-U8 全落）；YUK-406 剩 felt 备课台卡片 UI = claude-design-gated**。
- **🟢 在飞 = 🦀 Rust Phase 2 PR-1 = PR #637（`YUK-513` #123 prereq prior-propagation, inc-E）, owner-merge-ready**（worktree `tlp-wt-rust-phase2`，branch `yuk-513-phase2-prior-propagation`，HEAD `e2c41f63`）。`propagate_priors` Rust-first 核（crate 首个图算法）+ **23 Rust-native KAT**（∏ 精确判别 bit-for-bit/深度复利 C<B<A/菱形 DAG/三守卫）。设计 **owner-in-loop 多轮收敛**：`gap=1−∏E_mastery`（KST surmise 合取，**非 Σ/avg**）+ `weight=σ(−s·θ)` bit-exact（复用 poly_sigmoid，无 FMA）+ `argmin` attribution（**强度/身份正交**——短板身份走 attribution 不挤进标量）；跨域用「E_mastery 标量坐标无关」化解 offset-grid 风险（⑦）。**2 独立对抗 opus review**（n=1 红线直攻不破 + bit-exact 确认，0 CRIT/HIGH，findings 全修）+ 全承重 CI 绿 + Cursor Approval PASS + **OCR 2 minor 修+resolve**（fix commit OCR re-run pending）。`#![deny(unsafe_code)]`（实测 `forbid` 与 napi-derive E0453 不兼容）+ **amend ADR-0046 §6**（code↔ADR lockstep）。**触 crate → owner-merge：`gh pr merge 637 --rebase`**（`Refs YUK-513` 非 Closes——Phase 2 还有 PR-2/3；合后清 worktree）。**Phase 2 剩**：~~PR-2~~ **→ PR #644 OPEN（见下）** → PR-3（UI badge「最弱前置:X」design-gated，末 PR `Closes YUK-513`）。研究锚 ALEKS/KST surmise（`docs/design/2026-06-20-cold-start-day-one-design.md` §1.3）；设计对话见本 session。
- **🟢 在飞 = 🦀 Rust Phase 2 PR-2 = PR #644（`Refs YUK-513`，stacked base=`yuk-513-phase2-prior-propagation`）**：TS dark-ship 接线——flag `PREREQ_PROPAGATION_ENABLED`（定义在 `@/core/theta-grid` 镜像 `THETA_GRID_ENABLED`，便于 getter-mock 同改 wrapper 内部读+route）+ binding wrapper `src/server/coldstart/propagate-priors.ts` `loadDayOnePriors`（skip-if-absent NO-OP）+ `placement-profile.ts` `ProfileKc.day_one_prior?` dark field（镜像 `sigma_mode` 先例）。**建模决策（dark/可逆，loud 注释）**：day-one 纯拓扑——b=0/θg=0 全 KC，prior 只反映 prereq 结构（user-independent 可复现 = byte-identical-off 锚）；`DAY_ONE_SHRINK_COEFF` owner const 不估计（n=1 红线）；`mean_mastery` 经共享 poly σ ≡ kernel 内部 E_mastery；边方向 from=prereq→to=dep（topology-gate 确认）。**gate**：typecheck（tsgo+真 tsc 5.9.3 双验）·biome·audit:partition/schema·db 14/14（0 skip，kernel-driven 真跑 worktree .node）。**CI 首跑红 = created_by AgentRef shape（`{by}` 非 `{actor_kind}`）本地 tsgo 漏、CI tsgo 抓**→fix `953a464f` 推（教训：tsgo 非确定/有 gap，可疑处用真 tsc 交叉验）。**独立 opus review = SHIP**（0 CRIT/HIGH/MEDIUM，7 load-bearing 主张全验证：边方向/id round-trip/byte-identical-off/n=1/mean_mastery bit-parity/getter-mock/测试充分）。**OCR 3 轮 11 finding 全处理 + 11 threads 全 resolve**：2 major = flag-on kernel 异常此前会冲破 live placement-profile 路由 500（独立 reviewer 漏、OCR 抓）→ **try-catch 降级 NO-OP+log**（cycle 测试证 route 仍 200）；其余 = couple 字段（双向 airtight）/拆 zeros 数组/meanMastery length+value drift guard/widx bounds；1 个（NODE_PATH cwd-relative）decline-with-rationale（建议的 import.meta.url 在 esbuild bundle 后反断；prod loader = PR-3 激活前置，已落 YUK-513 comment）。**Cursor Approval = SUCCESS**（typecheck 修复后转批准）。承重 CI 绿。**7 commits**（feat+typecheck-fix+硬化+resilience+OCR 验证轮×3）。
  - **⚠️ MERGE-ROUTING 事故 + 恢复（2026-06-28）**：#637 已 merged 进 main ✓。**但 #644 的 base 是 `yuk-513-phase2-prior-propagation`（PR-1 分支），#637 squash-merge 时没删该分支 → GitHub 没 retarget #644 → owner 把 #644 合进了那条 dead 分支,PR-2 改动从未上 main**（#644 显示 merged 但 main 无 propagate-priors.ts/flag/day_one_prior）。我的疏失:#637 合后该把 #644 base 改成 main。**恢复 = PR #652**（`yuk-513-pr2-reland`，base=main）:把 PR-2 的 7 commit cherry-pick 到当下 main,解决与 **YUK-445（A11 axis 描述符）** 的 `placement-profile.ts` 冲突（4-entry Promise.all + axis/day_one_prior 双 dark field 并存）。内容 = 已审 #644（opus SHIP + OCR 11 全修）+ 仅冲突解决。gate:tsgo+tsc+biome+audit×2+**db 19/19（0 skip,axis 回归+dayone+kernel 全跑）**。**owner-merge:`gh pr merge 652 --rebase`（这才真上 main）**;dead 分支 `yuk-513-phase2-prior-propagation`/`yuk-513-phase2-ts-wiring` 可删。教训:**stacked PR 的 base 在父 PR 合后必须手动确认 retarget 到 main,否则会合进 dead 分支静默丢改动**。
  - **#652 OCR 又抓一真问题 + 修（commit `328e9648`）**：flag `PREREQ_PROPAGATION_ENABLED` **三方撞名**——并行合入的 **YUK-455** 已用同名两处（`src/core/prereq-propagation.ts` A6 定向 θ̂ 传播 + `src/server/mastery/prereq-propagation.ts` A13 向后风险 producer,都已接 live submit/placement),我 PR-2 加了第三个。三个是不同 inc-E 机制 → 我的改名 **`DAY_ONE_PRIOR_ENABLED`**（只 gate day-one prior 读面）。顺修 OCR/CR 4 nit(loadAddon dlopen-fail 加 log/`if(!post)` 改 throw/errSpy try/finally×2)。5 threads 全 resolve,db 19/19。YUK-455 自身就有两个同名 `PREREQ_PROPAGATION_ENABLED`(core vs server,gate 不同机制)。**设计文档钦定名被三个并行 inc-E PR 抢注 = 跨 PR flag 命名需协调**。owner 拍 option-1（改名消歧）→ **PR #655 OPEN**（`refactor(mastery)`，`Refs YUK-442 + YUK-455`，base=main）:A6 core→`PREREQ_THETA_PROPAGATION_ENABLED`、A13 server→`PREREQ_RISK_EMIT_ENABLED`,各加「renamed from」breadcrumb,消费/测试/注释全更,无行为变更（仍 default false）。gate:typecheck+biome+unit 13/13+db(state+submit)118/118。**#652 又修 stale 注释**（`d72b9789`:placement-profile.ts 两处旧 flag 名注释→`DAY_ONE_PRIOR_ENABLED`,OCR 0.90,改名时漏的）。**✅ #652 MERGED（9a2fd4d1，PR-2 真上 main，内容已确认）。#655（flag 消歧）CLEAN 待 owner `gh pr merge 655 --rebase`**;两者文件零重叠。dead 分支 yuk-513-phase2-prior-propagation/ts-wiring/pr2-reland/yuk-455-flag-rename 合后可删。
- **🧱 YUK-471 Wave 3 event-sourcing (artifact + question_block) = driver 全自主推进中（2026-06-26→27；owner「ulw, merge all, continue until finished」+ AskUser「full autonomous drive」）**。foundation + C1 全落 main，每 lane = worktree + opus impl(TDD/DB-gated) + 独立 opus review + bot(CodeRabbit/OCR 真有料) + squash-merge：**C0**(no-op，GIN 索引 `event_payload_idx` 已在 migration 0005) · **A1**(artifact event schemas #606) · **A2**(question_block schemas #608) · **B1**(foldArtifact #609) · **B2**(foldQuestionBlock #611) · **C1-β**(artifact create cutover #612) · **C1-δ**(question_block cutover #614，DB-gate 抓修 flat8ToBBox latent bug) · **C1-γ**(artifact edit/lifecycle/mutator #616) **全 MERGED**。**每 artifact 写已 fold-visible（KNOWN-UNFOLDED 空 = flip prereq 达成）**。**✅ Wave 3 CODE-COMPLETE（main @ed35a66d）**：C2 #617(gather hoist+artifact 入 index+backfill) · C3 #618(assertXParity+per-entity flag+audit:projection+硬化) · qb-lifecycle #619(5 eventless qb 写补 canonical event) **全 MERGED**。**两 entity（artifact+question_block）每写已 fold-visible，KNOWN-UNFOLDED 空**（独立 review 实证无残留 eventless 写）。**D runbook = PR #620**（per-entity flip 程序 + 4 前置；**SoT flag flip = owner-run prod op，driver 不执行翻转**）。canonical 设计 = `docs/superpowers/plans/2026-06-26-yuk471-wave3-artifact-structured-design.md`(#602)。Linear follow-ups 已建：**YUK-502**(legacy overflow-bbox sweep = qb flip 硬前置) · **YUK-503**(set_status parity 测试 + record_promotion 校验 + question_block writers audit) · **YUK-499**(ON-path version-guard 扩 artifact/qb)。**flip 顺序：artifact 现可翻 → question_block(待 YUK-502 sweep) → learning_item(W2，artifact 之后)。**
- **🦀 Rust Phase 1 ✅ DONE（全 merged，YUK-495 Closes via #634→Done）**：#596(P0 poly-exp/σ)·#607(S6 #125 一-KC solver + S3 σ→polySigmoid dark-ship)·#610(S4 schema 加宽 real→double)·#613(S5 WASM execution-parity，server napi ≡ browser WASM ≡ JS oracle bit-for-bit)。**S5 #41 recompute 徽章 + #45 校准卡视觉仍 claude-design-gated**（functional handoff `e0bba466` 已 ship；视觉走 claude design per `feedback_claude_design_workflow`）。**接力 → Phase 2 见上「在飞」YUK-513 PR #637**。Phase 3-4（#125 全量 / #124+#30+FSRS port）未启。
- **🧭 捕获（非 Rust 轴，本 session global-vision 扫出，留 owner，未动 Linear）**：
  - **#611（W3-B2 foldQuestionBlock）✅ 已 MERGED**（2026-06-26T14:49Z，= main HEAD `abc85e16`）—— global-workflow 标的 `UNSTABLE` 是合前瞬态，**无需处理**。
  - **#590（YUK-494 worker bundle fix）DRAFT** = `externalize claude-agent-sdk in build:worker`，prod worker 能否打包的承重 build-fix（关联「main 未合前别 `--build`」），**值得优先看**。
  - **#588（YUK-360 mem0 A/B cost）BLOCKED**：prod 上 inert（shim patch `globalThis.fetch` 但 mem0→`node-fetch` 绕过 → 0 cost 行；CI 绿是假阳性），CRITICAL+HIGH 待修再合。
  - **Linear 假 In Progress 待对齐 Done**（cockpit 标已完成、Linear 仍 In Progress）：YUK-493（Rust 滩头）· YUK-461（校准 harness merged #532）· YUK-448（paper RT merged #533）· YUK-494（worker fix #589）。28 个 In Progress 整体虚高——多是 parked epic 非 active lane。**不替 owner 批量改 Linear**。
  - **噪音 PR 待周期清**：6 audit-drift 周报 draft（#544/#555/#567/#578/#586/#600，关联 YUK-430）+ 8 Dependabot（biome 1.9→2.5 等）。
- **✅ YUK-471「全部拿下」4 项 = 全 merged 进 main @27a0cf08（owner 授权 driver 合，2026-06-26）**。每 PR 经对抗 review + **bot review 真有料**（OCR 抓到我 adversarial review 系统性 defer 的 data-loss-on-flip 缺陷）：
  - **✅ Item 5 retract（#592）+ ✅ Item 2 cascade（#593）→ main @d543d9e5**。#592 = retract 补 completion/relearn/knowledge_node + 整函数单 db.transaction + `LearningItemStatus` enum barrier；6 thread resolved。#593 = cascade-revert 编排器（fail-closed + 冲突护栏 + edge event-source archive，directed 验证抓 edge fold-parity HIGH 已修）。
  - **✅ Item 3 Wave 2 = 3 实体全 merged**：**#595 goal →main @80fb444e**（squash；review 抓 data-loss-on-flip blocker = updateGoalStatus/Scope ON 路径无 anchor-gate 删 pre-W2 行 → 修；A1 OFF 路径 retract 缺 hasGoalGenesisAnchor 门 → 修；**CI 抓到我让 worker 给 W1 KnowledgeRowSnapshot 加 .strict() 破 proposals.db live-row parse → 回退**）· **#603 mistake_variant →main @e9686c3c**（#597 rebuilt；ON 路径 unguarded projectMistakeVariant 删 pre-W2 行 → 改 guarded）· **#604 learning_item →main @27a0cf08**（#599 rebuilt；complete/relearn 无条件写动作事件 → pre-W2 fold-order 反转 + ON 静默 no-op → ensureLearningItemGenesisAnchor 先写 base + clamp earlier）。
    - **merge 机制坑（记下次）**：stack forked-pre-#592，retract 块要 rebase 进 #592 的 tx。**branch protection 拒 merge-commit + 拒含 merge-commit 的 rebase-merge；force-push 被 git-guard 拦** → #595 用 squash-merge（含 merge commit 的分支唯一可合法）；#597/#599 因 squash 后 SHA 变 → **rebuild onto main → push 新分支名 `-r2`（force-push-free）→ 新 PR #603/#604 → rebase-merge**（线性分支可 rebase-merge）。旧 #597/#599 closed superseded，threads 全 resolved 挂新 PR。
    - **per-entity SoT flag 全立**：`PROJECTION_IS_WRITER_{GOAL,MISTAKE_VARIANT,LEARNING_ITEM}` 各默认 OFF，独立 B3 gate 翻；全局 `PROJECTION_IS_WRITER` 仍只 gate knowledge/edge（LIVE）。
  - **📋 Item 4 Wave 3 = 设计修正 + readiness 落档，impl 现可启（Wave 2 已落 main）**。critic 5 BLOCKER 已解（`docs/design/2026-06-25-yuk471-wave3-artifact-question-block-fold-CORRECTED.md`），9 impl 前置 + 5 fork 全 driver 拍（`docs/design/2026-06-25-yuk471-wave3-readiness-and-prereqs.md`）：full-snapshot artifact / create-event-非-genesis / per-entity flag / artifact-before-learning_item 序 / 加 GIN 索引 / 改 applyRescue 写 crop_refs。复用 Wave 2 已立的共享基建。
  - **尾巴 Linear**：**YUK-496**（item_calibration 投影未入任何 wave）· **YUK-497**（checkpoint per-utterance revert = payoff 消费层，blockedBy YUK-471）。**⏳ 待补 Linear（MCP 断线，下 session 建）= learning_item ON 路径无 in-tx version/FOR-UPDATE 守卫（OCR major，flag-OFF 下 defer，翻 `PROJECTION_IS_WRITER_LEARNING_ITEM` 前硬化；同核 goal/mistake_variant ON 路径）**。横切编排器=#593（已合）；per-entity SoT flip=owner 激活动作。
- **🟢 LIVE 部署 + flip（2026-06-25，owner's Mac，本地非 NAS）**：docker-compose 生产栈（`docker-compose.mac.yml` override：app→localhost:8787、postgres→5433、无 cloudflared、`PROJECTION_IS_WRITER=1`）复用现有 pgdata（真实数据）。app+worker+postgres 全 healthy，flip 生效（post-flip audit CLEAN）。**YUK-471 SoT flip 已上线本地**。回滚=去 override 的 flag + recreate。部署暴露并修了 prod worker bug（`build:worker` 漏 externalize claude-agent-sdk → bundle 崩）→ **YUK-494 / PR #589**（owner-merge；**`main` 未合前别 `--build`，否则 worker 重新崩**）。better-sqlite3 deps 层 gyp 硬化是 #589/YUK-494 的 follow-up。
- **PR #588（接手 = reviewed → BLOCKED，draft，不 merge）**：Cursor-agent 起草的 YUK-360 mem0 A/B 成本观测（global fetch shim）。独立 Opus review + 我亲自实测复核：**🔴 CRITICAL = 功能在 prod 上完全 inert**——shim patch `globalThis.fetch`，但 mem0→openai@4.104.0 绑 **node-fetch**（实测 `client.fetch===globalThis.fetch→false`、`===node-fetch→true`），mem0 add/search 全绕过 patch → 0 条 cost 行写入；CI 绿是假象（测试直接 mock globalThis.fetch、不走真路径）。**🟠 HIGH** = `search-memory-facts.ts:81` 三元死代码（costTracking 分支永不命中）。+ placeholder CNY 价（owner call）、URL-only 误归因、fire-and-forget 晚写。设计本身（body-clone/幂等/fail-open/ALS 不泄漏）干净。verdict + 修复清单已贴 #588 + YUK-360。**owner 后续：fix CRITICAL+HIGH 再 merge。**

- **origin/main @ 4b7ef242**（YUK-471 W1 **PR-A2b #580 rebase-merged 2026-06-24 12:04Z**；PR-A2a #577 @d9d66c0a、PR-A1 #576 @d227e3a7、W0 #575 @ac9c3f45 先前）。⚠️ **本地 main @ac9c3f45 落后 origin 10**（纯同步差，全是 GitHub rebase-merge 的 W1 commits；下次推进前 `git pull` 即可）。**YUK-471 In Progress**（event-sourcing 地基，多 wave per ADR-0044）：
  - **W0 ✅ merged**：θ̂/FSRS 快照可逆（`experimental:state_snapshot` 同 tx append，HARD-REQ1/2）+ restore 原语 + cascade CTE 骨架（仓库首个 `WITH RECURSIVE`）。review 轮 **augment 抓到真 bug**（snapshot `before` 把 question-card fallback 当 knowledge 行存在性 → cold-start 误 UPSERT）已修 + 回归测试；CodeRabbit 三硬化已采纳 + 两 skip（false-positive nullable / 延 orchestrator 的 count round-trip）。**owner merged（跳过 OCR）**。
  - **W1 driving（三 fork 已拍 2026-06-23；PR-A1 + PR-A2a ✅ merged，PR-A2b 下一站=真热路径、摆给 owner）**：knowledge/knowledge_edge 真 fold + genesis backfill + payload #1 materialized_ids + ADR-0034 闸。grounding 计划 `scratchpad/w1-PLAN.md`（5 agent，1.13M tok）。**决策锁定**：(a)**A2 专用反查表** `materialized_id_index(materialized_id PK, genesis_event_id, subject_kind)`——accept+backfill 同 tx 填，热路径 O(1) PK 查；(b)**B3 全 gated 不浸泡**——两表都「prod-clone 全表 audit 一干净就翻 SoT」，无日历浸泡，翻序铁律 knowledge→edge(FK)；(c)**C1 全合流意图**——reconcile(GLM 判断)钉 **live 边写入咽喉**(`createKnowledgeEdge` single-owner + 收编绕过它的 E1-caller-a 直 POST)、落成 archive 事件；**fold-apply 只重放 + 跑纯拓扑** `checkEdgeTopology`(确定性，回填不放 LLM)。已定非 fork：(d)genesis **typed** `GenesisExperimental`(W0 state_snapshot 同款 3-site)、(e)version **STORE**。
    - **W1 增量计划（多 PR `Refs YUK-471`，全完才 `Closes`）**：
      - **PR-A1 基底 ✅ merged #576 → main @d227e3a7**（行为保持，reducer 运行时未接线）= 纯 reducers(`src/core/projections/{knowledge,knowledge_edge}.ts` + 共享 `fold-event.ts`) + payload#1(`known.ts` `.strict()`) + genesis typed schema(3-site superRefine) + ADR-0034 拓扑入 edge fold-apply + 单测。**2 轮 review**：round-1 一组收敛真 finding（集中 edge reducer：死 isLive/空串绕拓扑/裸 cast/漏排序/漏 subject_id 校验）全修+测；裁决 **genesis=信任 ground truth 不再 gate**（回填已过闸行重放再 gate 会破 fold==row）。CI 绿；OCR 超 13min cap skip（findings 已全修）。两 fixup agent 都 API 断连、edge 半边我手写。**self-merged**（行为保持基底+高信心，per owner 授权）。
      - **PR-A2a 基础设施 ✅ merged #577 → main @d9d66c0a**（行为保持，shells 运行时未接线）= IO shells(`src/server/projections/{knowledge,knowledge_edge}.ts` + 共享 `gather.ts`，3-query 聚合 Q1 subject_id/Q2 反查 anchor/Q3 merge-from jsonb `@>`/rate resolution) + A2 反查表 `materialized_id_index`(migration 0050) + backfill(原子 event+index tx/ingest_at outbox opt-out/幂等) + `audit:projection`(独立 B3 工具非进 pnpm test)/rebuild + DB 测试。备份登记 FK_ORDER+SCHEMA_VERSION 4.8→4.9。**4 轮 CI/review**抓真 finding 全修（备份 reverse-lockstep 守卫/constants lockstep/audit key-order 误报/backfill 非原子）。**self-merged**（行为保持+CI 绿+全 bot 干净）。**defer→PR-B B3**：audit 窄列读 + edge O(E²) liveMesh（纯 perf）。
      - **PR-A2b 接线 ✅ merged #580 → origin/main @4b7ef242（2026-06-24 12:04Z；owner merged，3 bot round）** = accept 穿 materialized_ids + 同 tx 写 `materialized_id_index`（proposals.ts acceptProposal + actions.ts edge accept）+ 双写+内联 `fold==row` 断言 + reconcile 钉 live 边写入咽喉(`createKnowledgeEdge` + E1-caller-a) + **PR-A1 残留 writer 修**：① accept-path 用 rate 事件 created_at 戳行时间戳求 parity ② `actions.ts` generate payload `reasoning ?? ''`→`?? null` 后 reducer `reasonOrNull`→`?? null`。
      - **B3 audit perf ✅ merged #582 → main**（`yuk-471-w1-b3-audit-perf`）= A2a-deferred「audit 窄列读 + edge O(E²)→O(E)」；3-lens 对抗 review C1/C3/C4 全 PASS，2 测试充分性 finding 已修；全 bot 干净 owner-merged。
      - **B3 gate orchestrator = ✅ merged #583 → main（2026-06-25，owner-authorized self-merge，CI 全绿 + 31-agent 两轮 review）** = `scripts/b3-gate.ts` + `pnpm b3:gate`：prod-clone GO/NO-GO 闸（snapshot→**scoped backfill**→**audit BEFORE rebuild**(真值齿)→rebuild→**对称 rowset parity**(删+复活双向)）。`GO iff audit.clean && rebuild.ok && survival.ok`；`B3_GATE_CONFIRM_CLONE=1` 安全门。R1 抓 HIGH false-GO(F1)→scoped backfill + audit 前移修；R2 抓残留 creation gap→对称 parity 修。nits(typed topology error / edge dead-index) 已 triage 不 block。
      - **BYPASS-2 围栏 ✅ merged #585 → main（2026-06-25，owner-authorized self-merge，CI 全绿含 OCR）** = POST `/api/edges` + reconcile SUPERSEDE 此前写 `created_by`(裸串)/`created_at` **fold≠row** → B3 audit 正确 NO-GO。修：`createKnowledgeEdge` 存 fold 的 `{actor_kind,actor_ref}` 对象 + `created_at` 对齐 paired generate 事件；POST 同 tx 写 edge + `generate(create)` 事件(定 `{user,self}`，client `created_by` 忽略)；reconcile 传 `{agent,dreaming}`；一次性迁移 `pnpm normalize:edge-created-by`(幂等)修存量。**13-agent 对抗 review**：P1(reconcile fold==row)+P2(signature)**验证干净=正向修扎实**；1 真 perf(normalize O(E²)→O(E) 修)；**HIGH「genesis backfill 先于 normalize 跑→静默冻裸串→假 GREEN」= 假阳性，实测 testcontainer 证伪**（`writeEvent` 经 `GenesisExperimental→KnowledgeEdgeRowSnapshot` 的 `created_by: z.record(...)` 校验，裸串**写期即 throw** = fail-loud 非静默 seed；加永久回归守卫钉死该不变量）。**等 CI/bot + owner merge**。
      - **本地 flip 验证 ✅ PASS（2026-06-25，scratch clone `loom_b3verify`，全程未碰 dev DB/不改 .env.local）**：① `audit:projection` before=6 drift(5 event-less 节点 + ke_legacy `created_by`/`created_at` 漂移) ② `normalize:edge-created-by`→normalized 1 ③ `db:backfill:genesis`→节点 seeded 5 / 边 **skipped 1（scoped-skip 实证 generate-sourced 边不 seed）** ④ `audit:projection` after=CLEAN ⑤ `B3_GATE_CONFIRM_CLONE=1 b3:gate`→**GO**(audit clean·rebuild OK·survival OK) ⑥ flag-ON 写路径(`acceptProposal` propose_new)→**projection 写了行(imperative INSERT 跳过)+ fold==row**。整条 prod runbook + flip 在真 Postgres 上跑通。
      - **pre-flip 操作序（owner，翻 `PROJECTION_IS_WRITER=1` 前，prod+clone）**：① `pnpm normalize:edge-created-by`(在任何 unscoped backfill **之前**，或靠 #583 scoped backfill 跳过 generate-sourced 边；二者皆验证 fail-loud 无静默腐败) ② `pnpm db:backfill:genesis` ③ `pnpm b3:gate`(来自 #583)须 GO ④ owner 翻 flag。**⚠️ 真 prod flip gated on 首次部署（owner 还没部署 NAS；prod runbook 待部署日执行）。**
      - **PR-B 翻 SoT 实现 = 🔵 OPEN PR #581（`worktree-yuk-471-w1-prb1-minting-flip`），等 owner merge**——「complete the SoT flip behind `PROJECTION_IS_WRITER`（flag OFF）」。**全 check 绿**（CI Gate / CodeQL / OCR 承重 / PR-Agent / Cursor Approval / CodeRabbit 全 SUCCESS，mergeStateStatus CLEAN）。2 bot round 已收敛。**flag OFF 落地 = 只把完整 flip 实现摆进树后熄火，不真翻 SoT**；真翻（flag ON）仍 gated on prod-clone 全表 audit 干净（B3，可能需 NAS DB）。**flip wave = owner-in-loop merge gate，非 self-merge**（per driving 纪律）。
    - **W1 follow-up→Linear**：reconcile 未进 apply 是判断非完整性缺口(C1 残留)·过期注释(edge-reconcile.ts/propose_edge header)·`approval_status` 惰性字段(已挂 YUK-422)·E1-caller-b 绕 single-owner。
    - **driving 纪律**：PR-A1（行为保持基底）self-merge ✓；**PR-A2（接 live 写入路径）+ PR-B（翻 SoT）= merge 前摆给 owner**（真风险面、owner 在环）。
  - **orchestrator-wave 延项**（Linear YUK-471 comment）：restore count 列 round-trip · last_review 真 prior · A2 ability_global · 冲突护栏。
  - W0 worktree `.claude/worktrees/yuk-471-wave0-snapshot-reversibility` 保留（branch merged，rebase-SHA 故 `-d` 会拒）。
- `main` 旧记 @ 387dda35；**#553 #554 #557 #558 #559 全 merged（2026-06-22）= YUK-482 全 lane 代码落地**。#557/#558/#559 含 owner review-fix commit（均我审过质量好、CI/OCR 全绿才合）。5 分支远程已删;本地 -d 因 rebase-SHA 留 dangling ref（git-guard 拦 -D，owner shell 可批删）。
- **grade 链全景**:② 答案生成 = cold-start-bridge(#553✅)· ③ 学生作答判分 = #558✅ · vision→Opus = #557✅ · per-小题收窄 = #554✅。**承重 grade 链全落 main。**
- **YUK-482 grade 链 + ⑤ 轴清理:5 PR 全 merged ✅**。剩余非代码 follow-up:prod flag flips · YUK-485 · 上传按组（见下）。stash「559-review-context-stash」已 drop（3 文件全冗余、工作树已有,零损失;其余 12 个 pre-existing stash 未碰）。
- **本地分支清理（2026-06-22）**：删了 193 个分支（72 workflow-wf 临时 + 119 已 ff-merged 工作分支，全走安全 `-d`）+ 4 个 stale worktree（pr557/pr558/yuk350w0/yuk383）。剩 172。**外部 AI 工具分支（codex/cursor/t3code/agent-/devin/claude/yukovaldakia09/ocr/smoke）全程未碰**。
  - **待 owner 自己 shell 跑（git-guard 拦我的 `-D`）**：45 个 `[gone]` 分支（远程已删=PR合/废弃,内容已进 main,rebase-SHA 故 `-d` 拒）。安全批删命令见对话。
  - **保留**：45 个 `[ahead*]`（有未推 commit,删了丢工作）+ 16 个 worktree-wf 残余（未合实验）。
  - **5 个 dirty worktree**（tlp-wt-yuk387/388/389/390/396,含未提交改动）没 `--force` remove,留 owner 判断。
- **Lane C/D 剩余**：prod flag flips(`VISION_JUDGE_PROVIDER=anthropic-sub` + `WORKFLOW_JUDGE_STUDENT_ANSWER_GRADING_ENABLED` + 既有 auto-enroll/placement,有 OAuth token 的部署翻)· **YUK-485**(密集页多子题归属 fan-out,defer)· 上传按组处理(cross-page 前置)。
- 工作树脏（不进 #553）：`PLAN.md`（cockpit，我更）+ pre-existing prototype/docs/png + owner 的 `scripts/figtest-*.ts`（未跟踪，提取测试 harness；有 biome format 待你 `pnpm format`，我没碰）。
- **Lane C cut-1 ✅ merged #554**：YUK-484 拍 **B**（不拆行 / per-sub judge 收窄 YUK-212 / per-KC 调度 / verdict 带 sub_ref 落点）。`narrow-part.ts` 窄到 passage-preserving 单子题 stem（窄 structured + reference_md 都不漏兄弟 = C1 两通道）+ invoker 单点收窄 + event sub_ref + 两路 caller。**后续 cut 全 merged：③=#557 vision→Opus · ④=#558 录入判分 · Lane D⑤=#559；YUK-485 归属串题仍 defer。**
- 大量 stale worktree 残留（yuk350/383/387/388/389/390/396）+ 外部 agent worktree（.augment/.codex/.cursor/.t3）——非当前 scope，未清。
- 详细 handoff 见 `.remember/remember.md`（2026-06-20 axis-2 校准 + inc-B 波）。

## ✅ 本 session 已落（防遗落，下次别重做）

- **YUK-491 + YUK-492 收尾（2026-06-23，owner「把这两个收一下」）**：
  - **YUK-491**（rejudge inert singletonKey，Low Bug）→ **✅ merged PR #574 → main @a603f581（Linear Done completedAt 11:14Z）**。option 1（加 `REJUDGE_SINGLETON_SECONDS=60`，镜像 YUK-486 `AUTO_ENROLL_SINGLETON_SECONDS` + `memory/triggers.ts`）；常量放轻量 `rejudge-config.ts` 避免 appeal API 路由静态拉 handler 重图（`handlers.ts` 本就 dynamic-import）。新增 `appeal.enqueue.db.test.ts` 钉死 send 携带 `singletonSeconds`。Gate 全绿（typecheck 0 · biome · build 3-bundle+vite · `pnpm test` exit0 DB 2537+migration 19 · audit schema/partition[P0 none]/draft-status clean）+ 独立 Opus review **SHIP-WITH-NITS**（两 comment nit 已修；correctness 在 pg-boss 12.18.2 源码层核实）。**待 owner merge**（`Closes YUK-491` 自动翻 Done）。**Scope 刻意收窄**：只恢复 send 层去重，不加硬 exactly-once（handler `caused_by` 非事务兜底，pre-existing，按 Low 定级刻意不做；若 owner 要再拆单）。**已 merged，`Closes YUK-491` 触发 Linear 自动翻 Done。**
  - **YUK-492**（整页 holistic + 同页归属，Medium Feature）→ **确认 clean parked，不实现**（实现会 front-run owner 自己写下的「等真实需求再做」demand-gate）。逐条对 main @c06984b9 验真：窄版 `pageScopedQuestionImageRefs` 在 main ✓ / holistic 终态确未实现（`auto-enroll.ts:1187` defer 注释）✓ / YUK-485 Done 但 headline 残留未真修→本单正确接管 ✓ / 交叉链全实 ✓。加今日 re-verify Linear 注释硬化 parked 防蒸发。A/B/C 架构决策 tee 给真实密集页需求起来时再拍。
  - **翻 placement flag** = 纯 owner 部署动作（NAS `.env` flag + OAuth token），代码侧前置（YUK-470/479）全清，我无事。

- **冷启翻-flag 前硬化打包 全收官（2026-06-23，PR #572 rebase-merged 进 main @bfdb897d）= YUK-486 + YUK-479 Done**（owner 拍 option 3：先硬化清障 → 再 holistic 判分）：
  - **YUK-486** auto_enroll 幂等：① `singletonKey + singletonSeconds`（AUTO_ENROLL_SINGLETON_SECONDS=60，两 producer tencent+docx 都加）杀重复 job；② enroll tx 内 per-block `FOR UPDATE` claim（非 draft 即 bail）= **结构性**防双 INSERT。独立 Opus reviewer 抓到「裸 singletonKey 在 standard 队列 inert（pg-boss v12 需 singletonSeconds）」→ 加 seconds 让 dedup 真生效 + 诚实归功 FOR UPDATE。
  - **YUK-479** source_verify FAIL 的 **scoped demote**（active→draft）：关掉冷启 auto-promote 单向门。**scoped-by-construction 无需 marker 列**——决策点上唯一 active 的题就是冷启预promote（正常 draft 此处 demote=no-op、已 verify 被幂等门短路、verify-and-promote 派发前 gate draft）。独立 reviewer 穷举验证 scoping **无洞**。
  - **gate**：typecheck 0 · biome 干净 · 全量 unit 2697 · 全量 DB 2528 · 11 CI checks 全绿（OCR 承重 success/0 comments）· CodeRabbit/PR-Agent/augment/Cursor 全 clean · 独立 Opus reviewer SHIP。
  - **承重缺口清空**：翻 placement flag 的代码侧前置（YUK-470 ✅ + YUK-479 ✅）全清，剩纯部署翻转（flag + NAS OAuth token）。
  - **follow-up 已捕获**：**YUK-491**（appeal.ts/rejudge 同款 inert singletonKey，pre-existing，低优）→ **✅ merged PR #574 → main @a603f581，Linear Done**（见本 session 顶部条目）。

- **Phase B = YUK-488 判分 page-scope 收官（2026-06-23，PR #573 rebase-merged 进 main @c06984b9）= Done（窄版）**：
  - **scope owner 重定向**：YUK-488 原 = 整页 holistic（一页一次 detect+分题+判分、解耦 extraction、折 YUK-485/484）。本 session 设计讨论 owner 收窄为 **per-question page-scope（窄价值）**，延迟 holistic。
  - **交付**：`pageScopedQuestionImageRefs(block)` — 判分只喂该题对应页图（structured per-node `page_index`→`source_asset_ids[idx]`），削**跨页/多页 upload inter-page bleed** + 净 cost。**complete-signal gate**：仅每节点都带合法整数 page_index 才 scope，部分/零/非整数→喂全页（零回归、绝不丢答案页）。scope prompt+student+stored-answer image refs（题 display image_refs 保持全页）。
  - **两 reviewer 收敛 findings 全修**：augment + 独立 Opus 都抓 NaN/float guard（`typeof number`→`Number.isInteger`）；独立 reviewer 的 #2 partial-page_index over-narrow → complete-signal gate 关窗。
  - **gate**：typecheck 0 · biome 干净 · 全量 unit 2697 · 全量 DB 2534 · 11 CI checks 全绿（OCR 承重在终态 0034fc0c success/0）· 独立 Opus reviewer SHIP。
  - **明确未交付（真残留 → YUK-492 承接）**：同页密集归属（YUK-485 headline，page-scope 对单页 no-op）· 解耦 extraction · holistic detect+分题 · YUK-484 per-小题折入。窄版对单页无效，靠现有 needs-review 闸兜底（无静默错判）。YUK-488 留 comment 记录收窄 + delivered-vs-deferred。

- **YUK-489 统一 match-or-propose KC 标注轴 全阶段收官（2026-06-23，6 PR 全 merged 进 main，main @721d93cf）= Done**：
  - **P1** `matchKnowledgeBySimilarity`（KC 余弦检索器 #561）· **P2** `tagKnowledge` match-or-propose（#562，阈值 0.35→**0.55** 真嵌入探针 n=6：query=整题 vs KC=`name\ndomain` 非对称、0.35→0/5 匹配）· **P3** 接 4 入口+删死的零匹配门+解体 bridge①+D1 subject 过滤（#566）· **P4a** reference 解耦→`reference_answer_backfill`（#569）· **P5** `kc_dedup_nightly`（cosine 自连接→merge 提议，**propose-only 不 import applyMerge**，跨夜幂等 skip-set，`kc_dedup_scan` 可观测 #570）· **closeout**（#571）：ref-backfill 饥饿修（gate 下推进 SQL）+ 文档对齐（architecture.md §5.1 + **ADR-0045** + ADR-0026 amend + drift 报告）。
  - bridge 角色收窄=subject 分类器 + tagKnowledge 命名引擎（未删）。OBSERVE 仍走旧 TaggingTask。mistakes/import 暂 ids-required（无 request 级 subject 信号）。
  - 全程 OCR（承重）逐轮回帖+resolve + **augment findings 采纳**（教训：augment 即使不 resolve 其 thread，findings 仍有信号，抓到 OCR/reviewer 漏的真 bug——`seed:undefined:root`、ref-backfill 饥饿、merge 幂等缺失）。每阶段独立对抗 review + 全量 unit/DB 本地复跑。
  - **残留 follow-up**：阈值标定 YUK-396 · mistakes/import subject-signal · stale doc `docs/modules/lanes.md` · 夜产提议 UI 可观测面 · YUK-490（agent-notes 重复未挂载包，drift label）· YUK-487/488（带图判分 gate，先前 session）。
  - **drift audit 2026-06-23**（`docs/audit/`）：findings 1–3=本阶段文档债（已修于 closeout）；finding 4=YUK-490。

- **冷启 onboarding UI 流四片全建 + merged（2026-06-21）**：#551（Slice 1 welcome+冷拦截+goal / Slice 2 上传屏 / Slice 3 placement 作答环，rebase-merge）· #552（Slice 4 起始档案 + 12 条 bot review 修复 + `Boolean(goalId)` + CLAUDE.md resolve-threads 规则）。flag `PLACEMENT_PROBE_ENABLED` 改 env-driven（默认 false，dev on）。新记忆 `feedback_resolve_review_threads`。Linear：YUK-473/475 ✅Done，YUK-476 待 owner 翻 Done。
- **cold-start inc-B placement 后端全 scope 完成（YUK-468 Done）**：#538 状态机+终止+flag · #541 选题层 · #542 start/next/end API（Closes）。flag `PLACEMENT_PROBE_ENABLED=false` dark-ship，无 live UI caller。
- **axis-2 校准 harness 波全 merged**：#528（YUK-393 retrieval）· #532（YUK-461 harness Closes）· #533（YUK-448 RT 后端）· #534（YUK-462 错因两段式 Closes）· #535 docs · #536（YUK-466 θ_global 保真）· #539（YUK-467 polish）。
- **follow-ups 已捕获**：YUK-469（hints_used off-by-one，UI 接 hint 计数时修）· YUK-470（placement 上线前硬化，翻 flag 前必修）。
- **Linear 状态腐败修正**：审计抓到 YUK-363「假 Done」（地基主轴只交付设计/ADR-0044，实施未落，代码 ground 坐实 `WITH RECURSIVE` 等全缺）→ 方案 A：YUK-363 收窄为「设计腿」保持 Done + 加状态更正横幅，实施拆出 **YUK-471**（挂 YUK-203，Backlog，High）。
- **新记忆**：`feedback_gate_evidence_contract`（`cmd|tail` exit-code 陷阱）· `feedback_codex_review_13min_cap`（review gate 收窄：只 OCR 承重）。
- 注：本文件先前的「P2 第一批 / YUK-400 / ci-gate skip」等更早波次记录已并入 `.remember` 历史。
