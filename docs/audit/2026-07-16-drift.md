# Drift Audit — 2026-07-16

**Scope**: ADR-0001..0049, `docs/design/*.md`（最近 30 天，triaged 后深查约 12 篇 locked spec）, `CLAUDE.md` Architecture/Layering/Design principles 三段
**Run by**: Claude Code (scheduled `/audit-drift` run)

## Summary
- Aligned: ~101（不展开，含 3 项 priority check verified-non-drift，见下）
- Documented-only: 2
- Undocumented: 2
- Contradicted: 0
- Phase-deferred: several, e.g. ADR-0016（proposed/spike-only）(informational, not counted individually)

## Priority checks — verified NOT drift

- **ADR-0023（Redis editing presence）**：已被自身 "M5 迁移注 (YUK-321)" 段标注 superseded → PG 表 `editing_presence` / `PgPresenceStore`（`src/server/artifacts/presence/pg.ts:52`，`src/db/schema.ts:1497`）。`src/` 内无 ioredis/redis client 残留。与 CLAUDE.md 当前栈描述一致。
- **ADR-0047（KG 承重借用）**：Status 仍 Proposed，但 §8「act-flip 闸」原文自陈"代码建+接线+电到 live 已完成；只 defer 最终 flag 翻转"——doc 与 code 状态一致自洽，非漂移。
- **ADR-0034（bitemporal supersede）**：`knowledge_edge` 无 `valid_at`/`invalid_at`，schema 注释明确"archived_at is the ONLY time dimension"；无旧 bitemporal 残留。

## Findings

### ⚠️ Undocumented

#### [ADR-0048 §1] A11 EZ-diffusion 轴：生产已上线，Status 仍标 Proposed
- **声明**：`docs/adr/0048-a11-caution-speed-accuracy-axis-ez-diffusion.md:5` — "**Status**: Proposed (2026-06-27) — 草案，待 owner ratify，勿自行改 Accepted。"
- **代码**：`learner_axis_state` 表（`src/db/schema.ts`）+ 迁移 `drizzle/0055_tan_lightspeed.sql` + writer `src/server/calibration/ez-diffusion.ts`/`axis-writer.ts` + 夜间 cron `src/capabilities/practice/jobs/axis_state_nightly.ts` + `placement-profile.ts` 读面，全套已落生产 schema 并跑起来。
- **冲突**：ADR §1 本身论证 A11"不需 dark-ship flag"故可以直接上线（合理），但这意味着一旦合并即等同于已 ratify——Status 头仍写"待 owner ratify"与生产已有 live 迁移/cron 的事实不一致，容易让人误判此决策仍可撤回。
- **建议**：owner 确认后把 Status 翻 Accepted；若尚未定论，应评估是否需要临时下线 cron 或至少在 ADR 里补一句"已上线，ratify 是形式追认"（同 ADR-0047 §8 的写法）。
- Linear：无需新建；owner 判词后一行 Status 编辑即可关闭。

#### [docs/design/2026-07-12-prep-desk-card-design.md] doc 状态落后于已上线代码
- **声明**：文档头 "Status: DRAFT — 待 owner 判词。UI 代码零动，等批"。
- **代码**：`src/capabilities/shell/ui/PrepDeskConjectures.tsx`、`ProbeAnswers.tsx`（首行注释 "YUK-567 slice-2 — 备课台「待你试做」probe 作答区"）、`probe-answer-api.ts` 均已实现并挂进 `TodayPage.tsx`（对应 commit 28dcd67 / 15b1d90 / e86c44c，2026-07 上旬）。
- **冲突**：doc 仍读作"未批准草案"，但其所述功能已作为 YUK-567 slice-1/2 shipped 且已修过一轮 CodeRabbit review fix，状态字段未回填。
- **建议**：把 Status 更新为已批准/已实现，或补一条 supersede 指向实际 shipped 版本，避免误读为待办。
- Linear：无需新建，doc 一行状态更新即可关闭。

### ⚠️ Documented-only

#### [ADR-0036 §决定4] `encompassing_weight` 列未落地
- **声明**：`docs/adr/0036-dual-layer-heterogeneous-knowledge-graph.md` §决定4 — "只给 `knowledge_edge` 加一 nullable 列 `encompassing_weight real`（仅对 `prerequisite` 行有意义…该加列触发 `audit:schema`）。"
- **代码**：全仓（`*.ts` + `drizzle/*.sql`，排除 ADR 自身）搜索 `encompassing_weight`/`encompassingWeight` 零命中；`knowledge_edge` 无此列。RT2 其余交付物（`misconception`/`misconception_edge`/`misconception_reconciliation_log`/`audit:relations`）均已落地。
- **冲突**：单一命名列被跳过，可能是被后续 ADR-0047 A6 的 `knowledge_edge.weight`（confidence 权重）机制取代，但 ADR-0036 未记录 supersede 指针。
- **建议**：补一句 supersede 说明指向 ADR-0047 A6，或确认仍需该列后排期加。

#### [ADR-0002/0005/0007/0022/0024/0026] M5 重构后路径引用未同步（跨 6 篇 ADR 的同根问题）
- **声明**：上述 ADR 内联引用 `src/server/<domain>/…`（ingestion/workflow-judge/tagging/auto-enroll/structure/rescue）、`src/ui/block-tree/…`、`middleware.ts` 等 pre-M5 路径；ADR-0004 任务表把 `KnowledgeProposeTask` 标"✅ 已实装"。
- **代码**：这些路径均已随 capability manifest 重构（YUK-321 M5）迁至 `src/capabilities/<domain>/{server,ui}/…`；`middleware.ts` 已删，auth 中间件现在 `server/app.ts:42`；`KnowledgeProposeTask` 已删（`src/ai/registry.ts:178` 注释 "was removed"，Lane D / YUK-482），仅剩 `KnowledgeEdgeProposeTask`。
- **冲突**：决策本体全部 Aligned，只是引用路径/任务名过期——ADR-0013/0021/0024/0025/0026 已有"M5 路径注"覆盖 `app/api/**` 迁移，但未覆盖 `src/server/*`/`src/ui/*` 的搬迁，ADR-0022/0007 完全没有该注记。
- **建议**：把现有"M5 路径注"模式扩展到这 6 篇 ADR，补一条统一的路径映射脚注（`src/server/<domain>/` → `src/capabilities/<domain>/server/`、`src/ui/block-tree/` → `src/capabilities/notes/ui/`、`middleware.ts` → `server/app.ts`），并把 ADR-0004 任务表的 `KnowledgeProposeTask` 行标记为已移除。

## 假阳性防控 note

本轮为 audit-drift 首次系统扫；`scripts/audit-drift-allowlist.json` 尚不存在。上述 4 条 finding 全部为低风险文档滞后（无一条属于「代码违反已批准决策」的 ❌ Contradicted），符合"假阳性率 <30% → 可考虑 cron 化"的信号，建议下一轮跑完再评估自动化。
