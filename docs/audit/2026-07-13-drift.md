# Drift Audit — 2026-07-13

**Scope**: ADR-0001..ADR-0049, docs/superpowers/plans/* (active), CLAUDE.md, docs/design/* (last 30 days)
**Run by**: Claude Code (manual /audit-drift)

## Summary
- Aligned: 8（不展开）
- Documented-only: 0
- Undocumented: 2
- Contradicted: 0
- Phase-deferred: 1 (informational)

## Findings

### ⚠️ Undocumented

#### [docs/design/2026-07-12-prep-desk-card-design.md] Prep-desk conjecture card shipped, doc still says "UI 代码零动，等批"
- **声明**: 文档头部 `Status: **DRAFT — 待 owner 判词**。UI 代码零动，等批` 及正文「缺口 = 目的地：点了那个 count 之后，看不到那 ≤3 张真卡。**YUK-567 = 建这批卡 + probe 作答区**」——即声明该 UI 尚未实现，等待 owner 批准设计稿后才动工。
- **代码**: `src/capabilities/shell/ui/PrepDeskConjectures.tsx`（184 行，完整组件）已 `import` 并挂载进 `src/capabilities/shell/ui/TodayPage.tsx:322`。该文档本身与实现代码在**同一个 commit**（`15b1d90` "feat(shell): YUK-567 slice-1 — 备课台 conjecture card 点亮 (/today 就地展开) (#782)"，2026-07-12）落地，且当天有后续修复提交 `e86c44c` "fix(shell): YUK-567 review-782 — 备课台 面板卡死 + decide 静默失败修 (#783)"。
- **冲突**: 设计稿的 Status header／Ground-truth 段落描述的是「未动工」状态，但同一批提交已经把 UI 建完、接线、review 修复并合并——文档没有随实现同步翻转成「SHIPPED」，读者据文档会误判该功能仍是 gap。
- **建议**: 把 `docs/design/2026-07-12-prep-desk-card-design.md` 头部 Status 更新为「SHIPPED（#782/#783，2026-07-12）」并同步「Ground truth」段落的缺口清单；顺带核对 `PLAN.md` BLOCKED-ON 里同一条 YUK-567 描述（仍写"design-gated：card 组件不存在"）是否也需要一并翻新（PLAN.md 不在本 skill 直接扫描范围内，仅作旁证）。
- Linear issue（报告完成后按 closeout gate 去重并创建 / 更新，label `drift`）：
  - title: `Drift: docs/design/2026-07-12-prep-desk-card-design.md ↔ PrepDeskConjectures.tsx`

#### [CLAUDE.md §Commands] `pnpm audit:*` 命令清单少列 6 个已存在脚本
- **声明**: CLAUDE.md 「## Commands」bash 代码块逐条列出 `pnpm audit:schema` / `audit:partition` / `audit:profile` / `audit:draft-status` / `audit:relations` / `audit:calibration` / `audit:mastery-provenance` 七条，作为本仓库 audit 脚本的权威参考清单（紧接一段说明「Each `pnpm audit:*` script has its own allowlist format...」）。
- **代码**: `package.json:36-56` 实际登记 13 个 `audit:*` script：额外的 `audit:draft-status-reads`（散见正文提到但未入代码块）、`audit:fold-writes`、`audit:flags`、`audit:projection`、`audit:golden`、`audit:judge-golden`、`audit:judge-prompts` 均不在该 bash 代码块内。其中 `audit:fold-writes` / `audit:flags` 是 `PLAN.md` 明确记录的交付物（"批③ 工程单候选 2/8 已落（audit:fold-writes + audit:flags #726）"）。
- **冲突**: CLAUDE.md 的 Commands 参考清单已经落后于实际脚本面，读者（含未来的 Claude session）据此清单不会知道 `audit:fold-writes` / `audit:flags` / `audit:projection` / `audit:golden` 系列的存在。
- **建议**: 把缺失的 6-7 个 `audit:*` 命令补进 Commands bash 代码块（一行一注释，参照现有格式），或至少把 `audit:fold-writes` / `audit:flags` 两个已确认落地的工程单命令补上。
- Linear issue（报告完成后按 closeout gate 去重并创建 / 更新，label `drift`）：
  - title: `Drift: CLAUDE.md Commands ↔ package.json audit:* scripts`

## 假阳性防控说明（本轮核查过、未列入 finding 的候选）

- ADR-0023（Redis presence）：文档自带「Superseded by: 部分迁移 — YUK-321 M5」小节，与代码（`PgPresenceStore`，无 Redis）已自洽，非 drift。
- ADR-0016（OpenAI Codex provider）：Status 明写 `proposed（评估阶段，不立即落地）`，代码确未接 Codex；`AI_PROVIDER_OVERRIDE=anthropic-sub` 是另一条已批准的不同决策（YUK-365），不冲突。
- ADR-0046（Rust single-source-of-truth）：正文自陈 `θ̂/PFA 当前仍用 Math.exp，polySigmoid swap 是未落的 Phase-1 动作`——⏳ Phase-deferred，不报。
- ADR-0049（conjecture-wire dark-loop）：`answerProbe`/`serveProbeOnce` 已有真实 caller（`probe-answer.ts` 路由 + `probe-lifecycle.ts`），与「wire」判词一致，非 drift。
- YUK-575/596 durable-lane opt-in flag（`api/chat.ts:64 parsed.durable`）：代码确认默认仍走 inline，与 design doc「PR1 机制藏 opt-in flag 后，默认 inline」一致。
- YUK-601/602 trait 合同设计稿（2026-07-10/11）：Status 均为 owner 已批准，与 PLAN.md 记录的生产部署状态一致。
- `server/app.ts` 组合根路由挂载（`toHonoPath`/`buildHonoApp`）、`/api/*` token 鉴权 + `/api/health` 豁免：与 CLAUDE.md Auth/Request-flow 段描述一致。
- docker-compose.yml 无 Redis 服务，注释显式记录 M5 迁移：与 CLAUDE.md「无 Redis」声明一致。
