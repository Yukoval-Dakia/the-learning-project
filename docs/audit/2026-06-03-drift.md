# Drift Audit — 2026-06-03

**Scope**: ADR-0021..ADR-0027, design/2026-06-02 (BlockAssembly path-B), CLAUDE.md, docs/planning/v0.4-complete-form-roadmap.md
**Run by**: Claude Code (manual /audit-drift)
**Baseline**: `main` HEAD (~`159948d` — ADR-0027 + target-domain-model landed)
**Prior audit**: `docs/audit/2026-05-30-drift.md` (0 contradicted / 0 undocumented / 1 validator gap)

## Summary

- Aligned: 6（不展开）
- Documented-only: 2
- Undocumented: 1
- Contradicted: 0
- Phase-deferred: 3 (informational)

---

## Findings

### ⚠️ Undocumented

#### [design/2026-06-02-blockassembly-pathb-design §1–4] BlockAssembly path-B concrete decisions

- **声明**: `docs/superpowers/specs/2026-06-02-blockassembly-pathb-design.md` §1–4 锁定了五个关键决策：① semantic-only v1（不用 bbox/page-edge，deferred to slice-2b）；② per-session inline pass in `runAutoEnrollForSession`（不新建 pg-boss queue）；③ stale-block 静默处理协议（`'skipped:*'` 返回 UI friendly reason）；④ 语义 dedup 只靠 `cooldown_key`（不硬 suppress 重写）；⑤ accept-side `mergeQuestions` 是 lossy（retract = correction-event-only，无 unmerge）。这些是 scoped engineering decisions，级别足以建 ADR（对标 ADR-0026 的「单 pass 纯函数 aggregator」决策粒度）。
- **代码**: `src/server/ingestion/block-assembly.ts`（BlockAssemblyTask + writeBlockMergeProposal）; `src/core/schema/proposal.ts:30`（`'block_merge'` kind）; `src/server/proposals/actions.ts:1473`（`acceptBlockMergeProposal`）；注释都引用 "design 2026-06-02 §1/§4"，无 ADR 编号。
- **漂移**: ADR-0004 只说 "BlockAssemblyTask 作为 lane 级编排概念保留"（docs/architecture.md:131）；没有 ADR 捕获 v1 的技术边界（semantic-only、inline-pass、stale protocol）。设计 spec 存在但不等于 ADR——后续重构者无法从 ADR index 找到这个决策的理由链。
- **建议**: 写 ADR-0028 捕获 BlockAssembly path-B，至少记录：① semantic-only v1 + bbox deferred coupling point；② inline-pass 触发点选择；③ stale-block 处理协议（lossy retract 是有意为之）。design spec 可 link-in 作附录，不需重写。
- Linear issue（closeout gate）：
  - title: `Drift: BlockAssembly path-B has no ADR (design spec only)`
  - labels: `drift`，team `Yukoval Studios`

---

### ⚠️ Documented-only

#### [docs/planning/v0.4-complete-form-roadmap.md:235] Profile validator 7-invariant coverage gap (carried from 2026-05-30 audit)

- **声明**: `docs/planning/v0.4-complete-form-roadmap.md` line 235 声明 "profile validator 检 7 invariant（pipeline schema 兼容 / fallback 同 family / renderer 装了 / prompt 不引用缺失 section 等）"。Line 240 自陈 "待 audit"，2026-05-30 drift report 完成 audit，确认差距 3 项。
- **代码**: `src/core/capability/validate-profile.ts`（178 行），无新增 invariant 检查（git log --since=2026-05-30 -- src/core/capability/validate-profile.ts: 唯一后续 commit 为 solve-tutor feature，无关）。实检 ~4 类语义 invariant；缺失三项：
  1. "prompt 不引用缺失 section"（profile prompt fragment / section template 不交叉核对 renderConfig）
  2. "fallback 同 family"（preferredRoutes fallback chain 不校验 capability family 一致性）
  3. "pipeline schema 兼容"（capability inputSchema/outputSchema 不与 profile 期望交叉校验）
- **漂移**: roadmap 完全体目标 → validator 实装 delta 已稳定 3 个工作日无修复，属于 documented gap 但非 ADR↔code 矛盾。当前 3 个 profile `pnpm audit:profile` 全绿，但 onboarding 第 4+ 学科时不会被自动挡住。
- **建议**: 补三项检查，或在 v0.4 roadmap 把 "7 invariant" 下修为实际的 ~4 项 + 注明其余由 registry / `pnpm audit:profile` 分担。
- Linear issue（closeout gate）：
  - title: `Drift: profile-validator 7-invariant gap — 3 unchecked invariants`
  - labels: `drift`
  - 上次已建 issue（2026-05-30 audit）；若已开 → update；否则 create。

#### [ADR-0027 §6] P1-PR2 read-layer follow-up outstanding

- **声明**: ADR-0027（accepted 2026-06-03）§6 决定："`resolveOwningLearningItemIds` 改为'引用/representative'语义（或退役）；所有依赖 ownership 的读（`node-page.ts`、`artifacts/block-refs.ts`、`orchestrator/learning_intent.ts` 创建路径、`/api/learning-items/[id]`、`/api/knowledge/[id]`）相应重排。" 并注明 "兼容期：primary_artifact_id 列保留为可空 representative 指针，直到读层全切到 label 交集"。
- **代码**: P1-PR1 完成（uniqueIndex 已 drop → `src/db/schema.ts:237-243`；`GET /api/notes/[id]` 存在；`notesForKnowledge` 在 node-page.ts:345 调用）。P1-PR2 pending：`src/server/artifacts/block-refs.ts:211` 注释明确 "Rendering all referencing items is a follow-up (see notesForItem, P1-PR2)"；函数名仍为 `resolveOwningLearningItemIds`（语义更新了，名字未改）；`orchestrator/learning_intent.ts`、`/api/learning-items/[id]`、`/api/knowledge/[id]` 路径是否重排未验证。
- **漂移**: ADR 当日（2026-06-03）接受，P1-PR2 是明确的 staged rollout，不算 contradiction。但 3–5 天内若无后续 PR，"兼容期双语义"会成为真实债务（DB 说"可空 representative"，代码仍说 "Owning"）。属 documented-only：ADR 写了要做，PR2 未做。
- **建议**: 尽快开 P1-PR2 PR：① rename `resolveOwningLearningItemIds` → `resolveReferencingLearningItemIds`（或 retire）；② 校验 `learning_intent.ts` / learning-items / knowledge API 路由的 ownership 假设；③ 更新 block-refs.ts:211 注释（P1-PR2 完成后）。
- Linear issue（closeout gate）：
  - title: `Drift: ADR-0027 P1-PR2 read-layer follow-up pending`
  - labels: `drift`，关联 YUK-203

---

## Aligned（targeted 查证，有代码证据）

| 决策 | 证据文件 | 状态 |
|---|---|---|
| ADR-0021 transactional outbox | `src/db/schema.ts:585`（ingest_at 列）; `src/server/memory/triggers.ts:299`（SELECT FOR UPDATE SKIP LOCKED）; `src/server/events/queries.ts:1063`（INSERT-only） | aligned |
| ADR-0022 TipTap PM node schema | `src/ui/block-tree/BlockTreeRenderer.tsx`（无 @tiptap import）; `app/api/artifacts/[id]/body-blocks/route.ts`（PATCH endpoint）; `ArtifactBlockTree.tsx:20`（dynamic boundary） | aligned |
| ADR-0023 Redis cross-process presence | `src/server/artifacts/editing-session.ts:31`（REDIS_URL factory）; `src/server/artifacts/presence/{redis,in-memory,types}.ts`; `docker-compose.yml:114`（redis:7-alpine） | aligned |
| ADR-0025 goal entity + ND-5 | `src/db/schema.ts:723`（goal table）; `src/core/schema/proposal.ts:24`（goal_scope kind）; `src/server/goals/{scope,accept,queries}.ts` | aligned |
| ADR-0027 P1 schema migration | `src/db/schema.ts:237-243`（uniqueIndex → plain index，注释 ADR-0027）; `app/api/notes/[id]/route.ts`（GET 存在）; `src/server/knowledge/node-page.ts:345`（notesForKnowledge 调用） | aligned；P1-PR2 见 Documented-only |
| design-2026-06-02 block_merge 实现 | `src/server/ingestion/block-assembly.ts`; `src/core/schema/proposal.ts:30`; `src/server/proposals/actions.ts:1473`（acceptBlockMergeProposal） | aligned（实现对齐设计 spec）；ADR 缺失见 Undocumented |

---

## Phase-deferred (informational — 不报为 drift)

- **ADR-0014 前端科目感知**：`app/globals.css` / `app/(app)/review/page.tsx` 大量 `wenyan` 硬编码（font-family、notation 类型）。ADR-0014 §现状 line 32 明确列为已知 gap，Phase N+1/N+2/N+3 分批推进。
- **ADR-0014 question_part review UI / parent 聚合**：`2026-05-30 status update` DEFERRED（需 design pre-flight / 仅当碎片化时）。
- **BlockAssembly bbox/spatial v2**：design spec 2026-06-02 §0 明确 "page_index=0 placeholder; precise bbox is slice 2b, DEFERRED"。

---

## 范围声明

- 未改任何 `.ts` / `.css`；仅产出本报告。
- Schema 字段漂移由 `pnpm audit:schema` 管，本报告不重审。
- ADR-0024、ADR-0026 在 2026-05-30 报告中已验证 aligned，本次无新 commit 涉及，不重审。
