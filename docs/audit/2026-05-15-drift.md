# Drift Audit — 2026-05-15

**Scope**: ADR-0001..ADR-0012（缺 0009），`docs/superpowers/plans/2026-05-14-phase1c1-encounter-session-ui-scaffold.md` + `2026-05-14-phase1c2-ui-main.md`（active），CLAUDE.md
**Run by**: Claude Code (manual /audit-drift)
**Code anchor**: main @ 275c691
**Prior run**: `docs/audit/2026-05-16-drift.md`（首次，anchor 1b02dff）

## Summary

| 类别 | 数量 |
|---|---|
| ✅ Aligned | 8 (含 4 条首次报告 finding 已 resolved) |
| ⚠️ Undocumented | **2** |
| ⚠️ Documented-only | 0 |
| ❌ Contradicted | 0 |
| ⏳ Phase-deferred | 9 (ADR-0006/0008/0010/0011/0012 Phase 1c.1 队列；不报) |

**本轮实际待处理 drift = 2 项**（均为 Undocumented，低优先级）。首次报告的 4 条 finding 由 ADR-0003/0004 refresh（commit 275c691）完全解决。

---

## ⚠️ Undocumented

### [src/db/schema.ts:278] `artifact` 表注释说 "dead"，与 Phase 1c.1 决策矛盾

- **代码**: `src/db/schema.ts:278` 注释：`"标记为 dead schema。保留以等待 Phase 1c brainstorm 决定"`
- **ADR/Plan 现实**:
  - ADR-0006 v2（2026-05-15）明确决定：artifact 表**不 DROP**，作为"C 档 AI 主动产出落点"激活；GenerateArtifact event 写 `artifact` 表
  - Phase 1c.1 plan banner（2026-05-15 refresh）: `artifact 表不再 DROP（C 档 AI 主动产出激活）`
- **漂移**: schema.ts 注释描述的"brainstorm 待定"状态在 2026-05-15 ADR-0006 v2 决策后已过期；注释导致代码读者以为该表仍是 dead schema
- **建议**: 把 `src/db/schema.ts:278-280` 注释更新为 "激活 — C 档 AI 主动产出落点（per ADR-0006 v2, Phase 1c.1）"
- ```bash
  gh issue create --title "Drift: artifact table schema comment stale (says 'dead', ADR-0006v2 activates it)" \
    --label "drift,documentation" \
    --body "src/db/schema.ts:278 comment reads 'dead schema waiting for Phase 1c brainstorm'. ADR-0006 v2 (2026-05-15) and Phase 1c.1 plan explicitly decided: artifact table is NOT dropped and is activated as C-grade AI output landing point. Update comment to reflect current decision."
  ```

### [app/api/echo/ + src/server/boss/handlers/echo.ts] echo_jobs 表无 ADR / 设计文档追认

- **代码**: `src/db/schema.ts:410`（echo_jobs 表），`app/api/echo/route.ts`（POST），`app/api/echo/[id]/events/route.ts`（SSE），`src/server/boss/handlers/echo.ts`（pg-boss handler）——完整实现了一套 INSERT + pg-boss job + SSE push 的 echo 流程
- **ADR/Plan**: 任何 ADR（0001-0012）和活跃 plan 均未提及 `echo_jobs`；`docs/architecture.md` 无此词条；CLAUDE.md api surface 列表不含 `/api/echo`
- **漂移**: echo_jobs 是 pg-boss 框架集成的 demo/test harness，未决定它是生产功能还是开发脚手架；若前者需追认（何时清理、是否保留到 Phase 2）；若后者应在 Phase 1c.1 前 DROP
- **建议**: 在 Phase 1c.1 收尾前明确：(a) 保留并在 architecture.md 注明为 "pg-boss dev harness"，或 (b) DROP `echo_jobs` 表 + 删 `/api/echo` routes
- ```bash
  gh issue create --title "Drift: echo_jobs table + /api/echo routes undocumented — dev harness or production?" \
    --label "drift,documentation" \
    --body "echo_jobs table + app/api/echo/ routes are fully implemented (INSERT, pg-boss job, SSE) but not mentioned in any ADR, plan, or architecture.md. Decide before Phase 1c.1: (a) keep as pg-boss dev harness → add to architecture.md, or (b) drop table + routes (YAGNI). The ambiguity leaves it unclear whether it should survive the Phase 1c.1 schema cleanup."
  ```

---

## ✅ Aligned（已对齐，不展开）

1. **ADR-0001 TypeScript 单语种** — 仓库零 `.py` 文件（src/app 范围）
2. **ADR-0002 抽取层 = Tencent Mark Agent** — `src/server/ingestion/tencent_mark.ts` 使用 `SubmitQuestionMarkAgentJob` + `DescribeQuestionMarkAgentJob`；QuestionSplitOCR 全仓零引用；`/api/ingestion/[id]/rescue` 端点存在；`structuredToPromptMarkdown()` 在 `src/core/schema/structured_question.ts:128` 已实现
3. **ADR-0003 Anthropic 直连（providers.ts DEFERRED）** — `src/server/ai/runner.ts:3` 直接 `import { anthropic } from '@ai-sdk/anthropic'`；`src/server/ai/providers.ts` 不存在；ADR-0003 "当前状态（2026-05-16 audit）" 节正确描述现状；fallbackChain 决策明确延至 Sub 0d refresh
4. **ADR-0004 两类 Agent + task 现状** — registry.ts 包含 5 个 ✅ 已实装 task（AttributionTask / KnowledgeProposeTask / KnowledgeReviewTask / VisionExtractTask / VisionExtractTaskHeavy）；ADR-0004 task 表已 refresh 与代码一致；Sub 0d 5 task 明确标 ⏳；Provider Manager / Task Model Selector 有 status banner
5. **ADR-0005 IngestionSession single-owner** — `src/server/ingestion/session.ts` 存在；import/route.ts 的直接 db.update 已在代码注释中标为"已知缺口，Phase 1c.1 Step 4+5 修"；ADR-0005 invariant 在其他所有路径中成立
6. **ADR-0007 单用户假设** — schema 零 `user_id` 列；`middleware.ts` 实施 `x-internal-token` 唯一 auth
7. **ADR-0008 §修订 (agent_sessions 作废)** — 全仓零 `agent_sessions` / `agent_messages` 引用
8. **首次报告 4 finding → 全部 resolved**（commit 275c691 ADR refresh）：ADR-0003 Contradicted、ADR-0004 Contradicted、ADR-0004 task list Undocumented、fallbackChain Undocumented 均在 ADR 文本更新后转为 Aligned / Phase-deferred

---

## ⏳ Phase-deferred（Phase 1c.1 队列，**不算 drift**）

以下 9 项已在 `docs/superpowers/plans/2026-05-14-phase1c1-encounter-session-ui-scaffold.md` 明确排期，plan status = "ready for execution"：

| 项 | ADR | Phase 1c.1 Step |
|---|---|---|
| `event` 表 + 4 主索引 + GIN payload | ADR-0006 v2 | Step 1.1 |
| `learning_session` 表（多 type） | ADR-0008 | Step 1.1 |
| `material_fsrs_state` 投影表 | ADR-0006 v2 | Step 1.1 |
| `knowledge_edge` 表 + 5 relation_type | ADR-0010 | Step 1.1 + 7 |
| DROP `mistake` / `review_event` / `dreaming_proposal` / `judgment` / `user_appeal` | ADR-0006 v2 + 1c.1 Step 1.4 | Step 3 + 1.4 |
| DROP `knowledge.base_mastery` / `ai_delta_mastery` / `last_active_at` | ADR-0012 | Step 1.2 |
| CREATE VIEW `knowledge_mastery` | ADR-0012 | Step 1.3 |
| 12 KnownEvent Zod discriminated union（ADR-0006 v2 7 + ADR-0011 4 stable） | ADR-0011 | Step 2 |
| `src/server/session/` 多态模块（IngestionSession → LearningSession） | ADR-0008 | Step 5 |
