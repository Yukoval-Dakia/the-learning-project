# Drift Audit — 2026-05-15

**Scope**: ADR-0001..ADR-0012（缺 ADR-0009），`docs/superpowers/plans/2026-05-14-phase1c1-encounter-session-ui-scaffold.md` + `2026-05-14-phase1c2-ui-main.md`（active），`CLAUDE.md`
**Run by**: Claude Code (manual /audit-drift)
**Code anchors**:
- Run A: `main @ 275c691`（Phase 1c.1 Step 1 前）
- Run B: `phase1c1-lane-a-schema @ f6b944e`（Step 1.1+1.2+1.4 done；Steps 2-13 pending）
**Prior run**: `docs/audit/2026-05-16-drift.md`（首次，anchor 1b02dff；4 finding，全部由 ADR refresh 解决）

## Summary（合并）

| 类别 | 数量 |
|---|---|
| ✅ Aligned | 9（不展开） |
| ❌ Contradicted | **1**（Run B 新增） |
| ⚠️ Undocumented | **2**（Run A） |
| ⚠️ Documented-only | **7**（Run B，Phase 1c.1 Steps 2-13 在途） |
| ⏳ Phase-deferred | 3（Sub 0d DEFERRED，informational） |

**实际待处理 drift = 3 项**：1 Contradicted（高优）+ 2 Undocumented（低优）。7 条 Documented-only 为 Phase 1c.1 正常在途状态，不计入"真 drift"。

---

## Findings

### ❌ Contradicted（优先，Run B）

#### [Phase 1c.1 plan Steps 3-4 ↔ ADR-0006 v2] 计划 body 使用 `encounter` 表，ADR-0006 v2 无此表

- **声明**（plan Step 3，line 386）:
  > "读 `mistake` 全表 → 每行映射… → INSERT 进 `encounter`"；Step 4（line 400-404）同样要求建 `encounter` 表并将所有 `from '@/db/schema'` 改为导入 `encounter`
- **代码现实**:
  - `src/db/schema.ts` 有 `event` 表，**无 `encounter` 表**
  - ADR-0006 v2（2026-05-15）将 v1 的 `encounter` 概念整体替换为 `event`（action='attempt' + outcome 字段替代原 encounter outcome enum）
  - plan 第 29 行 REFRESH banner 也说"entity 名是 event 而非 encounter"，但 Step 3-4 body 未同步
- **冲突**: Agent 按 Step 3-4 字面执行时，会尝试写入不存在的 `encounter` 表（运行时报错）或误建此表（引入 ADR 禁止的实体）
- **建议**: Step 3-4 body 做 mechanical rename：`encounter` → `event`，payload 字段对齐 ADR-0006 v2 §event 表 schema（actor\_kind / action / subject\_kind / outcome / payload 替代原 encounter outcome enum + evidence jsonb）
- `gh issue create`:
  ```bash
  gh issue create \
    --title "Drift: Phase 1c.1 plan Steps 3-4 say 'encounter' but ADR-0006 v2 has 'event'" \
    --label "drift,documentation" \
    --body "Plan Steps 3-4 still use 'encounter' table from ADR-0006 v1. ADR-0006 v2 replaces encounter with event (action='attempt', outcome field). Schema has \`event\` not \`encounter\`. Steps 3-4 must be refreshed before execution or an agent will write to a non-existent table. See docs/audit/2026-05-15-drift.md §Contradicted."
  ```

---

### ⚠️ Undocumented（Run A，code anchor main @ 275c691）

#### [src/db/schema.ts:278] `artifact` 表注释说 "dead"，与 Phase 1c.1 决策矛盾

- **代码**: `src/db/schema.ts:278` 注释：`"标记为 dead schema。保留以等待 Phase 1c brainstorm 决定"`
- **ADR/Plan 现实**:
  - ADR-0006 v2（2026-05-15）明确决定：artifact 表**不 DROP**，作为"C 档 AI 主动产出落点"激活；GenerateArtifact event 写 `artifact` 表
  - Phase 1c.1 plan banner（2026-05-15 refresh）: `artifact 表不再 DROP（C 档 AI 主动产出激活）`
- **漂移**: schema.ts 注释描述的"brainstorm 待定"状态在 ADR-0006 v2 决策后已过期；注释导致代码读者以为该表仍是 dead schema
- **建议**: 把 `src/db/schema.ts:278-280` 注释更新为"激活 — C 档 AI 主动产出落点（per ADR-0006 v2, Phase 1c.1）"
- `gh issue create`:
  ```bash
  gh issue create --title "Drift: artifact table schema comment stale (says 'dead', ADR-0006v2 activates it)" \
    --label "drift,documentation" \
    --body "src/db/schema.ts:278 comment reads 'dead schema waiting for Phase 1c brainstorm'. ADR-0006 v2 (2026-05-15) and Phase 1c.1 plan explicitly decided: artifact table is NOT dropped and is activated as C-grade AI output landing point. Update comment to reflect current decision."
  ```

#### [app/api/echo/ + src/server/boss/handlers/echo.ts] echo\_jobs 表无 ADR / 设计文档追认

- **代码**: `src/db/schema.ts:410`（echo\_jobs 表），`app/api/echo/route.ts`（POST），`app/api/echo/[id]/events/route.ts`（SSE），`src/server/boss/handlers/echo.ts`（pg-boss handler）——完整实现了一套 INSERT + pg-boss job + SSE push 的 echo 流程
- **ADR/Plan**: 任何 ADR（0001-0012）和活跃 plan 均未提及 `echo_jobs`；`docs/architecture.md` 无此词条；CLAUDE.md api surface 列表不含 `/api/echo`
- **漂移**: echo\_jobs 是 pg-boss 框架集成的 demo/test harness，未决定它是生产功能还是开发脚手架
- **建议**: 在 Phase 1c.1 收尾前明确：(a) 保留并在 architecture.md 注明为"pg-boss dev harness"，或 (b) DROP `echo_jobs` 表 + 删 `/api/echo` routes
- `gh issue create`:
  ```bash
  gh issue create --title "Drift: echo_jobs table + /api/echo routes undocumented — dev harness or production?" \
    --label "drift,documentation" \
    --body "echo_jobs table + app/api/echo/ routes are fully implemented (INSERT, pg-boss job, SSE) but not mentioned in any ADR, plan, or architecture.md. Decide before Phase 1c.1: (a) keep as pg-boss dev harness → add to architecture.md, or (b) drop table + routes (YAGNI)."
  ```

---

### ⚠️ Documented-only（Run B，Steps 2-13 pending — 正常在途，不含 issue create 命令）

以下 7 条均为 Phase 1c.1 Step 1 schema 已建、对应代码尚未迁移的预期中间状态。仅供追踪，不建议单独开 issue（会在 Steps 2-9 执行时自动消解）。

#### [ADR-0006 v2 + ADR-0011] `src/core/schema/event.ts` Zod discriminated union 缺失

- **现实**: `src/core/schema/` 无 `event.ts`；event 表已建但无 parse barrier — 任何 `db.insert(event)` 调用均绕过 Zod 守护
- **计划落点**: Phase 1c.1 Step 2（Lane B）
- **注意**: Step 3/4 写入代码必须等 Step 2 Zod schema 合并后才能执行，否则违反 ADR-0006 v2 invariant

#### [ADR-0006 v2 + ADR-0008] `event` + `learning_session` 表运行时零写路径

- **现实**: 全库生产代码零 `db.insert(event|learning_session)` 调用；所有实际写入仍流向旧表
- **计划落点**: Phase 1c.1 Steps 3-9

#### [ADR-0006 v2 §DROP mistake] `mistake` 仍被生产路由写入

- **现实**: `app/api/mistakes/route.ts:6`、`app/api/ingestion/[id]/import/route.ts` 写 mistake 行
- **计划落点**: Phase 1c.1 Steps 3-4（migrate）+ Step 9（DROP）

#### [ADR-0006 v2 §DROP review\_event] `review_event` 仍被生产路由写入

- **现实**: `app/api/review/submit/route.ts:8` `db.insert(review_event)` 写 FSRS 状态
- **计划落点**: Phase 1c.1 Steps 3-4

#### [ADR-0006 v2 §DROP dreaming\_proposal] `dreaming_proposal` 仍被 proposals.ts 写入

- **现实**: `src/server/knowledge/proposals.ts:68` `db.insert(dreaming_proposal)`
- **计划落点**: Phase 1c.1 Step 3

#### [ADR-0008] `ingestion_session` 未迁入 `learning_session(type='ingestion')`

- **现实**: `src/server/ingestion/session.ts`、`src/server/boss/handlers/tencent_ocr_extract.ts:58`、`src/server/export/constants.ts:18` 仍用 ingestion\_session；learning\_session 零读写
- **计划落点**: Phase 1c.1 Step 5

#### [ADR-0005 → ADR-0008] `src/server/session/` 多态模块未建

- **现实**: `src/server/session/` 目录不存在；ingestion 子状态机未并入多态模块
- **计划落点**: Phase 1c.1 Step 5

---

## ✅ Aligned（不展开）

1. ADR-0001 TypeScript 单语种 — 仓库零 `.py` 文件
2. ADR-0002 VisionExtract\* `invocation: 'manual_rescue_only'` — registry.ts:62/76 已标注；QuestionSplitOCR 零引用
3. ADR-0003 Anthropic 唯一实现 — runner.ts 直接 `import { anthropic }`；ADR 已 refresh 注明当前状态
4. ADR-0004 task 注册表 — 5 个 ✅ 已实装 task；ADR-0004 task 表已 refresh（2026-05-16 audit）
5. ADR-0007 单用户 + x-internal-token — middleware.ts:7-8；schema 零 user\_id
6. ADR-0010 knowledge\_edge 表 + 4 indexes — schema.ts 完整落地
7. ADR-0011 event.subject\_kind（chip / query / knowledge\_edge）— schema event 表注释对齐
8. ADR-0012 mastery stub DROP + knowledge\_mastery pgView — Phase 1c.1 Step 1.2+1.3 落地 ✅
9. 首次报告 4 finding → 全部 resolved（ADR refresh commit 275c691）

---

## ⏳ Phase-deferred（informational，不算 drift）

| 项 | ADR | 延后原因 |
|---|---|---|
| Provider Manager (`src/server/ai/providers.ts`) | ADR-0003/0004 | Sub 0d DEFERRED（1c.1+1c.2 后 refresh） |
| `resolveTaskModel()` + TaskModelConfig | ADR-0004 | Sub 0d DEFERRED |
| Copilot session（`learning_session(type='conversation')`） | ADR-0008 修订 | Sub 0d DEFERRED |

---

## 信号校准

**本轮合并（Run A + Run B）**：10 条 findings（1 ❌ + 2 ⚠️ Undocumented + 7 ⚠️ Documented-only）。
7 条 Documented-only 全部是 Phase 1c.1 在途工作，信噪比偏低（70% in-flight）。
**建议下一轮调整**：把"有明确 plan Step 的 Documented-only"移入 Phase-deferred 不报，仅报"有 ADR 决策但无任何 plan Step 承接"的真 Documented-only。这样本轮只剩 3 条（1 ❌ + 2 ⚠️ Undocumented）= 信号更干净。
