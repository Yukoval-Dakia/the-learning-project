# Drift Audit — 2026-05-15

**Scope**: ADR-0001..ADR-0012（缺 0009），`docs/superpowers/plans/2026-05-14-phase1c1-encounter-session-ui-scaffold.md` + `2026-05-14-phase1c2-ui-main.md`（active），`CLAUDE.md`，`docs/architecture.md`，`docs/design/*.md`（近 30d）
**Run by**: Claude Code (manual /audit-drift)
**Code anchor**: main @ ce3ae09（Phase 1c.1 Lane C1 UI scaffold merged）
**Prior run**: `docs/audit/2026-05-16-drift.md`（首次，anchor 1b02dff；ADR-0003/0004 refresh 后 4 条 finding 已 resolved）

## Summary

| 类别 | 数量 |
|---|---|
| ✅ Aligned | 8（不展开） |
| ❌ Contradicted | **4** |
| ⚠️ Documented-only | **4** |
| ⚠️ Undocumented | **2** |
| ⏳ Phase-deferred | 9 (informational) |

**实际待处理 drift = 10 项**（4 Contradicted + 4 Documented-only + 2 Undocumented）。

---

## Findings

### ❌ Contradicted（优先）

#### [ADR-0012 §"决策"] `knowledge` stub mastery 字段 + 旧 check constraint 仍在 schema

- **声明**: ADR-0012 (2026-05-15) §"决策": Phase 1c.1 Step 1.2 **同步执行** DROP `base_mastery / ai_delta_mastery / last_active_at` 三字段及配套 check constraint；明确以 derived view 取代"hybrid mastery"方案。
- **代码**: `src/db/schema.ts:54-56, 70-72` — 三字段仍存在；`L71` 注释 `// per ADR: hybrid mastery` 引用已被 ADR-0012 废弃的方案。
- **冲突**: schema 注释引用了 ADR-0012 所取消的"hybrid mastery"，新旧 ADR 在同一文件名义冲突；Phase 1c.1 agentic worker 读到 L71 注释会误认为仍有 ADR 支撑。
- **建议**: Phase 1c.1 Step 1.2 执行时删三字段 + 两 check constraint；注释改为 `// see ADR-0012: mastery is a derived view from events`。
- `gh issue create`:
  ```bash
  gh issue create \
    --title "Drift: ADR-0012 — mastery stub fields + hybrid-mastery constraint still in schema" \
    --label "drift,schema" \
    --body "ADR-0012 (2026-05-15) decides to DROP knowledge.base_mastery / ai_delta_mastery / last_active_at and replace with knowledge_mastery PG view. All three fields still at src/db/schema.ts:54-56; check constraints at L70-72 still enforce hybrid mastery invariant that ADR-0012 cancels. Phase 1c.1 Step 1.2 must execute DROP."
  ```

#### [ADR-0006 v2 §"保留"] artifact 表注释声称"awaiting decision"但决策已落 ADR

- **声明**: ADR-0006 v2 (2026-05-15): "artifact 表'激活'，从 dead schema 变为 AI 主要产出落点"；Phase 1c.1 REFRESH banner 确认"artifact 表不再 DROP"。
- **代码**: `src/db/schema.ts:276-279` — "标记为 dead schema。保留以**等待 Phase 1c brainstorm 决定**其与 encounter / Note 的关系；若 Phase 1c spec 拍板不用，应在该 phase 同步 migration 删表。"
- **冲突**: 代码注释声称"awaiting decision"，实际决策已在 ADR-0006 v2 落定；Phase 1c.1 Step 9 可能被误导执行 DROP。
- **建议**: 删除"dead schema / waiting for brainstorm"注释，改为 `// 激活 — C 档 AI 主动产出落点（per ADR-0006 v2, 2026-05-15）；write paths 在 Phase 1c.2 AI tasks 中实现`。
- `gh issue create`:
  ```bash
  gh issue create \
    --title "Drift: artifact schema comment contradicts ADR-0006 v2 activation decision" \
    --label "drift,documentation" \
    --body "src/db/schema.ts:276-279 says 'dead schema, waiting for Phase 1c brainstorm to decide'. ADR-0006 v2 (2026-05-15) already decided: artifact IS activated as AI primary output target, NOT dropped. Stale comment risks misleading agentic workers executing Phase 1c.1 Step 9."
  ```

#### [Plan 2026-05-14 Phase1c1 §Step3/4/6] 步骤体引用 `encounter` 表，REFRESH banner + ADR-0006 v2 决策是 `event` 表（无 encounter）

- **声明**: Phase 1c.1 REFRESH banner (2026-05-15): "entity 名是 event 而非 encounter"；ADR-0006 v2: DROP `mistake` → `event WHERE action='attempt' AND outcome='failure'`（schema 无 encounter 表）。
- **代码（文档）**: `docs/superpowers/plans/2026-05-14-phase1c1-encounter-session-ui-scaffold.md:382, 396, 433` — Step 3 `INSERT 进 encounter`；Step 4 `mistake → encounter`；Step 6 `/api/mistakes → /api/encounters`。
- **冲突**: REFRESH banner 与步骤体自相矛盾——Step 3/4/6 照字面执行将创建 ADR-0006 v2 schema 不存在的 `encounter` 表和 `/api/encounters` 路由。
- **建议**: 更新 Step 3/4/6 步骤体：`encounter` → `event`；Step 6 若保留用户面词"/mistakes"，内部改为查 `event WHERE action='attempt' AND outcome='failure'`。
- `gh issue create`:
  ```bash
  gh issue create \
    --title "Drift: Phase 1c.1 plan Step 3/4/6 reference 'encounter' table (ADR-0006 v2 uses 'event', no encounter)" \
    --label "drift,planning" \
    --body "Plan 2026-05-14-phase1c1 REFRESH banner says 'entity name is event not encounter', but Step 3 (INSERT into encounter), Step 4 (rename to encounter), Step 6 (/encounters) have not been updated. Executing literally creates a table absent from ADR-0006 v2 schema. Must update Steps 3/4/6 before execution."
  ```

#### [ADR-0010 §relation_type] `docs/architecture.md:9` 声称"三种边"，ADR-0010 定义 5 种

- **声明**: ADR-0010 (2026-05-15) §"relation_type 核心集合": 5 个正式类型——`prerequisite / related_to / contrasts_with / applied_in / derived_from` + `experimental:*` 命名空间。
- **代码**: `docs/architecture.md:9` — "每个知识点是一个节点，节点之间有「前置 / 关联 / 同属」三种边。"
- **冲突**: "三种边"且枚举"同属"（无 ADR-0010 对应项），与 ADR-0010 的 5 种不符；读者会误判 mesh 方向数与语义。
- **建议**: `docs/architecture.md §一` 第 1 段末行改为"5 种有类型横向边（prerequisite / related_to / contrasts_with / applied_in / derived_from，per ADR-0010）"，或删除枚举改为"见 ADR-0010"引用。
- `gh issue create`:
  ```bash
  gh issue create \
    --title "Drift: architecture.md '三种边' contradicts ADR-0010 5 relation_types" \
    --label "drift,documentation" \
    --body "docs/architecture.md line 9 says knowledge nodes have 3 edge types (前置/关联/同属). ADR-0010 (2026-05-15) defines 5 types: prerequisite/related_to/contrasts_with/applied_in/derived_from + experimental:*. '同属' has no ADR-0010 counterpart. Update architecture.md §一 to reference ADR-0010 relation types."
  ```

---

### ⚠️ Documented-only

#### [ADR-0006 v2 §event核] event 表 + DROP 五旧表：Phase 1c.1 Step 1/9 未执行

- **声明**: ADR-0006 v2: ADD `event` 表（含 payload GIN index）；DROP `mistake / review_event / dreaming_proposal / judgment / user_appeal` 迁移进 event；Phase 1c.1 Step 1.1 + Step 9。
- **代码**: `src/db/schema.ts` — 无 `event` 表；五旧表仍在；`app/api/review/submit/route.ts:83` 仍用 `review_event`。
- **建议**: Phase 1c.1 Step 1.1 (ADD event) → Step 3 数据迁移 → Step 9 DROP；review/submit 路由改写 event 路径。
- `gh issue create`:
  ```bash
  gh issue create \
    --title "Drift: ADR-0006 v2 — event table absent; five old tables not dropped" \
    --label "drift,schema" \
    --body "Phase 1c.1 Step 1.1 + Step 9 not yet executed. No event table. Five tables (mistake / review_event / dreaming_proposal / judgment / user_appeal) still in schema; app/api/review/submit/route.ts:83 still writes review_event."
  ```

#### [ADR-0008 §决策] `learning_session` 表 + `src/server/session/` 模块未实现

- **声明**: ADR-0008: `learning_session` 替代 `ingestion_session`；`src/server/session/` 多态模块（`ingestion.ts` + `review.ts`）；Phase 1c.1 Step 5。
- **代码**: `src/db/schema.ts` 无 `learning_session`；`src/server/session/` 不存在；`src/server/ingestion/session.ts` 仍是唯一入口。
- **建议**: Phase 1c.1 Step 5：创建 `src/server/session/` + drizzle migration for `learning_session`；ADR-0005 single-owner invariant 在新模块内复现。
- `gh issue create`:
  ```bash
  gh issue create \
    --title "Drift: ADR-0008 — learning_session table and src/server/session/ module not implemented" \
    --label "drift,schema" \
    --body "Phase 1c.1 Step 5 not executed. ingestion_session still in schema; no learning_session; no src/server/session/ polymorphic module."
  ```

#### [ADR-0010 + ADR-0011 §schema/Zod] `knowledge_edge` 表 + event Zod discriminated union 未实现

- **声明**: ADR-0010: `knowledge_edge` 表（5 relation_type + experimental:*）；ADR-0011: `src/core/schema/event/` 12 KnownEvent + ExperimentalEvent Zod union；Phase 1c.1 Step 1.1 + Step 2。
- **代码**: `src/db/schema.ts` 无 `knowledge_edge`；`src/core/schema/` 无 `event/` 目录（仅 business.ts / generated.ts / index.ts / structured_question.ts）。
- **建议**: Phase 1c.1 Step 1.1 加 `knowledge_edge` 表；Step 2 建 `src/core/schema/event/known.ts + experimental.ts + index.ts` + 测试。
- `gh issue create`:
  ```bash
  gh issue create \
    --title "Drift: ADR-0010+ADR-0011 — knowledge_edge table and event Zod schemas absent" \
    --label "drift,schema" \
    --body "Phase 1c.1 Step 1.1 (knowledge_edge per ADR-0010) and Step 2 (src/core/schema/event/ discriminated union, 12 KnownEvent + ExperimentalEvent per ADR-0011) not executed. AI output has no Zod contract guard."
  ```

#### [ADR-0012 §"落地步骤"] `knowledge_mastery` PG view 未建

- **声明**: ADR-0012: Phase 1c.1 Step 1.3 建 `knowledge_mastery` VIEW（mastery / evidence_count / last_active_at），drizzle 注册 `.existing()` view。
- **代码**: `src/db/schema.ts` 无 `knowledge_mastery` view；无对应 SQL migration；mastery 查询当前返回 stub 值 0（stub 字段零 write path）。
- **建议**: Phase 1c.1 Step 1.3：手写 SQL migration + `pgView('knowledge_mastery', {...}).existing()`；Step 1.2 DROP stub 字段后同步跑 smoke test。
- `gh issue create`:
  ```bash
  gh issue create \
    --title "Drift: ADR-0012 — knowledge_mastery derived view not created" \
    --label "drift,schema" \
    --body "Phase 1c.1 Step 1.3 not executed. CREATE VIEW knowledge_mastery absent. All mastery queries return stub 0.0 (zero write paths). Sub 0d get_weak_points tool depends on this view."
  ```

---

### ⚠️ Undocumented（代码做了 ADR 没追认）

#### [schema.ts:410 + /api/echo] `echo_jobs` 表与 `/api/echo` 路由无 ADR / 设计文档追认

- **代码**: `src/db/schema.ts:410`（echo_jobs 表）；`app/api/echo/route.ts`（POST enqueue）；`app/api/echo/echo.e2e.test.ts`（E2E 验收）；`src/server/boss/handlers/echo.ts`（pg-boss handler）。
- **ADR / Plan**: ADR-0001..0012 和所有活跃 plan 均未提及；CLAUDE.md API surface 列表不含 `/api/echo`；`docs/architecture.md` 无此词条。
- **漂移**: echo_jobs 应是 pg-boss E2E 验收门的 dev harness，但未在任何 ADR / CONTEXT.md 中定位；Phase 1c.1 schema 清理期需要决策：保留（加文档）or DROP。
- **建议**: 在 CLAUDE.md 或 architecture.md 补一句"echo_jobs + /api/echo 是 pg-boss E2E dev harness，非业务路由，NOT DROPPED in Phase 1c.1"；或在 Phase 1c.1 Step 9 一并 DROP。
- `gh issue create`:
  ```bash
  gh issue create \
    --title "Drift: echo_jobs table and /api/echo routes undocumented (no ADR / architecture.md mention)" \
    --label "drift,documentation" \
    --body "echo_jobs table (src/db/schema.ts:410) + app/api/echo/* routes + boss/handlers/echo.ts are fully implemented but mentioned in no ADR or architecture.md. Phase 1c.1 schema cleanup must decide: retain as dev harness (add doc) or DROP."
  ```

#### [ADR-0004 refresh] `docs/architecture.md §5.1` 任务表与 ADR-0004 2026-05-16 refresh 不同步

- **代码**: `docs/architecture.md §5.1`（或相关章节）仍列旧任务集（含不存在的 VisionAnswerExtractTask / VariantVerifyTask 等）；缺 KnowledgeReviewTask（已实装）。
- **ADR**: ADR-0004 2026-05-16 refresh 提供正确 Task 现状表（AttributionTask / KnowledgeProposeTask / KnowledgeReviewTask / VisionExtract* 四类已实装；其余五类 Sub 0d DEFERRED）。
- **漂移**: architecture.md 任务表与 ADR-0004 已更新的正确列表不符，阅读者会误判实装状态。
- **建议**: 同步 `docs/architecture.md` 任务章节至 ADR-0004 2026-05-16 版本的 Task 现状表。
- `gh issue create`:
  ```bash
  gh issue create \
    --title "Drift: architecture.md task list out of sync with ADR-0004 2026-05-16 refresh" \
    --label "drift,documentation" \
    --body "docs/architecture.md still lists stale task set (includes non-existent VisionAnswerExtractTask / VariantVerifyTask; missing KnowledgeReviewTask which is implemented). ADR-0004 2026-05-16 refresh has correct task status table. Sync architecture.md to match."
  ```
