# Drift Audit — 2026-05-23

**Scope**: ADR-0001..ADR-0016, plans/2026-05-{18..22}-*, CLAUDE.md
**Run by**: Claude Code (manual /audit-drift)

## Summary

- Aligned: 28（不展开）
- Documented-only: 3
- Undocumented: 0
- Contradicted: 0
- Phase-deferred: 2 (informational — memory_brief_note write path ADR-0015 §2 Phase 2C; Codex ADR-0016 proposed/spike-pending)

## Findings

### ⚠️ Documented-only

#### [ADR-0002 §extracted_prompt_md] `question_block.extracted_prompt_md` column 未 DROP

- **声明**: "extracted_prompt_md 不持久化，所有 markdown 视图由 structured 现场派生，杜绝双源不一致" (`docs/adr/0002-structured-extraction-vs-llm-analysis.md`)
- **代码**: `src/db/schema.ts:117` — 列仍存在（nullable）；`src/server/session/ingestion.ts:242` — 新路径显式写 `extracted_prompt_md: null`；`src/server/ingestion/rescue.ts:121` — 读 `b.extracted_prompt_md` 作 `prompt_text`（永远为 null，dead code）
- **漂移**: ADR 已宣告"不持久化"，且 ingestion 全面写 null，但从未产出 DROP COLUMN 迁移。rescue.ts 读 null 列是死代码。
- **建议**: 新建 migration DROP COLUMN `question_block.extracted_prompt_md`；同步移除 `rescue.ts:121` 的死读行。
- `gh issue create`:
  ```bash
  gh issue create \
    --title "Drift: ADR-0002 ↔ question_block.extracted_prompt_md column not dropped" \
    --label "drift,technical-debt" \
    --body "ADR-0002 declares extracted_prompt_md is not persisted, ingestion writes null, but the column is never DROP'd via migration. rescue.ts:121 reads it as dead code. Add a Drizzle migration to DROP COLUMN + remove the dead read in rescue.ts. See docs/audit/2026-05-23-drift.md"
  ```

---

#### [ADR-0002 §2026-05-21 / post-M3] `question.metadata.prompt_image_refs` 写路径未清理

- **声明**: "question.metadata.prompt_image_refs deprecated (M-1 / 2026-05-21)：保留以兼容老 reader（answer history rendering 等），**M3 后视使用情况移除**" (`docs/adr/0002-structured-extraction-vs-llm-analysis.md` §2026-05-21 修订)
- **代码**: M3 已落地（PR #84，2026-05-22）。当前零读路径从 `metadata.prompt_image_refs` 取值（`src/` + `app/` 全扫无命中）。但两处仍写入：
  - `app/api/ingestion/[id]/import/route.ts:347` — `prompt_image_refs: block.image_refs`（含 `// M3+ removal` 注释）
  - `app/api/mistakes/route.ts:101` — `prompt_image_refs: body.prompt_image_refs`（无 removal 注释）
- **漂移**: M3 触发了"视使用情况移除"的评估时机；零 reader 说明迁移条件满足，但 write path 未清理。`mistakes/route.ts` 甚至缺少 deprecated 注释。
- **建议**: (1) 删除两处 `metadata.prompt_image_refs` 写入；(2) 验证 mistakes route 同步写入新的 `question.image_refs` 列；(3) 更新 import route 注释。
- `gh issue create`:
  ```bash
  gh issue create \
    --title "Drift: ADR-0002 post-M3 — metadata.prompt_image_refs write cleanup" \
    --label "drift,technical-debt" \
    --body "ADR-0002 (2026-05-21 revision) marks question.metadata.prompt_image_refs for removal post-M3. M3 is now shipped, zero readers exist, but two write paths remain (import/route.ts:347, mistakes/route.ts:101). Drop the metadata writes; verify mistakes route writes question.image_refs instead. See docs/audit/2026-05-23-drift.md"
  ```

---

#### [ADR-0004 §第一类 Backend Purpose Agent] 审计表名与实际实现不符

- **声明**: ADR-0004 第一类 Backend Purpose Agent 审计行声明: `审计 | ai_task_runs + ai_tool_calls + ai_cost_ledger` (`docs/adr/0004-pattern-c-two-type-agent-architecture.md:56`)
- **代码**: 实际表名为 `ai_task_runs`（✅ 一致）、`tool_call_log`（≠ `ai_tool_calls`）、`cost_ledger`（≠ `ai_cost_ledger`）。注：ADR-0004 自身第 18 行的 prose 段已正确写 `tool_call_log / cost_ledger / event mirror`，与代码一致。不一致仅在该 ADR 的 table row。
- **漂移**: ADR-0004 表格行沿用了更早的草案名（带 `ai_` 前缀），与落地名不符；文档内部自相矛盾。代码是正确的，文档是错的。
- **建议**: 修改 ADR-0004 第 56 行 table row，将 `ai_tool_calls` → `tool_call_log`，`ai_cost_ledger` → `cost_ledger`。
- `gh issue create`:
  ```bash
  gh issue create \
    --title "Drift: ADR-0004 table row uses stale audit table names (ai_tool_calls / ai_cost_ledger)" \
    --label "drift,docs" \
    --body "ADR-0004 §Backend Purpose Agent table row says ai_task_runs + ai_tool_calls + ai_cost_ledger, but actual tables are tool_call_log and cost_ledger (the ADR's own prose on line 18 has the correct names). Needs a one-line fix to the table row. See docs/audit/2026-05-23-drift.md"
  ```
