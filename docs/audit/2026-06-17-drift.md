# Drift Audit — 2026-06-17

**Scope**: ADR-0021..ADR-0044, CLAUDE.md stack note, `docs/design/` 2026-05-17..2026-06-16
**Run by**: Claude Code (manual /audit-drift)

## Summary

- Aligned: 8（不展开）
- Documented-only: 4
- Undocumented: 2
- Contradicted: 1
- Phase-deferred: ~4（informational，不展开：ADR-0038 Variant Wave 4；ADR-0036 Misconception gated ADR-0034；ADR-0040 mastery trigger gated B1；ADR-0043 active-PPI phase 6）

---

## Findings

### ❌ Contradicted

#### [ADR-0037 §5+§8] review_plan 全删决定 vs 仍然在线

- **声明**: "review_plan 退役并入引擎...随即直接退役/全删（不再独立注册、不再另开 paper channel）；ReviewPlanTask 同步退场" (ADR-0037 §5)；备选「review_plan 降为 proposer 长期保留」已被**否决**（ADR-0037 §8）。
- **代码**: `src/server/ai/tools/allowlists.ts:100,329` — `'write_review_plan'` 仍列入 allowlists；`allowlists.ts:340` — `review_plan: REVIEW_PLAN_TOOLS` 仍作独立 surface 被 bootstrap；`src/server/review/` fallback 路径仍调 review-plan 逻辑。
- **冲突**: ADR-0037 否决了"长期保留 proposer"备选，要求全删；代码仍将 review_plan 作 live bootstrapped surface 维护，与决策相反。ADR-0037 §8 代价段也承认"退役需迁移（migration + 测试覆盖）"，但迁移尚未启动。
- **建议**: 开迁移任务：注销 ReviewPlanTask、删 `write_review_plan` allowlist entry、把其产品 intent（知识点排期/复习配比）迁入合并引擎；同步补测试覆盖。
- **Linear issue**: 若无在追，建议创建：`title: "Drift: ADR-0037 review_plan 全删 ↔ allowlists.ts"`, `labels: ["drift"]`

---

### ⚠️ Documented-only

#### [ADR-0044 §5] Wave 0 全部可交付物未启动

- **声明**: "Wave 0：`mastery_state`+`material_fsrs_state` 快照可逆 + recursive CTE 遍历器骨架 + `ctx.causedByEventId` worker 透传补全" (ADR-0044 §5)；Wave 0 与 reach/endurance 并行起跑，是 checkpoint 腿的硬前置。
- **代码**:
  - `src/server/events/cascade.ts` — 不存在（仓库首个 `WITH RECURSIVE` 遍历器）
  - `src/core/projections/` — 目录不存在（fold reducer 落点）
  - `src/core/schema/event/known.ts` — 无 `experimental:state_snapshot` event schema
  - `package.json` — 无 `audit:projection` 脚本（ADR-0044 §3.3 + design doc §5.2 mandated）
- **冲突**: ADR 于 2026-06-15 接受，声明 Wave 0 与 reach/endurance 并行；但 Wave 0 所有交付物代码侧零。缺 cascade 遍历器则 checkpoint 腿（ADR-0041）无法落地。
- **建议**: 确认 Wave 0 启动时间点；优先补 `ctx.causedByEventId` worker 透传（ADR-0044 §3.4 头号硬前置）和 cascade CTE 骨架，再做 snapshot event schema。
- **Linear issue**: `title: "Drift: ADR-0044 Wave 0 未启动（cascade CTE + snapshot event + fold reducer）"`, `labels: ["drift"]`

#### [B4/decisions-ledger 2026-06-14 §1] mem0 P3 读路径零实现

- **声明**: "P3 读路径接通（`searchMemories` wrapper：topK 放大 + superseded 过滤 + recency 半衰期重排；两消费者 search_memory_facts + brief searchFacts 透明获益）= task #23" (decisions-ledger §1 B4，owner 2026-06-14 拍板)
- **代码**: `src/server/memory/` — grep `searchMemories` 在 `src/` 下**零命中**；`src/capabilities/copilot/server/context-builder.ts` 和 memory brief 路径均直接返回已 SUPERSEDE 的旧记忆，无过滤。AI pipeline current map（2026-06-15）独立确认"grep searchMemories 在 src 下零命中"。
- **冲突**: 软取代标记（SUPERSEDE）写入后从不读取——过时/已撤销的记忆原样注入 Copilot 上下文，污染 AI 推理输出。Owner 明确拍板接通，代码无任何实现。
- **建议**: 实现 `searchMemories` wrapper（topK 放大 → superseded 过滤 → recency 半衰期重排），接入两消费者（context-builder / brief）。
- **Linear issue**: `title: "Drift: mem0 P3 读路径 searchMemories 未实现（superseded 记忆污染上下文）"`, `labels: ["drift"]`

#### [ADR-0034 §3] 知识结构一致性闸零实现

- **声明**: "写入期结构一致性闸（环检测 / 方向矛盾 / 传递冗余）= 独立前置，priority High，RT1/RT2/RT4 三个方向的共同前置" (ADR-0034；decisions-ledger §B2 关系结构"gated 在一致性闸地基之后")
- **代码**: `src/capabilities/knowledge/server/proposals.ts:486-624` — `acceptProposal` 写 knowledge 突变时无任何 cycle/direction/redundancy 验证；无 `validateStructuralConstraints` 或等价函数存在于 `src/`。
- **冲突**: 一致性闸是 High-priority 独立前置，不是 phase-deferred，但代码侧零实现。随着 KG 边累积，无环检测意味着可以写入逻辑循环依赖（A→B→A）。
- **建议**: 在 `acceptProposal` 写路径前加入 topo 一致性检查（cycle detect + direction constraint + transitive redundancy pruning）；可复用 ADR-0034 §3 指定的 rubric-validator 语义闸补充层。
- **Linear issue**: `title: "Drift: ADR-0034 知识结构一致性闸（cycle/direction/redundancy）零实现"`, `labels: ["drift"]`

---

### ⚠️ Undocumented

#### [ADR-0032 + ADR-0041 §55] Worker copilot 工具注册半接线，无 ADR 背书

- **声明**: ADR-0032 mandates manifest-contributed DomainTool aggregation via `registerCapabilityCopilotTools`；ADR-0041 §55 flags worker tool registration as a "risk gap" but no ADR formally endorses the bootstrap workaround.
- **代码**: `scripts/worker.ts`（或 start-worker）— worker durable job 走 CORE_TOOLS bootstrap，**未调用** `registerCapabilityCopilotTools`；worker 内 copilot job 实际获得 flat core-tools set，而非 manifest 贡献全集。ADR-0041 §55 自认"half-wired gap"但未将 bootstrap 方案升级为正式决定。
- **冲突**: 没有任何 ADR 正式裁定"worker 用 CORE_TOOLS bootstrap 作长期方案"；ADR-0032 + ADR-0041 的意图是 manifest 完整贡献。两者间的差距未追认。
- **建议**: 在 worker startup 接入 `registerCapabilityCopilotTools`；或在 ADR-0032/ADR-0041 补注明 CORE_TOOLS bootstrap 为正式临时方案并写明切换条件。
- **Linear issue**: `title: "Drift: worker copilot 工具注册走 bootstrap 而非 manifest（ADR-0032/ADR-0041 未追认）"`, `labels: ["drift"]`

#### [ADR-0044 §3.3 + design-doc §5.2] `pnpm audit:projection` 脚本缺失

- **声明**: "新增 `pnpm audit:projection`（仿 `audit:schema`）：全量 fold diff 重算 vs 现表，非空 diff = drift fail — 接 CLAUDE.md audit 家族" (ADR-0044 §3.3；`2026-06-15-event-sourcing-foundation-redesign.md` §5.2)
- **代码**: `package.json` — 无 `audit:projection` 条目；`scripts/` — 无 `audit-projection.ts`；CLAUDE.md Commands 段 audit 家族（audit:schema / audit:partition / audit:profile / audit:draft-status）缺此项。
- **冲突**: ADR-0044 将 `audit:projection` 列为事件真相闸的机器守门（与 `/audit-drift` skill 并列）。脚本不存在意味着 projection 表 vs event 流的漂移无自动检测，是事件溯源地基缺口。
- **建议**: 至少 stub `scripts/audit-projection.ts`（可先输出 TODO 提示）并加入 `package.json`，标记为 Wave 0-3 后填充；同步更新 CLAUDE.md Commands 段。
- **Linear issue**: `title: "Drift: pnpm audit:projection 脚本缺失（ADR-0044 mandate）"`, `labels: ["drift"]`
