# Drift Audit — 2026-05-25

**Scope**: ADR-0001..ADR-0018, plans/2026-05-{22..25} (active), CLAUDE.md Architecture/Layering/Design-principles
**Run by**: Claude Code (manual /audit-drift)

## Summary

- Aligned: 12（不展开）
- Documented-only: 1
- Undocumented: 2
- Contradicted: 0
- Phase-deferred: 0 (informational — all prior phase-deferred items shipped)

## Findings

### ❌ Contradicted

None.

### ⚠️ Documented-only

#### [ADR-0017 §Layer-1 + §Layer-2] Mem0 fact layer + brief layer 零实现

- **声明**: "Implement memory as two coupled layers — `src/server/memory/client.ts` (Mem0 write path) + `src/server/memory/brief.ts` (brief regen write path)." ADR accepted 2026-05-23.
- **代码**: `src/server/memory/` 目录不存在。`client.ts`（Mem0 SDK 调用）与 `brief.ts`（brief refresh 写路径）均缺失；`memory_brief_note` 表有 schema/Zod types，但整张表 write path 为零。`MemoryBriefRefreshExperimental` event schema 存在于 `src/core/schema/event/experimental.ts:100`，但无任何 handler 写入它。
- **冲突**: ADR 已 accept，memory layer 预期应在 `src/server/memory/` 落地，当前完全缺席；读路径下游（ReviewIntentTask / SessionSummaryTask / TeachingTurnTask 依赖 brief 作为上下文）无数据可读。
- **建议**: 按 ADR-0017 落地 `src/server/memory/client.ts`（Mem0 init + `add/search`）和 `src/server/memory/brief.ts`（brief regen + `memory_brief_note` upsert）；对应 pg-boss handler 写 `MemoryBriefRefreshExperimental` event。Linear issue 创建命令（报告完成后执行）：

  ```bash
  # gh issue create → Linear via issue-tracker.md 流程; 替换为 Linear connector save_issue:
  # team="Yukoval Studios", title="Drift: ADR-0017 ↔ src/server/memory/", labels=["drift"]
  ```

### ⚠️ Undocumented

#### [ADR-0002 §"extracted_prompt_md 不持久化"] 字段仍在 schema，rescue 路径读取

- **声明**: `docs/adr/0002-structured-extraction-vs-llm-analysis.md` — "extracted_prompt_md 不持久化，所有 markdown 视图由 structured 现场派生，杜绝双源不一致。"
- **代码**: `src/db/schema.ts:117` — 字段仍以 nullable 形式存在；`src/server/ingestion/rescue.ts:121` 读取 `b.extracted_prompt_md` 作为 `prompt_text` fallback；schema comment（L113-117）内联记录偏差（DROP 推迟到 Step 11.5）。主录入路径 `src/server/session/ingestion.ts:242` 写 `null`。
- **冲突**: ADR 明令"不持久化"，但字段在 schema 保留且 rescue 仍读其值；偏差仅在代码注释中追踪，无 ADR amendment。
- **建议**: 在 ADR-0002 末尾加 Revision 段，正式追认 "nullable 暂留 + DROP deferred to Step 11.5" 的临时决策，消除 ADR ↔ code 文字矛盾；或加快 Step 11.5 清理（改 rescue 读 structured 派生 prompt_text）。

#### [ADR-0008 §"conversation 行为延后"] conversation 全生命周期已实装，ADR 未更新

- **声明**: `docs/adr/0008-learning-session-multi-type-envelope.md` — "其余四类（tutor / explore / create / conversation）enum 占位、行为延后——等真实需求触发再实现状态机分支。"（2026-05-15 revision 补充了 conversation 使用 learning_session + event 容器，但未移除"行为延后"限定）
- **代码**: `src/server/session/conversation.ts` — 完整实装 `active → idle → ended | abandoned` 状态机，含 1min promote cron + 6h orphan cron + sendBeacon 路径；`src/server/session/conversation.test.ts:23-206` 覆盖 started / ended / idle / abandoned 四个状态。YUK-14 plan（2026-05-24）注明"不升格为 ADR（design doc 即 SoT）"。
- **冲突**: ADR-0008 body 说 conversation 是 enum 占位/行为延后，代码已全量实装；2026-05-15 revision 没有移除"行为延后"限定语，导致 ADR 现描述与实现不符。
- **建议**: 在 ADR-0008 末尾加 Revision 段（标注日期 2026-05-24），记录"conversation 状态机已在 YUK-14 实装（design SoT: `docs/design/2026-05-24-teaching-idle-state-machine.md`），移除 enum-only 限定"；将 tutor / explore / create 仍标为 enum-only。

---

_Linear closeout gate: Mem0 drift 属于可开 issue 的 actionable follow-up — 在完成本报告后，按 `docs/agents/issue-tracker.md` 去重后创建 Linear issue（team="Yukoval Studios", label="drift", title="Drift: ADR-0017 ↔ src/server/memory/"）。ADR-0002 + ADR-0008 两条建议为文档小修，可附在同 PR 或单独 doc-fix PR，不需独立 issue。_
