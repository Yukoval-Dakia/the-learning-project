# Drift Audit — 2026-05-18

**Scope**: ADR-0001..ADR-0013, `docs/superpowers/status.md`, `CLAUDE.md`
**Run by**: Claude Code (manual /audit-drift)
**Code anchor**: branch `claude/lucid-gates-d6t2q`
**Prior run**: `docs/audit/2026-05-15-drift.md`（10 findings，全部已 resolve）

## Summary

| 类别 | 数量 |
|---|---|
| ✅ Aligned | 11（不展开） |
| ❌ Contradicted | **2** |
| ⚠️ Undocumented | **2** |
| ⚠️ Documented-only | **1** |
| ⏳ Phase-deferred | 4 (informational) |

**实际待处理 drift = 5 项**，根因一致：Sub 0d + Phase 2 已 ship，ADR-0003/0004 未更新。

---

## Findings

### ❌ Contradicted（优先）

#### [ADR-0003 §决策 + §当前状态（2026-05-16 audit）] Sub 0d 已落地，ADR 冻结在迁移前状态

- **声明**：
  - §决策 line 3：「保持 Anthropic 直连…作为**唯一实现的 provider**」
  - §当前状态 line 21：「`src/server/ai/providers.ts` 文件不存在」
  - §当前状态 line 22：「runner.ts 仍直接 `import { anthropic } from '@ai-sdk/anthropic'`」
  - §当前状态 line 23：「Anthropic 仍是唯一实际运行的 provider」
- **代码**：
  - `src/server/ai/providers.ts` 已存在，完整实装 `resolveTaskProvider()`
  - `src/server/ai/runner.ts:42` `import { type ResolvedProvider, resolveTaskProvider } from './providers'`（不再直接 import anthropic）
  - `src/ai/registry.ts:48+`：全部 12 个 task 的 `defaultProvider: 'xiaomi'`
  - `docs/superpowers/status.md`：Phase 0d 标 ✅「xiaomi/mimo Anthropic-compat endpoint；7 个 task 全切」
- **冲突**：ADR 描述状态与代码现实相反；Sub 0d 已完成，ADR-0003 status section 从未刷新。
- **建议**：刷新 ADR-0003 §当前状态为「Sub 0d 已落地（2026-05-17）；providers.ts 存在；`xiaomi` 为当前唯一运行 provider」。
- `gh issue create`:
  ```bash
  gh issue create \
    --title "Drift: ADR-0003 frozen at pre-Sub-0d state — providers.ts exists, xiaomi is default" \
    --label "drift,documentation" \
    --body "ADR-0003 §决策 still says 'Anthropic 直连 as only provider'; §2026-05-16 audit says providers.ts doesn't exist. Both are false: Sub 0d shipped (status.md ✅), providers.ts exists at src/server/ai/providers.ts, all 12 tasks use defaultProvider='xiaomi'. Refresh ADR-0003 status section to reflect landing."
  ```

#### [ADR-0004 §Provider Manager + §Task Model Selector] 函数接口命名与实装不符

- **声明**（ADR-0004 code blocks lines 25, 38）：
  - `src/server/ai/providers.ts` 导出 `export function getProvider(name: ProviderName)`
  - `src/ai/registry.ts` 导出 `export function resolveTaskModel(kind: TaskKind, override?): LanguageModel`
- **代码**：
  - `src/server/ai/providers.ts:72` 导出 `resolveTaskProvider(kind, override): ResolvedProvider`（无 `getProvider`）
  - `src/ai/registry.ts`：无 `resolveTaskModel` 函数（registry 不返回 LanguageModel；由 runner 在调用时组合）
- **冲突**：ADR 描述的"计划形态"API 接口未按 spec 实现，实际实现改了函数名和返回类型。
- **建议**：更新 ADR-0004 §"Provider Manager" code block 为 `resolveTaskProvider()`；§"Task Model Selector" 改为「registry 的 `tasks[kind]` 直接持有 provider/model 声明，runner 调 `resolveTaskProvider()` 组合」，删去不存在的 `resolveTaskModel()` 签名。
- `gh issue create`:
  ```bash
  gh issue create \
    --title "Drift: ADR-0004 getProvider/resolveTaskModel don't exist — actual is resolveTaskProvider" \
    --label "drift,documentation" \
    --body "ADR-0004 code blocks describe getProvider(name) in providers.ts and resolveTaskModel(kind) in registry.ts. Neither exists. Actual export: resolveTaskProvider(kind, override) in providers.ts; registry has no LanguageModel-returning function. Update ADR-0004 §Provider Manager and §Task Model Selector code blocks."
  ```

---

### ⚠️ Undocumented（代码做了 ADR 没追认）

#### [src/server/ai/providers.ts:32-37 + src/ai/registry.ts:11,48+] xiaomi/Mimo provider 无 ADR 追认

- **代码**：
  - `src/server/ai/providers.ts:32-37`：`xiaomi` provider，baseUrl `https://api.xiaomimimo.com/anthropic`，模型 mimo-v2.5 / mimo-v2.5-pro
  - `src/ai/registry.ts:11`：`export type Provider = 'anthropic' | 'xiaomi' | 'openrouter' | 'gateway' | 'openai'`
  - 全部 12 个 task 的 `defaultProvider: 'xiaomi'`（AttributionTask / KnowledgeProposeTask / VisionExtract* / Phase 2 全部）
- **ADR**：ADR-0003 的 Provider 类型列表（`'anthropic' | 'openrouter' | 'gateway' | 'openai'`）不含 `'xiaomi'`；ADR-0003 §"触发抽象化的明确条件"列出 4 个 trigger，无 ADR 记录哪个 trigger 被满足、为何选择 xiaomi 而非 OpenRouter/Gateway、成本与 observability 影响。
- **建议**：在 ADR-0003 增加修订段，说明：(a) 满足的 trigger condition；(b) xiaomi 选型理由；(c) `Provider` type 更新为含 `'xiaomi'`；(d) 提示 prompt caching 行为差异（若有）。
- `gh issue create`:
  ```bash
  gh issue create \
    --title "Drift: xiaomi/Mimo provider wired as default for all tasks, no ADR rationale documented" \
    --label "drift,documentation" \
    --body "src/server/ai/providers.ts implements xiaomi provider (api.xiaomimimo.com/anthropic, mimo-v2.5/pro). All 12 tasks use defaultProvider='xiaomi'. ADR-0003 Provider type enum missing 'xiaomi'; no ADR records which trigger condition was met. Add ADR-0003 revision section: trigger met, xiaomi selection rationale, updated Provider type."
  ```

#### [src/ai/registry.ts] ADR-0004 task 表缺 7 个 Phase 2+ 任务

- **代码**：registry 现有 12 个 task（AttributionTask / VisionExtract* / KnowledgeProposeTask / KnowledgeReviewTask + **7 新增**：`KnowledgeEdgeProposeTask`、`SessionSummaryTask`、`LearningIntentOutlineTask`、`NoteGenerateTask`、`VariantGenTask`、`TeachingTurnTask`、`ReviewIntentTask`）
- **ADR**：ADR-0004 task 现状表（2026-05-16 refresh）9 条（4 实装 + 5 Sub-0d 计划）；`VariantGenTask` 仍标「Sub 0d 计划」实为已实装；另 6 个 Phase 2 任务完全未列。
- **建议**：ADR-0004 §"Backend Purpose Agent — Task 现状" 全面刷新：将 7 个新 task 加入表中，并将 `VariantGenTask` 标记从「⏳ Sub 0d 计划」改为「✅ 已实装」。
- `gh issue create`:
  ```bash
  gh issue create \
    --title "Drift: ADR-0004 task table missing 7 Phase-2 tasks (KnowledgeEdgeProposeTask, SessionSummaryTask, etc.)" \
    --label "drift,documentation" \
    --body "Registry has 12 tasks; ADR-0004 table (2026-05-16) has 9 and marks VariantGenTask as deferred (it's implemented). Missing: KnowledgeEdgeProposeTask, SessionSummaryTask, LearningIntentOutlineTask, NoteGenerateTask, TeachingTurnTask, ReviewIntentTask. Refresh ADR-0004 task table."
  ```

---

### ⚠️ Documented-only

#### [ADR-0010 §KnowledgeProposeTask] 边提议能力未注入 KnowledgeProposeTask

- **声明**（ADR-0010 line 65）：「KnowledgeProposeTask + KnowledgeReviewTask 扩 prompt：rather than only propose new nodes, also propose edges between existing nodes」
- **代码**：
  - `KnowledgeReviewTask` system prompt（registry.ts:210）✅ 含 mesh-shape 分支（`propose_knowledge_edge` tool）
  - `KnowledgeProposeTask` system prompt（registry.ts:56）：仅提议树节点，无边提议 capability
  - 已存在独立 `KnowledgeEdgeProposeTask`（registry.ts:99）和 `src/server/knowledge/propose_edge.ts`——但这是独立 task，未扩展 KnowledgeProposeTask prompt
- **漂移**：ADR 说"扩 existing KnowledgeProposeTask prompt"，实际选择了"新建独立 task"——若为有意设计变更，需在 ADR-0010 修订段追认。
- **建议**：在 ADR-0010 追加修订段，说明「边提议通过独立 KnowledgeEdgeProposeTask 而非扩展 KnowledgeProposeTask prompt 实现，原因 X」；或将边提议能力补入 KnowledgeProposeTask。
- `gh issue create`:
  ```bash
  gh issue create \
    --title "Drift: ADR-0010 says extend KnowledgeProposeTask for edges, but separate KnowledgeEdgeProposeTask created instead" \
    --label "drift,documentation" \
    --body "ADR-0010 line 65: 'KnowledgeProposeTask + KnowledgeReviewTask 扩 prompt: also propose edges'. KnowledgeReviewTask ✅ extended. KnowledgeProposeTask NOT extended — a separate KnowledgeEdgeProposeTask was created instead. If intentional, add ADR-0010 revision explaining the design change."
  ```

---

## ⏳ Phase-deferred（informational — 不算 drift）

| 项 | 出处 | 说明 |
|---|---|---|
| `openrouter` / `gateway` / `openai` provider 实装 | ADR-0003 | `providers.ts:98`：非 anthropic/xiaomi throw 'not implemented'；按 ADR 触发条件仍等待 |
| Maintenance agent cron + accept UI | ADR-0004 + status.md ⬜ | KnowledgeReviewTask 已注册，cron + UI 尚未接通 |
| VariantVerifyTask Pass 2 | status.md ⬜ | variant 双 pass 计划中 |
| Phase 3 Coach Orchestrator | status.md ⬜ | ADR 无专项，Phase 3 整体延后 |

---

## ✅ Aligned（11 项，不展开）

ADR-0001 Python sidecar / ADR-0002 OCR 抽取层 / ADR-0005 single-owner invariant（演化至 session/）/ ADR-0006 v2 event-driven core（event 表 + 旧表 DROP）/ ADR-0007 单用户 middleware / ADR-0008 learning_session 多态 / ADR-0010 knowledge_edge schema + relation_types / ADR-0011 Zod event discriminated union / ADR-0012 mastery derived view / ADR-0013 review session lifecycle（server + client + cron 全通）/ ADR-0004 Claude Agent SDK maxTurns/mcpServers/allowedTools 机制

---

## 信号校准

- 5 条 finding，全部指向同一根因：**Sub 0d + Phase 2 ship 后 ADR-0003/0004 未同步更新**
- 假阳性率预估 0%（代码变更有明确 status.md 记录）
- 建议：ADR-0003/0004 同一 PR 内联合更新，可一次性清零所有 finding
