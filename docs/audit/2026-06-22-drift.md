# Drift Audit — 2026-06-22

**Scope**: ADR-0023..ADR-0044, docs/design/2026-06-* (last 30 days), CLAUDE.md
**Run by**: Claude Code (manual /audit-drift)

## Summary

- Aligned: 6（不展开）
- Documented-only: 4
- Undocumented: 0
- Contradicted: 0
- Phase-deferred: 3 (informational)

---

## Findings

### ⚠️ Documented-only

#### [ADR-0037 §决定5] review_plan job 应退役 — 代码仍全量运行

- **声明**: 「`review_plan` job 从 ADR-0029 的 separate paper channel **先降为引擎内的 proposer 角色，随即直接退役/全删**（不再独立注册、不再另开 paper channel）；ReviewPlanTask 同步退场」（ADR-0037 Accepted 2026-06-14）
- **代码**: `src/capabilities/practice/jobs/review_plan.ts`（全量实现），`src/capabilities/practice/manifest.ts:220`（仍注册 `review_plan` job），`src/capabilities/agency/jobs/coach_daily.ts:485`（`boss.send('review_plan', payload)` 仍链式触发）
- **Gap**: 退役计划已进 ADR，实现完全未动。two-channel 反模式（due 确定性队列 + review_plan AI paper channel 双轨）仍在线上，与合并引擎「单一编排者一处产流」主线矛盾。
- **建议**: 按 ADR-0037 §决定5 退役步骤：注销 `review_plan` job manifest 注册、删 `coach_daily` 的 chain-send、把 ReviewPlanTask 治理意图迁入 `composeDailyStream` / stream-composer 合并引擎。

```bash
# gh issue create example
gh issue create \
  --repo yukoval-dakia/the-learning-project \
  --title "Drift: ADR-0037 ↔ practice/jobs/review_plan.ts — 退役 review_plan job" \
  --label "drift" \
  --body "ADR-0037 (2026-06-14) 裁定 review_plan 退役/全删，但 src/capabilities/practice/jobs/review_plan.ts 仍全量运行，coach_daily.ts:485 仍 boss.send。合并引擎双轨反模式未消除。参见 docs/audit/2026-06-22-drift.md。"
```

---

#### [ADR-0039 §决定6a] ambient_context 未升格为 Postgres session-work-memory 表

- **声明**: 「会话级工作记忆（Postgres，所有 surface 写入 / 编排者读取）：把 `ambient_context` 从 `chat.ts` 私有入参升格为正式表，存当前面 / focused_entity / 上一轮练习结果 / **刚 dismiss 哪条**」（ADR-0039 §决定6a，Accepted 2026-06-14）
- **代码**: `src/capabilities/copilot/server/chat.ts:168` — `ambient_context` 仍是 chat API request body 的 JSON 字段，无对应 Postgres 表；`src/db/schema.ts` 无 `session_work_memory`/`ambient_context` 表。
- **Gap**: 「刚 dismiss 哪条」回流闭环（B 档 dismiss 不重推同类提议）依赖 session-work-memory 持久化，当前仅 in-request 传递 → D14 每次启动无法读取历史 dismiss 记录，重复推同类提议的反模式未消除。
- **建议**: 新建 PG 表（`session_work_memory` 或同义），迁移 `ambient_context` 字段写路径，chat.ts 改为读表而非从请求体注入。

```bash
gh issue create \
  --repo yukoval-dakia/the-learning-project \
  --title "Drift: ADR-0039 §6a ↔ chat.ts — ambient_context 未升格为 session-work-memory 表" \
  --label "drift" \
  --body "ADR-0039 (2026-06-14) 要求 ambient_context 升格为 Postgres 正式表（含 focused_entity / 刚 dismiss 哪条），但 src/capabilities/copilot/server/chat.ts:168 仍是 request-body JSON，无 DB 写路径，dismiss 回流闭环断裂。参见 docs/audit/2026-06-22-drift.md。"
```

---

#### [ADR-0039 §后果/代价] CORE_TOOLS bootstrap latch 未退役 — copilotTools 贡献制未真生效

- **声明**: 「须先让 copilotTools 贡献制真生效（退役 CORE_TOOLS latch）再谈 job 收编，否则「合为同一 D14」只是叙事不是真相源」（ADR-0039 §后果/代价，Accepted 2026-06-14）
- **代码**: `src/server/ai/tools/bootstrap.ts:68` — `CORE_TOOLS` 41 工具数组仍是主注册路径（`registerCoreTools()` 在 bootstrap 时全量注册）；`src/server/ai/tools/register-capability-tools.ts:23` 只作 additive fallback（`getTool(decl.name) → skip`，CORE_TOOLS 到达后贡献制永远跳过）。
- **Gap**: 工具归属真相源仍在 `bootstrap.ts` 硬编码数组，而非 capability manifests 的 `copilotTools` 贡献制。ADR-0039 明确指出这是「D14 四 job 合为同一编排者」的前置条件，当前 D14 合并只是 ADR 叙事，未落代码真相源。
- **建议**: 按 YUK-321 M5 设计意图，逐步把 `CORE_TOOLS` 条目迁入各 capability 的 `manifest.copilotTools.tools`，最终退役 `registerCoreTools` / `CORE_TOOLS` 数组，让 `registerCapabilityCopilotTools` 成为唯一注册路径。

```bash
gh issue create \
  --repo yukoval-dakia/the-learning-project \
  --title "Drift: ADR-0039 ↔ bootstrap.ts — CORE_TOOLS latch 未退役阻断 copilotTools 贡献制" \
  --label "drift" \
  --body "ADR-0039 (2026-06-14) 明确要求退役 CORE_TOOLS latch 才能让 D14 合并从叙事变真相源。src/server/ai/tools/bootstrap.ts:68 的 CORE_TOOLS 41 工具数组仍是主注册路径，registerCapabilityCopilotTools 只做 additive fallback，贡献制永远被 CORE_TOOLS 先到抢占。参见 docs/audit/2026-06-22-drift.md。"
```

---

#### [ADR-0044 §1/§4/§7] Event-sourcing Wave 0 基础设施缺失

- **声明**: Wave 0 = mastery_state + material_fsrs_state 快照可逆（同 tx 写 `experimental:state_snapshot` event）+ cascade CTE 遍历器骨架（`src/server/events/cascade.ts`）；`pnpm audit:projection` 机器守门（ADR-0044 §7，Accepted 2026-06-15）
- **代码**:
  - `grep 'state_snapshot' src/` → 零命中（快照事件未写入任何地方）
  - `src/server/events/cascade.ts` → 文件不存在（CTE 遍历器未创建）
  - `src/core/projections/` → 目录不存在（fold reducers 未建）
  - `package.json` → `audit:projection` script 不存在
- **Gap**: ADR-0044 Wave 0 是 copilot checkpoint/revert 腿（ADR-0041）的「硬前置」，同时也是 YUK-203 整条 event-sourcing 改造的地基。当前进度：四个 Wave 全部未启动，没有任何 snapshot append 逻辑、没有 cascade CTE、没有 fold reducer、没有机器守门脚本。
- **建议**: 按 ADR-0044 §wave 0 范围启动：① `src/server/events/cascade.ts` CTE traverser 骨架；② 在 `/api/review/submit` 的 `judgeAndUpdate` 同 tx 内 append `experimental:state_snapshot`；③ `pnpm audit:projection` 仿 `audit:schema` 新增（初始可只检查有无 snapshot writer）。

```bash
gh issue create \
  --repo yukoval-dakia/the-learning-project \
  --title "Drift: ADR-0044 Wave 0 ↔ codebase — event-sourcing 地基基础设施全缺" \
  --label "drift" \
  --body "ADR-0044 (2026-06-15) Wave 0 要求：① 同 tx 写 experimental:state_snapshot（mastery/FSRS）；② src/server/events/cascade.ts CTE traverser 骨架；③ pnpm audit:projection 守门。三者均不存在。此地基是 ADR-0041 copilot checkpoint/revert 腿的硬前置。参见 docs/audit/2026-06-22-drift.md。"
```

---

## Phase-deferred（informational，不报 drift）

| 决策 | 来源 | 等待条件 |
|---|---|---|
| `propose_learning_item_transition` 四件套合并 | ADR-0032 D3 | 显式标「=L9，高耦合低价值，本阶段 defer」 |
| misconception 节点晋升环 (RT1) | ADR-0036 §决定3 | gated on ADR-0034（已实现）+ 观测窗口 N 周 |
| event-sourcing Wave 1-3（knowledge/artifact fold） | ADR-0044 §6 | sequenced after Wave 0 |
