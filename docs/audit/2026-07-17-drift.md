# Drift Audit — 2026-07-17

**Scope**: ADR-0044..ADR-0049 (active window), CLAUDE.md (Layering / Request flow / Commands sections), README.md, `.claude/skills/audits-reference/SKILL.md`
**Run by**: Claude Code (manual /audit-drift, scheduled routine)

## Summary
- Aligned: 12（不展开）— ADR-0044 event-sourcing foundation (fold reducers, cascade CTE, snapshot/checkpoint experimental actions, revert-bracket runbook all present); ADR-0045 tagging axis; ADR-0046 Rust numerical engine (`deny(unsafe_code)`, poly-sigmoid flag scaffolding, A13 proper-scoring stub self-documents its Rust-deferred status); ADR-0047/0048 KG-borrow + A11 axis artifacts (flags default false, `learner_axis_state`, `ez-diffusion.ts`, `axis_state_nightly`); ADR-0049 conjecture wire (producer/consumer/admin-reader all wired); ADR-0023 Redis→PG presence migration note; README/docker-compose/no-Redis/no-Next.js claims all hold.
- Documented-only: 0
- Undocumented: 2
- Contradicted: 0
- Phase-deferred: 1 (informational — worker `registerCapabilityCopilotTools` manifest-alignment gap, ADR-0044 §5, explicitly folded into a future lane, correctly non-blocking)

## Findings

### ⚠️ Undocumented

#### [CLAUDE.md §Layering / §Request flow] `onboarding` capability missing from the capability list
- **声明**: CLAUDE.md:127-136 enumerates the capability packages under `src/capabilities/` as `copilot / observability / ingestion / knowledge / notes / practice / agency / shell` only; CLAUDE.md:109-112 (§Request flow) likewise lists only those manifests as "the backend surface."
- **代码**: `src/capabilities/onboarding/manifest.ts` (added 2026-07-14, YUK-619, commit "[YUK-619] Harden shipped product usability (#790)") defines a full `onboardingCapability` (welcome/upload/placement/profile UI pages) and is imported and registered in `src/capabilities/index.ts` alongside the other eight. The manifest's own comment notes it was "shipped but not yet in the static composition root" before this PR — the composition-root wiring is done, but CLAUDE.md's capability inventory was never updated to match.
- **冲突**: CLAUDE.md undercounts the live capability surface by one domain; a reader following CLAUDE.md's Layering section would not know `onboarding/` exists or where cold-start/placement code lives.
- **建议**: Add `onboarding/` to the `src/capabilities/` tree in CLAUDE.md:127-136 and to the §Request flow manifest bullet list (CLAUDE.md:109-112).
- Issue:
  ```
  gh issue create --repo yukoval-dakia/the-learning-project \
    --title "Drift: CLAUDE.md capability list ↔ src/capabilities/onboarding" \
    --label drift,automated \
    --body "docs/audit/2026-07-17-drift.md finding #1: CLAUDE.md:127-136 (Layering) and CLAUDE.md:109-112 (Request flow) omit the onboarding/ capability (added 2026-07-14, YUK-619, src/capabilities/onboarding/manifest.ts, registered in src/capabilities/index.ts). Fix: add onboarding/ to both CLAUDE.md sections."
  ```

#### [CLAUDE.md §Commands] Three `pnpm test`-gating audit scripts undocumented
- **声明**: CLAUDE.md's Commands block lists only `audit:schema / audit:partition / audit:profile / audit:draft-status / audit:relations / audit:calibration / audit:mastery-provenance` (plus the closing line "`pnpm test` covers `audit:profile`/`audit:draft-status`/`audit:draft-status-reads --strict` + unit/db/migration").
- **代码**: `package.json`'s `"test"` script is actually `audit:api-contracts && audit:profile && audit:learner-copy && audit:no-learning-styles && audit:draft-status && audit:draft-status-reads --strict && test:unit && test:db && test:migration` — three additional gating audits (`audit:api-contracts` — added 2026-07-15, YUK-647 OpenAPI contracts; `audit:learner-copy` and `audit:no-learning-styles` — added 2026-07-16, YUK-506 deterministic-method safety spine) run on every `pnpm test` invocation but appear nowhere in CLAUDE.md's Commands list or its `pnpm test` summary sentence. The companion `.claude/skills/audits-reference/SKILL.md` (which bills itself as covering "Full mechanics of this repo's pnpm audit:* scripts") is equally silent on all three, plus five other longer-standing scripts (`audit:fold-writes`, `audit:flags`, `audit:projection`, `audit:golden`, `audit:judge-golden`, `audit:judge-prompts`).
- **冲突**: A contributor reading CLAUDE.md's `pnpm test` description would not know three of its nine gating steps exist, what they check, or where their allowlists live — same failure mode the skill exists to prevent for older audits.
- **建议**: Update the CLAUDE.md Commands block + `pnpm test` summary sentence to list all nine scripts actually invoked; extend `audits-reference` SKILL.md with entries for the three newly-gating scripts (lower priority: backfill the remaining report-only ones).
- Issue:
  ```
  gh issue create --repo yukoval-dakia/the-learning-project \
    --title "Drift: CLAUDE.md Commands ↔ pnpm test audit scripts" \
    --label drift,automated \
    --body "docs/audit/2026-07-17-drift.md finding #2: package.json's \"test\" script runs audit:api-contracts (added 2026-07-15, YUK-647), audit:learner-copy, and audit:no-learning-styles (both added 2026-07-16, YUK-506) — none listed in CLAUDE.md's Commands block or its pnpm-test summary sentence, and none covered by .claude/skills/audits-reference/SKILL.md. Fix: update both docs to list all nine pnpm-test gating scripts."
  ```

## Note on repo issue-tracker policy

This repo's default policy (CLAUDE.md §Issue tracker) is Linear-only, with GitHub Issues treated as historical. The commands above follow this run's explicit task-level override requesting `gh issue create` for drift findings; the owner may prefer to redirect these two follow-ups into Linear instead — the finding bodies above carry enough detail to file either way.
