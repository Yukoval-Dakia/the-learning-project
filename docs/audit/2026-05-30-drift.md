# Drift Audit — 2026-05-30

**Scope**: ADR-0001..ADR-0026 (all), plans/2026-05-30-*, CLAUDE.md (Architecture/Layering/Design principles)
**Run by**: Claude Code (manual /audit-drift)
**Focus**: ADR-0023..ADR-0026 (2026-05-30 new ADRs: Redis presence, generalized capture, north-star goal, WorkflowJudge) + ADR-0014 T-QP slice 1 (question_part) + previous open findings

## Summary
- Aligned: 18 (不展开)
- Documented-only: 0
- Undocumented: 1
- Contradicted: 0
- Phase-deferred: 0 (informational)

## Findings

### ⚠️ Undocumented

#### [CLAUDE.md §Layering] `server/knowledge/seed.ts` imports subject-specific wenyan module
- **声明**: CLAUDE.md Architecture §Layering："`core/` is cross-subject; `subjects/<name>/` is single-subject specialisation. Keep that boundary — don't leak subject-specific logic into `core/` or `server/`."
- **代码**: `src/server/knowledge/seed.ts:3` `import { getCurriculum } from '@/subjects/wenyan/seed';`，把 wenyan 特定数据直接耦合进 `server/` 层。文件注释写明"仅 PR A 范围：单 domain 'wenyan'"，说明是 Phase 1 刻意绑定，但无 ADR 追认。
- **冲突**: `server/` 层持有 subject-specific 耦合，与 CLAUDE.md 明文边界相反；文件在过去 30 天内被 T-D7 (a4e36d3) 触碰。
- **建议**: 三选一 —— (a) 将 `seedKnowledge` 移到 `scripts/seed-wenyan.ts`（一次性脚本不属于 server/）；(b) 改为接收 curriculum 作参数，调用方传 `getCurriculum()`；(c) 在 CLAUDE.md 或新 ADR 注记 "Phase 1 seed 是已知例外，多科目 seed 落地（Phase 2）时统一重构"。
- **Linear issue**（closeout gate — 报告完成后去重再建）：
  - title: `Drift: CLAUDE.md Layering ↔ src/server/knowledge/seed.ts (wenyan coupling)`
  - labels: `drift`, `tech-debt`
  - team: `Yukoval Studios`

## Aligned (verified, 不展开)

**ADR-0023** (Redis presence): PresenceStore 接口 + in-memory / Redis 双实现 + `REDIS_URL` factory（`editing-session.ts:31`）；redis singleton 对齐 db/boss client 模式（`src/server/redis/client.ts`）；docker-compose `redis:7-alpine` 无 volume（ephemeral per ADR）；app + worker 均注入 `REDIS_URL=redis://redis:6379`。

**ADR-0024** (generalized capture): `src/server/ingestion/enroll.ts::enrollCapturedBlock` 泛化 outcome 路由（failure→mistake / success→worked_example / partial→worked_example / unanswered→open_question，无 attempt event）；`AttemptOnQuestion` schema 无需改动；`LearningRecordKind` 复用。

**ADR-0025** (north-star goal): `goal` 表 schema（`src/db/schema.ts:695`，status enum `'active'|'dormant'|'done'`，ND-4 无进度字段）；`learning_session.goal_id` 保持 stub（nullable text，非 FK `schema.ts:514`）；`GoalScopeTask` 注册（registry.ts:462）；`goal_scope` kind 在 `AiProposalPayload`（proposal.ts:170）；`acceptGoalScopeProposal` + `insertGoal`（`src/server/goals/accept.ts`）；TodayPlan `goal_ids` + `serves_goal_id` + `knowledge_ids` 字段（coach_daily.ts:32-35 COACH_GOAL_STRAND_GUIDANCE）；ND-5 conservation test（`src/server/boss/handlers/coach_daily.northstar.test.ts`：FSRS due 在有无 active goals 两种 fixture 下字节级一致）。

**ADR-0026** (WorkflowJudge): `TaggingTask` 注册（registry.ts:476，单次结构化，maxIterations:1，allowedTools:[]）；`WorkflowJudge` 纯函数（`src/server/ingestion/workflow-judge.ts`，零 LLM 调用，最弱环节闸门）；`WORKFLOW_JUDGE_AUTO_ENROLL_ENABLED` 默认 OFF（`workflow-judge-config.ts:8,36,45`）；`auto-enroll.ts::runAutoEnrollForSession` 走同一 `enrollCapturedBlock` owner（ADR-0024），`generated_by='workflow_judge'`；`prefilled.outcome='unanswered'` 保守信号。

**ADR-0021** (outbox): `writeEvent` 纯 INSERT，不 import boss/triggers（queries.ts:1012）；`ingest_at IS NULL` partial index + `buildMemoryIngestOutboxPollHandler` SELECT FOR UPDATE SKIP LOCKED + UPDATE 同 tx（triggers.ts:165-187）；7 个 iter2 band-aid 已全部移除（grep 0 命中）。

**ADR-0020/0022** (block-tree): `body_blocks JSONB` SOT（schema.ts:314）；sections/outline_json/child_artifact_ids/knowledge_id 已 DROP（无残留）；`artifact_block_ref` + `artifact_block_ref_to_idx`（schema.ts:331-355）；`hub_auto_sync_nightly` @ 02:45 BJT（handlers.ts:75）；`CorrectArtifactEvent.payload.block_id`（非 section_id）（known.ts:242）；`BlockTreeRenderer` 不 import `@tiptap/react`（grep 0 命中）；split 保左 id + derived_from_block_id（pm.ts:90）。

**ADR-0014** (capability registry + question_part): `ActivityRef`/`ActivityKind`（`src/core/schema/activity.ts`）；`CapabilityRegistry`（`src/core/capability/registry.ts`）；question_part slice 1 — `kind='question_part'` + `parent_question_id` + `part_index`（T-QP 163a2ed）；`SchedulerCapabilityRunner` + `fsrs` scheduler（validates `schedulingHints.default_policy`）。

**ADR-0011** (event schema): `ToolUseQuery` (action='tool_use') 晋升（known.ts:426）；`AcceptSuggestionChip` / `ProposeKnowledgeEdge` / `GenerateKnowledgeEdge` / `RateKnowledgeEdge` 全在 KnownEvent union（known.ts:521-531）。

**ADR-0004** (Pattern C agent): `src/server/ai/runner.ts` 直接用 `@anthropic-ai/claude-agent-sdk` query loop（runner.ts:3,40）；无手写 tool-calling loop；`src/server/ai/log.ts` AI run 审计 ✓。

**ADR-0007** (single user): schema.ts 全局 grep `user_id` = 0（无 user_id 列）；middleware `x-internal-token` 单用户 token gate ✓。

## Previous open findings status

- **YUK-146** (knowledge-readers.ts derived_from 方向注释)：✅ 已修 — knowledge-readers.ts:252 现为 `'from concept is derived from / builds on the to concept'`，与 ADR-0010 方向一致。
- **YUK-115** (NoteVerificationIssue.section_id)：✅ 已处理 — `business.ts` 已无 section_id；`artifact-corrections.ts:33-37` 有 legacy normalization shim（section_id → block_id 后向兼容）。
- **p2-basic ADR-0021→0022 ref**：✅ 已修 — `yuk88-p2-basic.md` 全文已改为 ADR-0022（Wave 8 T-PD 认领落地）。
