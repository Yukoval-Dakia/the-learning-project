# Drift Audit — 2026-05-24

**Scope**: ADR-0001..ADR-0017, plans/2026-05-{22..23}-*, CLAUDE.md §Architecture/Layering/Design principles
**Run by**: Claude Code (manual /audit-drift)

**Post-W1 update (2026-05-24, after W1 closeout @ a6c4251)**: Original report was generated at commit 9854983 baseline. Codex PR review flagged that since then YUK-17 shipped `VariantVerifyTask` + `mistake_variant` table (ADR-0018) which now appears in registry. Architecture.md §5.1 finding recounted to 6 missing tasks (was 5). Status.md finding unchanged — both items still ⬜. Fixes applied in same PR (see follow-up commits).

## Summary

- Aligned: 22（不展开）
- Documented-only: 0
- Undocumented: 2
- Contradicted: 0
- Phase-deferred: 5（informational）

## Findings

### ⚠️ Undocumented

#### [status.md §Product Track 2] AiProposalPayload + maintenance cron 已落地，状态栏未更新

- **声明**：`docs/superpowers/status.md` §"Product Track 2" 将以下两条标为 `⬜`（未完成）：
  1. `统一 AiProposalPayload — kind / target / reason_md / evidence_refs / rollback_plan / cooldown_key`
  2. `Maintenance agent — KnowledgeReviewTask cron + accept UI（cron + UI 缺）`
- **代码**：
  - `src/core/schema/proposal.ts:4-100`：`AiProposalPayload` discriminated union 完整实现，包含 kind / target / reason_md / evidence_refs / rollback_plan / cooldown_key（commit c4bb22d, YUK-42）。
  - `src/server/proposals/{writer,inbox,producers,signals}.ts`：提议写入 + inbox 读取 + 生产者 + 信号全部落地。
  - `src/server/boss/handlers/knowledge_maintenance_nightly.ts` + `src/server/boss/handlers.ts:47-53`：`knowledge_maintenance_nightly` 队列已注册，BJT 03:00 schedule 已生效（commit 9854983, YUK-48）。
- **冲突**：status.md §Track 2 显示两项尚待实现，但 `src/core/schema/proposal.ts` 和 `handlers.ts` 均已落地。读者或 agent 会误以为这些功能缺失。
- **建议**：更新 status.md — 将"统一 AiProposalPayload"标 ✅，将"Maintenance agent cron"标 ✅（accept UI 仍 `⬜`）。

#### [architecture.md §5.1] Task 注册表快照过期，5 个任务缺失

- **声明**：`docs/architecture.md:105`：  
  > **Canonical source**: `src/ai/registry.ts` + `docs/adr/0004-pattern-c-two-type-agent-architecture.md` §"Task 现状"。本节为同步快照（2026-05-17）。
- **代码**：`src/ai/registry.ts` 当前注册 18 个 TaskKind（post-W1 recount，含 YUK-17 新增 `VariantVerifyTask`）；快照中缺失以下 6 个（2026-05-17 之后新增）：
  - `NoteVerifyTask`（pg-boss `note_verify`）
  - `EmbeddedCheckGenerateTask`（pg-boss `embedded_check_generate`）
  - `SemanticJudgeTask`（judge router fallback）
  - `UnitDimensionFallback`（unit_dimension LLM fallback，YUK-36）
  - `StepsJudgeTask`（steps@1 partial credit）
  - `VariantVerifyTask`（pg-boss `variant_verify`，YUK-17 + ADR-0018）
- **冲突**：architecture.md §5.1 被当作 AI 任务层的参考表，但与 registry 偏差 6 条，会让读者（含 agent）对当前任务面产生误判。
- **建议**：将 architecture.md §5.1 表格同步到 registry 当前状态，并将快照日期改为 2026-05-24。

---

## Phase-deferred（informational，不计入 finding 数）

| ADR / Plan | 条目 | 推迟原因 |
|---|---|---|
| ADR-0017 §implementation | `src/server/memory/{client,brief}.ts` 未创建；`event.affected_scopes` + `memory_brief_note.latest_evidence_at/evidence_count` 未入 schema | ADR §"Phase B per YUK-37" 明确 forward-lock；Mem0 spike 未完成 |
| ADR-0004 §第二类 | User Copilot（agent_sessions 双表）| Phase 2 未启动 |
| ADR-0006 v2 §场景3 | Dreaming agent 完整事件流 | Phase 2C 未启动 |
| ADR-0014 §Phase N+1 | 归因分类法 profile-driven（wenyan 部分迁移中）| 仍在 incremental migration |
| Plan 2026-05-22-math-m1-drift-targets §C | UI 层 `question_id` refactor | math embedded check 未落地（后续 sweep） |

---

## 上次 finding 变动

| 2026-05-22 Phase-deferred 条目 | 本次状态 |
|---|---|
| ADR-0014 §Phase N+2 `steps@1` judge（M2 ramp 中）| **CLEARED** — math MVP M2.2 (#82) 已落地；`src/core/capability/judges/steps.ts` + `src/server/ai/judges/steps-judge.ts` 均存在，`JudgeInvoker` dispatch 路径已验证 |

---

## Method Notes

- 本次检查范围（相比 2026-05-22）：新增 ADR-0017（memory layer）、L1-L6 / YUK-48 plan 落地状态、registry task 表同步；抽样验证 ADR-0005/0006/0007/0011/0012 写路径 invariant。
- ADR-0017 implementation 未找到 `src/server/memory/` 目录及 `event.affected_scopes` 列——但 ADR 明确标注 "Phase B per YUK-37" forward-lock，归为 Phase-deferred。
- 假阳性自评：status.md 和 architecture.md 两项均有直接代码证据，无误判风险。steps@1 cleared 有 git commit 证据链。
