# Drift Audit — 2026-07-15

**Scope**: ADR-0045..ADR-0049, docs/planning/2026-07-14-api-resource-contract-convergence.md, CLAUDE.md (Architecture/Layering/Request-flow), design/(last 30d)
**Run by**: Claude Code (scheduled /audit-drift)
**Lens**: 30-day recency since last drift audit (2026-06-23-drift.md); focus on decisions touched by commits since then — YUK-489 tagging-axis ratification follow-through, ADR-0046 Rust-oracle reversal, ADR-0049 conjecture dark-loop wire + YUK-567 prep-desk UI, and the YUK-641→647 HTTP resource-contract unification wave.

## Summary
- Aligned: 5 (not expanded — ADR-0045 tagging axis now ratified + ADR-0026 amended in-place, resolving 2026-06-23's top findings; prior duplicate `agent-notes` capability package is gone; ADR-0046 Rust-oracle reversal matches code phase-state; ADR-0049 producer/consumer wiring + FSRS/attempt dark-loop isolation both hold)
- Documented-only: 0
- Undocumented: 2
- Contradicted: 0
- Phase-deferred: several (ADR-0046 §3 prod `.node`/WASM ship, POLY_SIGMOID_ENABLED swap, MISCONCEPTION_PROMOTE_ENABLED; not reported)

## Findings

### ⚠️ Undocumented

#### [YUK-641→647 wave] HTTP resource-contract + OpenAPI convergence has no ADR; CLAUDE.md Request-flow section is stale
- **声明**: CLAUDE.md "Request flow" (§Architecture) describes the backend surface as "后端逻辑全在 Hono route handlers 里，经 capability manifests 贡献制登记进组合根 `server/app.ts`" — no mention of a shared resource-contract layer or generated OpenAPI spec.
- **代码**: 6 merged PRs (`93fb3d3` YUK-642 paper/review-session split, `1774d14` proposal-decision resources, `20a2469` YUK-643 ingestion-as-operations, `8fa166b` YUK-644 practice/attempt/correction convergence, `12603b9` YUK-646 unify HTTP resource contracts, `a1c3bd9` YUK-647 OpenAPI generation) introduced `src/kernel/http-contracts.ts` (shared error/pagination/cursor schemas), `src/kernel/openapi.ts` (`generateOpenApiDocument()`, served at `/api/openapi.json`, wired `server/app.ts:53`), and an extended `ApiRouteDecl` in `src/kernel/manifest.ts` — a load-bearing kernel-level pattern, not a one-off.
- **冲突**: A 6-PR architectural convergence is captured in a planning doc (`docs/planning/2026-07-14-api-resource-contract-convergence.md`, added in `12603b9`) but has no ADR-series entry (`docs/adr/` stops at 0049, unrelated), and that planning doc itself is still marked `状态：执行中` even though all 6 PRs are on HEAD — the tracking doc's own status has drifted from the code.
- **建议**: flip the planning doc status to done; either fold a short ADR-0050 ratifying the resource-contract + OpenAPI pattern or explicitly mark the planning doc as the ADR-equivalent record; refresh CLAUDE.md's Request-flow paragraph to name the contract layer + `/api/openapi.json`.
- Tracking: aggregate audit issue filed against this PR (see below) — no per-finding Linear issue filed by this automated run.

#### [ADR-0049 §Consequences / prep-desk handoff] Conjecture UI design-gate landed un-closed
- **声明**: ADR-0049 explicitly defers UI as design-gated ("本 ADR 不含"); `docs/design/handoff/2026-06-27-prep-desk-conjectures.md` calls for a design pre-flight + owner approval before UI ships, with §8 listing open owner-decisions (mount point B1/B2/B3, visual fidelity, slice boundaries, emphasis signal).
- **代码**: `docs/design/2026-07-12-prep-desk-card-design.md` landed in the *same commit* (`15b1d90`, YUK-567 slice-1) as the UI it's supposed to gate (`PrepDeskConjectures.tsx`), and still carries `Status: DRAFT — 待 owner 判词. UI 代码零动，等批` with §8's decisions unresolved on paper — yet the code already resolved the mount-point decision (B1: inline `/today` expansion) and shipped slice-2 (`28dcd67`) plus a follow-up fix (`e86c44c`).
- **冲突**: the pre-flight doc exists (not a missing-doc case) but the gate it names — recorded owner approval before UI code moves — was never closed; the DRAFT status and unresolved §8 decisions are stale against merged code.
- **建议**: flip `2026-07-12-prep-desk-card-design.md` from DRAFT→Accepted, record the resolved §8 decisions (B1 mount + the others as implicitly settled by what shipped), and check off the handoff doc's §5 design-lane TODO. Minor secondary: `KIND_META.conjecture` (`inbox-api.ts:43`) still uses generic `label:'诊断推测', icon:'review'` rather than the "教研团" framing the design doc's §1 calls for — low impact, fold into the same cleanup.
- Tracking: aggregate audit issue filed against this PR (see below) — no per-finding Linear issue filed by this automated run.

## Notes / Phase-deferred (not counted as drift)
- **ADR-0046 §3 prod `.node`/WASM ship**: Rust crate (`crates/calibration-native/`) exists, is napi-wired with skip-if-absent dark-ship flags, but not yet in the Docker build — matches the ADR's explicit forward-only phasing, not drift.
- **`src/core/coldstart-solver.ts` TS oracle**: parity-locked scaffold the Rust port diff-tests against; fits ADR-0046 §5/§6's migration-period allowance. Worth tracking that it gets deleted once the Rust port is trusted, but not drift today.
- **live θ̂ (`theta.ts`/`pfa.ts`) still `Math.exp`**: `POLY_SIGMOID_ENABLED=false` — ADR-0046 background section marks this swap as unshipped Phase-1, correctly deferred.
- **ADR-0047/0048**: both still `Status: Proposed` (draft, "勿自行 ratify") — out of scope for this run; re-check once ratified.
