# Drift Audit — 2026-05-31

**Scope**: ADR-0001..ADR-0026, plans/2026-05-27-master-roadmap.md, CLAUDE.md, docs/design/ (last 30 days)
**Run by**: Claude Code (manual /audit-drift)
**Baseline**: main `b143ecd9` (scenario B 第一批 shipped 2026-05-30) + P5.1 context budget commits (`6fbd6b0`, `0ca46d0`, `bd18490`)

## Summary

- Aligned: 8（不展开）
- Documented-only: 0
- Undocumented: 0
- Contradicted: 1
- Phase-deferred: 5 (informational)

新增验证重点：P5.1 Context Budget Policy（`budgets.ts` 新文件 + `context-throttle.ts` Copilot 节流）、scenario B 第一批落地的 ADR-0023..0026 + ADR-0014 question_part / cross-subject scheduling。

---

## Findings

### ❌ Contradicted

#### [ADR-0025 §决策2 / ND-2] GoalScopeTask registry 条目引用了错误的 ADR 编号

- **声明**：ADR-0025 §决策2 锁定 `GoalScopeTask`（复用 proposal 模式，ND-2 = 北极星通过 `goal_scope` proposal 物化）。ADR-0025 明确声明 ND-1..ND-5 是北极星不变式。
- **代码**：`src/ai/registry.ts:465`，GoalScopeTask 描述字符串：`'YUK-143 / ADR-0024 — North-Star goal→scope translation (ND-2)...'`
- **冲突**：ADR-0024 是"泛化捕获结果 = 信号"（T-OC slice1），不涉及 GoalScopeTask / ND-2。正确的 ADR 是 ADR-0025。这个错误引用会让代码审计人员（+ pnpm audit:schema 这类工具）以为 GoalScopeTask 受 ADR-0024 管辖，而实际上是 ADR-0025。
- **建议**：将 `src/ai/registry.ts:465` 中的 `ADR-0024` 改为 `ADR-0025`。单行改动。
- Linear issue（closeout gate — 需去重后创建）：
  - title: `Drift: ADR-0025 ↔ src/ai/registry.ts GoalScopeTask ADR ref typo`
  - labels: `drift`

### ⚠️ Documented-only

无。

### ⚠️ Undocumented

无。

---

## Aligned（targeted 查证，有代码证据）

| 决策 | 代码证据 |
|---|---|
| ADR-0021 transactional outbox：writeEvent 纯 INSERT，不带 enqueue 副作用 | `src/server/events/queries.ts` 注释明确"outbox poll handler in `triggers.ts` picks it up"；无 `boss`/`triggers` import |
| ADR-0021 outbox poll/recover handlers 每分钟/每小时调度 | `src/server/memory/triggers.ts:261-270`：`MEMORY_INGEST_OUTBOX_POLL_QUEUE` 注册 `'* * * * *'`，recover 注册 `'0 * * * *'` |
| ADR-0025 ND-5：soft-bias / round-robin 只改顺序，不改集合/数量/due_at | `src/server/review/due-list.ts:25-26,398-449`：ND-5 逐字注释，`rerankOverdueByGoals` 和 `roundRobinBySubject` 均明确"changes ONLY the ORDER…count unchanged" |
| ADR-0026 auto-enroll 默认 OFF | `src/server/ingestion/workflow-judge-config.ts:43`：`autoEnrollEnabled({}) → false`（测试用例 L19 确认） |
| ADR-0015 §2 memory_brief_note 单一 write owner = `src/server/memory/brief.ts` | 全 `src/server/**/*.ts` 扫描：仅 `brief.ts:99` 有 `.insert(memory_brief_note)`；`context-readers.ts` 和 `copilot-summary.ts` 是只读 SELECT |
| ADR-0017 Write trigger #1 outbox 路径（ADR-0021 更新）+ anti-storm 6-min singleton 仍生效 | `triggers.ts:168-270`（outbox）；`triggers.ts` 另处 brief regen singletonKey 仍在 |
| ADR-0003 provider 抽象：`providers.ts` + runner 通过 `resolveTaskProvider()`；fallbackChain schema 保留但 runner 不读 | `src/server/ai/runner.ts` 无 fallbackChain 读取；`registry.ts:17-37` 明确 `INACTIVE (phase-deferred, T-PD4)` |
| P5.1 spec CB-1..6：budgets.ts 单一来源；Dreaming/Coach handler 从 budgets.ts import；context-throttle 软停止不硬抛 | `dreaming_nightly.ts:15,38`、`coach_daily.ts:22,34`：均 `import { *_CONTEXT_BUDGET } from '@/server/ai/tools/budgets'`；`context-throttle.ts:159,220`："graceful soft-stop as the tool-call ceiling — NEVER a thrown error"；无 `throw` 在 budget 路径 |
| ADR-0014 cross-subject scheduling + question_part：均在 ADR-0014 追认，代码已落地 | ADR-0014 文件 2026-05-30 更新段落；`src/server/review/due-list.ts` round-robin；`src/server/questions/parts.ts` + `question_part` ActivityKind |

---

## Phase-deferred（informational，不报为 drift）

- **T-PD4 maxCost / fallbackChain 实装**：`registry.ts:17-37` 标 `INACTIVE (phase-deferred, T-PD4 @ 2026-05-29)`，非当前 Phase。
- **W10 North-Star UI**（goal cards / views / KG lens）：ADR-0025 + status.md 明确 DEFERRED，blocked on WR 设计 pre-flight（YUK-169）。
- **T-OC slice-2b figure-matching（YUK-163）+ slice-3b auto-enroll wiring（YUK-164）**：status.md DEFERRED。
- **T-MR deep maintenance ranking**：data-gated（≥100 proposals 才有信号），status.md 明确 DEFERRED。
- **question_part parent-level 聚合调度 / part review UI / 自动拆分**：ADR-0014 §1 注明仅当观察到碎片化再处理，status.md DEFERRED。

---

## 范围声明

- 本报告未改任何 `.ts` 代码；仅新建此审计文档。
- Schema 字段漂移由 `pnpm audit:schema` 独立管理，不在本报告范围。
- 未全 codebase Read；所有查证均为 targeted grep / Read（ADR 指名路径 or 关键词）。
- P5.2 brief refresh design spec（`docs/superpowers/specs/2026-05-31-p5.2-brief-refresh-design.md`）今日新建（design approved）、无代码实现——属于 specs/ 目录而非 plans/ 或 ADR/，不在本次 audit scope，不列为 documented-only。
- 历史 drift 报告（2026-05-15 至 2026-05-30 系列）故意不改——那是 point-in-time 记录。
