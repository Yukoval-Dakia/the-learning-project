# Drift Audit — 2026-07-11

**Scope**: ADR-0003 (YUK-576 supersede), docs/design/2026-07-07..2026-07-11 (YUK-573/575/576/577/579/597), CLAUDE.md (2026-07-09 slimming)
**Run by**: Claude Code (scheduled /audit-drift)

## Summary
- Aligned: 6（不展开）
- Documented-only: 0
- Undocumented: 1
- Contradicted: 0
- Phase-deferred: 1 (informational)

## Findings

### ⚠️ Undocumented

#### [docs/design/2026-07-07-yuk575-durable-lane-revival.md] Doc status header stale post-merge
- **声明**: 文档头部 `Status: DRAFT v2 — owner 拍板重构后重写，等 coordinator 新对抗面板判词。判词前零测试零生产代码。`
- **代码**: commit `36709c9`（"feat(copilot): YUK-575 PR1 — durable lane revival (opt-in mechanism, no default flip)"）已把该文档与生产代码在同一提交合入 main：`assembleCopilotRunInput`（`src/capabilities/copilot/server/copilot-run-input.ts:164-227`）、durable handler 改用 `streamTaskCollecting`（`src/server/boss/handlers/copilot_run.ts:50,166,361`）、`DURABLE_BUDGET` 三旋钮（`copilot_run.ts:113-117`）等均已实装并测试。
- **冲突**: 文档自称的状态（"判词前，零测试零生产代码"）与该文档在 git 历史中始终伴随生产代码合入的事实不符——任何拉取该文档的读者会误判该机制尚未落地。
- **建议**: 补一次性 status 行更新提交（"Status: MERGED (36709c9) — PR1 landed, opt-in mechanism only, default still inline"），或在文末追加 supersede 注（仿 ADR-0003 的 2026-07-07 supersede 注格式）。低成本、非阻塞。

## Verified Aligned（不展开，逐条已用 targeted code-ground 核实）
1. ADR-0003 + YUK-576 registry honesty：`maxCost`/`fallbackChain` 已删，`transientRetries` 必填字段 + judges=1，`runner.ts` retry loop（`AgentRunError`/`RETRY_ELAPSED_CAP_MS=10_000`）落地，`docs/architecture.md` §5.2/§5.3 同步。
2. YUK-575 durable lane PR1 机制：opt-in flag（`chat.ts` `parsed.durable`）保留、默认仍 inline，无任何代码路径把 durable 设为默认。
3. YUK-577 proactive-nudge trigger cut-1：`nudge-triggers.ts` 硬编码仅 `ingestion_complete` kind，两个生产者（docx / OCR 两条 ingestion 路径）之外零 enqueue 点。
4. YUK-579 coverage lattice：`/admin/coverage-lattice` 第五页已挂载（`observability/manifest.ts:221-226`），route 文件零写路径（grep insert/update/delete/POST 全零命中）、零 LLM 调用。
5. YUK-597 custom subjects（臂B + opaque id + trait 家族）：PR 链（YUK-598~603）已全量合入，`src/db/schema.ts:1718-1849` trait 表族、`src/server/subjects/thin-create.ts` 等均落地，字段契约与文档一致。
6. YUK-573 judge calibration MVP：golden-set 脚本、nightly disagreement-rate 采样 job、admin 只读端点均已落地。

## Phase-deferred（informational，不计入 findings）
- **YUK-602**（`docs/design/2026-07-11-yuk601-602-ui-design.md`）：文档自述 `DRAFT，停批等 owner 判词` —— 尚无实施授权，不构成 drift。

---

## 备注
- 全部 6 条 Aligned 决策经 4 路并行 targeted code-ground 验证（grep + 文件级读取），未做全仓库扫读。
- 本次 30 天窗口内 ADR 目录唯一实质内容变更 = ADR-0003 的 YUK-576 supersede 注（其余 ADR 文件的 `git log --since` 命中系一次性历史快照提交 `4f12473`，非真实决策变更，已按 SKILL.md 假阳性防控规则排除）。
- CLAUDE.md 2026-07-09 瘦身提交声称的下沉目标文件（`.claude/skills/audits-reference/`、`.claude/skills/postman-api/`、`.claude/skills/pr/`、`src/server/ai/AGENTS.md`）均存在，判 Aligned，未单列 finding。
