# Drift Audit — 2026-06-23

**Scope**: ADR-0002..ADR-0044, plans/<active>, CLAUDE.md, docs/architecture.md, design/(last 30d)
**Run by**: Claude Code (manual /audit-drift)
**Lens**: 30-day recency; focus on YUK-489 P1–P3 tagging-axis reshape (landed on main) + recently-touched capabilities (knowledge / ingestion / copilot / agency).

## Summary
- Aligned: ~30 (not expanded; e.g. ADR-0023 Redis→PG already amended in-place; ADR-0007 single-user; ADR-0024 enroll owner intact)
- Documented-only: 1
- Undocumented: 2
- Contradicted: 1
- Phase-deferred: 2 (informational — ADR-0036 observation-window unavailability is "特性非 bug"; ADR-0035 慢热阶段; not reported)

## Findings

### ❌ Contradicted (priority)

#### [architecture.md §5.1 AI-task table + auto-enroll flow] TaggingTask mechanism is the pre-YUK-489 confidence-gate shape
- **声明**: architecture.md:132 `| TaggingTask | … | 题面 + 网格 → knowledge_id 建议 + confidence |`; line 152 lists KC creation paths as "cold-start-bridge / image-candidate-accept matcher / agent proposal-tools" with no mention of unified cosine tagging.
- **代码**: `src/capabilities/knowledge/server/tag-knowledge.ts` (YUK-489 P2/P3) replaced this with embedding retrieval (`matchKnowledgeBySimilarity` + `MATCH_THRESHOLD=0.55`) → match-or-propose; auto-enroll now calls `tagKnowledge` and `synthesizeTaggingOutput` (`auto-enroll.ts:456,521`), and the dead `knowledge_ids:[]` zero-match gate is removed.
- **冲突**: The doc still describes grid-snapshot + confidence-suggestion tagging; the live axis is cosine match-or-propose with auto-approved KC mint. architecture.md was touched 2026-06-22 but its tagging rows are stale; grep for `YUK-489 / unified tagging / cosine / MATCH_THRESHOLD` returns NONE.
- **建议**: amend architecture.md §5.1 row + line 152 KC-creation list to name `tagKnowledge` (cosine match-or-propose, auto-approve+audit `experimental:auto_tag_kc_created`) as the unified content-axis path across all 4 entry points.

### ⚠️ Documented-only

#### [ADR-0026 §1–2] WorkflowJudge confidence-gate tagging not amended for the YUK-489 reshape
- **声明**: ADR-0026 §1 "TaggingTask … 输出 = {suggestions:[{knowledge_id,confidence}], overall_confidence}"; §2 WorkflowJudge = `min(extraction, tagging)` confidence → route auto/review.
- **代码**: tagging is now `tag-knowledge.ts` match-or-propose; the confidence→route gate no longer drives KC attribution (auto-enroll synthesizes a tagging output from `tagKnowledge` ids). ADR-0026 has NO YUK-489 amendment note (grep confirms).
- **冲突**: ADR-0026 §1–2 read as current but describe a superseded mechanism. The flag-gated auto-enroll rollout (§3) and `enrollCapturedBlock` reuse (§4) still hold; only the tagging/judge front-half is stale.
- **建议**: add an amendment note to ADR-0026 (mirroring the ADR-0023 "M5 迁移注" pattern) marking §1–2 partially superseded by YUK-489 / `docs/design/2026-06-22-unified-tagging-axis.md`; keep §3–4.

### ⚠️ Undocumented

#### [docs/design/2026-06-22-unified-tagging-axis.md] Landed tagging axis has no ratifying ADR
- **声明**: design doc §1 owner decisions (cosine-driven match-vs-propose, async dedup, auto-approve new KC, runs on all entry points) — design doc only, not an ADR.
- **代码**: `tag-knowledge.ts`, `match-similarity.ts`, `tagging-flags.ts` are live on main (P1–P3 merged); cold-start-bridge ① subsumed, bridge KEPT as subject classifier + naming engine (`tag-knowledge.ts` imports `runColdStartBridge` as the `NameKcFn` default).
- **冲突**: A material architecture decision (unified content/KC attribution axis, orthogonal to grading YUK-488) lives only in a design doc; no ADR ratifies it, so the ADR series implies the old confidence-gate path is still authoritative.
- **建议**: write an ADR ratifying the unified tagging axis (supersedes ADR-0026 §1–2; references design doc) so the decision is discoverable from docs/adr/.

#### [src/capabilities] Duplicate `agent-notes` capability package not in composition root
- **声明**: no doc — `src/capabilities/index.ts` mounts `agencyCapability` only.
- **代码**: `src/capabilities/agent-notes/**` (manifest, server, ui, tests) was committed/changed in the last 30 days and mirrors `src/capabilities/agency/ui/AgentNotes*` 1:1, but `agentNotesCapability` is NOT imported in `src/capabilities/index.ts` (only `agencyCapability` is).
- **冲突**: An unmounted parallel capability package is dead/duplicate surface; unclear whether it's an in-progress extraction of agency's agent-notes UI or abandoned. Risk: divergence + audit confusion.
- **建议**: confirm intent — either wire `agent-notes` into composition (+ document) or delete the duplicate; capture as a follow-up (not ADR drift, structural debt).

## Notes / Phase-deferred (not counted as drift)
- **P4a reference-answer decoupling** (`reference_md===null` trigger): correctly NOT on main yet — code comments mark it "decoupled to P4a"; matches in-flight branch. Aligned-as-deferred, no action.
- **ADR-0036 dual-layer KG / misconception_edge / credit graphs**: ADR explicitly states observation-window unavailability is "特性非 bug"; phase-deferred, not reported.
- **ADR-0023 Redis editing presence**: already amended in-place (M5 迁移注 → PgPresenceStore); CLAUDE.md aligned. Not drift.
