# Drift Audit — 2026-06-07

**Scope**: ADR-0001..ADR-0029, CLAUDE.md (Architecture / Layering / Design principles), docs/design/2026-05-08..2026-06-07
**Run by**: Claude Code (manual /audit-drift)

## Summary

- Aligned: 26（不展开）
- Documented-only: 1
- Undocumented: 1
- Contradicted: 0
- Phase-deferred: 3 (informational)

## Findings

### ⚠️ Documented-only

#### [ADR-0027 §5-6] node-page multi-note read path not implemented

- **声明**: ADR-0027 decision #5（2026-06-03 accepted）："**label 交集列表读**：`notesForKnowledge(kid)` / `notesForItem(item)`；`node-page` 从'单 primary atomic（LIMIT 1）'拓宽到'该节点标签命中的多笔记'，atomic 节点简介仍优先呈现。" Decision #6："`resolveOwningLearningItemIds` 改为'引用/representative'语义（或退役）；所有依赖 1:1 ownership 的读（node-page、backlinks、learning_intent 创建路径）相应重排。"
- **代码**:
  - `src/server/knowledge/node-page.ts:L226` — 仍是 `.limit(1)`，只返回单条 atomic；`notesForKnowledge`（`src/server/artifacts/notes-read.ts:L77`）已实现但未在 node-page 中接入。
  - `src/server/knowledge/node-page.ts:L257,L295` — `resolveOwningLearningItemIds` 仍以 ownership 语义调用，ADR-0027 要求切换到 representative/引用语义。
- **冲突**: ADR-0027 明确把"重排所有依赖 ownership 的读"列为 cost/risk 并标为需配套 DB testcontainer 测试，ADR 已 accept 但读重排清单尚未落地。
- **建议**: 按 ADR-0027 §5-6 更新 `node-page.ts`：将 atomic 查询去掉 `.limit(1)` 改用 label 交集（`notesForKnowledge`）+ 优先展示 atomic；将 `resolveOwningLearningItemIds` 调用改为 representative 语义或去除（对齐 ADR-0027 迁移 spec）。
- Linear issue（closeout gate）：
  - title: `Drift: ADR-0027 ↔ src/server/knowledge/node-page.ts`
  - labels: `drift`

---

### ⚠️ Undocumented

#### [ADR-0011 T-D7 deferred, unfulfilled] 三个 Copilot experimental event action 无 ADR schema 或晋升路径

- **声明**: ADR-0011 §1.1 T-D7（2026-05-28）明文："Wave 5 closeout drift audit 提及的 2 个其它 experimental action —— `experimental:copilot_user_ask` 与 `experimental:copilot_chip_trigger` —— 不在本 T-D7 范围内。它们未在本 ADR §1 拥有 dedicated schema，按 generic ExperimentalEvent 走，将在后续 **ADR-0011 erratum 或 Wave 7+ 独立处理**。" ADR-0006 §"接受的代价"："**每 quarter review 一次，promote 该 promote 的**。"
- **代码**:
  - `src/server/copilot/chat.ts:L139-145`：生产写入 `experimental:copilot_user_ask`、`experimental:copilot_chip_trigger`、`experimental:copilot_reply`（U3 新增，2026-06-04 ship）。
  - `src/server/copilot/turns.ts:L68-71`：读路径过滤这三个 action。
  - 三个 action 全走 generic `ExperimentalEvent` escape hatch，无专属 Zod schema，无 stabilization criteria 文档。
- **冲突**: ADR-0011 承诺的"后续 erratum 或 Wave 7+"从未兑现；`experimental:copilot_reply`（U3 引入）在任何 ADR 中均无追认。ADR-0006 季度 promote 机制没有覆盖这组 action。
- **建议**: 写 ADR-0011 erratum（或新 ADR-0030），为三个 action 明确：(a) payload Zod schema（或确认继续走 generic ExperimentalEvent + 说明理由），(b) stabilization criteria（参照 ADR-0011 §1 的 `experimental:tool_use` 格式），(c) 预计 promote 时间窗。
- Linear issue（closeout gate）：
  - title: `Drift: ADR-0011 erratum promised but not written — copilot experimental actions`
  - labels: `drift`

---

## Phase-deferred（informational，不计入 finding 数）

- `experimental:user_cause`：ADR-0011 experimental.ts:L24 记录了 "1c.2 落地 + 真实使用 4 周后 promote" stabilization criteria，条件未触发，属正常 experimental 周期。
- Paper quiz UI（做卷一级页 Coach 卷+用户卷统一列出）：ADR-0029 D3 ④ 明文"UI 落地走 design pre-flight"；redraw preflight 文档（docs/design/2026-06-04-*）正在进行中。
- `subject_profile_*` DB 四张表（ADR-0029 D9）：显式 DEFER，触发条件 = "git 流证明不够用"。
