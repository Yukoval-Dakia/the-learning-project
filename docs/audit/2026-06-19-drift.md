# Drift Audit — 2026-06-19

**Scope**: ADR-0023 M5 amendment, ADR-0037, ADR-0040, ADR-0041, ADR-0043, ADR-0044; CLAUDE.md §Architecture  
**Run by**: Claude Code (manual /audit-drift)  
**Method**: targeted grep/read against ADR decision points accepted 2026-06-13 – 2026-06-15

## Summary

- Aligned: 4（不展开）
- Documented-only: 5
- Undocumented: 0
- Contradicted: 2
- Phase-deferred: 0

---

## Findings

### ❌ Contradicted

#### [ADR-0040 §7] note_verify 仍在产 patch-less note_update proposal

- **声明**: "note_verify **不再产** patch-less note_update proposal（死提议根源——唯一 accept applier 硬要求 `NotePatch.safeParse(change.patch)`，对 undefined 必抛 400，proposal 进收件箱却只能 dismiss，零测试覆盖）。verify 发现的 issues 落 **advisory**（artifact 已有 `verification_summary` 字段承载）"
- **代码**: `src/capabilities/notes/jobs/note_verify.ts:186`，`status === 'needs_review'` 分支仍调 `writeNoteUpdateProposal(tx, { artifact_id, verification_event_id, summary_md, issues, reason_md, ... })`，无 patch 字段（patch-less）。
- **冲突**: ADR 明令停止产此类提议，改写 advisory；代码仍产 dead proposal，inbox 收件箱中这类提议永远只能 dismiss。
- **建议**: 将 `writeNoteUpdateProposal` 调用替换为写 `artifact.verification_summary`（或 `writeEvent` advisory），并按 ADR-0040 §7 可选地触发 note_refine 产真正可应用 patch。
- **Linear issue**:
  - title: `Drift: ADR-0040 §7 ↔ note_verify.ts — patch-less note_update proposal 仍在产`
  - labels: `drift`

---

#### [ADR-0040 §1] apply-note-patch 路径未实现 user_verified 硬边界

- **声明**: "`apply-note-patch` 对 `user_verified` 块拒绝 mutator 直改，强制走 propose——**修最严重故障缺口**（现 apply 路径对 user_verified/source_tier grep 零命中，AI 静默覆写用户已验证内容，破 propose-only 红线）"
- **代码**: `src/capabilities/notes/server/note-refine-apply.ts` — grep `user_verified` 零命中，无任何 block-level guard；note-refine.ts 同样零命中。
- **冲突**: ADR-0040 §1 把 user_verified 边界列为"修最严重故障缺口"，要求 A 档自动应用路径拒直改 user_verified 块；代码路径完全未实现该 guard。AI refine 仍可静默覆写用户已验证内容。
- **建议**: 在 `note-refine-apply.ts` mutator 入口对 body_blocks 按 `user_verified` 过滤，触碰该类 block 时抛错/降级走 propose 路径；补回归测试断言「不破坏 user_verified 块」。
- **Linear issue**:
  - title: `Drift: ADR-0040 §1 ↔ note-refine-apply.ts — user_verified 硬边界未实现`
  - labels: `drift`

---

### ⚠️ Documented-only

#### [ADR-0040 §6] dwell 触发未下线，editing-heartbeat 仍调 enqueueDwellNoteRefine

- **声明**: "删 `editing-heartbeat`→refine 触发（最弱信号「纯编辑驻留」、最频繁、kill switch 实际无效、跨进程 debounce 失效、⚖️ 从 M3 拖到现在）... 了结拖三个里程碑的 ⚖️ 争议"
- **代码**: `src/capabilities/notes/api/editing-heartbeat.ts:30`，`body.status === 'editing'` 时仍调 `await enqueueDwellNoteRefine(...)` ；`note-refine-triggers.ts:12` 仍注册 `dwell: 'WAVE6_TRIGGER_DWELL_ENABLED'`，flag 缺省值为 **enabled**（undefined → true）；manifest.ts 描述字段仍列 `dwell` 为 Living Note 信号源。
- **冲突程度**: ADR 明确决策下线，代码保留且默认开启。注释"dwell ⚖️ 争议行未裁——等价平移，行为不动"是 M5 平移注释，早于 ADR-0040 落地；ADR 已裁定，注释已过时。
- **建议**: 删除 `editing-heartbeat.ts` 中的 `enqueueDwellNoteRefine` 调用 + `enqueueDwellNoteRefine` 导出 + `FLAG_BY_KIND.dwell` 条目；更新 `NoteRefineTriggerKind` 类型；更新 manifest.ts 描述。
- **Linear issue**:
  - title: `Drift: ADR-0040 §6 ↔ editing-heartbeat.ts — dwell 触发仍开启`
  - labels: `drift`

---

#### [ADR-0037 §5] review_plan job 仍以完整 handler 注册，未退役

- **声明**: "`review_plan` 退役并入引擎 ... 先降为引擎内的 proposer 角色，随即直接退役/全删（不再独立注册、不再另开 paper channel）；ReviewPlanTask 同步退场"
- **代码**: `src/capabilities/practice/manifest.ts:183`，`review_plan` 仍作完整 handler 注册（`load: () => import('./jobs/review_plan').then(m => m.buildReviewPlanHandler)`）；`src/capabilities/practice/jobs/review_plan.ts` 文件存在。
- **冲突程度**: ADR-0037 要求不再独立注册，代码仍完整注册并活跃；合并引擎未交付的情况下直接删除会破坏现有 review 路径，但代码未标记任何 deprecation 或过渡状态。
- **建议**: 在合并引擎（ADR-0037 §1）交付前，至少在 manifest 注释中标记 `review_plan` 为 deprecated/transitional；合并引擎就绪后执行真正退役。
- **Linear issue**:
  - title: `Drift: ADR-0037 §5 ↔ practice/manifest.ts — review_plan 仍注册，未退役`
  - labels: `drift`

---

#### [ADR-0040 §3] embedded_check 孤儿链已满足删除前提，但未清理

- **声明**: "真删 embedded_check 假删链 ... 须先确认 SPA 零消费后安全删：embedded_check_generate boss.send / embedded_check_status 列 / embedded-check·attempt 路由（199 行 judge 逻辑）"
- **代码**: `src/server/boss/handlers/embedded_check_generate.ts`、`src/capabilities/notes/api/embedded-check-attempt.ts` 仍存在；SPA (`web/`) 零命中 `embedded-check-attempt`（ADR 要求的前置核查已可确认满足）。
- **冲突程度**: 删除前提（SPA 零消费）已满足，但孤儿端点/handler/列仍活在代码里，维护负担持续。
- **建议**: 安全执行清理：删 `embedded_check_generate.ts`、`embedded-check-attempt.ts`（及 test），并在 schema migration 中 drop `embedded_check_status` 列；清理 `queue-config.ts` 中的 queue 注册。
- **Linear issue**:
  - title: `Drift: ADR-0040 §3 ↔ codebase — embedded_check 孤儿链未清理（SPA 零消费已确认）`
  - labels: `drift`

---

#### [ADR-0044 Wave 0 / ADR-0041 §后果] event-sourcing 地基 Wave 0 尚未落地

- **声明**: ADR-0044 Wave 0 = "mastery_state + material_fsrs_state 快照可逆 + cascade CTE 遍历器骨架 ... `collectCascadeFromCheckpoint`（`src/server/events/cascade.ts`，首个 WITH RECURSIVE + cycle guard + depth 64 熔断）由 YUK-363 Wave 0 引入"；同时 "新 `pnpm audit:projection`（仿 audit:schema）"；ADR-0041 checkpoint 腿 gated 在本改造后。
- **代码**: `src/server/events/cascade.ts` — 不存在；`src/core/projections/` — 不存在；`pnpm audit:projection` — package.json 中未找到；`state_snapshot` event 写路径 — 未确认。
- **冲突程度**: ADR-0044 接受于 2026-06-15（4 天前），Wave 0 是最低优先级入口；这是新近批准尚待实施，不是遗忘性漂移。但 checkpoint 腿（ADR-0041 核心 deliverable）依赖此地基，持续时间越长 blocker 越突出。
- **建议**: Wave 0 按 ADR-0044 §6 路线图推进：① `cascade.ts` WITH RECURSIVE CTE + cycle guard + depth 64 熔断；② `state_snapshot` event schema 校验 + memory outbox 豁免（ADR-0044 §Codex #1/#2）；③ `pnpm audit:projection` lint 入 pre-PR gate。
- **Linear issue**:
  - title: `Drift: ADR-0044 Wave 0 ↔ codebase — cascade.ts / projections / audit:projection 未实施`
  - labels: `drift`
