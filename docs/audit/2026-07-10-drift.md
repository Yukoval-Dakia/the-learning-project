# Drift Audit — 2026-07-10

**Scope**: ADR-0002..ADR-0049, docs/superpowers/plans/<active>, CLAUDE.md, docs/design/(last 30d)
**Run by**: Claude Code (manual /audit-drift)
**Lens**: 30-day recency — commits since 2026-06-10 touching docs/adr, docs/design, docs/superpowers/plans, CLAUDE.md (15 commits); targeted verification against `src/`, `server/`, `package.json`, `Dockerfile`.

## Summary
- Aligned: 8 (not expanded — YUK-576 maxCost/fallbackChain deletion ↔ ADR-0003 supersede note; ADR-0025 决策3 rewrite ↔ northstar guard fix; ADR-0025 ND-5 note ↔ step9 single-writer assertions; ADR-0023 Redis→PG "Superseded by" note ↔ CLAUDE.md "无 Redis"; capability composition root (8 manifests) ↔ CLAUDE.md Layering list; `server/app.ts` token auth ↔ CLAUDE.md Auth section; `dev:local` 3-process spawn ↔ CLAUDE.md Commands; ADR-0016 Codex eval still "proposed" so doesn't contradict the live anthropic-sub lane)
- Documented-only: 0
- Undocumented: 1
- Contradicted: 0
- Phase-deferred: 0

## Findings

### ⚠️ Undocumented

#### [ADR-0046, Accepted 2026-06-26] Rust numerical-engine stack not reflected in CLAUDE.md
- **声明**: ADR-0046 (`docs/adr/0046-rust-numerical-engine-single-source-of-truth.md`) is **Accepted**: deterministic numerical kernels (θ̂/PFA/calibration/proper-scoring) are now single-source-of-truth in Rust, shipped via napi (server) + WASM (browser), reversing the prior "TS oracle + Rust dev/CI-only mirror" stance (ADR YUK-493).
- **代码**: `crates/calibration-native/` exists with `Cargo.toml`/`lib.rs`; `package.json` has `build:native` / `build:native:wasm` scripts; 10 non-test source files already consume it (`src/core/theta-grid.ts`, `src/core/poly-exp.ts`, `src/server/coldstart/propagate-priors.ts`, `src/server/conjectures/scoring.ts`, plus parity test suites). CLAUDE.md's "Stack note" section (last dated 2026-06-13, pre-dates this ADR) and its "Commands" block contain **zero** mentions of `rust`, `napi`, `crates`, or `calibration-native` (`grep -i` confirms no hits).
- **冲突**: CLAUDE.md's Stack note explicitly says "Keep it aligned with this note when the runtime stack changes" — a new production-bound language/toolchain (Rust + napi/WASM) with an Accepted ADR and live code consumers is exactly that kind of change, and it's silent here. A reader following only CLAUDE.md would not know this stack component exists, nor that `pnpm build:native`/`build:native:wasm` are real commands.
- **建议**: add a "Rust numerical core (YUK-495, ADR-0046)" line to the Stack note (crates/calibration-native, napi+WASM, forward-only migration of θ̂/PFA/calibration kernels) and list `build:native`/`build:native:wasm` in the Commands block. Note ADR-0046 itself still leaves Dockerfile `.node` build wiring as **Open** (Q1 NAS arch decision, napi-vs-WASM-in-Node undecided) — so the doc update should describe current state as "crate exists, consumed by 10 files, prod Docker wiring not yet landed" rather than claim full production rollout.
- Linear issue（报告完成后按 `docs/agents/issue-tracker.md` closeout gate 去重并创建 / 更新，label `drift`）：
  - title: `Drift: ADR-0046 ↔ CLAUDE.md Stack note`
