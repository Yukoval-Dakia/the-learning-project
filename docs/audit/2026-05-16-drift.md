# Drift Audit — 2026-05-16

**Scope**: ADR-0001..ADR-0012（缺 0009），`docs/superpowers/plans/2026-05-14-phase1c1-encounter-session-ui-scaffold.md` (active), `CLAUDE.md`
**Run by**: Claude Code (manual /audit-drift, first run)
**Code anchor**: main @ 1b02dff

## Summary

| 类别 | 数量 |
|---|---|
| ✅ Aligned | 6 (foundational decisions in code) |
| ❌ Contradicted | **2** |
| ⚠️ Undocumented | **2** |
| ⚠️ Documented-only | 0 (其余 docs-only 项都已落入 1c.1 Step 1/2 plan) |
| ⏳ Phase-deferred | 8 (all queued in Phase 1c.1 Step 1+2，**informational only — not drift**) |

**🔴 实际待处理 drift = 4 项**（2 Contradicted + 2 Undocumented），全部围绕 ADR-0003 / ADR-0004 与 Sub 0d-DEFERRED 现实不一致。

---

## ❌ Contradicted（优先修）

### [ADR-0003 §"2026-05-11 更新"] 声称 `providers.ts` + `resolveTaskModel()` 已落地

- **声明**（ADR-0003 §"2026-05-11 更新" line 18）:
  > "Sub 0d 引入了 `providers.ts`（Provider Manager）+ `resolveTaskModel()`（Task Model Selector），这是 ADR 原文所说"~1 天局部重构"的最轻实现"
- **代码现实**:
  - `src/server/ai/providers.ts` **不存在**（`ls src/server/ai/` = judges/, log.ts, log.test.ts, runner.ts, runner.test.ts）
  - `src/server/ai/runner.ts:3` 直接 `import { anthropic } from '@ai-sdk/anthropic'`，未走 Provider Manager 抽象
  - `resolveTaskModel` 全代码 0 引用
- **冲突原因**: ADR-0003 该段 update 当时预期 Sub 0d 即将合并，但 Sub 0d plan 2026-05-14 banner 标 **DEFERRED — REFRESH REQUIRED**，从未执行。ADR 文本却以肯定句式描述这些代码存在。
- **建议**: 把 ADR-0003 §"2026-05-11 更新" 改写为"**计划在 Sub 0d 引入**...（Sub 0d 已 deferred 至 Phase 1c.1 后 refresh）"，或加 status banner。
- `gh issue create`:
  ```bash
  gh issue create --title "Drift: ADR-0003 2026-05-11 update overstates Sub-0d landing status" \
    --label "documentation,drift" \
    --body "ADR-0003 §2026-05-11 update reads as if providers.ts / resolveTaskModel() have landed. They have not — Sub 0d is DEFERRED. Update ADR-0003 text to reflect that this 1-day refactor is planned post-1c.1, not done."
  ```

### [ADR-0004 §"Provider Manager" + §"Task Model Selector"] 同上根因

- **声明**（ADR-0004 lines 11-33）: 完整描述 `src/server/ai/providers.ts` `getProvider()` 函数 + `src/ai/registry.ts` `resolveTaskModel()` 函数 + `TaskModelConfig` 类型，配代码块。
- **代码现实**:
  - `src/ai/registry.ts:43` 的 `tasks` 对象用 `defaultProvider: 'anthropic'` + `fallbackChain: [...]` 字段，**不是** ADR 描述的 `TaskModelConfig = { provider, model }` 形态
  - 没有任何函数叫 `getProvider` / `resolveTaskModel`
- **冲突原因**: ADR-0004 把 Sub 0d 计划写成"已落地"。同 ADR-0003。
- **建议**: 同 ADR-0003。ADR-0004 §"调用约定" 加 status banner "Provider Manager / Task Model Selector 计划于 Sub 0d refresh 后落地"。或更彻底——把这两段 demote 为 "Planned shape" 附录。
- `gh issue create`:
  ```bash
  gh issue create --title "Drift: ADR-0004 Provider Manager / Task Model Selector described as implemented" \
    --label "documentation,drift" \
    --body "ADR-0004 lines 11-33 describe getProvider() and resolveTaskModel() with code blocks. Neither exists. Add a 'Planned' banner or move these sections to 'Future shape' until Sub 0d lands."
  ```

---

## ⚠️ Undocumented（代码做了 ADR 没追认）

### [src/ai/registry.ts:43] `tasks` 内的 task 集合与 ADR-0004 task table 严重不一致

- **代码 reality**（4 个 task）:
  - `AttributionTask` (line 47)
  - `VisionExtractTask` (line 60)
  - `VisionExtractTaskHeavy` (line 74)
  - `KnowledgeProposeTask` (line 88)
- **ADR-0004 §"已知 task" table**（6 个，line 54-61）: EnrichMistakeTask / JudgeMistakeTask / VariantGenTask / DreamingTask / MaintenanceProposeTask / BlockAssemblyTask —— **0 个重合**
- **解读**:
  - `AttributionTask`（已实装）可能是 EnrichMistake + JudgeMistake 合体——ADR-0004 没追认这次重构
  - `KnowledgeProposeTask`（已实装）ADR-0004 完全没提
  - ADR-0004 列的 6 个任务在 Sub 0d-deferred 阶段全部 0 实装
- **建议**: ADR-0004 §"已知 task" 重写为 "**Phase 1 已实装**: AttributionTask, VisionExtractTask(Heavy), KnowledgeProposeTask. **Sub 0d 计划实装**: VariantGen / Dreaming / Maintenance / BlockAssembly." 同时显式注明 "EnrichMistakeTask + JudgeMistakeTask 合并为 AttributionTask"（如属实——需要 grill 用户）。
- `gh issue create`:
  ```bash
  gh issue create --title "Drift: ADR-0004 task list outdated — 0 of 6 implemented tasks match registry" \
    --label "documentation,drift" \
    --body "ADR-0004 lists EnrichMistake/Judge/VariantGen/Dreaming/Maintenance/BlockAssembly. Registry has AttributionTask/VisionExtract(Heavy)/KnowledgeProposeTask. Rewrite ADR-0004 task table to reflect current state + 'planned via Sub 0d' section."
  ```

### [src/ai/registry.ts] task config 字段 `fallbackChain` 未在 ADR-0003/0004 出现

- **代码 reality**: 每个 task 有 `fallbackChain: Array<{ provider, model }>` 字段（line 24, 49, 62, 76, 90）
- **ADR 提及**: ADR-0003 只说"Anthropic 直连"。ADR-0004 `TaskModelConfig` 是 `{ provider, model }` 没有 fallbackChain。
- **解读**: 这是个未追认的设计——已经在 registry 里预留 fallback 容错路径，但没有 ADR 解释 (a) 何时触发 fallback (b) cost / observability 怎么处理。低优先级——这个字段目前是 dead config（没有代码 read 它）。
- **建议**: 要么 ADR-0003 update 段加一行"registry 预留 fallbackChain，激活时机待定"；要么把 fallbackChain 从 registry 删掉（YAGNI）。
- `gh issue create`:
  ```bash
  gh issue create --title "Drift: registry fallbackChain not in any ADR" \
    --label "documentation,drift,low-priority" \
    --body "src/ai/registry.ts has per-task fallbackChain field, currently dead (no readers). Either document in ADR-0003 update or remove (YAGNI)."
  ```

---

## ⏳ Phase-deferred（informational — **not drift**）

以下 8 项 ADR 决策**已**在 `docs/superpowers/plans/2026-05-14-phase1c1-encounter-session-ui-scaffold.md` Step 1+2 banner 显式排期，**不算 drift**：

| 项 | 出处 | 落地点 |
|---|---|---|
| `event` 表 | ADR-0006 v2 | Step 1.1 |
| `learning_session` 表 | ADR-0008 + 1c.1 | Step 1.1 |
| `material_fsrs_state` 表 | ADR-0006 v2 | Step 1.1 |
| `knowledge_edge` 表 + 5 relation_types | ADR-0010 | Step 1.1 + 7 |
| DROP `mistake` / `review_event` / `dreaming_proposal` / `judgment` | ADR-0006 v2 | Step 3 |
| DROP `knowledge.base_mastery` / `ai_delta_mastery` / `last_active_at` | ADR-0012 | Step 1（同步） |
| CREATE VIEW `knowledge_mastery` | ADR-0012 | Step 1 |
| 5 个新 Zod schema (ToolUseExperimental / AcceptSuggestionChip / Propose/Generate/Rate KnowledgeEdge) | ADR-0011 | Step 2 |

`scripts/audit-schema-allowlist.json` 的 3 个 mastery stub 字段 entries 含 `resolves_when: "ADR-0012 implemented in Phase 1c.1 Step 1"`——自检通过，符合预期。

---

## ✅ Aligned（已对齐，不展开）

1. ADR-0001 TypeScript 单语种 + Python sidecar 逃生 — 仓库零 `.py` 文件
2. ADR-0005 IngestionSession single-owner — 只有 `src/server/ingestion/session.ts` 写 ingestion_session/question_block
3. ADR-0007 单用户假设 — `middleware.ts` 实施 x-internal-token；schema 零 user_id 列
4. ADR-0008 §修订（agent_sessions / agent_messages 作废）— 代码零引用
5. CLAUDE.md Architecture — `src/server/r2.ts`、`src/subjects/wenyan/`、`app/api/**` 全部存在
6. CLAUDE.md Stack note(workers/) — workers/ 确实在 disk 但 `app/api/` 是唯一 backend surface

---

## 信号校准

**首轮跑结果**:
- 实际报告 4 条 drift（远低于 top-15 cap）→ 信号干净
- 全部 4 条都指向同一根因（**ADR-0003 update + ADR-0004 描述了 Sub 0d 计划但 Sub 0d 已 deferred**）→ 可以一次性 ADR refresh 解决
- 假阳性率：0 / 4 = 0%（待用户确认）
- 通过 Phase-deferred 类别正确忽略 8 个 docs-only 项 → 防"诚实漂移"误报有效

**下一步**:
1. 用户 review 这 4 个 finding，确认非假阳性
2. 修 ADR-0003 + ADR-0004（一个 PR 覆盖）
3. 跑第 2 次 audit-drift（约 2 周后或 Phase 1c.1 Step 1 落地后）
4. 如假阳性率持续 < 30%，按 SKILL.md §信号校准 考虑上 GitHub Actions cron

**未跑的检查**（受 top-15 cap 限制 + 时间约束）:
- ADR-0002 OCR layer 与 `src/server/ingestion/` 实装的对齐度（应该 aligned，未抽样验证）
- design brief v2.1 与 UI 代码对齐（UI 还没动，Phase 1c.2 才做）
- `docs/architecture.md` 与代码（CLAUDE.md 已经覆盖核心架构面）
