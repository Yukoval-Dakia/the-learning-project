# Drift Audit — 2026-06-05

**Scope**: ADR-0001..ADR-0029, CLAUDE.md §Architecture/Layering/Design-principles, `docs/design/2026-06-03-target-domain-model.md`, `docs/superpowers/specs/2026-06-04-agent-framework-design.md`, status.md U-sequence
**Run by**: Claude Code (manual /audit-drift)

## Summary
- Aligned: 20
- Documented-only: 1
- Undocumented: 2
- Contradicted: 0
- Phase-deferred: 4 (informational, not reported)

## Findings

### ⚠️ Undocumented

#### [ADR-0025 §3 structural guarantee] due-list.ts couples to goal table despite "零耦合" claim

- **声明**: ADR-0025 §3 "结构性保证"：「FSRS 到期队列（`app/api/review/due/route.ts`）只读 `material_fsrs_state` + `event(action='attempt', outcome='failure')`，与 `goal` 表 / Coach 输出**零耦合**」
- **代码**: `src/server/review/due-list.ts:27`，import `listActiveGoals` from `@/server/goals/queries`（YUK-167 W10 soft-bias re-rank）
- **冲突**: ADR 的结构性保证已不成立——due-list 确实读 goal 表。ND-5 的**精神**（order-only，不抑制项、不改 FSRS 状态）由 soft-bias 代码和 `app/api/review/due/soft-bias.test.ts` 守住，但 ADR 正文的"零耦合"字面描述与代码不符
- **建议**: 在 ADR-0025 §3 结构性保证段追加 errata，记录 YUK-167（W10）goal soft-bias re-rank 引入了 goal 表读耦合，并说明此耦合是 order-only、ND-5 不变量已有 conservation test 守护
- Linear issue（报告完成后按 `docs/agents/issue-tracker.md` closeout gate 去重并创建 / 更新，label `drift`）：
  - title: `Drift: ADR-0025 §3 ↔ src/server/review/due-list.ts`

---

#### [ADR-0004 §"第二类：User Copilot"] Copilot 会话存储实现与 ADR 正文不符

- **声明**: ADR-0004 §"第二类：User Copilot"：「对话历史：`agent_sessions` + `agent_messages` 写 DB」
- **代码**: `src/server/copilot/turns.ts:39`，会话历史通过 `learning_session(entrypoint='copilot')` + `event(action='experimental:copilot_reply')` 实现（U3/YUK-203）。`agent_sessions` / `agent_messages` 表在 `src/db/schema.ts` 中不存在
- **冲突**: ADR-0004 正文的存储方案（两张专用表）与实际实现（event 表 + learning_session）不符。AF spec（`docs/superpowers/specs/2026-06-04-agent-framework-design.md:41`）已注明「Older ADR text still mentions agent_sessions even though conversation has moved into learning_session」并明确禁止复活旧表，但 ADR-0004 正文本身没更新，仍会误导后续开发者
- **建议**: 在 ADR-0004 §"第二类" 追加 update note，说明 U3/YUK-203 采用 `learning_session(entrypoint='copilot')` + `event(action='experimental:copilot_reply')` 替代原设计的 `agent_sessions`/`agent_messages`，并 link 到 AF spec
- Linear issue（报告完成后按 `docs/agents/issue-tracker.md` closeout gate 去重并创建 / 更新，label `drift`）：
  - title: `Drift: ADR-0004 §Copilot storage ↔ src/server/copilot/turns.ts`

---

### ⚠️ Documented-only

#### [ADR-0020 §8 Living Note] artifact 页内 "AI 改动" tab 未在 UI 中实现

- **声明**: ADR-0020 §8「集中入口：`/today` 加 Living Note 活动卡（24h digest + 批量 undo）+ **artifact 页内 'AI 改动' tab（本 note timeline）**」——原文标注"day1 ship"
- **代码**: `/today` 活动卡已实装（`app/(app)/today/page.tsx`，`/api/today/ai-changes`）；后端 API `app/api/artifacts/[id]/ai-changes/route.ts` 存在；但 `app/(app)/notes/[id]/page.tsx` 只有"阅读/编辑"双 tab，无 AI 改动 timeline tab
- **冲突**: ADR 声称 day1 ship 的 per-artifact AI 改动 tab 仅有后端，缺 UI 入口
- **建议**: 在 notes/[id] 页增加"AI 改动"tab（调用已有 `/api/artifacts/[id]/ai-changes`）；或在 ADR-0020 §8 追加 note 将其明确标为 deferred + 指向对应 Linear issue
- Linear issue（报告完成后按 `docs/agents/issue-tracker.md` closeout gate 去重并创建 / 更新，label `drift`）：
  - title: `Drift: ADR-0020 §8 ↔ app/(app)/notes/[id]/page.tsx`
