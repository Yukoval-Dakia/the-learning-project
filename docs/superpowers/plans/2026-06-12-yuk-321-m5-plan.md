# M5 实施计划 — Copilot 收口 + 拆除（YUK-321）

> 2026-06-12 · 分支 `yuk-321-m5-copilot-teardown`（fresh main 切出，main 含 M0-M4 全部 merge）
> spec：`docs/superpowers/specs/2026-06-10-architecture-redesign-design.md` §4 L340
> 地图：T1 四路勘察汇总（本 plan 已吸收其结论，并修正其中数处与源码不符的判定，见「裁决记录」）

## Goal

把 Copilot 与 observability 收编为 capability 包（D14 单人格 + 统一记忆读取面 + copilotTools 工具贡献制），随后单 commit 拆除整个旧栈采石场（app/、旧 src/server 残余、Next/Redis 依赖、旧 vitest 配置、postman 按 manifest 对账重生成），最后文档收编 + 终局 drift 审计——验收 = **仓库里只剩新形状**。

## Architecture

新增 `src/capabilities/copilot/` 与 `src/capabilities/observability/` 两包，经既有 manifest 贡献制（api.routes 懒加载 + ui.pages）登记进静态组合根 `src/capabilities/index.ts`；kernel manifest 契约新增第 7 个字段 `copilotTools`（name + load thunk），validateComposition 加第 6 个唯一性循环，组合根侧新增 `registerCapabilityCopilotTools` 聚合器（registerCapabilityJobs 先例）。SSE 从手工 ReadableStream 适配为 hono `SSEStreamingApi`（delta + reply 帧语义逐字节不变）。拆除阶段 Redis 退役；prod 进程拓扑与 presence 承接经 Task 8/9 之间的用户决断 gate **已于 2026-06-12 裁决：选项 b——保持双进程 + presence 进 PG 表**（三选项对照表保留在 gate 节作决策记录），Task 9 正文已按选项 b 收敛书写。

## Tech Stack

- Hono（组合根 `server/app.ts` / `hono/streaming` SSEStreamingApi / `@hono/node-server` serveStatic）
- Vite SPA（`web/`，TanStack Router + TanStack Query）+ React 19 + Tailwind v4
- Postgres + Drizzle（M5 唯一 schema 变更 = presence 表，gate 选项 b 前置子任务）；pg-boss worker 独立进程（`dist/worker.cjs`，gate 选项 b）
- esbuild server bundle（沿用 build:worker 配方 + 追加 `--external:@anthropic-ai/claude-agent-sdk`，见 Task 8）；Vitest unit/db/migration 三分区；Biome

## 红线（全程有效）

- 工作区脏文件（`.claude/settings.json`、`.codex/config.toml`、`.github/workflows/ocr-codex-review.yml`、`docs/planning/`、`docs/superpowers/plans/2026-05-2*`、`kg-*.png`/`kg-*.html`、`d-ui-*`、`.clinerules`、`.factory/`、`.serena/`、`docling-docx-parse-output/`、`visual-demo-today.png`、`docs/audit/2026-06-08-*`、`docs/superpowers/plans/2026-06-08-*`）**不属本工作，任何 commit 不得包含**。每次 commit 前 `git status` 逐文件核对，只 `git add` 本 plan 列出的路径。
- 每个 commit 标题含 `YUK-321`，正文末尾 footer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。
- 两组 UI 任务（Task 4 / Task 6）开头有 ⏸ gate：**等待用户 pre-flight 批准后才动 UI 代码**。
- Task 8 与 Task 9 之间的 ⏸ **prod 拓扑/presence 用户决断 gate** 已于 2026-06-12 裁决：**选项 b（双进程 + presence 进 PG 表）**。PgPresenceStore 子计划仍须在 gate 时点现场写好、**经用户过目后才动 schema**（见 gate 节「选项 b 执行步骤」），过目前不得开始 Task 9。
- T5c（Task 9）严格单 commit，可整体 revert。
- 测试分区：no-DB → `vitest.unit.config.ts`（`src/capabilities/**/*.unit.test.ts` / `src/kernel/**/*.unit.test.ts` 约定 glob 自动接管；src/server 下的 unit 测试须在 `vitest.shared.ts` fastTestInclude 显式登记）；DB/API → `vitest.db.config.ts`（capabilities 树命名 `*.db.test.ts`）。mock 掉 DB/R2/AI 的 route 测试可进 unit。

## 裁决记录（plan 写作期定案，逐条已对源码核实）

| # | 议题 | 裁决 | 证据 |
|---|---|---|---|
| a | 地图 §3 称 src/server 的 records/quiz/events/proposals/questions/session/orchestrator/judge/http 等「可删」 | **不可按清单删**——http/events/proposals/session/questions 等是 capabilities 活依赖（如全部包 api 引 `@/server/http/errors`）。T5c 改为现场 importer 闭包 grep 决定，零活引用才删 | `grep -rln "@/server/http" src/capabilities` 命中全部包 |
| b | 地图称 gen-postman「完全依赖 app/ 目录结构」 | 实际真相源是 `postman/api-endpoints.json`（手工规范）。重写方向 = 保留 spec 文件承载请求体/参数，新增 **manifest 对账层**（spec 条目必须存在于 manifest 路由清单，死条目剪除），非 app/ 扫描替换 | `scripts/gen-postman.ts` 头部 |
| c | CopilotDock 挂载点：spec §3.6 L318「主题/导航/移动端外壳 keep \| shell 包」，Dock 本体是 copilot 域 UI | 组件归 copilot 包（`src/capabilities/copilot/ui/`），挂载点在 `web/src/router.tsx` 根壳组件——沿用「路由耦合只存在于壳层」的 M0 规则（router.tsx 文件头注释） | router.tsx L1-3 |
| d | `query_records` / `get_record_context` D11 已裁渐废，仍在 COPILOT_TOOLS（25 名单内） | M5 等价平移不删：归 **ingestion** 包声明 + decl 旁注记 D11 渐废、何时移除（phase-deferred 注释惯例） | allowlists.ts L123-183 |
| e | Coach 周报页 spec keep（§3.6 L317「归工作台/复盘面」）但 M5 brief 未列 | 纳入本 plan Task 6；按 spec 归 **shell** 包（工作台域），数据端点 `/api/review/weekly` M2 已在 Hono，无新后端 | `app/(app)/coach/page.tsx` 仅 `useRouter` 一处 Next 耦合 |
| f | agent-notes 证据链接指向 `/events/$id` 等 SPA 未登记 surface（已知 404） | 不属 M5 范围；主 session 在 Linear capture gate 开 follow-up issue | router.tsx L58-60 注释 |
| g | spec §1.8 内嵌自测三刀 vs SPA 仍渲染 EmbeddedCheckSection | M5 只做**等价平移**收编 `/api/embedded-check/attempt` 路由（notes 包）；三刀是 feature 级工作，不属拆除 | vite.config.ts L31-32 注释 |
| h | COPILOT_TOOLS 数组是否改为从 manifests 派生 | **保持字面量**：`src/ai/registry.ts`（浏览器共享面）import allowlists，派生会把 `@/capabilities` 拉进 web bundle 图。归属真相 = manifests；一致性由 set-equality 单测强制（Task 3） | `grep -rln tools/allowlists src/ai` 命中 registry.ts |
| i | Redis 退役连带：editing presence 跨进程前提 + prod 进程拓扑 | **已用户裁决（2026-06-12）：选项 b——双进程 + presence 进 PG 表**（部署拓扑级决策，超出 plan 自决权限，三选项对照保留在 Task 8/9 之间的 gate 节作决策记录）；Task 9 正文已按选项 b 收敛书写 | 跨进程契约实证：worker 侧 `src/capabilities/notes/jobs/note-refine.ts` L229 经 `enqueueOrApplyNoteRefinePatch` 读 presence；web 侧 `app/api/editing-session/heartbeat/route.ts` L27 写 presence；双实现 seam editing-session.ts L29-31 |
| j | SSE 适配形态 | RouteHandler 是 Web 标准签名 `(req, params) => Promise<Response>`，不经 hono Context，故不用 `streamSSE(c)`，改用 `new SSEStreamingApi(writable, readable)` + `responseReadable` 自构 Response（hono streamSSE 内部即此形态）；delta 回调是同步 `(text) => void`、writeSSE 是 async → promise chain 保 FIFO | `hono/streaming` exports 实测 `['SSEStreamingApi','stream','streamSSE','streamText']` |
| k | D14「统一记忆读取面」落点 | chat.ts 既有 ambient context 装配 + `getRecentCopilotTurns` 即第一实例；收编进包即「明确读取面」，**不发明新抽象**（第二实例原则） | chat.ts 零 Next 耦合 |
| l | solve-skill / teaching-skill 归属 | 随 copilot 包迁（`server/skills/`）——`src/capabilities/practice/server/solve-session.ts` 活引用 solve-skill，import 路径随迁更新，**不能随旧栈死** | repo-wide grep "solve-skill" |

## 任务总览（10 任务 ≈ 10 commits + UI 视觉环修正）

| Task | 里程碑段 | 一句话 | commit |
|---|---|---|---|
| 1 | M5-T3a | kernel `copilotTools` 契约 + 组合根聚合器 | 1 |
| 2 | M5-T3b | copilot 包：server 面平移 + Hono SSE 四路由 | 1 |
| 3 | M5-T3c | 25 工具按归属五包声明 + allowlist 对账测试 | 1 |
| 4 | M5-T3d ⏸ | CopilotDock / CopilotHeroCard 迁 SPA 根壳 | 1 |
| 5 | M5-T4a | observability 包 server 面（四 GET 平移 + subjects 新建 + cost/today） | 1 |
| 6 | M5-T4b ⏸ | admin 四页 + Coach 周报 + TodayPage CostRibbon 迁 SPA | 1 |
| 7 | M5-T5a | 残余活路由收编（questions / mistakes / editing-session / embedded-check / 备份恢复 / event-correct） | 1 |
| 8 | M5-T5b | prod 形态定稿（server bundle + serveStatic，纯增量） | 1 |
| ✅ | — | **用户决断 gate：prod 拓扑 / presence 承接——已裁决选项 b（2026-06-12），见 Task 8/9 之间专节** | 0 |
| 9 | M5-T5c | **拆除采石场（严格单 commit 可整体 revert）** | 1 |
| 10 | M5-T6 | 文档收编 + 终局 drift 审计 | 1 |

T7/T8（Review / gate / PR）由主 session 驱动，不在本 plan 展开；每个 Task 自带定向验证步骤。

---

## Task 1 — kernel `copilotTools` 契约 + 组合根聚合器（M5-T3a）

### Files

- Modify: `src/kernel/manifest.ts`（+CopilotToolDecl、+copilotTools 字段、validateComposition 第 6 循环）
- Modify: `src/kernel/manifest.unit.test.ts`（+3 用例）
- Create: `src/server/ai/tools/register-capability-tools.ts`
- Create: `src/server/ai/tools/register-capability-tools.unit.test.ts`
- Modify: `vitest.shared.ts`（fastTestInclude +1 条）
- Modify: `server/index.ts`（启动期注册调用）

### 步骤

- [ ] 在 `src/kernel/manifest.unit.test.ts` 追加用例（沿用文件内既有五循环用例的构造手法）：

```ts
it('copilotTools 工具名跨包重复 → throw', () => {
  const a: CapabilityManifest = {
    name: 'a',
    description: 'a',
    copilotTools: { tools: [{ name: 'query_events' }] },
  };
  const b: CapabilityManifest = {
    name: 'b',
    description: 'b',
    copilotTools: { tools: [{ name: 'query_events' }] },
  };
  expect(() => validateComposition([a, b])).toThrow(/query_events.*declared by both/);
});

it('copilotTools 工具名唯一时通过', () => {
  const a: CapabilityManifest = {
    name: 'a',
    description: 'a',
    copilotTools: { tools: [{ name: 'query_events' }] },
  };
  const b: CapabilityManifest = {
    name: 'b',
    description: 'b',
    copilotTools: { tools: [{ name: 'query_mistakes' }] },
  };
  expect(() => validateComposition([a, b])).not.toThrow();
});

it('未声明 copilotTools 的包不参与第 6 循环', () => {
  const a: CapabilityManifest = { name: 'a', description: 'a' };
  expect(() => validateComposition([a])).not.toThrow();
});
```

- [ ] `pnpm vitest run --config vitest.unit.config.ts src/kernel/manifest.unit.test.ts` → 新用例 fail（类型不存在）
- [ ] `src/kernel/manifest.ts` 实现契约。新增类型（放在 JobDecl 之后、UiPageDecl 之前）：

```ts
/**
 * M5-T3 (YUK-321) — Copilot 工具声明（贡献制）。
 * name = DomainTool.name（全局唯一，validateComposition 第 6 循环守护）。
 * load 是懒加载 thunk（api.routes / jobs.handlers 同款先例）；缺省时为纯归属
 * 元数据，不被组合根挂载。返回类型用最小结构 { name }——kernel 不依赖
 * src/server 的 DomainTool 类型，类型窄化集中在组合根聚合器一处
 * （JobHandlerFactory 同约）。
 */
export interface CopilotToolDecl {
  name: string;
  load?: () => Promise<{ name: string }>;
}
```

CapabilityManifest 接口加字段：

```ts
  copilotTools?: { tools: CopilotToolDecl[] };
```

validateComposition 在第 5 循环（proposal kind）之后追加：

```ts
  // 6) copilot 工具名唯一（M5-T3 / YUK-321）
  const copilotToolOwners = new Map<string, string>();
  for (const cap of capabilities) {
    for (const tool of cap.copilotTools?.tools ?? []) {
      const owner = copilotToolOwners.get(tool.name);
      if (owner) {
        throw new Error(
          `validateComposition: copilot tool '${tool.name}' declared by both '${owner}' and '${cap.name}'`,
        );
      }
      copilotToolOwners.set(tool.name, cap.name);
    }
  }
```

- [ ] 重跑 → pass
- [ ] 写 `src/server/ai/tools/register-capability-tools.unit.test.ts`：

```ts
import { afterEach, describe, expect, it, vi } from 'vitest';

import type { CapabilityManifest } from '@/kernel/manifest';
import { registerCapabilityCopilotTools } from '@/server/ai/tools/register-capability-tools';
import { __resetRegistryForTests, getTool, registerTool } from '@/server/ai/tools/registry';
import type { DomainTool } from '@/server/ai/tools/types';

const fakeTool = (name: string) =>
  ({ name, description: name, effect: 'read', execute: vi.fn() }) as unknown as DomainTool<
    unknown,
    unknown
  >;

const cap = (tools: CapabilityManifest['copilotTools']): CapabilityManifest => ({
  name: 'x',
  description: 'x',
  copilotTools: tools && { tools: tools.tools },
});

afterEach(() => __resetRegistryForTests());

describe('registerCapabilityCopilotTools', () => {
  it('懒加载 thunk 注册进 DomainTool registry', async () => {
    await registerCapabilityCopilotTools([
      cap({ tools: [{ name: 't1', load: async () => fakeTool('t1') }] }),
    ]);
    expect(getTool('t1')).toBeDefined();
  });

  it('已注册（CORE_TOOLS 幂等兜底先到）→ 跳过，不触发 duplicate throw', async () => {
    registerTool(fakeTool('t1'));
    await expect(
      registerCapabilityCopilotTools([
        cap({ tools: [{ name: 't1', load: async () => fakeTool('t1') }] }),
      ]),
    ).resolves.toBeUndefined();
  });

  it('manifest 声明名与模块导出名不一致 → throw', async () => {
    await expect(
      registerCapabilityCopilotTools([
        cap({ tools: [{ name: 't1', load: async () => fakeTool('OTHER') }] }),
      ]),
    ).rejects.toThrow(/t1.*OTHER/);
  });

  it('无 load 的 decl 是纯归属元数据，不被挂载', async () => {
    await registerCapabilityCopilotTools([cap({ tools: [{ name: 't1' }] })]);
    expect(getTool('t1')).toBeUndefined();
  });
});
```

- [ ] 跑 → fail（模块不存在）
- [ ] 实现 `src/server/ai/tools/register-capability-tools.ts`：

```ts
// M5-T3 (YUK-321) — copilotTools 贡献制的组合根聚合器（registerCapabilityJobs 先例）。
//
// 归属真相源 = 各包 manifest.copilotTools；运行时注册仍落进既有 DomainTool
// registry（MCP 桥接 / AI runner 一律不动）。bootstrap.ts 的 CORE_TOOLS 保持
// 幂等兜底（per-tool getTool 判空后才 registerTool），与本聚合器谁先到达均安全。
// `as DomainTool` 窄化集中在此一处：kernel CopilotToolDecl 的 load 返回最小
// 结构 { name }，避免 kernel 依赖 src/server 类型（JobHandlerFactory 同约）。

import type { CapabilityManifest } from '@/kernel/manifest';

import { getTool, registerTool } from './registry';
import type { DomainTool } from './types';

export async function registerCapabilityCopilotTools(
  capabilities: CapabilityManifest[],
): Promise<void> {
  const decls = capabilities
    .flatMap((cap) => cap.copilotTools?.tools ?? [])
    .filter((decl) => decl.load);
  for (const decl of decls) {
    if (getTool(decl.name)) continue; // CORE_TOOLS 兜底已注册 → 跳过
    const tool = (await decl.load?.()) as DomainTool<unknown, unknown>;
    if (tool.name !== decl.name) {
      throw new Error(
        `registerCapabilityCopilotTools: manifest declares '${decl.name}' but module exports '${tool.name}'`,
      );
    }
    registerTool(tool);
  }
}
```

- [ ] 跑 → pass
- [ ] `vitest.shared.ts` fastTestInclude 登记（与 `src/server/proposals/adaptive-bias.unit.test.ts` 条目同形，插在 src/server 区）：

```ts
  // M5-T3 (YUK-321) — copilotTools 组合根聚合器：纯 registry 操作，无 DB。
  'src/server/ai/tools/register-capability-tools.unit.test.ts',
```

- [ ] `server/index.ts` 启动期注册（插在 RW_WORKER 块之前，同款动态 import 形态——工具模块可能传递 import `@/db/client`，须在 loadEnv() 之后求值）：

```ts
// M5-T3 (YUK-321)：copilotTools 贡献制——启动期把各包声明的工具注册进 DomainTool
// registry。失败不拖死 API 面（mcp-bridge 的 registerCoreTools 幂等兜底仍覆盖全集，
// 该兜底何时退役见 Task 3 的 allowlists 注记）。
void (async () => {
  const { registerCapabilityCopilotTools } = await import(
    '@/server/ai/tools/register-capability-tools'
  );
  await registerCapabilityCopilotTools(capabilities);
  console.log('[rw:api] capability copilot tools registered');
})().catch((err) => {
  console.error('[rw:api] copilot tool registration failed — CORE_TOOLS fallback covers', err);
});
```

- [ ] `pnpm typecheck` + touched-file Biome → 绿
- [ ] commit（只 add 上列 6 文件）：

```
feat(kernel): M5-T3 copilotTools 贡献制契约 + 组合根聚合器 (YUK-321)

kernel manifest 第 7 字段 copilotTools（name + 懒加载 thunk）+
validateComposition 第 6 唯一性循环 + registerCapabilityCopilotTools
聚合器（registerCapabilityJobs 先例）。CORE_TOOLS 保持幂等兜底。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
```

---

## Task 2 — copilot 包：server 面平移 + Hono SSE 四路由（M5-T3b）

### Files

- Create: `src/capabilities/copilot/manifest.ts`
- Create: `src/capabilities/copilot/api/chat.ts`、`api/chat.unit.test.ts`、`api/turns.ts`、`api/copilot-summary.ts`、`api/accept-chip.ts`
- git mv（含测试随迁改名，资产守护规则）：
  - `src/server/copilot/chat.ts` → `src/capabilities/copilot/server/chat.ts`
  - `src/server/copilot/chat.test.ts` → `src/capabilities/copilot/server/chat.unit.test.ts`（现 fast allowlist L137 显式条目，迁后由约定 glob 接管，删该条目）
  - `src/server/copilot/turns.ts` → `src/capabilities/copilot/server/turns.ts`
  - `src/server/copilot/turns.test.ts` → `src/capabilities/copilot/server/turns.db.test.ts`
  - `src/server/copilot/skills/solve-skill.ts`、`solve-skill.test.ts`、`teaching-skill.ts`、`teaching-skill.test.ts` → `src/capabilities/copilot/server/skills/`（两测试改名 `*.db.test.ts`——均含 db/drizzle import，写作期已核）
  - `src/server/teaching/active-question.ts`、`active-question.test.ts`（→ `.db.test.ts`）、`materialize-ask-check.ts` → `src/capabilities/copilot/server/teaching/`
- Modify: `src/capabilities/index.ts`（+copilot）
- Modify: `vitest.shared.ts`（删 L137 `'src/server/copilot/chat.test.ts'` 条目及其伴随注释）
- Modify: 全仓 `@/server/copilot/*`、`@/server/teaching/*` importer 改路径（含 `src/capabilities/practice/server/solve-session.ts`、app/ 旧树内引用——旧树须持续 typecheck 绿）
- Modify: `web/vite.config.ts`（proxy + `/api/copilot`、`/api/today`、`/api/teaching-sessions` → :8787）

**不删** `app/api/copilot/*`、`app/api/today/copilot-summary`、`app/api/teaching-sessions/*`——旧 surface 统一在 Task 9 单 commit 删除。

### 步骤

- [ ] 执行上列全部 `git mv`；repo-wide 改 import：`grep -rln "@/server/copilot\|@/server/teaching" app src server web scripts tests` 逐文件把前缀替换为 `@/capabilities/copilot/server`（skills 子路径 `@/server/copilot/skills/solve-skill` → `@/capabilities/copilot/server/skills/solve-skill`；teaching 子路径同理）
- [ ] 迁移文件内部的相对 import（chat.ts ↔ turns/skills/teaching 间）按新目录关系修正；`pnpm typecheck` → 绿
- [ ] `vitest.shared.ts` 删 L137 条目；`pnpm vitest run --config vitest.unit.config.ts src/capabilities/copilot/server/chat.unit.test.ts` → pass（glob 接管验证）
- [ ] 写 `src/capabilities/copilot/api/chat.unit.test.ts`（mock 全模块满足「mock 掉 DB/AI 才可进 unit」红线）：

```ts
import { describe, expect, it, vi } from 'vitest';
import { z } from 'zod';

const runMock = vi.hoisted(() => vi.fn());

vi.mock('@/db/client', () => ({ db: {} }));
vi.mock('@/capabilities/copilot/server/chat', () => ({
  CopilotChatRequest: z.object({ message: z.string() }),
  runCopilotChatStreaming: runMock,
}));

import { POST } from '@/capabilities/copilot/api/chat';

const post = (body: unknown) =>
  POST(
    new Request('http://test/api/copilot/chat', {
      method: 'POST',
      body: JSON.stringify(body),
    }),
    {},
  );

const readAll = (res: Response) => new Response(res.body).text();

describe('POST /api/copilot/chat — SSE via SSEStreamingApi', () => {
  it('delta 帧 FIFO 先于终态 reply 帧，framing 与旧栈逐字节一致', async () => {
    runMock.mockImplementation(async (_db, _req, onDelta) => {
      onDelta('你');
      onDelta('好');
      return { session_id: 's1', reply_event_id: 'e1' };
    });
    const res = await post({ message: 'hi' });
    expect(res.headers.get('Content-Type')).toBe('text/event-stream; charset=utf-8');
    expect(res.headers.get('Cache-Control')).toBe('no-cache, no-transform');
    expect(await readAll(res)).toBe(
      'event: delta\ndata: {"text":"你"}\n\n' +
        'event: delta\ndata: {"text":"好"}\n\n' +
        'event: reply\ndata: {"session_id":"s1","reply_event_id":"e1"}\n\n',
    );
  });

  it('zod 解析失败 → JSON errorResponse，绝不开流', async () => {
    const res = await post({});
    expect(res.status).toBe(400);
    expect(res.headers.get('Content-Type') ?? '').toContain('application/json');
  });

  it('runCopilotChatStreaming 抛错 → 固定 Internal Server Error，真实信息不出站', async () => {
    runMock.mockRejectedValue(new Error('db exploded: secret detail'));
    const res = await post({ message: 'hi' });
    const text = await readAll(res);
    expect(text).toBe('event: reply\ndata: {"error":"Internal Server Error"}\n\n');
    expect(text).not.toContain('secret detail');
  });
});
```

- [ ] 跑 → fail（api/chat.ts 不存在）
- [ ] 实现 `src/capabilities/copilot/api/chat.ts`（旧 route.ts 的三件契约逐字保留：parse-before-stream / 固定错误串 + console.error 真实信息 / 三 header）：

```ts
// M5-T3 (YUK-321) — POST /api/copilot/chat（SSE）。
// 等价平移 app/api/copilot/chat/route.ts：两 surface（chat | chip）路由契约、
// delta/reply 帧语义、parse-before-stream、错误串脱敏三件不变。
// 形态变更仅一处（M5 唯一运行时形态变更）：手工 ReadableStream → hono
// SSEStreamingApi 自构 Response（裁决 j：RouteHandler 是 Web 标准签名，不经
// hono Context，故不用 streamSSE(c)）。delta 回调是同步 (text)=>void、
// writeSSE 是 async —— promise chain 保 FIFO。

import { SSEStreamingApi } from 'hono/streaming';

import { CopilotChatRequest, runCopilotChatStreaming } from '@/capabilities/copilot/server/chat';
import { db } from '@/db/client';
import { errorResponse } from '@/server/http/errors';

export async function POST(req: Request): Promise<Response> {
  // Parse BEFORE constructing the stream：坏 body 走普通 JSON error（既有契约），
  // 绝不开半截 SSE 流。
  let parsed: ReturnType<typeof CopilotChatRequest.parse>;
  try {
    parsed = CopilotChatRequest.parse(await req.json());
  } catch (err) {
    return errorResponse(err);
  }

  const { readable, writable } = new TransformStream();
  const sse = new SSEStreamingApi(writable, readable);

  void (async () => {
    let chain: Promise<void> = Promise.resolve();
    const writeFrame = (event: string, payload: unknown) => {
      chain = chain.then(() => sse.writeSSE({ event, data: JSON.stringify(payload) }));
      return chain;
    };
    try {
      const result = await runCopilotChatStreaming(
        db,
        parsed,
        (text) => void writeFrame('delta', { text }),
        {},
        req.signal,
      );
      await writeFrame('reply', result);
    } catch (err) {
      // runCopilotChatStreaming 内部降级后 resolve；这里是最后兜底。
      // 脱敏契约同 errorResponse：真实 message+stack 只进服务端日志，
      // 客户端拿固定串。
      const message = err instanceof Error ? err.message : String(err);
      const stack = err instanceof Error ? err.stack : undefined;
      console.error('[copilot/chat] unhandled streaming error', {
        message,
        stack,
        timestamp: new Date().toISOString(),
      });
      await writeFrame('reply', { error: 'Internal Server Error' });
    } finally {
      await chain.catch(() => undefined);
      await sse.close();
    }
  })();

  return new Response(sse.responseReadable, {
    headers: {
      'Content-Type': 'text/event-stream; charset=utf-8',
      'Cache-Control': 'no-cache, no-transform',
      Connection: 'keep-alive',
    },
  });
}
```

- [ ] 跑 chat.unit.test.ts → pass（三用例全绿；若 hono 帧内 event/data 行序与断言不符，以测试输出为准对照旧栈帧序修 writeFrame，不改断言）
- [ ] 实现 `src/capabilities/copilot/api/turns.ts`（等价平移，删 `export const runtime` 行）：

```ts
// M5-T3 (YUK-321) — GET /api/copilot/turns?limit=20（重放 last-N，等价平移）。

import { getRecentCopilotTurns } from '@/capabilities/copilot/server/turns';
import { db } from '@/db/client';
import { errorResponse } from '@/server/http/errors';

export async function GET(req: Request): Promise<Response> {
  try {
    const url = new URL(req.url);
    const raw = url.searchParams.get('limit');
    const limit = raw === null ? undefined : Number.parseInt(raw, 10);
    const turns = await getRecentCopilotTurns(db, { limit });
    return Response.json({ turns });
  } catch (err) {
    return errorResponse(err);
  }
}
```

- [ ] 实现 `src/capabilities/copilot/api/copilot-summary.ts`（薄 shim，`@/server/today` 是 staying 模块不迁）：

```ts
// M5-T3 (YUK-321) — GET /api/today/copilot-summary（等价平移薄 shim）。
// loadCopilotSummary 留在 @/server/today（staying：boss/ai/memory/artifacts/today
// 是新栈活依赖，见 plan 裁决 a）。

import { db } from '@/db/client';
import { errorResponse } from '@/server/http/errors';
import { loadCopilotSummary } from '@/server/today/copilot-summary';

export async function GET(): Promise<Response> {
  try {
    return Response.json(await loadCopilotSummary(db));
  } catch (err) {
    return errorResponse(err);
  }
}
```

- [ ] 实现 `src/capabilities/copilot/api/accept-chip.ts`：整文件平移 `app/api/teaching-sessions/[id]/accept-chip/route.ts`，仅四处机械变换——(1) 删 `export const runtime`；(2) 签名改 `(req: Request, params: Record<string, string>)`，原 Next ctx.params 取 id 的代码换成 `z.object({ id: z.string().min(1) }).parse(params)`；(3) `@/server/session` 等 staying import 不动，`@/server/teaching/*` import 改 `@/capabilities/copilot/server/teaching/*`；(4) 文件头注释补「M5-T3 (YUK-321) 等价平移」一行。其余 body schema / 事件写入 / acceptAiProposal 逻辑逐字保留
- [ ] 写 `src/capabilities/copilot/manifest.ts`：

```ts
import { defineCapability } from '@/kernel/manifest';

// M5-T3 (YUK-321) — copilot 包：D14 单人格对话面（D13 权限继承框架内）。
// 统一记忆读取面 = server/chat.ts 既有 ambient context 装配 + server/turns.ts
// getRecentCopilotTurns（第一实例原则：不另立抽象，包边界即读取面——裁决 k）。
// copilotTools 五条本包工具在 Task 3 与其余四包一并声明。
export const copilotCapability = defineCapability({
  name: 'copilot',
  description:
    'Copilot 单人格对话（D13/D14）：自由对话 + chip 直触 SSE 流、turns 重放、' +
    '今日摘要、教学 accept-chip；工具面经 copilotTools 贡献制聚合。',
  events: {
    actions: [
      'experimental:copilot_user_ask',
      'experimental:copilot_chip_trigger',
      'experimental:copilot_reply',
      'accept_suggestion',
    ],
  },
  api: {
    routes: [
      {
        method: 'POST',
        path: '/api/copilot/chat',
        load: () => import('./api/chat').then((m) => m.POST),
      },
      {
        method: 'GET',
        path: '/api/copilot/turns',
        load: () => import('./api/turns').then((m) => m.GET),
      },
      {
        method: 'GET',
        path: '/api/today/copilot-summary',
        load: () => import('./api/copilot-summary').then((m) => m.GET),
      },
      {
        method: 'POST',
        path: '/api/teaching-sessions/[id]/accept-chip',
        load: () => import('./api/accept-chip').then((m) => m.POST),
      },
    ],
  },
});
```

- [ ] `src/capabilities/index.ts` 数组加 `copilotCapability`（import + 数组项，命名同既有六包 `xxxCapability = defineCapability({...})` 形制；放 notes 与 shell 之间保持字母序观感与现有顺序一致性自查）
- [ ] `web/vite.config.ts` proxy 表加三行（catch-all `'/api'` 之前）：

```ts
      // M5-T3 (YUK-321)：copilot 域收编新栈。
      '/api/copilot': 'http://localhost:8787',
      '/api/today': 'http://localhost:8787',
      '/api/teaching-sessions': 'http://localhost:8787',
```

- [ ] 验证：`pnpm typecheck`；`pnpm vitest run --config vitest.unit.config.ts src/capabilities/copilot` → 绿；`pnpm vitest run --config vitest.db.config.ts src/capabilities/copilot` → 绿（随迁 db 测试完好）；起 `pnpm rw:api` 后 `curl -s -X POST :8787/api/copilot/chat -H "x-internal-token: $TOKEN" -d '{}'` 应回 400 JSON（parse-before-stream 冒烟）
- [ ] commit：

```
feat(copilot): M5-T3 copilot 包——server 平移 + Hono SSE 四路由 (YUK-321)

chat/turns/skills/teaching 零耦合平移入包（solve-session 活引用随迁改路）；
SSE 手工 ReadableStream → SSEStreamingApi（delta/reply 帧逐字节钉测）；
chat+turns+copilot-summary+accept-chip 四路由经 manifest 挂 Hono。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
```

---

## Task 3 — 25 工具按归属五包声明 + allowlist 对账（M5-T3c）

### Files

- Modify: `src/capabilities/copilot/manifest.ts`（+5）、`src/capabilities/knowledge/manifest.ts`（+6）、`src/capabilities/practice/manifest.ts`（+7）、`src/capabilities/agency/manifest.ts`（+5）、`src/capabilities/ingestion/manifest.ts`（+2）
- Create: `src/capabilities/copilot/server/copilot-tools.unit.test.ts`（set-equality 对账）
- Create: `src/capabilities/copilot/server/copilot-tools.db.test.ts`（25 个 load thunk 真实 resolve + name 对账）
- Modify: `src/server/ai/tools/allowlists.ts`（L123 `const COPILOT_TOOLS` → `export const COPILOT_TOOLS`，仅加 export、数组内容不变；+归属真相源与 CORE_TOOLS 退役注记；数组保持字面量——裁决 h）

### 步骤

- [ ] 写对账 unit 测试 `src/capabilities/copilot/server/copilot-tools.unit.test.ts`：

```ts
import { describe, expect, it } from 'vitest';

import { capabilities } from '@/capabilities';
import { COPILOT_TOOLS } from '@/server/ai/tools/allowlists';

// 裁决 h：COPILOT_TOOLS 数组保持字面量（src/ai 浏览器共享面不能 import
// @/capabilities）；归属真相源 = 各包 manifest.copilotTools，本测试强制两面相等。
describe('copilotTools 贡献制 ↔ COPILOT_TOOLS allowlist 对账', () => {
  it('五包声明聚合与运行时 allowlist 集合相等且无重复', () => {
    const declared = capabilities.flatMap(
      (c) => c.copilotTools?.tools.map((t) => t.name) ?? [],
    );
    expect(new Set(declared)).toEqual(new Set(COPILOT_TOOLS));
    expect(declared).toHaveLength(COPILOT_TOOLS.length);
  });
});
```

- [ ] `src/server/ai/tools/allowlists.ts` L123：`const COPILOT_TOOLS` → `export const COPILOT_TOOLS`（仅加 export 关键字；数组内容与 `as const satisfies readonly DomainToolName[]` 收窄不动；此前是模块私有——文件内 L283-284 两处 SURFACE 表引用不受影响，上面 unit 测试的 import 依赖本步）
- [ ] 跑 → fail（声明聚合为空）
- [ ] 五包 manifest 各加 `copilotTools` 字段。load thunk 全部直指 `src/server/ai/tools/` 的 staying 模块（模块/导出名对照 bootstrap.ts L10-62 import 区，写作期已逐名核实）：

`src/capabilities/knowledge/manifest.ts`：

```ts
  // M5-T3 (YUK-321) — copilot 工具归属声明（图谱读 4 + 知识提议写 2）。
  copilotTools: {
    tools: [
      {
        name: 'query_knowledge',
        load: () => import('@/server/ai/tools/knowledge-readers').then((m) => m.queryKnowledgeTool),
      },
      {
        name: 'get_subject_graph_overview',
        load: () =>
          import('@/server/ai/tools/knowledge-readers').then((m) => m.getSubjectGraphOverviewTool),
      },
      {
        name: 'expand_knowledge_subgraph',
        load: () =>
          import('@/server/ai/tools/knowledge-readers').then((m) => m.expandKnowledgeSubgraphTool),
      },
      {
        name: 'find_knowledge_paths',
        load: () =>
          import('@/server/ai/tools/knowledge-readers').then((m) => m.findKnowledgePathsTool),
      },
      {
        name: 'propose_knowledge_edge',
        load: () =>
          import('@/server/ai/tools/proposal-tools').then((m) => m.proposeKnowledgeEdgeTool),
      },
      {
        name: 'propose_knowledge_mutation',
        load: () =>
          import('@/server/ai/tools/proposal-tools').then((m) => m.proposeKnowledgeMutationTool),
      },
    ],
  },
```

`src/capabilities/practice/manifest.ts`：

```ts
  // M5-T3 (YUK-321) — copilot 工具归属声明（题/错题/复习读 4 + 出题组卷写 3）。
  copilotTools: {
    tools: [
      {
        name: 'get_question_context',
        load: () =>
          import('@/server/ai/tools/context-readers').then((m) => m.getQuestionContextTool),
      },
      {
        name: 'get_review_due',
        load: () => import('@/server/ai/tools/context-readers').then((m) => m.getReviewDueTool),
      },
      {
        name: 'get_attempt_context',
        load: () =>
          import('@/server/ai/tools/get-attempt-context').then((m) => m.getAttemptContextTool),
      },
      {
        name: 'query_mistakes',
        load: () => import('@/server/ai/tools/query-mistakes').then((m) => m.queryMistakesTool),
      },
      {
        name: 'author_question',
        load: () => import('@/server/ai/tools/proposal-tools').then((m) => m.authorQuestionTool),
      },
      {
        name: 'query_questions',
        load: () => import('@/server/ai/tools/query-questions').then((m) => m.queryQuestionsTool),
      },
      {
        name: 'write_quiz',
        load: () => import('@/server/ai/tools/write-quiz').then((m) => m.writeQuizTool),
      },
    ],
  },
```

`src/capabilities/agency/manifest.ts`：

```ts
  // M5-T3 (YUK-321) — copilot 工具归属声明（learning_item 上下文读 1 + 生命周期提议 4）。
  copilotTools: {
    tools: [
      {
        name: 'get_learning_item_context',
        load: () =>
          import('@/server/ai/tools/context-readers').then((m) => m.getLearningItemContextTool),
      },
      {
        name: 'propose_learning_item_completion',
        load: () =>
          import('@/server/ai/tools/proposal-tools').then(
            (m) => m.proposeLearningItemCompletionTool,
          ),
      },
      {
        name: 'propose_learning_item_relearn',
        load: () =>
          import('@/server/ai/tools/proposal-tools').then((m) => m.proposeLearningItemRelearnTool),
      },
      {
        name: 'propose_learning_item_defer',
        load: () =>
          import('@/server/ai/tools/proposal-tools').then((m) => m.proposeLearningItemDeferTool),
      },
      {
        name: 'propose_learning_item_archive',
        load: () =>
          import('@/server/ai/tools/proposal-tools').then((m) => m.proposeLearningItemArchiveTool),
      },
    ],
  },
```

`src/capabilities/ingestion/manifest.ts`：

```ts
  // M5-T3 (YUK-321) — copilot 工具归属声明。D11 已裁 record 渐废（裁决 d）：
  // 两工具等价平移不删；record 域退役时随本声明一并摘除（查 spec §1 D11 行）。
  copilotTools: {
    tools: [
      {
        name: 'query_records',
        load: () => import('@/server/ai/tools/context-readers').then((m) => m.queryRecordsTool),
      },
      {
        name: 'get_record_context',
        load: () => import('@/server/ai/tools/context-readers').then((m) => m.getRecordContextTool),
      },
    ],
  },
```

`src/capabilities/copilot/manifest.ts`：

```ts
  // M5-T3 (YUK-321) — copilot 自有工具（事件流读 + 记忆面读 + artifact авторing 写）。
  copilotTools: {
    tools: [
      {
        name: 'query_events',
        load: () => import('@/server/ai/tools/query-events').then((m) => m.queryEventsTool),
      },
      {
        name: 'query_memory_brief',
        load: () => import('@/server/ai/tools/context-readers').then((m) => m.queryMemoryBriefTool),
      },
      {
        name: 'search_memory_facts',
        load: () =>
          import('@/server/ai/tools/search-memory-facts').then((m) => m.searchMemoryFactsTool),
      },
      {
        name: 'author_artifact',
        load: () => import('@/server/ai/tools/author-artifact').then((m) => m.authorArtifactTool),
      },
      {
        name: 'update_artifact',
        load: () => import('@/server/ai/tools/author-artifact').then((m) => m.updateArtifactTool),
      },
    ],
  },
```

（注意 copilot 块注释里 "авторing" 是笔误示例——落地时写 "authoring"。）

- [ ] 跑 unit 对账 → pass（若 fail，逐名比对 allowlists.ts L123-183 的 25 名修 decl，**不改 allowlist**）
- [ ] 写 `src/capabilities/copilot/server/copilot-tools.db.test.ts`（load thunk 真实 resolve，DB 分区因工具模块顶层可能触 DATABASE_URL）：

```ts
import { describe, expect, it } from 'vitest';

import { capabilities } from '@/capabilities';

describe('copilotTools load thunks', () => {
  it('25 条 decl 全部可 resolve 且模块导出名与声明名一致', async () => {
    const decls = capabilities.flatMap((c) => c.copilotTools?.tools ?? []);
    expect(decls.length).toBe(25);
    for (const decl of decls) {
      const tool = await decl.load?.();
      expect(tool?.name, `decl '${decl.name}' 的模块导出名不匹配`).toBe(decl.name);
    }
  });
});
```

- [ ] `pnpm vitest run --config vitest.db.config.ts src/capabilities/copilot/server/copilot-tools.db.test.ts` → pass（25 名已逐字核对：agency 生命周期三工具全名取自 proposal-tools.ts L1094/L1759/L1826，其余对照 bootstrap.ts import 区与 allowlists.ts L123-183）
- [ ] `allowlists.ts` COPILOT_TOOLS 上方加注记（不动数组内容）：

```ts
// M5-T3 (YUK-321) — 归属真相源已移至各包 manifest.copilotTools（五包 25 工具），
// 本数组保持字面量是因为 src/ai/registry.ts（浏览器共享面）import 本文件，
// 不能把 @/capabilities 拉进 web bundle（plan 裁决 h）。两面一致性由
// src/capabilities/copilot/server/copilot-tools.unit.test.ts 强制。
//
// CORE_TOOLS 退役时点（phase-deferred）：bootstrap.ts registerCoreTools 不在 M5
// 退役——它注册的是全工具面（含 attribute_mistake / propose_variant /
// propose_record_links / propose_record_promotion 与题目结构编辑工具等非
// copilot allowlist 成员），copilotTools 贡献制只覆盖本数组 25 条。退役条件 =
// 其余 surface（mistake_action / orchestrator / dreaming 等）也完成 manifest 化
// （post-M5 follow-up，Linear capture 见 plan Task 10 交接清单）；届时删
// registerCoreTools 调用，由 register-capability-tools.unit.test.ts 的幂等用例
// 守护切换。
```

- [ ] `pnpm typecheck` + `pnpm vitest run --config vitest.unit.config.ts src/kernel src/capabilities/copilot` → 绿（validateComposition 对 25 真名无重复）
- [ ] commit：

```
feat(capabilities): M5-T3 copilotTools 25 工具按归属五包声明 (YUK-321)

knowledge 6 / practice 7 / agency 5 / copilot 5 / ingestion 2（D11 渐废注记）；
allowlist 保持字面量（浏览器共享面），set-equality 单测强制两面一致。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
```

---

## Task 4 — ⏸ CopilotDock / CopilotHeroCard 迁 SPA 根壳（M5-T3d，UI）

> **⏸ 等待用户 pre-flight 批准**——以下 pre-flight 材料先呈给用户，批准后才执行本 Task 步骤。

### Pre-flight 材料

**Design-doc 逐字摘录**（`docs/superpowers/specs/2026-06-10-architecture-redesign-design.md` §3.6，L311-L313、L318）：

> ### 3.6 Copilot 与外壳
>
> | capability | 判定 | 理由 |
> |---|---|---|
> | 自由对话 / 持久化重放 / 知识 propose 工具 / 出题组卷工具 / hero 呈现 / interactive 沙盒 / ambient context / 快速芯片 | keep | D13/D14 框架下原样迁入 |
> | 主题/导航/移动端外壳 | keep | shell 包 |

**组件类型**：全局常驻 dock/drawer（非 route、非 modal）——挂载在 SPA 根壳组件，所有路由共享一个实例；CopilotHeroCard 是 dock 内部卡片组件。

**Touch 文件清单**：

- 移动（git mv，组件归 copilot 包——裁决 c）：`src/ui/copilot/{CopilotDock.tsx, CopilotHeroCard.tsx, hero.ts, replay.ts, skill-lifecycle.ts, hero.test.ts, replay.test.ts, skill-lifecycle.test.ts}` → `src/capabilities/copilot/ui/`（三个纯逻辑测试改名 `*.unit.test.ts` 走约定 glob；若它们现在 fastTestInclude 有显式条目，删条目）
- 修改：`CopilotDock.tsx`（仅一处 Next 耦合：L42 `usePathname()` → `pathname: string` prop；连带给 HeroCard 下传 `navigate` prop）
- 修改：`CopilotHeroCard.tsx`（`next/link` → `navigate: (to: string) => void` prop + `<button>`/`<a onClick>` 形态，沿用 M3/M4 迁移页的同款替换）
- 修改：`web/src/router.tsx`（rootRoute component 从裸 `Outlet` 改为 RootShell：渲染 `<Outlet />` + `<CopilotDock pathname={...} navigate={...} />`）
- 修改：旧树 importer（`grep -rn "ui/copilot" app src` 命中处改新路径；旧挂载点本地补 `usePathname()` 传 prop——这些文件 Task 9 整体删除，过渡补丁仅活两个 Task）

### 步骤（批准后执行）

- [ ] 执行 git mv + 测试改名；`grep -n "src/ui/copilot" vitest.shared.ts` 命中条目删除（glob 接管）
- [ ] `CopilotDock.tsx`：props 加 `pathname: string`、`navigate: (to: string) => void`；删 `usePathname` import，L42 改读 prop；ambient_context 的 route 字段语义不变
- [ ] `CopilotHeroCard.tsx`：删 `next/link`，跳转改 `navigate(to)`
- [ ] 旧树 importer 改路径 + 旧挂载点传 `pathname={usePathname()}`（临时垫片，Task 9 随 app/ 删）；`pnpm typecheck` → 绿（新旧两树同时绿）
- [ ] `web/src/router.tsx`：

```tsx
import CopilotDock from '@/capabilities/copilot/ui/CopilotDock';
import { useRouterState } from '@tanstack/react-router';

// M5-T3 (YUK-321) — 根壳：全路由共享一个 CopilotDock 实例（裁决 c：组件归
// copilot 包，路由耦合只存在于本壳层）。
function RootShell() {
  const router = useRouter();
  const pathname = useRouterState({ select: (s) => s.location.pathname });
  return (
    <>
      <Outlet />
      <CopilotDock pathname={pathname} navigate={(to) => router.history.push(to)} />
    </>
  );
}

const rootRoute = createRootRoute({ component: RootShell });
```

（CopilotDock 若是 named export 则 import 写法相应用花括号；以文件实际导出为准。）

- [ ] `pnpm vitest run --config vitest.unit.config.ts src/capabilities/copilot` → 绿
- [ ] 视觉环：起 `pnpm rw:api` + `pnpm rw:web`，playwright 打开 `:5173/today` 注入 token → 截图核对 dock 开合 / turns 重放 / chip 直触 / hero 卡片跳转（对照旧栈 :3000 同场景）；先判整体形态再核细节；测试数据用完即清
- [ ] commit：

```
feat(copilot): M5-T3 CopilotDock/HeroCard 迁 SPA 根壳 (YUK-321)

usePathname→pathname prop、next/link→navigate prop；根壳单实例挂载；
hero/replay/skill-lifecycle 纯逻辑随迁（unit glob 接管）。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
```

---

## Task 5 — observability 包 server 面（M5-T4a）

### Files

- Create: `src/capabilities/observability/manifest.ts`
- git mv: `src/server/admin/ai-observability.ts` → `src/capabilities/observability/server/ai-observability.ts`；`ai-observability.test.ts` → `server/ai-observability.db.test.ts`
- Create: `src/capabilities/observability/api/admin-runs.ts`、`api/admin-run-detail.ts`、`api/admin-cost.ts`、`api/admin-failures.ts`、`api/admin-subjects.ts`、`api/admin-subjects.unit.test.ts`、`api/cost-today.ts`
- Modify: `src/capabilities/index.ts`（+observability）
- Modify: `web/vite.config.ts`（+`/api/admin`、`/api/cost` → :8787）

**不删** `app/api/admin/*`、`app/api/cost/today`、`app/(admin)/*`——Task 9 统一删。

### 步骤

- [ ] git mv ai-observability 两文件；db 测试内 import 改相对路径；repo-wide `grep -rln "@/server/admin" app src` 命中 importer（admin 四 shim route + 其它）改 `@/capabilities/observability/server/ai-observability`；`pnpm typecheck` → 绿
- [ ] `pnpm vitest run --config vitest.db.config.ts src/capabilities/observability` → 绿（随迁测试完好）
- [ ] 四条 admin GET 平移：`app/api/admin/runs/route.ts`、`runs/[id]/route.ts`、`cost/route.ts`、`failures/route.ts` 逐文件整体复制到对应 `api/admin-*.ts`，统一三处机械变换——删 `export const runtime`；`[id]` 路由签名改 `(req: Request, params: Record<string, string>)` 并以 `params.id` 取值（`z.string().min(1)` 校验）；`@/server/admin/ai-observability` import 改包内相对 `../server/ai-observability`。query 解析（limit/status/task_kind/days）逐字保留
- [ ] 写 `api/admin-subjects.unit.test.ts`（registry 零 DB → unit）：

```ts
import { describe, expect, it } from 'vitest';

import { GET } from '@/capabilities/observability/api/admin-subjects';

describe('GET /api/admin/subjects', () => {
  it('返回 slim 投影（R11：不暴露 promptFragments / noteTemplate / causeCategories）', async () => {
    const res = await GET();
    expect(res.status).toBe(200);
    const body = (await res.json()) as { subjects: Record<string, unknown>[] };
    expect(body.subjects.length).toBeGreaterThan(0);
    for (const row of body.subjects) {
      expect(Object.keys(row).sort()).toEqual([
        'capabilityCount',
        'displayName',
        'id',
        'notation',
        'version',
      ]);
    }
  });
});
```

- [ ] 跑 → fail；实现 `api/admin-subjects.ts`（投影逻辑从 `src/ui/admin/subjects.tsx` 的 `toSlimRow` 原样上收——subjects 页 API 化是 admin 迁移唯一新增后端面）：

```ts
// M5-T4 (YUK-321) — GET /api/admin/subjects（新建：旧页是纯 RSC 直读 registry，
// SPA 化需要 API 面）。slim 投影沿 U7 的 R11 红线：id / displayName / version /
// notation / capabilityCount，绝不下发 promptFragments / noteTemplate /
// causeCategories 全血 profile。写操作未来也必须走 /api/admin/*（RL5）。

import { errorResponse } from '@/server/http/errors';
import { getDefaultSubjectRegistry } from '@/subjects/profile';

export async function GET(): Promise<Response> {
  try {
    const subjects = getDefaultSubjectRegistry()
      .listProfiles()
      .map((profile) => ({
        id: profile.id,
        displayName: profile.displayName,
        version: profile.version,
        notation: profile.renderConfig.notation,
        capabilityCount: profile.judgeCapabilities.length,
      }));
    return Response.json({ subjects });
  } catch (err) {
    return errorResponse(err);
  }
}
```

- [ ] 跑 → pass
- [ ] `api/cost-today.ts`：整文件平移 `app/api/cost/today/route.ts`（自包含 drizzle 聚合 + bjtMidnightUtc 助手随文件走），仅删 `export const runtime` 行
- [ ] 写 `src/capabilities/observability/manifest.ts`：

```ts
import { defineCapability } from '@/kernel/manifest';

// M5-T4 (YUK-321) — observability 包：AI 运行可观测性（admin 四页数据面）+
// 今日成本条。核心实现 server/ai-observability.ts（纯 drizzle，整体迁自
// src/server/admin）。ui.pages 在 T4b（admin 四页 SPA）落地时声明。
export const observabilityCapability = defineCapability({
  name: 'observability',
  description:
    'AI 可观测性：runs 列表/时间线、cost 汇总、failure 聚类、subject registry ' +
    '只读视图、今日成本条（cost_ledger + tool_call_log）。',
  api: {
    routes: [
      {
        method: 'GET',
        path: '/api/admin/runs',
        load: () => import('./api/admin-runs').then((m) => m.GET),
      },
      {
        method: 'GET',
        path: '/api/admin/runs/[id]',
        load: () => import('./api/admin-run-detail').then((m) => m.GET),
      },
      {
        method: 'GET',
        path: '/api/admin/cost',
        load: () => import('./api/admin-cost').then((m) => m.GET),
      },
      {
        method: 'GET',
        path: '/api/admin/failures',
        load: () => import('./api/admin-failures').then((m) => m.GET),
      },
      {
        method: 'GET',
        path: '/api/admin/subjects',
        load: () => import('./api/admin-subjects').then((m) => m.GET),
      },
      {
        method: 'GET',
        path: '/api/cost/today',
        load: () => import('./api/cost-today').then((m) => m.GET),
      },
    ],
  },
});
```

- [ ] `src/capabilities/index.ts` 登记 `observabilityCapability`；vite proxy 加 `'/api/admin': 'http://localhost:8787'`、`'/api/cost': 'http://localhost:8787'`
- [ ] 验证：`pnpm typecheck`；`pnpm rw:api` 起后 `curl -s :8787/api/admin/subjects -H "x-internal-token: $TOKEN"` 回 slim JSON、`curl -s :8787/api/cost/today -H ...` 回成本 JSON；无 token → 401（token gate 覆盖冒烟）
- [ ] commit：

```
feat(observability): M5-T4 observability 包——admin 数据面 + subjects API 化 + cost/today (YUK-321)

ai-observability.ts 整体迁包；四 GET 等价平移；GET /api/admin/subjects 新建
（旧纯 RSC 页的 R11 slim 投影上收 API）；cost/today 自包含聚合随迁。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
```

---

## Task 6 — ⏸ admin 四页 + Coach 周报 + CostRibbon 迁 SPA（M5-T4b，UI）

> **⏸ 等待用户 pre-flight 批准**——以下 pre-flight 材料先呈给用户，批准后才执行本 Task 步骤。

### Pre-flight 材料

**Design-doc 逐字摘录**（`docs/superpowers/specs/2026-06-10-architecture-redesign-design.md` §3.6，L317、L319；§3.5 范畴内 L316）：

> | Coach 周报 | keep | 归工作台/复盘面 |
> | today 仪表盘 | redesign | 重生为中央工作台，贡献制组装（D8） |
> | admin 四页 | keep | observability 包 |

另：TodayPage 成本面占位是 M4 已批准设计稿的一部分（`src/capabilities/shell/ui/TodayPage.tsx` L167 注释逐字：「成本面（设计稿 CostRibbon）：/api/cost/today 留旧栈，随 M5 Copilot 域收编后接通。」）——本 Task 只做「接通」，不引入新视觉。

**组件类型**：route pages × 5（`/admin/runs`、`/admin/cost`、`/admin/failures`、`/admin/subjects`、`/coach`）+ TodayPage 内既有占位块接线（非新组件）。

**Touch 文件清单**：

- 移动：`src/ui/admin/observability.tsx`、`src/ui/admin/subjects.tsx` → `src/capabilities/observability/ui/`；`app/(app)/coach/page.tsx` 的页面组件平移为 `src/capabilities/shell/ui/CoachPage.tsx`（裁决 e：spec「归工作台」→ shell 包）
- 修改：`observability.tsx`（`next/link` L9/L169 → `navigate` prop；确认/补 TanStack Query `refetchInterval: 60_000` 保 60 秒自动刷新语义）
- 修改：`subjects.tsx`（纯 RSC → client 数据面：`useQuery` + `apiJson('/api/admin/subjects')`；R11/RL5 注释块随迁，S3 段更新为「读已走 /api/admin/* token gate」）
- 修改：`subjects.tsx` L38 `import Link from 'next/link'`——搬入 `src/capabilities/observability/ui/` 时随 navigate-prop 改写一并剥离（用法替换为 navigate 回调；不剥则 Task 6 时点 vite 构建即破——bundle 图里出现 next 包）
- 修改：`CoachPage.tsx`（`useRouter` from next/navigation → `navigate` prop；其余 wiring 逐字保留）
- 修改：`src/capabilities/shell/ui/TodayPage.tsx`（L167 占位 quiet-empty → `useQuery('/api/cost/today')` 接线）
- 修改：`web/src/router.tsx`（+5 路由）、`src/capabilities/observability/manifest.ts`（ui.pages 四条）、`src/capabilities/shell/manifest.ts`（ui.pages +`/coach`）
- 修改：旧树 importer（`app/(admin)/admin/*/page.tsx` 三行薄壳改新 import 路径；coach 旧页改薄壳引 CoachPage 传 `navigate={(to) => router.push(to)}`——Task 9 整体删）

### 步骤（批准后执行）

- [ ] git mv 两个 admin UI 文件 → `src/capabilities/observability/ui/`；旧 `app/(admin)` 薄壳页 import 路径跟改；`pnpm typecheck` → 绿
- [ ] `observability.tsx`：三个 Surface（Runs/Cost/Failures）签名加 `navigate: (to: string) => void`，`next/link` 用法替换；逐个 useQuery 确认 `refetchInterval: 60_000`（缺则补，多余轮询不加）
- [ ] `subjects.tsx` 重写数据面（视觉结构 PageHeader/Card/Badge 全保留）：

```tsx
export function AdminSubjectsSurface({ navigate }: { navigate: (to: string) => void }) {
  const q = useQuery({
    queryKey: ['admin-subjects'],
    queryFn: () => apiJson<{ subjects: SlimSubjectRow[] }>('/api/admin/subjects'),
    refetchInterval: 60_000,
  });
  // 渲染部分沿用原 rows.map 结构，数据源从 registry 直读换成 q.data?.subjects ?? []，
  // loading/error 包 Stateful（兄弟 observability surface 同款）。
}
```

- [ ] 平移 CoachPage：`app/(app)/coach/page.tsx` 全文复制为 `src/capabilities/shell/ui/CoachPage.tsx`，删 `'use client'`，`useRouter()` + `router.push` 改 `navigate` prop（仅 L301 fail-row 跳转一处），导出 `export default function CoachPage({ navigate }: { navigate: (to: string) => void })`；CoachReport 内透传 navigate；旧 coach 页改三行薄壳（`'use client'` + useRouter + `<CoachPage navigate={...} />`）
- [ ] TodayPage CostRibbon 接线：占位 LoomCard 内改为 useQuery `/api/cost/today`，字段以 `src/capabilities/observability/api/cost-today.ts` 的 `Response.json` 实际形状为准（先打开该文件抄字段定义本地 interface，再渲染金额/任务分布；quiet-empty 语义保留——零成本时不噪）
- [ ] `web/src/router.tsx` +5 路由（navigate-prop 同款样板）：

```tsx
// M5-T4 (YUK-321) — observability 四页 + Coach 周报。
const coachRoute = createRoute({
  getParentRoute: () => rootRoute,
  path: '/coach',
  component: function CoachRoute() {
    const router = useRouter();
    return <CoachPage navigate={(to) => router.history.push(to)} />;
  },
});
```

（admin 四条同形：`/admin/runs`、`/admin/cost`、`/admin/failures`、`/admin/subjects`，component 各引对应 Surface 传 navigate；全部加进 routeTree。）

- [ ] manifest ui.pages：observability 四条 + shell `/coach`
- [ ] `pnpm typecheck` + `pnpm vitest run --config vitest.unit.config.ts src/capabilities/observability` → 绿
- [ ] 视觉环：playwright `:5173/admin/runs`（含 60s 刷新观察）、`/admin/subjects`、`/coach`、`/today`（CostRibbon 有数）截图对照旧栈同页；先整体形态后细节
- [ ] commit：

```
feat(observability,shell): M5-T4 admin 四页 + Coach 周报 + CostRibbon 迁 SPA (YUK-321)

observability ui 四页（subjects 改走新 API）+ Coach 周报归 shell（spec 工作台）；
TodayPage 成本面占位接通 /api/cost/today；60s 刷新经 refetchInterval 保留。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
```

---

## Task 7 — 残余活路由收编（M5-T5a）

### Files

- Create: `src/capabilities/practice/api/questions-list.ts`（GET，平移 `app/api/questions/route.ts`）、`api/question-detail.ts`（GET/PATCH/DELETE，平移 `app/api/questions/[id]/route.ts`）
- Create: `src/capabilities/ingestion/api/mistakes.ts`（POST+GET，平移 `app/api/mistakes/route.ts`）
- Create: `src/capabilities/notes/api/editing-heartbeat.ts`、`api/editing-blur.ts`（平移 `app/api/editing-session/{heartbeat,blur}/route.ts`）、`api/embedded-check-attempt.ts`（平移 `app/api/embedded-check/attempt/route.ts`——裁决 g：等价平移，D6 三刀不在 M5）
- Create: `src/capabilities/observability/api/backup-export.ts`、`api/backup-import.ts`（平移 `app/api/_/{export,import}/route.ts`——spec L320 备份恢复 keep 行，HTTP 路径 `/api/_/export`、`/api/_/import` 不变）
- Create: `src/capabilities/observability/api/event-correct.ts`（平移 `app/api/events/[id]/correct/route.ts`——统一事件流 keep 行的唯一撤回 HTTP 面，见 Task 9「/api/events 面处置」）
- Modify: `src/capabilities/practice/manifest.ts`（+4 routes）、`ingestion/manifest.ts`（+2）、`notes/manifest.ts`（+3）、`observability/manifest.ts`（+3：export/import/event-correct）
- Modify: `web/vite.config.ts`（+`/api/questions`、`/api/mistakes`、`/api/editing-session`、`/api/embedded-check`、`/api/_`、`/api/events` → :8787）
- 随迁：上列 app 路由旁若有 `*.test.ts` 伴随测试（`ls` 逐目录核对），git mv 到对应包 api/ 并改名 `*.db.test.ts` + 修 import；备份恢复另有三个守卫测试不在路由旁也必须随迁（否则随 app/ 静默死亡）：`app/api/_/_round_trip.test.ts`（round-trip 守卫）、`app/api/_/restore_column_allowlist.test.ts`（YUK-136 注入回归）、`app/api/_/import/route.test.ts`——三者均真 Postgres + memR2，git mv 至 `src/capabilities/observability/api/` 改名 `*.db.test.ts`（db 分区），`./export/route` / `./import/route` import 改 `./backup-export` / `./backup-import`，`tests/helpers/*` 相对路径修正

### 步骤

- [ ] questions 4 条平移：两文件整体复制，机械变换三件套（删 runtime / `[id]` 签名改 `(req, params)` + zod 校验 params.id / `@/server/questions/*` 等 staying import 不动）；practice manifest 追加：

```ts
      // M5-T5a (YUK-321) — 题库 CRUD（D16 出 M2 范围，留旧栈至 M5 收口——
      // vite.config M2 注释——现收编）。
      {
        method: 'GET',
        path: '/api/questions',
        load: () => import('./api/questions-list').then((m) => m.GET),
      },
      {
        method: 'GET',
        path: '/api/questions/[id]',
        load: () => import('./api/question-detail').then((m) => m.GET),
      },
      {
        method: 'PATCH',
        path: '/api/questions/[id]',
        load: () => import('./api/question-detail').then((m) => m.PATCH),
      },
      {
        method: 'DELETE',
        path: '/api/questions/[id]',
        load: () => import('./api/question-detail').then((m) => m.DELETE),
      },
```

- [ ] mistakes 平移：整文件复制；唯一非机械点是 L2 `import { after } from 'next/server'` + POST 内 `after(...)`，改写为：

```ts
// Next after() 等价改写（M5-T5a / YUK-321）：Hono 栈无 request-lifecycle 钩子。
// fire-and-forget + catch 落日志；语义差异（不等响应 flush 即启动）对单用户
// 工具无影响——回调不回写响应。原 after(() => fn(args)) 的 fn 与 args 逐字保留：
void runProposeAndWrite(/* 原 after 回调内实参原样 */).catch((err) =>
  console.error('[mistakes] propose-and-write failed', err),
);
```

（落地时打开源文件把 after 回调体原样搬进 void 表达式；GET 分支无改动。）ingestion manifest +POST/GET `/api/mistakes` 两条 decl（load 指 `./api/mistakes` 的 POST/GET）

- [ ] editing-session + embedded-check 平移：三文件复制 + 三件套变换；heartbeat 的 `enqueueDwellNoteRefine`（`@/capabilities/notes`）、blur 的 `markArtifactIdleAndFlush`、attempt 的 judge/writeEvent/createLearningRecord/boss enqueue 全部 staying import 不动；notes manifest +3 decl（`POST /api/editing-session/heartbeat`、`POST /api/editing-session/blur`、`POST /api/embedded-check/attempt`）
- [ ] 备份恢复收编（spec §3.6 L320「备份恢复 … keep | 内核与基础不变量」——**保留，不设退役选项**；不收编则 Task 9 闭包 loop 会把 `src/server/export` 判孤儿连根删掉）：`app/api/_/export/route.ts` → `src/capabilities/observability/api/backup-export.ts`、`app/api/_/import/route.ts` → `api/backup-import.ts`，整文件复制仅删 `export const runtime` 行（24/23 行薄包装；`@/server/export/archive`、`@/db/client`、`@/server/r2` 全部 staying import 不动）。归 **observability** 包——理由：该包已是内核横切运维读面（runs/cost/failures/审计）的家，备份恢复同属内核运维面；两条薄路由不为此新开 kernel-admin 包（裁决 k「不发明新抽象」同则）
- [ ] event correct 收编（统一事件流 keep 行的唯一撤回 HTTP 面，处置全貌见 Task 9「/api/events 面处置」）：`app/api/events/[id]/correct/route.ts` → `src/capabilities/observability/api/event-correct.ts`，77 行薄包装（zod CorrectBody + `getEventById` + `writeEvent`，staying import 不动），`[id]` 签名三件套变换同 questions。归 observability（事件流撤回面与审计读面同域）。observability manifest 追加三条：

```ts
      // M5-T5a (YUK-321) — 内核运维面收编：备份恢复（spec keep 行，路径沿旧
      // /api/_/{export,import}）+ 统一事件流撤回面（correction 内核不变量，
      // 原 app/api/events/[id]/correct）。裸查/rate 面退役见 Task 9。
      {
        method: 'GET',
        path: '/api/_/export',
        load: () => import('./api/backup-export').then((m) => m.GET),
      },
      {
        method: 'POST',
        path: '/api/_/import',
        load: () => import('./api/backup-import').then((m) => m.POST),
      },
      {
        method: 'POST',
        path: '/api/events/[id]/correct',
        load: () => import('./api/event-correct').then((m) => m.POST),
      },
```

- [ ] 伴随测试随迁改名（含备份恢复三守卫 + `app/api/events/[id]/correct/route.test.ts`，见 Files 节）；`pnpm vitest run --config vitest.db.config.ts src/capabilities/practice src/capabilities/ingestion src/capabilities/notes src/capabilities/observability` → 绿
- [ ] vite proxy +6 前缀；`pnpm rw:api` 起后冒烟：`curl :8787/api/questions -H "x-internal-token: $TOKEN"` 200、`curl -X POST :8787/api/editing-session/heartbeat ...` 按旧契约回包、`curl -sI :8787/api/_/export -H "x-internal-token: $TOKEN"` 回 `content-type: application/zip`
- [ ] `pnpm typecheck` + touched Biome → 绿
- [ ] commit：

```
feat(practice,ingestion,notes,observability): M5-T5a 残余活路由收编 (YUK-321)

questions CRUD 4 条归 practice；mistakes POST+GET 归 ingestion（after→
fire-and-forget 等价改写）；editing-session heartbeat/blur + embedded-check
attempt 归 notes（D6 三刀是 feature 级，不属拆除——plan 裁决 g）；备份恢复
/api/_/{export,import}（spec keep 行）+ 事件撤回面 events/[id]/correct 归
observability，round-trip / 注入回归三守卫随迁 db 分区。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
```

---

## Task 8 — prod 形态定稿（M5-T5b，纯增量，不动旧栈）

### Files

- Modify: `package.json`（+`build:server`、+`rw:build`；旧 dev/build/start 本 Task 不动）
- Modify: `server/index.ts`（serveStatic 静态面，env 开关）

### 步骤

- [ ] `package.json` scripts 追加。esbuild 配方沿用 build:worker，**但不止 entry/outfile 不同——须追加 `--external:@anthropic-ai/claude-agent-sdk`**：现行 build:worker 配方（package.json L11）没有这个 external，而 server bundle 经 copilot chat.ts 拉进 Agent SDK，SDK 必须外置（prod 镜像由 sdkdeps overlay 提供，见 Task 9 Dockerfile）。完整命令行如下，照抄勿漏 external：

```json
"build:server": "esbuild server/index.ts --bundle --platform=node --target=node22 --format=cjs --outfile=dist/server.cjs --external:pg-native --external:sharp --external:bufferutil --external:utf-8-validate --external:@anthropic-ai/claude-agent-sdk --define:process.env.NODE_ENV=\\\"production\\\"",
"rw:build": "pnpm rw:web:build && pnpm build:server"
```

- [ ] `server/index.ts` 静态面（buildHonoApp 调用之后、serve 之前）：

```ts
// M5-T5b (YUK-321) — prod 静态面：RW_STATIC_DIR 指向 vite build 产物（web/dist）。
// dev 不设此变量（Vite dev server 承担静态 + /api proxy）。serveStatic 未命中
// 文件时 next() 放行 /api/*；catch-all GET 回 index.html（TanStack Router
// 客户端路由 fallback），注册在 manifest 路由之后所以不抢任何 API 端点。
if (process.env.RW_STATIC_DIR) {
  const root = process.env.RW_STATIC_DIR;
  app.use('*', serveStatic({ root }));
  app.get('*', serveStatic({ root, path: 'index.html' }));
}
```

（顶部 import：`import { serveStatic } from '@hono/node-server/serve-static';`）

- [ ] 验证：`pnpm rw:build` → web/dist + dist/server.cjs 产出；`RW_STATIC_DIR=web/dist DATABASE_URL=<本地 :5433> INTERNAL_TOKEN=dev node dist/server.cjs` 起服 → `curl :8787/` 回 index.html、`curl :8787/today` 回 index.html（SPA fallback）、`curl :8787/api/health` 200、`curl :8787/api/proposals` 无 token 401
- [ ] `pnpm typecheck` → 绿；确认旧 `pnpm build`（next build）未受影响（本 Task 零旧栈触碰，跳过实跑，git diff 自查即可）
- [ ] commit：

```
feat(server): M5-T5b prod 形态——server bundle + serveStatic 静态面 (YUK-321)

esbuild 配方沿 build:worker（entry=server/index.ts → dist/server.cjs，SDK 外置）；
RW_STATIC_DIR 开关挂 web/dist + SPA fallback。纯增量，旧栈不动。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
```

---

## ✅ 用户决断 Gate — prod 进程拓扑与 presence 承接（Task 8 之后、Task 9 之前）

> ✅ **已裁决（2026-06-12）：选项 b——保持双进程 + presence 进 PG 表。** 本节三选项对照表与差异表**保留作决策记录，不删**；Task 9 正文已按选项 b 收敛改写。执行约束见本节末「选项 b 执行步骤」——PgPresenceStore 子计划现场写、经用户过目后才动 schema，该约束不因裁决落地而豁免。
>
> （原 gate 性质留档：这是部署拓扑级决策——影响 NAS compose 结构、presence 跨进程契约、pg-boss worker 进程存废——超出 plan 自决权限，故设三选项交用户裁决。）

**背景（跨进程契约实证）**：editing presence 当前是「web 进程写、worker 进程读」的跨进程契约——web 侧 `app/api/editing-session/heartbeat/route.ts` L27 `recordEditingHeartbeat(...)` 写 presence；worker 侧 `src/capabilities/notes/jobs/note-refine.ts` L229 `enqueueOrApplyNoteRefinePatch(...)` 读 presence 决定 apply-vs-defer。该契约现由 Redis 承载（`src/server/artifacts/editing-session.ts` L29-31 按 `REDIS_URL` 有无选 RedisPresenceStore / InMemoryPresenceStore）。Redis 退役后必须三选一：

| 选项 | 内容 | 后果 |
|---|---|---|
| **a** | **收敛单进程**：app 容器 `RW_WORKER=1`，Hono serve 进程内嵌 pg-boss worker loop；compose 删 worker 服务、删 `scripts/worker.ts` | presence 读写同进程，in-memory 即正确；compose 少一个 service，部署最简（dev `pnpm rw:api` 已验证该拓扑）；**风险 = 重 CPU job（文档转换 / OCR）阻塞请求面**——单用户工具影响有限但真实存在 |
| **b** | **保持双进程 + presence 改 PG 表**：新增 presence 表（artifact_id / status / heartbeat_at + 待 flush patch 队列）+ PgPresenceStore 第三实现，心跳写 PG、worker 读 PG | presence 语义最干净——真相源进库，跨进程天然成立；worker 与请求面隔离保留；**代价 = 一张新表 + migration + 心跳写路径 + `pnpm audit:schema` 登记**（PgPresenceStore 子计划在 gate 后由实施者现场写，经用户过目再动 schema） |
| **c** | **保持双进程 + 接受 in-memory 降级**：web 与 worker 各持一份进程内 presence，互不可见 | 零新增代码、worker 隔离保留；**代价 = note-refine 编辑期防护失效**——worker 读不到 web 进程的 editing 心跳，会在用户编辑中立即 apply patch 而非 defer（触发条件 = dwell-refine job 恰在编辑会话期间执行；单用户下概率极低，但语义上是回归，须在 editing-session.ts 文件头注释逐字标注） |

**Task 9 差异表**（决策记录——Task 9 正文已按选项 b 列收敛改写，无需再套用；保留 a/c 列仅为留痕）：

| Task 9 涉及处 | 选项 a（基线，正文原样） | 选项 b | 选项 c |
|---|---|---|---|
| `scripts/worker.ts` | 删除 | 保留：剥离 Next instrumentation 假设，改为裸 `startWorker()` 入口（entry 随 esbuild 配方打 `dist/worker.cjs`） | 同 b |
| compose worker 服务 | 删除 | 保留（command 改 `node dist/worker.cjs`） | 同 b |
| app 容器 `RW_WORKER` | `=1` | 不设（worker 容器独占 job 消费） | 不设 |
| `editing-session.ts` 改写 | 恒用 `InMemoryPresenceStore`（基线代码块） | 恒用新 `PgPresenceStore`（表 schema + migration + audit:schema 登记为 Task 9 前置子任务） | 同 a 代码块 + 文件头注释标注降级语义与受影响路径（note-refine defer 失效） |
| package.json `build:worker` / `worker:dev` | 删除 | 保留（entry/outfile 跟改） | 同 b |
| Dockerfile / commit message 中「单进程」措辞 | 原样 | 改「双进程（presence 走 PG 表）」 | 改「双进程（presence in-memory 降级，见 editing-session.ts 注释）」 |

**三个选项下均不变的部分**：Redis 本体退役照常执行——删 `src/server/redis/`、presence/redis 三件、ioredis / @testcontainers/redis 依赖、compose redis 服务与 `REDIS_URL` 注入，三选项均含，不受决断影响。

**选项 b 执行步骤（已裁决，Task 9 前置子任务）**：

- [ ] PgPresenceStore 子计划**在 gate 时点现场写**（禁预写）：presence 表 schema（artifact_id / status / heartbeat_at + 待 flush patch 队列）、migration（`pnpm db:generate`）、心跳写路径（heartbeat/blur 路由 → PG）、`pnpm audit:schema` 处置（写路径落实即自然通过；确属延后字段按 allowlist 协议登记 kind/ref/expected_by）
- [ ] 子计划**写好后呈用户过目，过目通过前不动 schema**；通过后实施（表 + migration + PgPresenceStore 第三实现 + 心跳写路径），`pnpm test:migration` + 定向 db 测试绿
- [ ] 以上完成后才进入 Task 9 正文（editing-session.ts 在 Task 9 内切换为恒用 PgPresenceStore）

---

## Task 9 — 拆除采石场（M5-T5c，**严格单 commit，可整体 revert**）

> **前置：gate 已裁决选项 b（2026-06-12，双进程 + presence 进 PG 表），本 Task 正文已按选项 b 收敛书写（差异表保留在 gate 节作决策记录）。开始本 Task 前，gate 节「选项 b 执行步骤」（PgPresenceStore 子计划现场写 + 用户过目 + 实施）必须已完成。**

### Files

**Delete**（全部进同一 commit）：

- `app/`（整目录；`app/globals.css` 先 git mv 至 `web/src/globals.css`）
- `next.config.ts`、`next-env.d.ts`、`instrumentation.ts`、`middleware.ts`、`middleware.test.ts`
- `src/server/copilot/`、`src/server/teaching/`、`src/server/admin/`（T3/T4 已搬空，删残壳）
- `src/server/redis/`、`src/server/artifacts/presence/redis.ts`、`presence/redis.test.ts`、`presence/redis.integration.test.ts`
- `vitest.config.ts`（legacy）
- 现场闭包 grep 判定为零活引用的其余 src/server 子目录（见步骤 1 的判定规则——**以现场输出为准，不按地图清单删**，裁决 a）
- 现场闭包判定为零活引用的 src/ui 老树孤儿组件（见步骤 1b——已知起点样本：`src/ui/components/ArtifactSections.tsx`、`src/ui/components/TeachingDrawer.tsx`、`src/ui/components/NoteRenderer/` 链、`src/ui/block-tree/` 组件件；以现场不动点输出为准）

**Modify**：

- `scripts/worker.ts`（**保留**，gate 选项 b：双进程拓扑 worker 独立进程；头注释剥离 Next/instrumentation.ts 表述改对照 Hono `server/index.ts`，入口逻辑（startBossWorker + shutdown/last-resort handlers）不动）
- `web/src/main.tsx`（globals.css import 改 `./globals.css`）
- `web/vite.config.ts`（proxy 表收敛为单条 `'/api': 'http://localhost:8787'`）
- `src/server/artifacts/editing-session.ts`（删 REDIS_URL 分支 + getRedis/RedisPresenceStore import；承接 = 恒用 `PgPresenceStore`——gate 选项 b 前置子任务已实施的第三实现）
- `src/server/boss/shutdown.ts`（删 closeRedis 调用与 import）
- `package.json`（删依赖 next/ioredis/@testcontainers/redis/@tailwindcss/postcss；scripts：dev→`tsx scripts/dev-local.ts` 语义保留但内部三 spawn、build→`pnpm rw:web:build && pnpm build:server && pnpm build:worker && pnpm build:migrate`、start→`node dist/server.cjs`、删 test:legacy、build:migrate outfile 改 `dist/migrate.cjs`、build:worker outfile 改 `dist/worker.cjs`（worker:dev 保留不动））
- `scripts/dev-local.ts`（spawn next dev → 三 spawn rw:api + rw:web + worker:dev——双进程拓扑下 LISTEN loop 不消费 boss job，dev 也必须有独立 worker 进程）
- `vitest.shared.ts`（allTestInclude 删 app/** 与 workers/** glob L44-47；fastTestInclude 删 middleware.test.ts L70 与 app/ 死条目 L246-266 区）
- `Dockerfile`（重写 Hono+Vite，双进程：app 容器 `dist/server.cjs`、worker 容器同镜像跑 `dist/worker.cjs`）
- `docker-compose.yml`（删 redis 服务 L114-127、REDIS_URL L31-32/L84-85、depends_on.redis L36-37/L89-90；worker 服务**保留**（command 改 `["node","dist/worker.cjs"]`，liveness probe 的 cmdline 匹配串 `worker.cjs` 对新路径仍命中、无需改）；app 不设 RW_WORKER；app：端口/healthcheck :8787、RW_STATIC_DIR、command）
- `scripts/gen-postman.ts`（+manifest 对账层，裁决 b）+ `postman/api-endpoints.json`（按本 Task 附录「死亡路由清单」剪死条目）+ 重生成 collection
- `postcss.config.*` 若存在且仅为 Next/Tailwind-postcss 服务 → 删（vite 走 @tailwindcss/vite）

### /api/events 面处置（spec keep 行核对）

spec 的统一事件流 keep 行由「内核事件存储 + 各包事件读写面」满足，HTTP 裸面不整族保留，逐条处置如下：

- **存续（数据面）**：`src/server/events/`（writeEvent/getEventById 及查询闭包）有活引用必留（见步骤 1 预判），统一事件流存储不动。
- **存续（各包面）**：ingestion `/api/ingestion/[id]/events`、notes artifacts 的 ai-changes/undo、agency 的 `/api/proposals/[id]/decide|retract`——事件读写已按包归位。
- **退役**：`GET /api/events`（74 行裸查）与 `GET /api/events/[id]`（48 行）——SPA 已换源统一收件箱（M4-T5/YUK-318；`src/capabilities/knowledge/ui/knowledge-api.ts` L3/L123 注释为证），唯一残余调用方 `app/(app)/events/[id]/page.tsx` 随 app/ 同死。
- **退役**：`POST /api/events/[id]/rate`（97 行）——旧 v2.1 proposal inbox 通道，已被统一收件箱取代；评议留痕走 `/api/proposals/[id]/decide|retract`，trace 路径不缺失。
- **收编**：`POST /api/events/[id]/correct`（77 行薄包装，correction 内核不变量的唯一 HTTP 入口）——已在 Task 7 收编进 observability manifest，本 Task 不再处置。

### 步骤

- [ ] **删除前置检查 1 — importer 闭包**（决定 src/server 删除集；只删输出 `(no live importer)` 的目录）：

```bash
for m in copilot teaching admin redis records quiz learning-items events proposals questions session orchestrator judge export http fsrs; do
  echo "== @/server/$m =="
  grep -rln "@/server/$m" src/capabilities src/kernel src/ai src/core src/subjects server web scripts 2>/dev/null \
    | grep -v '\.test\.' || echo '(no live importer)'
done
```

写作期预判：copilot/teaching/admin/redis 四个为空（前者三个 T3/T4 已搬空）；http/events/proposals/session/questions/quiz/judge/orchestrator/records/fsrs/export **有活引用必留**（export 在 Task 7 备份恢复收编后由 observability 的 backup-export/backup-import 引用 `@/server/export/archive`；staying 同 boss/ai/memory/artifacts/today/r2/runtime-env）。现场输出与预判不符时以现场为准并在 commit body 记录差异

- [ ] **删除前置检查 1b — src/ui 老树孤儿闭包**（决定 src/ui 删除集；形式同检查 1，扫描面加 src/ui 自身以捕捉孤儿引用孤儿的链）：

```bash
for c in components/ArtifactSections components/TeachingDrawer components/NoteRenderer block-tree; do
  echo "== @/ui/$c =="
  grep -rln "@/ui/$c" src/capabilities src/kernel src/ai src/core src/subjects src/server server web scripts src/ui 2>/dev/null \
    | grep -v '\.test\.' || echo '(no live importer)'
done
```

判定规则：命中文件若仅是 app/ 老页面（随 app/ 同死）或本轮同被判定为孤儿的 src/ui 文件，则视为无活引用；每删一层把新孤儿的引用面重扫一遍，**迭代至不动点**（孤儿只被孤儿引用即整链删）。src/ui 内部相对 import（`./ArtifactSections` 形）用裸文件名二次 grep 补扫；注释性命中（非 import 行）人工剔除。写作期已核起点样本：`ArtifactSections.tsx`、`TeachingDrawer.tsx`、`NoteRenderer/` 链、`block-tree/` 组件件的 import 链只从旧 learning-items 页可达——**以现场不动点输出为准，不按样本清单删**。判定为零活引用的组件与 app/ 同一 commit 删除

- [ ] **删除前置检查 2 — Next 符号闭包**：`grep -rn "next/\|from 'next'" src server web scripts --include="*.ts" --include="*.tsx" | grep -v node_modules` → 必须零命中（app/ 删除后无残留 Next import）；`grep -rn "ioredis\|getRedis\|REDIS_URL" src server scripts docker-compose.yml --include="*.ts" -l` → 仅 editing-session/shutdown 待改两处
- [ ] vite proxy 收敛单条 catch-all :8787；起双进程 `pnpm rw:api`+`pnpm rw:web` 全站冒烟（/today /record /practice /knowledge /notes/:id /inbox /agent-notes /coach /admin/runs + dock 对话）——**拆除前最后一次旧栈在场的回归点**
- [ ] git mv `app/globals.css` → `web/src/globals.css`；`web/src/main.tsx` import 改 `'./globals.css'`；`pnpm rw:web` 热载确认样式完好
- [ ] `git rm -r app/` + 删五个 Next 配置/中间件文件 + 闭包判定的 src/server 目录 + 步骤 1b 不动点判定的 src/ui 老树孤儿组件 + redis 三件 + vitest.config.ts（`scripts/worker.ts` **不删**——gate 选项 b 双进程入口，仅按 Files 节改头注释）
- [ ] `tests/integration/mistakes-routes-back-compat.test.ts` 处置（t7-reviewer LOW-1 补录，2026-06-13）：该文件 L11-12 import `app/api/mistakes/route`（GET_LIST）与 `app/api/mistakes/recent/route`——app/ 删除后 typecheck 必红。list 半边 repoint 到 `src/capabilities/ingestion/api/mistakes.ts` 的 GET；recent 半边无新家（上表「SPA 零命中」退役行），相关断言连同 import 一并删；若剥离后该文件只剩与 `ingestion/api/mistakes.db.test.ts` 重复的覆盖，整文件退役亦可（执行者现场裁量，二选一须在 commit 正文注明）
- [ ] `editing-session.ts` 改写（gate 选项 b——恒用 `PgPresenceStore`，构造细节以 gate 前置子任务实施的子计划为准）：

```ts
// M5-T5c (YUK-321) — Redis 退役（gate 选项 b）：prod 拓扑保持双进程
// （app + worker），跨进程 presence 改走 PG 表——恒用 PgPresenceStore
// （gate 前置子任务实施的第三实现，ADR-0023 的 PresenceStore seam 保留）。
// RedisPresenceStore 已随 ioredis 一并移除。
let store: PresenceStore | undefined;

function getPresenceStore(): PresenceStore {
  store ??= new PgPresenceStore();
  return store;
}
```

（删 `getRedis`/`RedisPresenceStore` import；文件其余转发函数不动。）`shutdown.ts` 删 closeRedis 段

- [ ] `package.json`：`pnpm remove next ioredis @testcontainers/redis @tailwindcss/postcss`；scripts 按上列改写；`scripts/dev-local.ts` 保留 compose Postgres :5433 启动/等待/DATABASE_URL 注入段，spawn 段改三 spawn（gate 选项 b：LISTEN loop 不消费 boss job，dev 也必须有独立 worker 进程；worker:dev 经此处 env 注入 DATABASE_URL，免手动 source .env）：

```ts
const api = spawn('pnpm', ['rw:api'], { stdio: 'inherit', env });
const web = spawn('pnpm', ['rw:web'], { stdio: 'inherit', env });
const worker = spawn('pnpm', ['worker:dev'], { stdio: 'inherit', env });
const children = [api, web, worker];
const stop = (code: number | null) => {
  for (const c of children) c.kill('SIGINT');
  process.exit(code ?? 0);
};
process.on('SIGINT', () => stop(0));
process.on('SIGTERM', () => stop(0));
for (const c of children) c.on('exit', stop);
```

- [ ] `vitest.shared.ts` 重写：删 allTestInclude 的 app/workers glob、fast 的 middleware 条目与 app/ 死条目区；`pnpm audit:partition` → 绿
- [ ] `Dockerfile` 重写（骨架如下；**sharpdeps / sdkdeps 两个 stage 与 runner 的三组 COPY（三科 skills / sharp 4 行 / SDK 2 行）从 `git show HEAD:Dockerfile` 整段搬运**——T9 执行到此步时工作树 Dockerfile 已被本骨架覆盖，原文只能从 git 取；搬运时**删去所有 `--chown=nextjs:nodejs`**（新 runner 不创建 nextjs 用户），含注释整段搬，骨架内已内嵌指令行作核对基准，搬完逐行 diff 核对）：

```dockerfile
# syntax=docker/dockerfile:1
# M5-T5c (YUK-321) — Hono + Vite 形态，双进程（gate 选项 b 已裁决）：
# app 容器跑 dist/server.cjs，worker 容器同镜像跑 dist/worker.cjs（compose 层
# command 覆盖），presence 走 PG 表（PgPresenceStore），不设 RW_WORKER。
FROM node:24-slim AS base
ENV PNPM_HOME=/pnpm PATH=$PNPM_HOME:$PATH
RUN corepack enable
WORKDIR /app

FROM base AS deps
COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile

FROM base AS builder
COPY --from=deps /app/node_modules ./node_modules
COPY . .
RUN pnpm rw:web:build && pnpm build:server && pnpm build:worker && pnpm build:migrate

# ↓ sharpdeps / sdkdeps 两个 stage 从 git show HEAD:Dockerfile 整段搬运（含注释；
#   指令行如下，作搬运后 diff 核对基准）。flat npm 布局原因见原注释。
FROM node:24-bookworm-slim AS sharpdeps
WORKDIR /sharp
RUN npm install --omit=dev --no-audit --no-fund sharp@^0.34.5

FROM node:24-bookworm-slim AS sdkdeps
WORKDIR /sdk
RUN npm install --omit=dev --no-audit --no-fund \
    @anthropic-ai/claude-agent-sdk@0.3.143 \
    @anthropic-ai/sdk@0.96.0 \
    @modelcontextprotocol/sdk@1.29.0

FROM base AS runner
ENV NODE_ENV=production
# 文档转换链 + sharp 运行库 —— apt 两层逐字沿旧 runner。
RUN apt-get update && apt-get install -y --no-install-recommends \
      libvips42 pandoc libreoffice-core libreoffice-writer fonts-noto-cjk \
    && rm -rf /var/lib/apt/lists/*
COPY --from=builder /app/dist ./dist
COPY --from=builder /app/web/dist ./web/dist
COPY --from=builder /app/drizzle ./drizzle
# ↓ 三组 COPY 从 git show HEAD:Dockerfile 整段搬运（含注释），删 --chown=nextjs:nodejs：
# 三科 skills（runner populateIsolatedSkills 按 src/subjects/<id>/skills/ 原路径读取）
COPY --from=builder /app/src/subjects/math/skills ./src/subjects/math/skills
COPY --from=builder /app/src/subjects/wenyan/skills ./src/subjects/wenyan/skills
COPY --from=builder /app/src/subjects/physics/skills ./src/subjects/physics/skills
# sharp + 原生依赖 4 行（来自 sharpdeps 的 flat node_modules）
COPY --from=sharpdeps /sharp/node_modules/sharp ./node_modules/sharp
COPY --from=sharpdeps /sharp/node_modules/@img ./node_modules/@img
COPY --from=sharpdeps /sharp/node_modules/detect-libc ./node_modules/detect-libc
COPY --from=sharpdeps /sharp/node_modules/semver ./node_modules/semver
# Claude Agent SDK + peers 2 行（namespace 整目录 overlay，来自 sdkdeps）
COPY --from=sdkdeps /sdk/node_modules/@anthropic-ai ./node_modules/@anthropic-ai
COPY --from=sdkdeps /sdk/node_modules/@modelcontextprotocol ./node_modules/@modelcontextprotocol
ENV API_PORT=8787 RW_STATIC_DIR=/app/web/dist
# gate 选项 b：不设 RW_WORKER（worker 独立进程，compose worker 服务 command 覆盖跑 dist/worker.cjs）。
EXPOSE 8787
CMD ["node", "dist/server.cjs"]
```

- [ ] `docker-compose.yml`：按 Files 节列出的行号逐处删/改；migrate 服务 command 改 `node dist/migrate.cjs`；worker 服务 command 改 `["node", "dist/worker.cjs"]`（liveness probe 匹配串 `worker.cjs` 仍命中，不动）；app healthcheck 改 `http://localhost:8787/api/health`
- [ ] `gen-postman.ts` 加对账层（真相源仍是 api-endpoints.json，裁决 b）：

```ts
// M5-T5c (YUK-321) — manifest 对账层：spec 条目必须存在于组合根路由清单，
// 死条目（旧 app/ 独有路由）进不了 collection；manifest 有而 spec 缺的路由
// 打印 WARN 鼓励补 spec（不 fail——内部调试端点允许不进 collection）。
import { capabilities } from '@/capabilities';

// 路径风格归一化：manifest 路由参数写 `[id]`（如 /api/ingestion/[id]/blocks），
// api-endpoints.json 写 `:id`（如 /api/_/logs/jobs/:id）——不归一化 Set 永不命中。
const normalize = (p: string) => p.replace(/\[([^\]]+)\]/g, ':$1');

const manifestRoutes = new Set(
  capabilities.flatMap((c) =>
    (c.api?.routes ?? []).map((r) => `${r.method} ${normalize(r.path)}`),
  ),
);
manifestRoutes.add('GET /api/health'); // 组合根直挂（server/app.ts，token 豁免），不在任何 manifest——缺此行对账层会误杀健康探针条目
for (const ep of endpoints) {
  for (const m of ep.methods) {
    const key = `${m.method} ${ep.path}`;
    if (!manifestRoutes.has(key)) {
      throw new Error(
        `gen-postman: '${key}' 不在任何 capability manifest 中（死条目，请从 api-endpoints.json 删除）`,
      );
    }
  }
}
```

（`endpoints` 即 gen-postman.ts 既有 `const endpoints: EndpointSpec[] = JSON.parse(readFileSync(SOURCE, 'utf8'))`——`EndpointSpec { path: string; methods: MethodSpec[] }` 双层嵌套，`MethodSpec.method` 为 HTTP 动词字符串；对账层插在该解析行之后、collection 组装之前。throw 清单驱动 api-endpoints.json 剪枝。）随后 `pnpm gen:postman` 重生成，迭代至零 throw

- [ ] **全 gate**（新形状首跑）：`pnpm typecheck`、`pnpm lint`、`pnpm audit:schema`（若红，按 allowlist 协议登记——`scripts/audit-schema-allowlist.json`，`reason` + `resolves_when` 的 `kind`/`ref`/`expected_by` 齐备——或现场补写路径）、`pnpm audit:partition`、`pnpm audit:profile`、`pnpm test`、`pnpm build`（= vite + server/worker/migrate 三 esbuild bundle）→ 全绿；gate 输出尾部留存进 commit 工作记录
- [ ] `pnpm dev:local` 三 spawn（api + web + worker）全站手动冒烟一轮（同上路由清单）
- [ ] **单 commit**（git status 逐文件核对脏文件红线后一次 add）：

```
refactor(teardown): M5-T5c 拆除采石场——仓库只剩新形状 (YUK-321)

删 app/(115 文件)、Next 配置五件、src/server 零引用残余（copilot/teaching/
admin 残壳 + redis）、src/ui 老树孤儿组件（步骤 1b 闭包不动点判定）、vitest
legacy 配置与 app allowlist 条目；依赖去 next/ioredis/@testcontainers/redis/
@tailwindcss/postcss；Dockerfile/compose 重写为 Hono+Vite 双进程（app :8787 +
worker dist/worker.cjs，Redis 服务退役，presence 走 PgPresenceStore——gate
选项 b）；gen-postman 加 manifest 对账层并重生成。
本 commit 可整体 revert 恢复双栈。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
```

### 附录 — 死亡路由清单（Task 9 删除依据）

下列 `app/api` 路由族未被任何 Task 收编，随 `app/` 整目录死亡。判据：SPA fetch 面（`web/src` + `src/capabilities`）grep 零命中——写作期已逐族核过，**执行步骤 1/1b 时点须复核一遍**（新调用方可能在 M5 期间出现）。本清单与 `gen-postman.ts` manifest 对账层互为防漏：`postman/api-endpoints.json` 按本清单剪死条目后 `pnpm gen:postman` 零 throw 即闭环，Task 10 文档对账再兜一层。

| 路由族 | 无活调用方依据 |
|---|---|
| `/api/_/backfill/knowledge_propose` | 一次性回填脚本面，SPA 零命中 |
| `/api/_/logs/{cost,jobs,jobs/[id],tool_calls}` | admin 四页 Task 6 已改走 observability 包新面 |
| `/api/_/seed`、`/api/_/seed/math` | dev 播种面，SPA 零命中 |
| `/api/_/tools/[name]` | 旧工具调试面，SPA 零命中 |
| `/api/ai/[task]` | 旧 AI 分发面，SPA 已全量走包内 AI 路由 |
| `/api/coach/today-plan` | Coach 周报走 `/api/review/weekly`（裁决 e），CoachPage 无调用 |
| `/api/echo`、`/api/echo/[id]/events` | 旧调试面，SPA 零命中 |
| `/api/events`、`/api/events/[id]`、`/api/events/[id]/rate` | 见「/api/events 面处置」专段（correct 已收编 Task 7） |
| `/api/learning-intents`（+ `/accept`） | 旧学习意图面，仅老 learning 页调用 |
| `/api/learning-items`（+ `/[id]`） | 仅旧 learning-items 页调用，该页随 app/ 死 |
| `/api/learning-sessions`（+ `/[id]`） | 同上，老树专属 |
| `/api/mistakes/recent` | SPA 零命中 |
| `/api/question-blocks/[id]/figures/[asset_id]` PATCH | 旧图块修订面，SPA 零命中 |
| `/api/questions/quiz-gen`、`/api/questions/[id]/timeline` | quiz-gen 走包内新面；timeline 仅老页调用 |
| `/api/records`（+ `/[id]`） | ls 对照补全；HTTP 面死，`src/server/records` 模块经活包引用存续 |
| `/api/study-log`（+ `/[id]`） | ls 对照补全；SPA 零命中 |
| `/api/teaching-sessions` 四条非 accept-chip 路由 | ls 对照补全；调用方 TeachingDrawer 系老树孤儿（见步骤 1b），accept-chip 已收编 Task 2 |

---

## Task 10 — 文档收编 + 终局 drift 审计（M5-T6）

### Files

- Modify: `README.md`（stack/本地/NAS setup：Hono(:8787)+Vite(:5173)、`pnpm dev:local` 三 spawn（api+web+worker）、双进程拓扑（app + worker 独立进程，presence 走 PG 表——gate 选项 b）、无 Redis、`pnpm build` 四产物（web + server + worker + migrate）、Docker 形态）
- Modify: `CLAUDE.md`（Stack note 段重写：Next.js 15 App Router → Hono + Vite SPA；「Treat `app/api/**` as the backend surface」→「Treat capability manifests（`src/capabilities/*/manifest.ts`）+ `server/app.ts` 组合根 as the backend surface」；Commands 段删 test:legacy 注记、worker 双进程注记保留但入口改写（`dist/worker.cjs` / `worker:dev`，不再有 Next instrumentation 表述）；历史栈警示段更新）
- Modify: `docs/architecture.md`（请求流 / 分层图对齐新形状；`app/` 小节移除，`server/` + `web/` + `src/capabilities/` 入图）
- Modify: `postman/README.md`（真相源链补 manifest 对账说明）
- Modify: `docs/superpowers/status.md`（+M5 行：本 PR 号 + 日期，供 audit:schema phase 引用解析）
- Create: `docs/audit/2026-06-12-final-drift.md`（终局 drift 审计输出）

### 步骤

- [ ] README/CLAUDE.md/docs/architecture.md/postman README 按上列逐文件改写；自查口径：文档中 grep `next dev|next build|3000|Redis|ioredis|app/api` 零残留（历史 docs/ 旧审计不动，仅入口三件 + architecture）
- [ ] status.md 补 M5 行
- [ ] 运行 `/audit-drift` skill（ADR / planning-doc ↔ 代码实现结构性漂移扫描），输出落 `docs/audit/2026-06-12-final-drift.md`；逐条阅读：M5 范围内的漂移当场修，范围外的列入审计文档「遗留」节供主 session Linear capture
- [ ] `pnpm lint`（docs 不进 Biome 但脚本/配置若被审计修复触碰则跑）+ `pnpm typecheck`（若审计修复触码）
- [ ] commit：

```
docs: M5-T6 文档收编 + 终局 drift 审计 (YUK-321)

README/CLAUDE.md/architecture 对齐 Hono+Vite 双进程新形状；postman README
补 manifest 对账链；终局 drift 审计落 docs/audit/2026-06-12-final-drift.md。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
```

- [ ] 主 session 交接清单（T7/T8 用）：① review 走 code-review skill；② 全 gate 证据含原始输出尾部；③ PR `yuk-321-m5-copilot-teardown → main`，正文 `Closes YUK-321`，禁 squash；④ Linear capture：裁决 f（/events/$id SPA 404）、裁决 g（D6 三刀 feature 级）、裁决 d（D11 record 工具渐废摘除时点）、CORE_TOOLS 全面 manifest 化退役（见 Task 3 allowlists 注记）四条 follow-up 查重后建 issue

---

## 自检记录（plan 写作期执行）

**1. spec §4 M5 行逐名词对账**（L340 原文 →落点）：

| spec 名词 | 落点 |
|---|---|
| copilot 包（D14 单人格） | Task 2（chat/turns/skills/teaching 入包）+ Task 4（Dock 单实例根壳） |
| 统一记忆读取面 | 裁决 k：chat.ts ambient context + getRecentCopilotTurns 收编即读取面（Task 2），不发明新抽象 |
| 工具贡献制 | Task 1（kernel 契约 + 聚合器）+ Task 3（25 工具五包声明 + 对账测试） |
| observability | Task 5（数据面 + subjects API 化）+ Task 6（四页 SPA） |
| 删 app/ | Task 9（git rm -r，单 commit） |
| 旧 src/server 残余 | Task 9 importer 闭包判定删除集（裁决 a 修正地图） |
| Next/Redis 依赖 | Task 9（pnpm remove + 配置五件 + presence 承接按 ⏸ gate 用户选项 + compose 去 Redis） |
| 旧 vitest 配置 + 112 allowlist | Task 9（vitest.config.ts 删 + shared allowlist 重写；app 条目随 app/ 死） |
| postman 按 manifest 重生成 | Task 9（gen-postman manifest 对账层 + 剪枝 + 重生成，裁决 b） |
| 文档收编 + 终局 drift 审计 | Task 10 |
| 验收「仓库里只剩新形状」 | Task 9 全 gate + Task 10 审计双重确认 |

**2. 占位符扫描**：全文 grep `TBD`、`TODO`、`适当处理`、`类似 Task`——零命中（Task 7 mistakes 改写块中的「原 after 回调内实参原样」与 Dockerfile 的「从 `git show HEAD:Dockerfile` 整段搬运」均为机械变换指令，指向确定源文件行，非待定占位）。

**3. 类型签名一致性**：

- 全部 api 代码块签名符合 kernel `RouteHandler = (req: Request, params: Record<string, string>) => Promise<Response>`（零参/单参 GET 是其合法子型，workbench-summary.ts 先例）；
- `CopilotToolDecl.load: () => Promise<{ name: string }>` 与 DomainTool（`name: string` 必有）结构兼容，`as DomainTool<unknown, unknown>` 窄化只在聚合器一处（JobHandlerFactory 同约）；
- `registerTool/getTool/__resetRegistryForTests` 用法与 registry.ts 实际导出一致（getTool 返回 `DomainTool | undefined`，duplicate-guard 在 registerTool）；`registerCoreTools` per-tool 幂等（`if (!getTool(name))`）保证与聚合器任意先后到达安全；
- `SSEStreamingApi(writable, readable)` 构造 + `responseReadable` 与 hono/streaming 实测导出一致；`writeSSE` 帧序 event 行在 data 行前，与旧栈手工 framing 逐字节同形（Task 2 单测钉死）；
- 25 个 load thunk 的模块路径与导出名逐条对照 bootstrap.ts L10-62 import 区核实；agency 生命周期三工具 `.name` 已逐字核对为全名 `propose_learning_item_relearn` / `propose_learning_item_defer` / `propose_learning_item_archive`（proposal-tools.ts L1094 / L1759 / L1826），Task 3 的 set-equality 单测再机械守护。
