# Wave 8 review-fix-2 lane plan (against fresh main 3f8dd071)

5 findings from the user's 2026-05-29 second-round review. Smallest-viable diffs;
no scope broadening.

## Fix 1 — High [Closes YUK-156] audit doc embeds a bare NUL
`docs/audit/2026-05-29-commit-review.md` L28 carried one literal NUL byte (ironic:
that line describes the block-refs NUL fix). git treated the file as binary.
- Replace the bare NUL with the 4-char text escape `\x00` via
  `perl -i -pe 's/\x00/\\x00/g'`.
- Verify: `grep -aPc '\x00'` == 0, `file` reports UTF-8 text, line semantics intact.

## Fix 2 — Medium [Closes YUK-155] hub dismiss lost-update + stale patch
`src/server/artifacts/hub-dismiss.ts persistHubLinkDismiss`: the hub `select` was
unlocked, so concurrent dismisses of different auto-links race on the JS
read-modify-write of `attrs.suppressed_block_refs` (lost update), and the
`body_blocks` read could go stale before `persistNoteRefineApply` re-reads.
- Add `.for('update')` to the hub select → Postgres row lock serializes
  concurrent dismisses + the nightly sync on the same hub. Second tx blocks until
  the first commits, then reads the already-updated attrs before appending.
- New DB tests in `dismiss-link/route.test.ts`: concurrent two-target dismiss
  (`Promise.all` over two `db.transaction(persistHubLinkDismiss)` calls) asserts
  BOTH suppressed entries survive + 2 suppress events; plus a sequential
  accumulation test.

## Fix 3 — perf: tree view O(n×m)
`app/(app)/knowledge/page.tsx` tree render did `edges.filter(...)` per node row.
- Add `edgeCountByNode` `useMemo` (Map built in one pass over `activeEdges`),
  render does O(1) `.get()`. Self-loops counted once to match the old `||` filter.

## Fix 4 — perf/UX: cross-link @ picker request storm
`src/ui/block-tree/CrossLinkSuggestion.tsx` fetched on every keystroke.
- `createDebouncedPickerSearch` wraps the Suggestion `items` callback with a
  180ms debounce + an `AbortController` that cancels the prior in-flight fetch.
  `cancel()` runs on Escape + `onExit` to drop a trailing timer/fetch.

## Fix 5 — test gap: hub-dismiss pure helpers
`appendSuppressedRef` + `buildRemoveAutoLinkPatch` only had indirect DB coverage.
- New no-DB `src/server/artifacts/hub-dismiss.test.ts` (added to `fastTestInclude`):
  null/missing/malformed attrs, dedup idempotency, accumulation, legacy entries;
  malformed doc / no container / no id / empty zone / no match / manual-link
  preservation / multi-container first-match.

## Gate
typecheck + lint + audit:partition + DB tests (nightly + block-refs + dismiss
route) + new unit test + production build. No push / no merge.
