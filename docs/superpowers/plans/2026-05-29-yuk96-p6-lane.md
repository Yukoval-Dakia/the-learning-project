# YUK-96 P6 Lane Plan — read-view deletions + mark-wrong drill + /knowledge/[id] node page

> 现场对 fresh wave8（657a109f）写。Wave 8 lane A。W8-1 scope-down：结构/功能/wiring + 删除型廉价缺陷；idiom CSS + 控件视觉打磨 defer 到 claude design 重绘。不做 visual QA / 截图眼检（redraw pending）。

## 设计 Pre-flight（UI lane 强制）

### 逐字引用的 design doc 段落
- `docs/design/2026-05-26-atomic-note-read-view.md` §5 控件隐现规则（line 110-112）：
  - `AI 单 pass` tier chip → **「完全移除」**（DEFECT 3，纯删）
  - `note · note_atomic` eyebrow → **「直接移走，技术词不进 UI」**（DEFECT 4，纯删）
  - 标题旁 `⋯` 菜单 → **「不做」**（已无此实现，nop）
  - 生成状态/verification chip → **「异常态显形（pending/failed/needs_review）；ready + not_required 隐藏」**（DEFECT 5，状态条）
- `docs/design/2026-05-26-atomic-note-read-view.md` §7 Token 映射（line 133）：section 间距 → `var(--s-5)`（比 `--s-4` 大，给阅读节奏）。
- `docs/adr/0020-block-tree-note-rebuild.md` §10 节点页（line 130-134）：分层视图 = 节点元数据 + mastery + mesh 邻居 chip → 主 atomic body_blocks inline 渲染 → 反链 panel → 最近活动 timeline；无主 atomic 占位卡 + 一键生成；节点页 = 视图职能（query 聚合不存盘）；day1 不做 D graph 视图（roadmap phase 2+）。
- `2026-05-29-wave8-ready-to-launch.md` W8-1（line 32）：read-view CSS 缺陷只做删除型廉价项（DEFECT 3/4/5）；5→3 semantic_kind idiom + 控件低对比（DEFECT 1/2/6）defer。

### 删除型 vs idiom/低对比分类（W8-1）
- **删除型（本 lane 做）**：DEFECT 3（去 tier chip）、DEFECT 4（去 eyebrow 技术词）、DEFECT 5（隐藏 ready/not_required 状态条）+ section 间距 var(--s-5)。
- **idiom / 低对比（defer 到 redraw）**：DEFECT 1（控件常驻→低对比稳态）、DEFECT 2（5→3 semantic_kind idiom：example quote 风 / pitfall callout / definition 字重）、DEFECT 6（节奏间距细化）。留 TODO 注释指向 redraw + design doc。

### 组件类型 + 文件清单
| 改动 | 组件类型 | 创建/修改 |
|---|---|---|
| P6/A 去 tier chip | renderer (BlockTreeRenderer.tsx) | 修改 |
| P6/A section 间距 var(--s-5) | CSS (globals.css) | 修改 |
| P6/A 去 eyebrow 技术词 | page (learning-items/[id]/page.tsx ArtifactView) | 修改 |
| P6/C+D /knowledge/[id] 全节点页 | route page (knowledge/[id]/page.tsx) | 重写 |
| P6/C 单节点 GET 端点 | API route (api/knowledge/[id]/route.ts) | 创建 |
| P6/C 节点页聚合 server module | server (server/knowledge/node-page.ts) | 创建 |
| P6/D 占位卡 + 引导 | page 内组件 | 创建（page 内） |
| 测试 | node-page server test + route test | 创建 |

## 已存在（不重建）
- `/learning-items/[id]` 已接 `ArtifactBlockTree`（内部已用 `BlockTreeRenderer` + mark-wrong 钻取 + getArtifactCorrectionStates + 反链 panel）。→ **P6/B mark-wrong 钻取 + P6/C 接 BlockTreeRenderer 已在 Wave 7 落地**；本 lane 只补删除型缺陷 + eyebrow。
- `BlockTreeRenderer` 已渲染 5 node 类型 + correction state + embedded check。

## Scope 决策（surface 给 coordinator）
- **P6/D 一键生成**：现有 `note_generate` 通过 `boss.send('note_generate', { artifact_id })` 触发，前提是已有 artifact stub 行（由 learning_intent accept 创建）。「凭空给一个 knowledge 生成 atomic」需先创建 artifact stub + learning_item + 挂 knowledge_ids —— 属 orchestrator + schema write path scope，**超出 UI lane**。故 P6/D 落地为：占位卡 + 引导 CTA（链到现有 learning-intent propose 入口），不自造 zero-scaffold 生成端点。完整一键生成留 follow-up Linear issue。

## Gate
typecheck + lint + audit:partition + 相关测试 + `DATABASE_URL=… INTERNAL_TOKEN=… pnpm build`。
